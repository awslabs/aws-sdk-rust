// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Configuration that specifies a naming pattern for destination log groups created during centralization. The pattern supports static text and dynamic variables that are replaced with source attributes when log groups are created.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct LogGroupNameConfiguration {
    /// <p>The pattern used to generate destination log group names during centralization. The pattern can contain static text and dynamic variables that are replaced with source attributes. If a variable cannot be resolved, it inherits the value from its parent variable in the hierarchy. The pattern must be between 1 and 512 characters.</p>
    /// <p>Supported variables:</p>
    /// <ul>
    /// <li>
    /// <p><b>${source.logGroup}</b> — The original log group name from the source account.</p></li>
    /// <li>
    /// <p><b>${source.accountId}</b> — The AWS account ID where the log originated.</p></li>
    /// <li>
    /// <p><b>${source.region}</b> — The AWS Region where the log originated.</p></li>
    /// <li>
    /// <p><b>${source.org.id}</b> — The AWS Organization ID of the source account.</p></li>
    /// <li>
    /// <p><b>${source.org.ouId}</b> — The organizational unit ID of the source account.</p></li>
    /// <li>
    /// <p><b>${source.org.rootId}</b> — The organization Root ID.</p></li>
    /// <li>
    /// <p><b>${source.org.path}</b> — The organizational path from account to root.</p></li>
    /// </ul>
    pub log_group_name_pattern: ::std::string::String,
}
impl LogGroupNameConfiguration {
    /// <p>The pattern used to generate destination log group names during centralization. The pattern can contain static text and dynamic variables that are replaced with source attributes. If a variable cannot be resolved, it inherits the value from its parent variable in the hierarchy. The pattern must be between 1 and 512 characters.</p>
    /// <p>Supported variables:</p>
    /// <ul>
    /// <li>
    /// <p><b>${source.logGroup}</b> — The original log group name from the source account.</p></li>
    /// <li>
    /// <p><b>${source.accountId}</b> — The AWS account ID where the log originated.</p></li>
    /// <li>
    /// <p><b>${source.region}</b> — The AWS Region where the log originated.</p></li>
    /// <li>
    /// <p><b>${source.org.id}</b> — The AWS Organization ID of the source account.</p></li>
    /// <li>
    /// <p><b>${source.org.ouId}</b> — The organizational unit ID of the source account.</p></li>
    /// <li>
    /// <p><b>${source.org.rootId}</b> — The organization Root ID.</p></li>
    /// <li>
    /// <p><b>${source.org.path}</b> — The organizational path from account to root.</p></li>
    /// </ul>
    pub fn log_group_name_pattern(&self) -> &str {
        use std::ops::Deref;
        self.log_group_name_pattern.deref()
    }
}
impl LogGroupNameConfiguration {
    /// Creates a new builder-style object to manufacture [`LogGroupNameConfiguration`](crate::types::LogGroupNameConfiguration).
    pub fn builder() -> crate::types::builders::LogGroupNameConfigurationBuilder {
        crate::types::builders::LogGroupNameConfigurationBuilder::default()
    }
}

/// A builder for [`LogGroupNameConfiguration`](crate::types::LogGroupNameConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct LogGroupNameConfigurationBuilder {
    pub(crate) log_group_name_pattern: ::std::option::Option<::std::string::String>,
}
impl LogGroupNameConfigurationBuilder {
    /// <p>The pattern used to generate destination log group names during centralization. The pattern can contain static text and dynamic variables that are replaced with source attributes. If a variable cannot be resolved, it inherits the value from its parent variable in the hierarchy. The pattern must be between 1 and 512 characters.</p>
    /// <p>Supported variables:</p>
    /// <ul>
    /// <li>
    /// <p><b>${source.logGroup}</b> — The original log group name from the source account.</p></li>
    /// <li>
    /// <p><b>${source.accountId}</b> — The AWS account ID where the log originated.</p></li>
    /// <li>
    /// <p><b>${source.region}</b> — The AWS Region where the log originated.</p></li>
    /// <li>
    /// <p><b>${source.org.id}</b> — The AWS Organization ID of the source account.</p></li>
    /// <li>
    /// <p><b>${source.org.ouId}</b> — The organizational unit ID of the source account.</p></li>
    /// <li>
    /// <p><b>${source.org.rootId}</b> — The organization Root ID.</p></li>
    /// <li>
    /// <p><b>${source.org.path}</b> — The organizational path from account to root.</p></li>
    /// </ul>
    /// This field is required.
    pub fn log_group_name_pattern(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.log_group_name_pattern = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The pattern used to generate destination log group names during centralization. The pattern can contain static text and dynamic variables that are replaced with source attributes. If a variable cannot be resolved, it inherits the value from its parent variable in the hierarchy. The pattern must be between 1 and 512 characters.</p>
    /// <p>Supported variables:</p>
    /// <ul>
    /// <li>
    /// <p><b>${source.logGroup}</b> — The original log group name from the source account.</p></li>
    /// <li>
    /// <p><b>${source.accountId}</b> — The AWS account ID where the log originated.</p></li>
    /// <li>
    /// <p><b>${source.region}</b> — The AWS Region where the log originated.</p></li>
    /// <li>
    /// <p><b>${source.org.id}</b> — The AWS Organization ID of the source account.</p></li>
    /// <li>
    /// <p><b>${source.org.ouId}</b> — The organizational unit ID of the source account.</p></li>
    /// <li>
    /// <p><b>${source.org.rootId}</b> — The organization Root ID.</p></li>
    /// <li>
    /// <p><b>${source.org.path}</b> — The organizational path from account to root.</p></li>
    /// </ul>
    pub fn set_log_group_name_pattern(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.log_group_name_pattern = input;
        self
    }
    /// <p>The pattern used to generate destination log group names during centralization. The pattern can contain static text and dynamic variables that are replaced with source attributes. If a variable cannot be resolved, it inherits the value from its parent variable in the hierarchy. The pattern must be between 1 and 512 characters.</p>
    /// <p>Supported variables:</p>
    /// <ul>
    /// <li>
    /// <p><b>${source.logGroup}</b> — The original log group name from the source account.</p></li>
    /// <li>
    /// <p><b>${source.accountId}</b> — The AWS account ID where the log originated.</p></li>
    /// <li>
    /// <p><b>${source.region}</b> — The AWS Region where the log originated.</p></li>
    /// <li>
    /// <p><b>${source.org.id}</b> — The AWS Organization ID of the source account.</p></li>
    /// <li>
    /// <p><b>${source.org.ouId}</b> — The organizational unit ID of the source account.</p></li>
    /// <li>
    /// <p><b>${source.org.rootId}</b> — The organization Root ID.</p></li>
    /// <li>
    /// <p><b>${source.org.path}</b> — The organizational path from account to root.</p></li>
    /// </ul>
    pub fn get_log_group_name_pattern(&self) -> &::std::option::Option<::std::string::String> {
        &self.log_group_name_pattern
    }
    /// Consumes the builder and constructs a [`LogGroupNameConfiguration`](crate::types::LogGroupNameConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`log_group_name_pattern`](crate::types::builders::LogGroupNameConfigurationBuilder::log_group_name_pattern)
    pub fn build(self) -> ::std::result::Result<crate::types::LogGroupNameConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::LogGroupNameConfiguration {
            log_group_name_pattern: self.log_group_name_pattern.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "log_group_name_pattern",
                    "log_group_name_pattern was not specified but it is required when building LogGroupNameConfiguration",
                )
            })?,
        })
    }
}
