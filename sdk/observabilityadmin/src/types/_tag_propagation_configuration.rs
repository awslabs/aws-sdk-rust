// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Specifies configuration for propagating resource tags from source log groups to centralized destination log groups. The service uses a customer-managed IAM role in the destination account to add, update, and remove tags on destination log groups.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct TagPropagationConfiguration {
    /// <p>The ARN of a customer-managed IAM role in the destination account. The service assumes this role to propagate tags to destination log groups. You must have <code>iam:PassRole</code> permission on this role.</p>
    pub destination_role_arn: ::std::string::String,
    /// <p>The strategy for resolving conflicts when a tag key exists on both the source and destination log groups. If not specified, defaults to <code>UPDATE_SYNC</code>.</p>
    /// <ul>
    /// <li>
    /// <p><code>ADD_ONLY</code> – Only adds new tags from the source without modifying existing destination tags.</p></li>
    /// <li>
    /// <p><code>UPDATE_SYNC</code> – Adds new tags and updates existing tags from the source. Does not remove destination tags that are absent from the source.</p></li>
    /// <li>
    /// <p><code>IN_SYNC</code> – Keeps destination tags fully synchronized with source tags, including removing destination tags that do not exist on the source.</p></li>
    /// </ul>
    pub tag_conflict_resolution_strategy: ::std::option::Option<crate::types::TagConflictResolutionStrategy>,
}
impl TagPropagationConfiguration {
    /// <p>The ARN of a customer-managed IAM role in the destination account. The service assumes this role to propagate tags to destination log groups. You must have <code>iam:PassRole</code> permission on this role.</p>
    pub fn destination_role_arn(&self) -> &str {
        use std::ops::Deref;
        self.destination_role_arn.deref()
    }
    /// <p>The strategy for resolving conflicts when a tag key exists on both the source and destination log groups. If not specified, defaults to <code>UPDATE_SYNC</code>.</p>
    /// <ul>
    /// <li>
    /// <p><code>ADD_ONLY</code> – Only adds new tags from the source without modifying existing destination tags.</p></li>
    /// <li>
    /// <p><code>UPDATE_SYNC</code> – Adds new tags and updates existing tags from the source. Does not remove destination tags that are absent from the source.</p></li>
    /// <li>
    /// <p><code>IN_SYNC</code> – Keeps destination tags fully synchronized with source tags, including removing destination tags that do not exist on the source.</p></li>
    /// </ul>
    pub fn tag_conflict_resolution_strategy(&self) -> ::std::option::Option<&crate::types::TagConflictResolutionStrategy> {
        self.tag_conflict_resolution_strategy.as_ref()
    }
}
impl TagPropagationConfiguration {
    /// Creates a new builder-style object to manufacture [`TagPropagationConfiguration`](crate::types::TagPropagationConfiguration).
    pub fn builder() -> crate::types::builders::TagPropagationConfigurationBuilder {
        crate::types::builders::TagPropagationConfigurationBuilder::default()
    }
}

/// A builder for [`TagPropagationConfiguration`](crate::types::TagPropagationConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct TagPropagationConfigurationBuilder {
    pub(crate) destination_role_arn: ::std::option::Option<::std::string::String>,
    pub(crate) tag_conflict_resolution_strategy: ::std::option::Option<crate::types::TagConflictResolutionStrategy>,
}
impl TagPropagationConfigurationBuilder {
    /// <p>The ARN of a customer-managed IAM role in the destination account. The service assumes this role to propagate tags to destination log groups. You must have <code>iam:PassRole</code> permission on this role.</p>
    /// This field is required.
    pub fn destination_role_arn(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.destination_role_arn = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The ARN of a customer-managed IAM role in the destination account. The service assumes this role to propagate tags to destination log groups. You must have <code>iam:PassRole</code> permission on this role.</p>
    pub fn set_destination_role_arn(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.destination_role_arn = input;
        self
    }
    /// <p>The ARN of a customer-managed IAM role in the destination account. The service assumes this role to propagate tags to destination log groups. You must have <code>iam:PassRole</code> permission on this role.</p>
    pub fn get_destination_role_arn(&self) -> &::std::option::Option<::std::string::String> {
        &self.destination_role_arn
    }
    /// <p>The strategy for resolving conflicts when a tag key exists on both the source and destination log groups. If not specified, defaults to <code>UPDATE_SYNC</code>.</p>
    /// <ul>
    /// <li>
    /// <p><code>ADD_ONLY</code> – Only adds new tags from the source without modifying existing destination tags.</p></li>
    /// <li>
    /// <p><code>UPDATE_SYNC</code> – Adds new tags and updates existing tags from the source. Does not remove destination tags that are absent from the source.</p></li>
    /// <li>
    /// <p><code>IN_SYNC</code> – Keeps destination tags fully synchronized with source tags, including removing destination tags that do not exist on the source.</p></li>
    /// </ul>
    pub fn tag_conflict_resolution_strategy(mut self, input: crate::types::TagConflictResolutionStrategy) -> Self {
        self.tag_conflict_resolution_strategy = ::std::option::Option::Some(input);
        self
    }
    /// <p>The strategy for resolving conflicts when a tag key exists on both the source and destination log groups. If not specified, defaults to <code>UPDATE_SYNC</code>.</p>
    /// <ul>
    /// <li>
    /// <p><code>ADD_ONLY</code> – Only adds new tags from the source without modifying existing destination tags.</p></li>
    /// <li>
    /// <p><code>UPDATE_SYNC</code> – Adds new tags and updates existing tags from the source. Does not remove destination tags that are absent from the source.</p></li>
    /// <li>
    /// <p><code>IN_SYNC</code> – Keeps destination tags fully synchronized with source tags, including removing destination tags that do not exist on the source.</p></li>
    /// </ul>
    pub fn set_tag_conflict_resolution_strategy(mut self, input: ::std::option::Option<crate::types::TagConflictResolutionStrategy>) -> Self {
        self.tag_conflict_resolution_strategy = input;
        self
    }
    /// <p>The strategy for resolving conflicts when a tag key exists on both the source and destination log groups. If not specified, defaults to <code>UPDATE_SYNC</code>.</p>
    /// <ul>
    /// <li>
    /// <p><code>ADD_ONLY</code> – Only adds new tags from the source without modifying existing destination tags.</p></li>
    /// <li>
    /// <p><code>UPDATE_SYNC</code> – Adds new tags and updates existing tags from the source. Does not remove destination tags that are absent from the source.</p></li>
    /// <li>
    /// <p><code>IN_SYNC</code> – Keeps destination tags fully synchronized with source tags, including removing destination tags that do not exist on the source.</p></li>
    /// </ul>
    pub fn get_tag_conflict_resolution_strategy(&self) -> &::std::option::Option<crate::types::TagConflictResolutionStrategy> {
        &self.tag_conflict_resolution_strategy
    }
    /// Consumes the builder and constructs a [`TagPropagationConfiguration`](crate::types::TagPropagationConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`destination_role_arn`](crate::types::builders::TagPropagationConfigurationBuilder::destination_role_arn)
    pub fn build(self) -> ::std::result::Result<crate::types::TagPropagationConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::TagPropagationConfiguration {
            destination_role_arn: self.destination_role_arn.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "destination_role_arn",
                    "destination_role_arn was not specified but it is required when building TagPropagationConfiguration",
                )
            })?,
            tag_conflict_resolution_strategy: self.tag_conflict_resolution_strategy,
        })
    }
}
