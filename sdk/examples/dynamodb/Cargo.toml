[package]
name = "dynamodb-code-examples"
version = "0.1.0"
authors = ["Russell Cohen <rcoh@amazon.com>", "Doug Schwartz <dougsch@amazon.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
aws-sdk-dynamodb = { package = "aws-sdk-dynamodb", path = "../../dynamodb" }
aws-http = { path = "../../aws-http"}
aws-hyper = { path = "../../aws-hyper"}
aws-types = { path = "../../aws-types" }
smithy-http = { path = "../../smithy-http" }
smithy-types = { path = "../../smithy-types" }
tokio = { version = "1", features = ["full"] }
rand = "0.8.3"
serde_json = "1"
structopt = { version = "0.3", default-features = false }
tracing-subscriber = { version = "0.2.16", features = ["fmt"] }
