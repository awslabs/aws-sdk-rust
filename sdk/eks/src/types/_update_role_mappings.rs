// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Updates to RBAC role mappings for an Argo CD capability. You can add, update, or remove role mappings in a single operation.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct UpdateRoleMappings {
    /// <p>A list of role mappings to add or update. If a mapping for the specified role already exists, it will be updated with the new identities. If it doesn't exist, a new mapping will be created.</p>
    pub add_or_update_role_mappings: ::std::option::Option<::std::vec::Vec<crate::types::ArgoCdRoleMapping>>,
    /// <p>A list of role mappings to remove from the RBAC configuration. Each mapping specifies an Argo CD role (<code>ADMIN</code>, <code>EDITOR</code>, or <code>VIEWER</code>) and the identities to remove from that role.</p>
    pub remove_role_mappings: ::std::option::Option<::std::vec::Vec<crate::types::ArgoCdRoleMapping>>,
}
impl UpdateRoleMappings {
    /// <p>A list of role mappings to add or update. If a mapping for the specified role already exists, it will be updated with the new identities. If it doesn't exist, a new mapping will be created.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.add_or_update_role_mappings.is_none()`.
    pub fn add_or_update_role_mappings(&self) -> &[crate::types::ArgoCdRoleMapping] {
        self.add_or_update_role_mappings.as_deref().unwrap_or_default()
    }
    /// <p>A list of role mappings to remove from the RBAC configuration. Each mapping specifies an Argo CD role (<code>ADMIN</code>, <code>EDITOR</code>, or <code>VIEWER</code>) and the identities to remove from that role.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.remove_role_mappings.is_none()`.
    pub fn remove_role_mappings(&self) -> &[crate::types::ArgoCdRoleMapping] {
        self.remove_role_mappings.as_deref().unwrap_or_default()
    }
}
impl UpdateRoleMappings {
    /// Creates a new builder-style object to manufacture [`UpdateRoleMappings`](crate::types::UpdateRoleMappings).
    pub fn builder() -> crate::types::builders::UpdateRoleMappingsBuilder {
        crate::types::builders::UpdateRoleMappingsBuilder::default()
    }
}

/// A builder for [`UpdateRoleMappings`](crate::types::UpdateRoleMappings).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct UpdateRoleMappingsBuilder {
    pub(crate) add_or_update_role_mappings: ::std::option::Option<::std::vec::Vec<crate::types::ArgoCdRoleMapping>>,
    pub(crate) remove_role_mappings: ::std::option::Option<::std::vec::Vec<crate::types::ArgoCdRoleMapping>>,
}
impl UpdateRoleMappingsBuilder {
    /// Appends an item to `add_or_update_role_mappings`.
    ///
    /// To override the contents of this collection use [`set_add_or_update_role_mappings`](Self::set_add_or_update_role_mappings).
    ///
    /// <p>A list of role mappings to add or update. If a mapping for the specified role already exists, it will be updated with the new identities. If it doesn't exist, a new mapping will be created.</p>
    pub fn add_or_update_role_mappings(mut self, input: crate::types::ArgoCdRoleMapping) -> Self {
        let mut v = self.add_or_update_role_mappings.unwrap_or_default();
        v.push(input);
        self.add_or_update_role_mappings = ::std::option::Option::Some(v);
        self
    }
    /// <p>A list of role mappings to add or update. If a mapping for the specified role already exists, it will be updated with the new identities. If it doesn't exist, a new mapping will be created.</p>
    pub fn set_add_or_update_role_mappings(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::ArgoCdRoleMapping>>) -> Self {
        self.add_or_update_role_mappings = input;
        self
    }
    /// <p>A list of role mappings to add or update. If a mapping for the specified role already exists, it will be updated with the new identities. If it doesn't exist, a new mapping will be created.</p>
    pub fn get_add_or_update_role_mappings(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::ArgoCdRoleMapping>> {
        &self.add_or_update_role_mappings
    }
    /// Appends an item to `remove_role_mappings`.
    ///
    /// To override the contents of this collection use [`set_remove_role_mappings`](Self::set_remove_role_mappings).
    ///
    /// <p>A list of role mappings to remove from the RBAC configuration. Each mapping specifies an Argo CD role (<code>ADMIN</code>, <code>EDITOR</code>, or <code>VIEWER</code>) and the identities to remove from that role.</p>
    pub fn remove_role_mappings(mut self, input: crate::types::ArgoCdRoleMapping) -> Self {
        let mut v = self.remove_role_mappings.unwrap_or_default();
        v.push(input);
        self.remove_role_mappings = ::std::option::Option::Some(v);
        self
    }
    /// <p>A list of role mappings to remove from the RBAC configuration. Each mapping specifies an Argo CD role (<code>ADMIN</code>, <code>EDITOR</code>, or <code>VIEWER</code>) and the identities to remove from that role.</p>
    pub fn set_remove_role_mappings(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::ArgoCdRoleMapping>>) -> Self {
        self.remove_role_mappings = input;
        self
    }
    /// <p>A list of role mappings to remove from the RBAC configuration. Each mapping specifies an Argo CD role (<code>ADMIN</code>, <code>EDITOR</code>, or <code>VIEWER</code>) and the identities to remove from that role.</p>
    pub fn get_remove_role_mappings(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::ArgoCdRoleMapping>> {
        &self.remove_role_mappings
    }
    /// Consumes the builder and constructs a [`UpdateRoleMappings`](crate::types::UpdateRoleMappings).
    pub fn build(self) -> crate::types::UpdateRoleMappings {
        crate::types::UpdateRoleMappings {
            add_or_update_role_mappings: self.add_or_update_role_mappings,
            remove_role_mappings: self.remove_role_mappings,
        }
    }
}
