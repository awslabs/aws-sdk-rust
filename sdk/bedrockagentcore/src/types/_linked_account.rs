// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Represents different linked accounts that can be linked to an embedded wallet. Supports email, SMS, JWT, and OAuth2 approaches.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq)]
pub enum LinkedAccount {
    /// <p>Developer JWT linked account with key ID and subject.</p>
    DeveloperJwt(crate::types::LinkedAccountDeveloperJwt),
    /// <p>Email-based linked account.</p>
    Email(crate::types::LinkedAccountEmail),
    /// <p>OAuth2 provider linked account (Google, Apple, X, Telegram, GitHub).</p>
    OAuth2(crate::types::LinkedAccountOAuth2),
    /// <p>SMS-based linked account using phone number.</p>
    Sms(crate::types::LinkedAccountSms),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl LinkedAccount {
    /// Tries to convert the enum instance into [`DeveloperJwt`](crate::types::LinkedAccount::DeveloperJwt), extracting the inner [`LinkedAccountDeveloperJwt`](crate::types::LinkedAccountDeveloperJwt).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_developer_jwt(&self) -> ::std::result::Result<&crate::types::LinkedAccountDeveloperJwt, &Self> {
        if let LinkedAccount::DeveloperJwt(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`DeveloperJwt`](crate::types::LinkedAccount::DeveloperJwt).
    pub fn is_developer_jwt(&self) -> bool {
        self.as_developer_jwt().is_ok()
    }
    /// Tries to convert the enum instance into [`Email`](crate::types::LinkedAccount::Email), extracting the inner [`LinkedAccountEmail`](crate::types::LinkedAccountEmail).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_email(&self) -> ::std::result::Result<&crate::types::LinkedAccountEmail, &Self> {
        if let LinkedAccount::Email(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Email`](crate::types::LinkedAccount::Email).
    pub fn is_email(&self) -> bool {
        self.as_email().is_ok()
    }
    /// Tries to convert the enum instance into [`OAuth2`](crate::types::LinkedAccount::OAuth2), extracting the inner [`LinkedAccountOAuth2`](crate::types::LinkedAccountOAuth2).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_o_auth2(&self) -> ::std::result::Result<&crate::types::LinkedAccountOAuth2, &Self> {
        if let LinkedAccount::OAuth2(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`OAuth2`](crate::types::LinkedAccount::OAuth2).
    pub fn is_o_auth2(&self) -> bool {
        self.as_o_auth2().is_ok()
    }
    /// Tries to convert the enum instance into [`Sms`](crate::types::LinkedAccount::Sms), extracting the inner [`LinkedAccountSms`](crate::types::LinkedAccountSms).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_sms(&self) -> ::std::result::Result<&crate::types::LinkedAccountSms, &Self> {
        if let LinkedAccount::Sms(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Sms`](crate::types::LinkedAccount::Sms).
    pub fn is_sms(&self) -> bool {
        self.as_sms().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
impl ::std::fmt::Debug for LinkedAccount {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        ::std::write!(f, "*** Sensitive Data Redacted ***")
    }
}
