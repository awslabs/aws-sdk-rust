// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Configuration for routing browser traffic through customer-managed proxy servers. Supports 1-5 proxy servers for domain-based routing and proxy bypass rules.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ProxyConfiguration {
    /// <p>An array of 1-5 proxy server configurations for domain-based routing. Each proxy can specify which domains it handles via <code>domainPatterns</code>, enabling flexible routing of different traffic through different proxies based on destination domain.</p>
    pub proxies: ::std::vec::Vec<crate::types::Proxy>,
    /// <p>Optional configuration for domains that should bypass all proxies and connect directly to their destination, like the internet. Takes precedence over all proxy routing rules.</p>
    pub bypass: ::std::option::Option<crate::types::ProxyBypass>,
}
impl ProxyConfiguration {
    /// <p>An array of 1-5 proxy server configurations for domain-based routing. Each proxy can specify which domains it handles via <code>domainPatterns</code>, enabling flexible routing of different traffic through different proxies based on destination domain.</p>
    pub fn proxies(&self) -> &[crate::types::Proxy] {
        use std::ops::Deref;
        self.proxies.deref()
    }
    /// <p>Optional configuration for domains that should bypass all proxies and connect directly to their destination, like the internet. Takes precedence over all proxy routing rules.</p>
    pub fn bypass(&self) -> ::std::option::Option<&crate::types::ProxyBypass> {
        self.bypass.as_ref()
    }
}
impl ProxyConfiguration {
    /// Creates a new builder-style object to manufacture [`ProxyConfiguration`](crate::types::ProxyConfiguration).
    pub fn builder() -> crate::types::builders::ProxyConfigurationBuilder {
        crate::types::builders::ProxyConfigurationBuilder::default()
    }
}

/// A builder for [`ProxyConfiguration`](crate::types::ProxyConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ProxyConfigurationBuilder {
    pub(crate) proxies: ::std::option::Option<::std::vec::Vec<crate::types::Proxy>>,
    pub(crate) bypass: ::std::option::Option<crate::types::ProxyBypass>,
}
impl ProxyConfigurationBuilder {
    /// Appends an item to `proxies`.
    ///
    /// To override the contents of this collection use [`set_proxies`](Self::set_proxies).
    ///
    /// <p>An array of 1-5 proxy server configurations for domain-based routing. Each proxy can specify which domains it handles via <code>domainPatterns</code>, enabling flexible routing of different traffic through different proxies based on destination domain.</p>
    pub fn proxies(mut self, input: crate::types::Proxy) -> Self {
        let mut v = self.proxies.unwrap_or_default();
        v.push(input);
        self.proxies = ::std::option::Option::Some(v);
        self
    }
    /// <p>An array of 1-5 proxy server configurations for domain-based routing. Each proxy can specify which domains it handles via <code>domainPatterns</code>, enabling flexible routing of different traffic through different proxies based on destination domain.</p>
    pub fn set_proxies(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::Proxy>>) -> Self {
        self.proxies = input;
        self
    }
    /// <p>An array of 1-5 proxy server configurations for domain-based routing. Each proxy can specify which domains it handles via <code>domainPatterns</code>, enabling flexible routing of different traffic through different proxies based on destination domain.</p>
    pub fn get_proxies(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::Proxy>> {
        &self.proxies
    }
    /// <p>Optional configuration for domains that should bypass all proxies and connect directly to their destination, like the internet. Takes precedence over all proxy routing rules.</p>
    pub fn bypass(mut self, input: crate::types::ProxyBypass) -> Self {
        self.bypass = ::std::option::Option::Some(input);
        self
    }
    /// <p>Optional configuration for domains that should bypass all proxies and connect directly to their destination, like the internet. Takes precedence over all proxy routing rules.</p>
    pub fn set_bypass(mut self, input: ::std::option::Option<crate::types::ProxyBypass>) -> Self {
        self.bypass = input;
        self
    }
    /// <p>Optional configuration for domains that should bypass all proxies and connect directly to their destination, like the internet. Takes precedence over all proxy routing rules.</p>
    pub fn get_bypass(&self) -> &::std::option::Option<crate::types::ProxyBypass> {
        &self.bypass
    }
    /// Consumes the builder and constructs a [`ProxyConfiguration`](crate::types::ProxyConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`proxies`](crate::types::builders::ProxyConfigurationBuilder::proxies)
    pub fn build(self) -> ::std::result::Result<crate::types::ProxyConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::ProxyConfiguration {
            proxies: self.proxies.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "proxies",
                    "proxies was not specified but it is required when building ProxyConfiguration",
                )
            })?,
            bypass: self.bypass,
        })
    }
}
