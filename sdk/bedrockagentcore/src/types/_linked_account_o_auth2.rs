// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// Authentication method using OAuth2 providers. Supports Google, Apple, X, Telegram, and GitHub providers.
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq)]
pub enum LinkedAccountOAuth2 {
    /// Apple OAuth2 authentication
    Apple(crate::types::OAuth2Authentication),
    /// GitHub OAuth2 authentication
    Github(crate::types::OAuth2Authentication),
    /// Google OAuth2 authentication
    Google(crate::types::OAuth2Authentication),
    /// Telegram OAuth2 authentication
    Telegram(crate::types::OAuth2Authentication),
    /// X (formerly Twitter) OAuth2 authentication
    X(crate::types::OAuth2Authentication),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl LinkedAccountOAuth2 {
    /// Tries to convert the enum instance into [`Apple`](crate::types::LinkedAccountOAuth2::Apple), extracting the inner [`OAuth2Authentication`](crate::types::OAuth2Authentication).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_apple(&self) -> ::std::result::Result<&crate::types::OAuth2Authentication, &Self> {
        if let LinkedAccountOAuth2::Apple(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Apple`](crate::types::LinkedAccountOAuth2::Apple).
    pub fn is_apple(&self) -> bool {
        self.as_apple().is_ok()
    }
    /// Tries to convert the enum instance into [`Github`](crate::types::LinkedAccountOAuth2::Github), extracting the inner [`OAuth2Authentication`](crate::types::OAuth2Authentication).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_github(&self) -> ::std::result::Result<&crate::types::OAuth2Authentication, &Self> {
        if let LinkedAccountOAuth2::Github(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Github`](crate::types::LinkedAccountOAuth2::Github).
    pub fn is_github(&self) -> bool {
        self.as_github().is_ok()
    }
    /// Tries to convert the enum instance into [`Google`](crate::types::LinkedAccountOAuth2::Google), extracting the inner [`OAuth2Authentication`](crate::types::OAuth2Authentication).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_google(&self) -> ::std::result::Result<&crate::types::OAuth2Authentication, &Self> {
        if let LinkedAccountOAuth2::Google(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Google`](crate::types::LinkedAccountOAuth2::Google).
    pub fn is_google(&self) -> bool {
        self.as_google().is_ok()
    }
    /// Tries to convert the enum instance into [`Telegram`](crate::types::LinkedAccountOAuth2::Telegram), extracting the inner [`OAuth2Authentication`](crate::types::OAuth2Authentication).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_telegram(&self) -> ::std::result::Result<&crate::types::OAuth2Authentication, &Self> {
        if let LinkedAccountOAuth2::Telegram(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Telegram`](crate::types::LinkedAccountOAuth2::Telegram).
    pub fn is_telegram(&self) -> bool {
        self.as_telegram().is_ok()
    }
    /// Tries to convert the enum instance into [`X`](crate::types::LinkedAccountOAuth2::X), extracting the inner [`OAuth2Authentication`](crate::types::OAuth2Authentication).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_x(&self) -> ::std::result::Result<&crate::types::OAuth2Authentication, &Self> {
        if let LinkedAccountOAuth2::X(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`X`](crate::types::LinkedAccountOAuth2::X).
    pub fn is_x(&self) -> bool {
        self.as_x().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
impl ::std::fmt::Debug for LinkedAccountOAuth2 {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        match self {
            LinkedAccountOAuth2::Apple(_) => f.debug_tuple("*** Sensitive Data Redacted ***").finish(),
            LinkedAccountOAuth2::Github(_) => f.debug_tuple("*** Sensitive Data Redacted ***").finish(),
            LinkedAccountOAuth2::Google(_) => f.debug_tuple("*** Sensitive Data Redacted ***").finish(),
            LinkedAccountOAuth2::Telegram(_) => f.debug_tuple("*** Sensitive Data Redacted ***").finish(),
            LinkedAccountOAuth2::X(_) => f.debug_tuple("*** Sensitive Data Redacted ***").finish(),
            LinkedAccountOAuth2::Unknown => f.debug_tuple("Unknown").finish(),
        }
    }
}
