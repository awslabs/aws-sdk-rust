// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains the payment challenge from a 402 Payment Required response. Forward the raw <code>WWW-Authenticate: Payment</code> header value verbatim. In response, you receive a payment credential that satisfies the challenge. Provide exactly one challenge per request.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct MppPaymentInput {
    /// <p>The MPP protocol version, for example "1" or "2".</p>
    pub version: ::std::string::String,
    /// <p>The raw <code>WWW-Authenticate: Payment</code> header value from the 402 response, passed verbatim. Provide exactly one entry. The service uses this value to generate the payment credential.</p>
    pub www_authenticate_headers: ::std::vec::Vec<::std::string::String>,
    /// <p>Authorizes the service to sign a payment whose blockchain network (gas) fees are charged to your wallet, on top of the payment amount.</p>
    /// <p>The challenge indicates who sponsors the network fees. When the challenge does not sponsor them, the service signs the payment only if this field is <code>true</code>. Otherwise it returns a validation error, so you can decide whether to pay the fees or obtain a challenge that sponsors them.</p>
    /// <p>Optional. When omitted or <code>false</code>, you decline to pay network fees. This field has no effect on challenges that already sponsor the fees.</p>
    pub buyer_pays_gas_fees: ::std::option::Option<bool>,
}
impl MppPaymentInput {
    /// <p>The MPP protocol version, for example "1" or "2".</p>
    pub fn version(&self) -> &str {
        use std::ops::Deref;
        self.version.deref()
    }
    /// <p>The raw <code>WWW-Authenticate: Payment</code> header value from the 402 response, passed verbatim. Provide exactly one entry. The service uses this value to generate the payment credential.</p>
    pub fn www_authenticate_headers(&self) -> &[::std::string::String] {
        use std::ops::Deref;
        self.www_authenticate_headers.deref()
    }
    /// <p>Authorizes the service to sign a payment whose blockchain network (gas) fees are charged to your wallet, on top of the payment amount.</p>
    /// <p>The challenge indicates who sponsors the network fees. When the challenge does not sponsor them, the service signs the payment only if this field is <code>true</code>. Otherwise it returns a validation error, so you can decide whether to pay the fees or obtain a challenge that sponsors them.</p>
    /// <p>Optional. When omitted or <code>false</code>, you decline to pay network fees. This field has no effect on challenges that already sponsor the fees.</p>
    pub fn buyer_pays_gas_fees(&self) -> ::std::option::Option<bool> {
        self.buyer_pays_gas_fees
    }
}
impl MppPaymentInput {
    /// Creates a new builder-style object to manufacture [`MppPaymentInput`](crate::types::MppPaymentInput).
    pub fn builder() -> crate::types::builders::MppPaymentInputBuilder {
        crate::types::builders::MppPaymentInputBuilder::default()
    }
}

/// A builder for [`MppPaymentInput`](crate::types::MppPaymentInput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct MppPaymentInputBuilder {
    pub(crate) version: ::std::option::Option<::std::string::String>,
    pub(crate) www_authenticate_headers: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    pub(crate) buyer_pays_gas_fees: ::std::option::Option<bool>,
}
impl MppPaymentInputBuilder {
    /// <p>The MPP protocol version, for example "1" or "2".</p>
    /// This field is required.
    pub fn version(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.version = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The MPP protocol version, for example "1" or "2".</p>
    pub fn set_version(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.version = input;
        self
    }
    /// <p>The MPP protocol version, for example "1" or "2".</p>
    pub fn get_version(&self) -> &::std::option::Option<::std::string::String> {
        &self.version
    }
    /// Appends an item to `www_authenticate_headers`.
    ///
    /// To override the contents of this collection use [`set_www_authenticate_headers`](Self::set_www_authenticate_headers).
    ///
    /// <p>The raw <code>WWW-Authenticate: Payment</code> header value from the 402 response, passed verbatim. Provide exactly one entry. The service uses this value to generate the payment credential.</p>
    pub fn www_authenticate_headers(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.www_authenticate_headers.unwrap_or_default();
        v.push(input.into());
        self.www_authenticate_headers = ::std::option::Option::Some(v);
        self
    }
    /// <p>The raw <code>WWW-Authenticate: Payment</code> header value from the 402 response, passed verbatim. Provide exactly one entry. The service uses this value to generate the payment credential.</p>
    pub fn set_www_authenticate_headers(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.www_authenticate_headers = input;
        self
    }
    /// <p>The raw <code>WWW-Authenticate: Payment</code> header value from the 402 response, passed verbatim. Provide exactly one entry. The service uses this value to generate the payment credential.</p>
    pub fn get_www_authenticate_headers(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.www_authenticate_headers
    }
    /// <p>Authorizes the service to sign a payment whose blockchain network (gas) fees are charged to your wallet, on top of the payment amount.</p>
    /// <p>The challenge indicates who sponsors the network fees. When the challenge does not sponsor them, the service signs the payment only if this field is <code>true</code>. Otherwise it returns a validation error, so you can decide whether to pay the fees or obtain a challenge that sponsors them.</p>
    /// <p>Optional. When omitted or <code>false</code>, you decline to pay network fees. This field has no effect on challenges that already sponsor the fees.</p>
    pub fn buyer_pays_gas_fees(mut self, input: bool) -> Self {
        self.buyer_pays_gas_fees = ::std::option::Option::Some(input);
        self
    }
    /// <p>Authorizes the service to sign a payment whose blockchain network (gas) fees are charged to your wallet, on top of the payment amount.</p>
    /// <p>The challenge indicates who sponsors the network fees. When the challenge does not sponsor them, the service signs the payment only if this field is <code>true</code>. Otherwise it returns a validation error, so you can decide whether to pay the fees or obtain a challenge that sponsors them.</p>
    /// <p>Optional. When omitted or <code>false</code>, you decline to pay network fees. This field has no effect on challenges that already sponsor the fees.</p>
    pub fn set_buyer_pays_gas_fees(mut self, input: ::std::option::Option<bool>) -> Self {
        self.buyer_pays_gas_fees = input;
        self
    }
    /// <p>Authorizes the service to sign a payment whose blockchain network (gas) fees are charged to your wallet, on top of the payment amount.</p>
    /// <p>The challenge indicates who sponsors the network fees. When the challenge does not sponsor them, the service signs the payment only if this field is <code>true</code>. Otherwise it returns a validation error, so you can decide whether to pay the fees or obtain a challenge that sponsors them.</p>
    /// <p>Optional. When omitted or <code>false</code>, you decline to pay network fees. This field has no effect on challenges that already sponsor the fees.</p>
    pub fn get_buyer_pays_gas_fees(&self) -> &::std::option::Option<bool> {
        &self.buyer_pays_gas_fees
    }
    /// Consumes the builder and constructs a [`MppPaymentInput`](crate::types::MppPaymentInput).
    /// This method will fail if any of the following fields are not set:
    /// - [`version`](crate::types::builders::MppPaymentInputBuilder::version)
    /// - [`www_authenticate_headers`](crate::types::builders::MppPaymentInputBuilder::www_authenticate_headers)
    pub fn build(self) -> ::std::result::Result<crate::types::MppPaymentInput, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::MppPaymentInput {
            version: self.version.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "version",
                    "version was not specified but it is required when building MppPaymentInput",
                )
            })?,
            www_authenticate_headers: self.www_authenticate_headers.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "www_authenticate_headers",
                    "www_authenticate_headers was not specified but it is required when building MppPaymentInput",
                )
            })?,
            buyer_pays_gas_fees: self.buyer_pays_gas_fees,
        })
    }
}
