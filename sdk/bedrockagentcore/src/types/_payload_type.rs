// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains the payload content for an event.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq)]
pub enum PayloadType {
    /// <p>The binary content of the payload.</p>
    Blob(::aws_smithy_types::Document),
    /// <p>The conversational content of the payload.</p>
    Conversational(crate::types::Conversational),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl PayloadType {
    /// Tries to convert the enum instance into [`Blob`](crate::types::PayloadType::Blob), extracting the inner [`Document`](::aws_smithy_types::Document).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_blob(&self) -> ::std::result::Result<&::aws_smithy_types::Document, &Self> {
        if let PayloadType::Blob(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Blob`](crate::types::PayloadType::Blob).
    pub fn is_blob(&self) -> bool {
        self.as_blob().is_ok()
    }
    /// Tries to convert the enum instance into [`Conversational`](crate::types::PayloadType::Conversational), extracting the inner [`Conversational`](crate::types::Conversational).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_conversational(&self) -> ::std::result::Result<&crate::types::Conversational, &Self> {
        if let PayloadType::Conversational(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Conversational`](crate::types::PayloadType::Conversational).
    pub fn is_conversational(&self) -> bool {
        self.as_conversational().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
impl ::std::fmt::Debug for PayloadType {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        match self {
            PayloadType::Blob(_) => f.debug_tuple("*** Sensitive Data Redacted ***").finish(),
            PayloadType::Conversational(val) => f.debug_tuple("Conversational").field(&val).finish(),
            PayloadType::Unknown => f.debug_tuple("Unknown").finish(),
        }
    }
}
