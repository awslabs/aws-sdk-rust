// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A content block within a message.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq)]
pub enum HarnessContentBlock {
    /// <p>Model reasoning content.</p>
    ReasoningContent(crate::types::HarnessReasoningContentBlock),
    /// <p>Text content.</p>
    Text(::std::string::String),
    /// <p>A tool execution result.</p>
    ToolResult(crate::types::HarnessToolResultBlock),
    /// <p>A tool use request from the model.</p>
    ToolUse(crate::types::HarnessToolUseBlock),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl HarnessContentBlock {
    /// Tries to convert the enum instance into [`ReasoningContent`](crate::types::HarnessContentBlock::ReasoningContent), extracting the inner [`HarnessReasoningContentBlock`](crate::types::HarnessReasoningContentBlock).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_reasoning_content(&self) -> ::std::result::Result<&crate::types::HarnessReasoningContentBlock, &Self> {
        if let HarnessContentBlock::ReasoningContent(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`ReasoningContent`](crate::types::HarnessContentBlock::ReasoningContent).
    pub fn is_reasoning_content(&self) -> bool {
        self.as_reasoning_content().is_ok()
    }
    /// Tries to convert the enum instance into [`Text`](crate::types::HarnessContentBlock::Text), extracting the inner [`String`](::std::string::String).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_text(&self) -> ::std::result::Result<&::std::string::String, &Self> {
        if let HarnessContentBlock::Text(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Text`](crate::types::HarnessContentBlock::Text).
    pub fn is_text(&self) -> bool {
        self.as_text().is_ok()
    }
    /// Tries to convert the enum instance into [`ToolResult`](crate::types::HarnessContentBlock::ToolResult), extracting the inner [`HarnessToolResultBlock`](crate::types::HarnessToolResultBlock).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_tool_result(&self) -> ::std::result::Result<&crate::types::HarnessToolResultBlock, &Self> {
        if let HarnessContentBlock::ToolResult(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`ToolResult`](crate::types::HarnessContentBlock::ToolResult).
    pub fn is_tool_result(&self) -> bool {
        self.as_tool_result().is_ok()
    }
    /// Tries to convert the enum instance into [`ToolUse`](crate::types::HarnessContentBlock::ToolUse), extracting the inner [`HarnessToolUseBlock`](crate::types::HarnessToolUseBlock).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_tool_use(&self) -> ::std::result::Result<&crate::types::HarnessToolUseBlock, &Self> {
        if let HarnessContentBlock::ToolUse(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`ToolUse`](crate::types::HarnessContentBlock::ToolUse).
    pub fn is_tool_use(&self) -> bool {
        self.as_tool_use().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
impl ::std::fmt::Debug for HarnessContentBlock {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        match self {
            HarnessContentBlock::ReasoningContent(_) => f.debug_tuple("*** Sensitive Data Redacted ***").finish(),
            HarnessContentBlock::Text(_) => f.debug_tuple("*** Sensitive Data Redacted ***").finish(),
            HarnessContentBlock::ToolResult(val) => f.debug_tuple("ToolResult").field(&val).finish(),
            HarnessContentBlock::ToolUse(val) => f.debug_tuple("ToolUse").field(&val).finish(),
            HarnessContentBlock::Unknown => f.debug_tuple("Unknown").finish(),
        }
    }
}
