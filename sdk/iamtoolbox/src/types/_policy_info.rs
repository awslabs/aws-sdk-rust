// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains details about a policy evaluated during authorization. Details include the policy type, whether it is inline or managed, a URI that identifies it, and the entities it is attached to.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct PolicyInfo {
    /// <p>The type of policy. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>IDENTITY_BASED_POLICY</code> - An identity-based policy attached to an IAM user, group, or role.</p></li>
    /// <li>
    /// <p><code>PERMISSIONS_BOUNDARY</code> - A permissions boundary for an IAM entity.</p></li>
    /// <li>
    /// <p><code>RESOURCE_BASED_POLICY</code> - A resource-based policy attached to a resource.</p></li>
    /// <li>
    /// <p><code>RESOURCE_CONTROL_POLICY</code> - A resource control policy (RCP) in AWS Organizations.</p></li>
    /// <li>
    /// <p><code>SERVICE_CONTROL_POLICY</code> - A service control policy (SCP) in AWS Organizations.</p></li>
    /// <li>
    /// <p><code>SESSION_POLICY</code> - A session policy passed during role assumption or federation.</p></li>
    /// <li>
    /// <p><code>VPC_ENDPOINT_POLICY</code> - A VPC endpoint policy.</p></li>
    /// </ul>
    pub r#type: ::std::option::Option<crate::types::PolicyType>,
    /// <p>Specifies whether this is an inline policy (<code>true</code>) or a managed policy (<code>false</code>).</p>
    pub inline: ::std::option::Option<bool>,
    /// <p>A URI that identifies the policy. Use this URI to cross-reference the policy with the matching policies in each evaluation. The value depends on the policy type:</p>
    /// <ul>
    /// <li>
    /// <p>For managed policies, this is the policy ARN.</p></li>
    /// <li>
    /// <p>For inline policies, which have no ARN, this is an opaque identifier.</p></li>
    /// </ul>
    pub uri: ::std::option::Option<::std::string::String>,
    /// <p>The entities that the policy is attached to. For identity, session, and resource-based policies, this is typically a single entity. For organization control policies (SCPs and RCPs), it can be multiple entities at different levels of the organization hierarchy.</p>
    pub attached_to: ::std::option::Option<::std::vec::Vec<crate::types::AttachedTo>>,
}
impl PolicyInfo {
    /// <p>The type of policy. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>IDENTITY_BASED_POLICY</code> - An identity-based policy attached to an IAM user, group, or role.</p></li>
    /// <li>
    /// <p><code>PERMISSIONS_BOUNDARY</code> - A permissions boundary for an IAM entity.</p></li>
    /// <li>
    /// <p><code>RESOURCE_BASED_POLICY</code> - A resource-based policy attached to a resource.</p></li>
    /// <li>
    /// <p><code>RESOURCE_CONTROL_POLICY</code> - A resource control policy (RCP) in AWS Organizations.</p></li>
    /// <li>
    /// <p><code>SERVICE_CONTROL_POLICY</code> - A service control policy (SCP) in AWS Organizations.</p></li>
    /// <li>
    /// <p><code>SESSION_POLICY</code> - A session policy passed during role assumption or federation.</p></li>
    /// <li>
    /// <p><code>VPC_ENDPOINT_POLICY</code> - A VPC endpoint policy.</p></li>
    /// </ul>
    pub fn r#type(&self) -> ::std::option::Option<&crate::types::PolicyType> {
        self.r#type.as_ref()
    }
    /// <p>Specifies whether this is an inline policy (<code>true</code>) or a managed policy (<code>false</code>).</p>
    pub fn inline(&self) -> ::std::option::Option<bool> {
        self.inline
    }
    /// <p>A URI that identifies the policy. Use this URI to cross-reference the policy with the matching policies in each evaluation. The value depends on the policy type:</p>
    /// <ul>
    /// <li>
    /// <p>For managed policies, this is the policy ARN.</p></li>
    /// <li>
    /// <p>For inline policies, which have no ARN, this is an opaque identifier.</p></li>
    /// </ul>
    pub fn uri(&self) -> ::std::option::Option<&str> {
        self.uri.as_deref()
    }
    /// <p>The entities that the policy is attached to. For identity, session, and resource-based policies, this is typically a single entity. For organization control policies (SCPs and RCPs), it can be multiple entities at different levels of the organization hierarchy.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.attached_to.is_none()`.
    pub fn attached_to(&self) -> &[crate::types::AttachedTo] {
        self.attached_to.as_deref().unwrap_or_default()
    }
}
impl PolicyInfo {
    /// Creates a new builder-style object to manufacture [`PolicyInfo`](crate::types::PolicyInfo).
    pub fn builder() -> crate::types::builders::PolicyInfoBuilder {
        crate::types::builders::PolicyInfoBuilder::default()
    }
}

/// A builder for [`PolicyInfo`](crate::types::PolicyInfo).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct PolicyInfoBuilder {
    pub(crate) r#type: ::std::option::Option<crate::types::PolicyType>,
    pub(crate) inline: ::std::option::Option<bool>,
    pub(crate) uri: ::std::option::Option<::std::string::String>,
    pub(crate) attached_to: ::std::option::Option<::std::vec::Vec<crate::types::AttachedTo>>,
}
impl PolicyInfoBuilder {
    /// <p>The type of policy. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>IDENTITY_BASED_POLICY</code> - An identity-based policy attached to an IAM user, group, or role.</p></li>
    /// <li>
    /// <p><code>PERMISSIONS_BOUNDARY</code> - A permissions boundary for an IAM entity.</p></li>
    /// <li>
    /// <p><code>RESOURCE_BASED_POLICY</code> - A resource-based policy attached to a resource.</p></li>
    /// <li>
    /// <p><code>RESOURCE_CONTROL_POLICY</code> - A resource control policy (RCP) in AWS Organizations.</p></li>
    /// <li>
    /// <p><code>SERVICE_CONTROL_POLICY</code> - A service control policy (SCP) in AWS Organizations.</p></li>
    /// <li>
    /// <p><code>SESSION_POLICY</code> - A session policy passed during role assumption or federation.</p></li>
    /// <li>
    /// <p><code>VPC_ENDPOINT_POLICY</code> - A VPC endpoint policy.</p></li>
    /// </ul>
    pub fn r#type(mut self, input: crate::types::PolicyType) -> Self {
        self.r#type = ::std::option::Option::Some(input);
        self
    }
    /// <p>The type of policy. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>IDENTITY_BASED_POLICY</code> - An identity-based policy attached to an IAM user, group, or role.</p></li>
    /// <li>
    /// <p><code>PERMISSIONS_BOUNDARY</code> - A permissions boundary for an IAM entity.</p></li>
    /// <li>
    /// <p><code>RESOURCE_BASED_POLICY</code> - A resource-based policy attached to a resource.</p></li>
    /// <li>
    /// <p><code>RESOURCE_CONTROL_POLICY</code> - A resource control policy (RCP) in AWS Organizations.</p></li>
    /// <li>
    /// <p><code>SERVICE_CONTROL_POLICY</code> - A service control policy (SCP) in AWS Organizations.</p></li>
    /// <li>
    /// <p><code>SESSION_POLICY</code> - A session policy passed during role assumption or federation.</p></li>
    /// <li>
    /// <p><code>VPC_ENDPOINT_POLICY</code> - A VPC endpoint policy.</p></li>
    /// </ul>
    pub fn set_type(mut self, input: ::std::option::Option<crate::types::PolicyType>) -> Self {
        self.r#type = input;
        self
    }
    /// <p>The type of policy. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>IDENTITY_BASED_POLICY</code> - An identity-based policy attached to an IAM user, group, or role.</p></li>
    /// <li>
    /// <p><code>PERMISSIONS_BOUNDARY</code> - A permissions boundary for an IAM entity.</p></li>
    /// <li>
    /// <p><code>RESOURCE_BASED_POLICY</code> - A resource-based policy attached to a resource.</p></li>
    /// <li>
    /// <p><code>RESOURCE_CONTROL_POLICY</code> - A resource control policy (RCP) in AWS Organizations.</p></li>
    /// <li>
    /// <p><code>SERVICE_CONTROL_POLICY</code> - A service control policy (SCP) in AWS Organizations.</p></li>
    /// <li>
    /// <p><code>SESSION_POLICY</code> - A session policy passed during role assumption or federation.</p></li>
    /// <li>
    /// <p><code>VPC_ENDPOINT_POLICY</code> - A VPC endpoint policy.</p></li>
    /// </ul>
    pub fn get_type(&self) -> &::std::option::Option<crate::types::PolicyType> {
        &self.r#type
    }
    /// <p>Specifies whether this is an inline policy (<code>true</code>) or a managed policy (<code>false</code>).</p>
    pub fn inline(mut self, input: bool) -> Self {
        self.inline = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether this is an inline policy (<code>true</code>) or a managed policy (<code>false</code>).</p>
    pub fn set_inline(mut self, input: ::std::option::Option<bool>) -> Self {
        self.inline = input;
        self
    }
    /// <p>Specifies whether this is an inline policy (<code>true</code>) or a managed policy (<code>false</code>).</p>
    pub fn get_inline(&self) -> &::std::option::Option<bool> {
        &self.inline
    }
    /// <p>A URI that identifies the policy. Use this URI to cross-reference the policy with the matching policies in each evaluation. The value depends on the policy type:</p>
    /// <ul>
    /// <li>
    /// <p>For managed policies, this is the policy ARN.</p></li>
    /// <li>
    /// <p>For inline policies, which have no ARN, this is an opaque identifier.</p></li>
    /// </ul>
    pub fn uri(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.uri = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>A URI that identifies the policy. Use this URI to cross-reference the policy with the matching policies in each evaluation. The value depends on the policy type:</p>
    /// <ul>
    /// <li>
    /// <p>For managed policies, this is the policy ARN.</p></li>
    /// <li>
    /// <p>For inline policies, which have no ARN, this is an opaque identifier.</p></li>
    /// </ul>
    pub fn set_uri(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.uri = input;
        self
    }
    /// <p>A URI that identifies the policy. Use this URI to cross-reference the policy with the matching policies in each evaluation. The value depends on the policy type:</p>
    /// <ul>
    /// <li>
    /// <p>For managed policies, this is the policy ARN.</p></li>
    /// <li>
    /// <p>For inline policies, which have no ARN, this is an opaque identifier.</p></li>
    /// </ul>
    pub fn get_uri(&self) -> &::std::option::Option<::std::string::String> {
        &self.uri
    }
    /// Appends an item to `attached_to`.
    ///
    /// To override the contents of this collection use [`set_attached_to`](Self::set_attached_to).
    ///
    /// <p>The entities that the policy is attached to. For identity, session, and resource-based policies, this is typically a single entity. For organization control policies (SCPs and RCPs), it can be multiple entities at different levels of the organization hierarchy.</p>
    pub fn attached_to(mut self, input: crate::types::AttachedTo) -> Self {
        let mut v = self.attached_to.unwrap_or_default();
        v.push(input);
        self.attached_to = ::std::option::Option::Some(v);
        self
    }
    /// <p>The entities that the policy is attached to. For identity, session, and resource-based policies, this is typically a single entity. For organization control policies (SCPs and RCPs), it can be multiple entities at different levels of the organization hierarchy.</p>
    pub fn set_attached_to(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::AttachedTo>>) -> Self {
        self.attached_to = input;
        self
    }
    /// <p>The entities that the policy is attached to. For identity, session, and resource-based policies, this is typically a single entity. For organization control policies (SCPs and RCPs), it can be multiple entities at different levels of the organization hierarchy.</p>
    pub fn get_attached_to(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::AttachedTo>> {
        &self.attached_to
    }
    /// Consumes the builder and constructs a [`PolicyInfo`](crate::types::PolicyInfo).
    pub fn build(self) -> crate::types::PolicyInfo {
        crate::types::PolicyInfo {
            r#type: self.r#type,
            inline: self.inline,
            uri: self.uri,
            attached_to: self.attached_to,
        }
    }
}
