// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct GetAttachmentUploadLinksOutput {
    /// <p>The unique identifier for the multipart upload. Use this value in subsequent calls to <code>GetAttachmentUploadLinks</code>, <code>DescribeAttachmentUploadStatus</code>, and <code>CompleteAttachmentUpload</code>, and to attach the upload to a case through the <code>uploadIds</code> parameter on <code>CreateCase</code> or <code>AddCommunicationToCase</code>.</p>
    pub upload_id: ::std::string::String,
    /// <p>The size, in bytes, of each part. Split the file into parts of this size before you upload them to the presigned URLs. For an upload with <code>n</code> total parts, parts 1 through <code>n</code> - 1 are exactly this size; the last part may be smaller. Maximum: 104,857,600 bytes (approximately 100 MB).</p>
    pub part_size_bytes: i64,
    /// <p>The total number of parts that the file is split into. Upload one part to each presigned URL.</p>
    pub total_parts: i32,
    /// <p>The next part index to request presigned URLs for. If all upload URLs for the file have been returned, this field is <code>null</code>. Use this value as the <code>startIndex</code> in <code>uploadRange</code> on a subsequent call to <code>GetAttachmentUploadLinks</code> to retrieve the next batch of upload URLs.</p>
    pub next_index: i32,
    /// <p>The list of presigned upload URLs for the requested range of parts. The list contains at most 10 URLs per call. Upload each part to its corresponding URL by using HTTP <code>PUT</code> before the URL expires.</p>
    pub upload_urls: ::std::vec::Vec<crate::types::UploadUrl>,
    _request_id: Option<String>,
}
impl GetAttachmentUploadLinksOutput {
    /// <p>The unique identifier for the multipart upload. Use this value in subsequent calls to <code>GetAttachmentUploadLinks</code>, <code>DescribeAttachmentUploadStatus</code>, and <code>CompleteAttachmentUpload</code>, and to attach the upload to a case through the <code>uploadIds</code> parameter on <code>CreateCase</code> or <code>AddCommunicationToCase</code>.</p>
    pub fn upload_id(&self) -> &str {
        use std::ops::Deref;
        self.upload_id.deref()
    }
    /// <p>The size, in bytes, of each part. Split the file into parts of this size before you upload them to the presigned URLs. For an upload with <code>n</code> total parts, parts 1 through <code>n</code> - 1 are exactly this size; the last part may be smaller. Maximum: 104,857,600 bytes (approximately 100 MB).</p>
    pub fn part_size_bytes(&self) -> i64 {
        self.part_size_bytes
    }
    /// <p>The total number of parts that the file is split into. Upload one part to each presigned URL.</p>
    pub fn total_parts(&self) -> i32 {
        self.total_parts
    }
    /// <p>The next part index to request presigned URLs for. If all upload URLs for the file have been returned, this field is <code>null</code>. Use this value as the <code>startIndex</code> in <code>uploadRange</code> on a subsequent call to <code>GetAttachmentUploadLinks</code> to retrieve the next batch of upload URLs.</p>
    pub fn next_index(&self) -> i32 {
        self.next_index
    }
    /// <p>The list of presigned upload URLs for the requested range of parts. The list contains at most 10 URLs per call. Upload each part to its corresponding URL by using HTTP <code>PUT</code> before the URL expires.</p>
    pub fn upload_urls(&self) -> &[crate::types::UploadUrl] {
        use std::ops::Deref;
        self.upload_urls.deref()
    }
}
impl ::aws_types::request_id::RequestId for GetAttachmentUploadLinksOutput {
    fn request_id(&self) -> Option<&str> {
        self._request_id.as_deref()
    }
}
impl GetAttachmentUploadLinksOutput {
    /// Creates a new builder-style object to manufacture [`GetAttachmentUploadLinksOutput`](crate::operation::get_attachment_upload_links::GetAttachmentUploadLinksOutput).
    pub fn builder() -> crate::operation::get_attachment_upload_links::builders::GetAttachmentUploadLinksOutputBuilder {
        crate::operation::get_attachment_upload_links::builders::GetAttachmentUploadLinksOutputBuilder::default()
    }
}

/// A builder for [`GetAttachmentUploadLinksOutput`](crate::operation::get_attachment_upload_links::GetAttachmentUploadLinksOutput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct GetAttachmentUploadLinksOutputBuilder {
    pub(crate) upload_id: ::std::option::Option<::std::string::String>,
    pub(crate) part_size_bytes: ::std::option::Option<i64>,
    pub(crate) total_parts: ::std::option::Option<i32>,
    pub(crate) next_index: ::std::option::Option<i32>,
    pub(crate) upload_urls: ::std::option::Option<::std::vec::Vec<crate::types::UploadUrl>>,
    _request_id: Option<String>,
}
impl GetAttachmentUploadLinksOutputBuilder {
    /// <p>The unique identifier for the multipart upload. Use this value in subsequent calls to <code>GetAttachmentUploadLinks</code>, <code>DescribeAttachmentUploadStatus</code>, and <code>CompleteAttachmentUpload</code>, and to attach the upload to a case through the <code>uploadIds</code> parameter on <code>CreateCase</code> or <code>AddCommunicationToCase</code>.</p>
    /// This field is required.
    pub fn upload_id(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.upload_id = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The unique identifier for the multipart upload. Use this value in subsequent calls to <code>GetAttachmentUploadLinks</code>, <code>DescribeAttachmentUploadStatus</code>, and <code>CompleteAttachmentUpload</code>, and to attach the upload to a case through the <code>uploadIds</code> parameter on <code>CreateCase</code> or <code>AddCommunicationToCase</code>.</p>
    pub fn set_upload_id(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.upload_id = input;
        self
    }
    /// <p>The unique identifier for the multipart upload. Use this value in subsequent calls to <code>GetAttachmentUploadLinks</code>, <code>DescribeAttachmentUploadStatus</code>, and <code>CompleteAttachmentUpload</code>, and to attach the upload to a case through the <code>uploadIds</code> parameter on <code>CreateCase</code> or <code>AddCommunicationToCase</code>.</p>
    pub fn get_upload_id(&self) -> &::std::option::Option<::std::string::String> {
        &self.upload_id
    }
    /// <p>The size, in bytes, of each part. Split the file into parts of this size before you upload them to the presigned URLs. For an upload with <code>n</code> total parts, parts 1 through <code>n</code> - 1 are exactly this size; the last part may be smaller. Maximum: 104,857,600 bytes (approximately 100 MB).</p>
    /// This field is required.
    pub fn part_size_bytes(mut self, input: i64) -> Self {
        self.part_size_bytes = ::std::option::Option::Some(input);
        self
    }
    /// <p>The size, in bytes, of each part. Split the file into parts of this size before you upload them to the presigned URLs. For an upload with <code>n</code> total parts, parts 1 through <code>n</code> - 1 are exactly this size; the last part may be smaller. Maximum: 104,857,600 bytes (approximately 100 MB).</p>
    pub fn set_part_size_bytes(mut self, input: ::std::option::Option<i64>) -> Self {
        self.part_size_bytes = input;
        self
    }
    /// <p>The size, in bytes, of each part. Split the file into parts of this size before you upload them to the presigned URLs. For an upload with <code>n</code> total parts, parts 1 through <code>n</code> - 1 are exactly this size; the last part may be smaller. Maximum: 104,857,600 bytes (approximately 100 MB).</p>
    pub fn get_part_size_bytes(&self) -> &::std::option::Option<i64> {
        &self.part_size_bytes
    }
    /// <p>The total number of parts that the file is split into. Upload one part to each presigned URL.</p>
    /// This field is required.
    pub fn total_parts(mut self, input: i32) -> Self {
        self.total_parts = ::std::option::Option::Some(input);
        self
    }
    /// <p>The total number of parts that the file is split into. Upload one part to each presigned URL.</p>
    pub fn set_total_parts(mut self, input: ::std::option::Option<i32>) -> Self {
        self.total_parts = input;
        self
    }
    /// <p>The total number of parts that the file is split into. Upload one part to each presigned URL.</p>
    pub fn get_total_parts(&self) -> &::std::option::Option<i32> {
        &self.total_parts
    }
    /// <p>The next part index to request presigned URLs for. If all upload URLs for the file have been returned, this field is <code>null</code>. Use this value as the <code>startIndex</code> in <code>uploadRange</code> on a subsequent call to <code>GetAttachmentUploadLinks</code> to retrieve the next batch of upload URLs.</p>
    pub fn next_index(mut self, input: i32) -> Self {
        self.next_index = ::std::option::Option::Some(input);
        self
    }
    /// <p>The next part index to request presigned URLs for. If all upload URLs for the file have been returned, this field is <code>null</code>. Use this value as the <code>startIndex</code> in <code>uploadRange</code> on a subsequent call to <code>GetAttachmentUploadLinks</code> to retrieve the next batch of upload URLs.</p>
    pub fn set_next_index(mut self, input: ::std::option::Option<i32>) -> Self {
        self.next_index = input;
        self
    }
    /// <p>The next part index to request presigned URLs for. If all upload URLs for the file have been returned, this field is <code>null</code>. Use this value as the <code>startIndex</code> in <code>uploadRange</code> on a subsequent call to <code>GetAttachmentUploadLinks</code> to retrieve the next batch of upload URLs.</p>
    pub fn get_next_index(&self) -> &::std::option::Option<i32> {
        &self.next_index
    }
    /// Appends an item to `upload_urls`.
    ///
    /// To override the contents of this collection use [`set_upload_urls`](Self::set_upload_urls).
    ///
    /// <p>The list of presigned upload URLs for the requested range of parts. The list contains at most 10 URLs per call. Upload each part to its corresponding URL by using HTTP <code>PUT</code> before the URL expires.</p>
    pub fn upload_urls(mut self, input: crate::types::UploadUrl) -> Self {
        let mut v = self.upload_urls.unwrap_or_default();
        v.push(input);
        self.upload_urls = ::std::option::Option::Some(v);
        self
    }
    /// <p>The list of presigned upload URLs for the requested range of parts. The list contains at most 10 URLs per call. Upload each part to its corresponding URL by using HTTP <code>PUT</code> before the URL expires.</p>
    pub fn set_upload_urls(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::UploadUrl>>) -> Self {
        self.upload_urls = input;
        self
    }
    /// <p>The list of presigned upload URLs for the requested range of parts. The list contains at most 10 URLs per call. Upload each part to its corresponding URL by using HTTP <code>PUT</code> before the URL expires.</p>
    pub fn get_upload_urls(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::UploadUrl>> {
        &self.upload_urls
    }
    pub(crate) fn _request_id(mut self, request_id: impl Into<String>) -> Self {
        self._request_id = Some(request_id.into());
        self
    }

    pub(crate) fn _set_request_id(&mut self, request_id: Option<String>) -> &mut Self {
        self._request_id = request_id;
        self
    }
    /// Consumes the builder and constructs a [`GetAttachmentUploadLinksOutput`](crate::operation::get_attachment_upload_links::GetAttachmentUploadLinksOutput).
    /// This method will fail if any of the following fields are not set:
    /// - [`upload_id`](crate::operation::get_attachment_upload_links::builders::GetAttachmentUploadLinksOutputBuilder::upload_id)
    /// - [`part_size_bytes`](crate::operation::get_attachment_upload_links::builders::GetAttachmentUploadLinksOutputBuilder::part_size_bytes)
    /// - [`upload_urls`](crate::operation::get_attachment_upload_links::builders::GetAttachmentUploadLinksOutputBuilder::upload_urls)
    pub fn build(
        self,
    ) -> ::std::result::Result<
        crate::operation::get_attachment_upload_links::GetAttachmentUploadLinksOutput,
        ::aws_smithy_types::error::operation::BuildError,
    > {
        ::std::result::Result::Ok(crate::operation::get_attachment_upload_links::GetAttachmentUploadLinksOutput {
            upload_id: self.upload_id.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "upload_id",
                    "upload_id was not specified but it is required when building GetAttachmentUploadLinksOutput",
                )
            })?,
            part_size_bytes: self.part_size_bytes.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "part_size_bytes",
                    "part_size_bytes was not specified but it is required when building GetAttachmentUploadLinksOutput",
                )
            })?,
            total_parts: self.total_parts.unwrap_or_default(),
            next_index: self.next_index.unwrap_or_default(),
            upload_urls: self.upload_urls.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "upload_urls",
                    "upload_urls was not specified but it is required when building GetAttachmentUploadLinksOutput",
                )
            })?,
            _request_id: self._request_id,
        })
    }
}
