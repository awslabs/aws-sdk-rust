// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
pub use crate::operation::complete_attachment_upload::_complete_attachment_upload_input::CompleteAttachmentUploadInputBuilder;

pub use crate::operation::complete_attachment_upload::_complete_attachment_upload_output::CompleteAttachmentUploadOutputBuilder;

impl crate::operation::complete_attachment_upload::builders::CompleteAttachmentUploadInputBuilder {
    /// Sends a request with this input using the given client.
    pub async fn send_with(
        self,
        client: &crate::Client,
    ) -> ::std::result::Result<
        crate::operation::complete_attachment_upload::CompleteAttachmentUploadOutput,
        ::aws_smithy_runtime_api::client::result::SdkError<
            crate::operation::complete_attachment_upload::CompleteAttachmentUploadError,
            ::aws_smithy_runtime_api::client::orchestrator::HttpResponse,
        >,
    > {
        let mut fluent_builder = client.complete_attachment_upload();
        fluent_builder.inner = self;
        fluent_builder.send().await
    }
}
/// Fluent builder constructing a request to `CompleteAttachmentUpload`.
///
/// <p>Completes an attachment upload that was started with <code>GetAttachmentUploadLinks</code>. After you upload a part of the file to its presigned Amazon S3 URL, call <code>CompleteAttachmentUpload</code> with the <code>partIndex</code> and <code>eTag</code> of that part. You can include one part per call, or multiple parts in a single call. After <code>CompleteAttachmentUpload</code> has been called for every part of the file, the service processes the upload asynchronously. The <code>attachment-ready</code> status might not be reflected immediately. Use <code>DescribeAttachmentUploadStatus</code> to poll for the <code>uploadStatus</code> to become <code>attachment-ready</code> before passing the <code>uploadId</code> to <code>CreateCase</code> or <code>AddCommunicationToCase</code>.</p>
#[derive(::std::clone::Clone, ::std::fmt::Debug)]
pub struct CompleteAttachmentUploadFluentBuilder {
    handle: ::std::sync::Arc<crate::client::Handle>,
    inner: crate::operation::complete_attachment_upload::builders::CompleteAttachmentUploadInputBuilder,
    config_override: ::std::option::Option<crate::config::Builder>,
}
impl
    crate::client::customize::internal::CustomizableSend<
        crate::operation::complete_attachment_upload::CompleteAttachmentUploadOutput,
        crate::operation::complete_attachment_upload::CompleteAttachmentUploadError,
    > for CompleteAttachmentUploadFluentBuilder
{
    fn send(
        self,
        config_override: crate::config::Builder,
    ) -> crate::client::customize::internal::BoxFuture<
        crate::client::customize::internal::SendResult<
            crate::operation::complete_attachment_upload::CompleteAttachmentUploadOutput,
            crate::operation::complete_attachment_upload::CompleteAttachmentUploadError,
        >,
    > {
        ::std::boxed::Box::pin(async move { self.config_override(config_override).send().await })
    }
}
impl CompleteAttachmentUploadFluentBuilder {
    /// Creates a new `CompleteAttachmentUploadFluentBuilder`.
    pub(crate) fn new(handle: ::std::sync::Arc<crate::client::Handle>) -> Self {
        Self {
            handle,
            inner: ::std::default::Default::default(),
            config_override: ::std::option::Option::None,
        }
    }
    /// Access the CompleteAttachmentUpload as a reference.
    pub fn as_input(&self) -> &crate::operation::complete_attachment_upload::builders::CompleteAttachmentUploadInputBuilder {
        &self.inner
    }
    /// Sends the request and returns the response.
    ///
    /// If an error occurs, an `SdkError` will be returned with additional details that
    /// can be matched against.
    ///
    /// By default, any retryable failures will be retried twice. Retry behavior
    /// is configurable with the [RetryConfig](aws_smithy_types::retry::RetryConfig), which can be
    /// set when configuring the client.
    pub async fn send(
        self,
    ) -> ::std::result::Result<
        crate::operation::complete_attachment_upload::CompleteAttachmentUploadOutput,
        ::aws_smithy_runtime_api::client::result::SdkError<
            crate::operation::complete_attachment_upload::CompleteAttachmentUploadError,
            ::aws_smithy_runtime_api::client::orchestrator::HttpResponse,
        >,
    > {
        let input = self
            .inner
            .build()
            .map_err(::aws_smithy_runtime_api::client::result::SdkError::construction_failure)?;
        let runtime_plugins = crate::operation::complete_attachment_upload::CompleteAttachmentUpload::operation_runtime_plugins(
            self.handle.runtime_plugins.clone(),
            &self.handle.conf,
            self.config_override,
        );
        crate::operation::complete_attachment_upload::CompleteAttachmentUpload::orchestrate(&runtime_plugins, input).await
    }

    /// Consumes this builder, creating a customizable operation that can be modified before being sent.
    pub fn customize(
        self,
    ) -> crate::client::customize::CustomizableOperation<
        crate::operation::complete_attachment_upload::CompleteAttachmentUploadOutput,
        crate::operation::complete_attachment_upload::CompleteAttachmentUploadError,
        Self,
    > {
        crate::client::customize::CustomizableOperation::new(self)
    }
    pub(crate) fn config_override(mut self, config_override: impl ::std::convert::Into<crate::config::Builder>) -> Self {
        self.set_config_override(::std::option::Option::Some(config_override.into()));
        self
    }

    pub(crate) fn set_config_override(&mut self, config_override: ::std::option::Option<crate::config::Builder>) -> &mut Self {
        self.config_override = config_override;
        self
    }
    /// <p>The identifier associated with the upload to complete.</p>
    pub fn upload_id(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.inner = self.inner.upload_id(input.into());
        self
    }
    /// <p>The identifier associated with the upload to complete.</p>
    pub fn set_upload_id(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.inner = self.inner.set_upload_id(input);
        self
    }
    /// <p>The identifier associated with the upload to complete.</p>
    pub fn get_upload_id(&self) -> &::std::option::Option<::std::string::String> {
        self.inner.get_upload_id()
    }
    ///
    /// Appends an item to `completedUploads`.
    ///
    /// To override the contents of this collection use [`set_completed_uploads`](Self::set_completed_uploads).
    ///
    /// <p>The list of parts being reported as completed in this call. Each entry must contain the <code>partIndex</code> of an uploaded part and the <code>ETag</code> returned by Amazon S3 when that part was uploaded.</p>
    pub fn completed_uploads(mut self, input: crate::types::CompletedUpload) -> Self {
        self.inner = self.inner.completed_uploads(input);
        self
    }
    /// <p>The list of parts being reported as completed in this call. Each entry must contain the <code>partIndex</code> of an uploaded part and the <code>ETag</code> returned by Amazon S3 when that part was uploaded.</p>
    pub fn set_completed_uploads(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::CompletedUpload>>) -> Self {
        self.inner = self.inner.set_completed_uploads(input);
        self
    }
    /// <p>The list of parts being reported as completed in this call. Each entry must contain the <code>partIndex</code> of an uploaded part and the <code>ETag</code> returned by Amazon S3 when that part was uploaded.</p>
    pub fn get_completed_uploads(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::CompletedUpload>> {
        self.inner.get_completed_uploads()
    }
    /// <p>Specifies whether to validate the request without actually completing the upload. When set to <code>true</code>, the request is validated but the upload isn't finalized, and the operation returns a <code>DryRunOperationException</code>. When omitted or set to <code>false</code>, the request runs normally.</p>
    pub fn dry_run(mut self, input: bool) -> Self {
        self.inner = self.inner.dry_run(input);
        self
    }
    /// <p>Specifies whether to validate the request without actually completing the upload. When set to <code>true</code>, the request is validated but the upload isn't finalized, and the operation returns a <code>DryRunOperationException</code>. When omitted or set to <code>false</code>, the request runs normally.</p>
    pub fn set_dry_run(mut self, input: ::std::option::Option<bool>) -> Self {
        self.inner = self.inner.set_dry_run(input);
        self
    }
    /// <p>Specifies whether to validate the request without actually completing the upload. When set to <code>true</code>, the request is validated but the upload isn't finalized, and the operation returns a <code>DryRunOperationException</code>. When omitted or set to <code>false</code>, the request runs normally.</p>
    pub fn get_dry_run(&self) -> &::std::option::Option<bool> {
        self.inner.get_dry_run()
    }
}
