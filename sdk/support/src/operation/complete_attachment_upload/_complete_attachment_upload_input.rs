// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct CompleteAttachmentUploadInput {
    /// <p>The identifier associated with the upload to complete.</p>
    pub upload_id: ::std::option::Option<::std::string::String>,
    /// <p>The list of parts being reported as completed in this call. Each entry must contain the <code>partIndex</code> of an uploaded part and the <code>ETag</code> returned by Amazon S3 when that part was uploaded.</p>
    pub completed_uploads: ::std::option::Option<::std::vec::Vec<crate::types::CompletedUpload>>,
    /// <p>Specifies whether to validate the request without actually completing the upload. When set to <code>true</code>, the request is validated but the upload isn't finalized, and the operation returns a <code>DryRunOperationException</code>. When omitted or set to <code>false</code>, the request runs normally.</p>
    pub dry_run: ::std::option::Option<bool>,
}
impl CompleteAttachmentUploadInput {
    /// <p>The identifier associated with the upload to complete.</p>
    pub fn upload_id(&self) -> ::std::option::Option<&str> {
        self.upload_id.as_deref()
    }
    /// <p>The list of parts being reported as completed in this call. Each entry must contain the <code>partIndex</code> of an uploaded part and the <code>ETag</code> returned by Amazon S3 when that part was uploaded.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.completed_uploads.is_none()`.
    pub fn completed_uploads(&self) -> &[crate::types::CompletedUpload] {
        self.completed_uploads.as_deref().unwrap_or_default()
    }
    /// <p>Specifies whether to validate the request without actually completing the upload. When set to <code>true</code>, the request is validated but the upload isn't finalized, and the operation returns a <code>DryRunOperationException</code>. When omitted or set to <code>false</code>, the request runs normally.</p>
    pub fn dry_run(&self) -> ::std::option::Option<bool> {
        self.dry_run
    }
}
impl CompleteAttachmentUploadInput {
    /// Creates a new builder-style object to manufacture [`CompleteAttachmentUploadInput`](crate::operation::complete_attachment_upload::CompleteAttachmentUploadInput).
    pub fn builder() -> crate::operation::complete_attachment_upload::builders::CompleteAttachmentUploadInputBuilder {
        crate::operation::complete_attachment_upload::builders::CompleteAttachmentUploadInputBuilder::default()
    }
}

/// A builder for [`CompleteAttachmentUploadInput`](crate::operation::complete_attachment_upload::CompleteAttachmentUploadInput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct CompleteAttachmentUploadInputBuilder {
    pub(crate) upload_id: ::std::option::Option<::std::string::String>,
    pub(crate) completed_uploads: ::std::option::Option<::std::vec::Vec<crate::types::CompletedUpload>>,
    pub(crate) dry_run: ::std::option::Option<bool>,
}
impl CompleteAttachmentUploadInputBuilder {
    /// <p>The identifier associated with the upload to complete.</p>
    /// This field is required.
    pub fn upload_id(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.upload_id = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The identifier associated with the upload to complete.</p>
    pub fn set_upload_id(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.upload_id = input;
        self
    }
    /// <p>The identifier associated with the upload to complete.</p>
    pub fn get_upload_id(&self) -> &::std::option::Option<::std::string::String> {
        &self.upload_id
    }
    /// Appends an item to `completed_uploads`.
    ///
    /// To override the contents of this collection use [`set_completed_uploads`](Self::set_completed_uploads).
    ///
    /// <p>The list of parts being reported as completed in this call. Each entry must contain the <code>partIndex</code> of an uploaded part and the <code>ETag</code> returned by Amazon S3 when that part was uploaded.</p>
    pub fn completed_uploads(mut self, input: crate::types::CompletedUpload) -> Self {
        let mut v = self.completed_uploads.unwrap_or_default();
        v.push(input);
        self.completed_uploads = ::std::option::Option::Some(v);
        self
    }
    /// <p>The list of parts being reported as completed in this call. Each entry must contain the <code>partIndex</code> of an uploaded part and the <code>ETag</code> returned by Amazon S3 when that part was uploaded.</p>
    pub fn set_completed_uploads(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::CompletedUpload>>) -> Self {
        self.completed_uploads = input;
        self
    }
    /// <p>The list of parts being reported as completed in this call. Each entry must contain the <code>partIndex</code> of an uploaded part and the <code>ETag</code> returned by Amazon S3 when that part was uploaded.</p>
    pub fn get_completed_uploads(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::CompletedUpload>> {
        &self.completed_uploads
    }
    /// <p>Specifies whether to validate the request without actually completing the upload. When set to <code>true</code>, the request is validated but the upload isn't finalized, and the operation returns a <code>DryRunOperationException</code>. When omitted or set to <code>false</code>, the request runs normally.</p>
    pub fn dry_run(mut self, input: bool) -> Self {
        self.dry_run = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether to validate the request without actually completing the upload. When set to <code>true</code>, the request is validated but the upload isn't finalized, and the operation returns a <code>DryRunOperationException</code>. When omitted or set to <code>false</code>, the request runs normally.</p>
    pub fn set_dry_run(mut self, input: ::std::option::Option<bool>) -> Self {
        self.dry_run = input;
        self
    }
    /// <p>Specifies whether to validate the request without actually completing the upload. When set to <code>true</code>, the request is validated but the upload isn't finalized, and the operation returns a <code>DryRunOperationException</code>. When omitted or set to <code>false</code>, the request runs normally.</p>
    pub fn get_dry_run(&self) -> &::std::option::Option<bool> {
        &self.dry_run
    }
    /// Consumes the builder and constructs a [`CompleteAttachmentUploadInput`](crate::operation::complete_attachment_upload::CompleteAttachmentUploadInput).
    pub fn build(
        self,
    ) -> ::std::result::Result<
        crate::operation::complete_attachment_upload::CompleteAttachmentUploadInput,
        ::aws_smithy_types::error::operation::BuildError,
    > {
        ::std::result::Result::Ok(crate::operation::complete_attachment_upload::CompleteAttachmentUploadInput {
            upload_id: self.upload_id,
            completed_uploads: self.completed_uploads,
            dry_run: self.dry_run,
        })
    }
}
