// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Describes plugin details.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct CommandPlugin {
    /// <p>The name of the plugin. Must be one of the following: <code>aws:updateAgent</code>, <code>aws:domainjoin</code>, <code>aws:applications</code>, <code>aws:runPowerShellScript</code>, <code>aws:psmodule</code>, <code>aws:cloudWatch</code>, <code>aws:runShellScript</code>, or <code>aws:updateSSMAgent</code>.</p>
    pub name: ::std::option::Option<::std::string::String>,
    /// <p>The status of this plugin. You can run a document with multiple plugins.</p>
    pub status: ::std::option::Option<crate::types::CommandPluginStatus>,
    /// <p>A detailed status of the plugin execution. <code>StatusDetails</code> includes more information than Status because it includes states resulting from error and concurrency control parameters. StatusDetails can show different results than Status. For more information about these statuses, see <a href="https://docs.aws.amazon.com/systems-manager/latest/userguide/monitor-commands.html">Understanding command statuses</a> in the <i>Amazon Web Services Systems Manager User Guide</i>. StatusDetails can be one of the following values:</p>
    /// <ul>
    /// <li>
    /// <p>Pending: The command hasn't been sent to the managed node.</p></li>
    /// <li>
    /// <p>In Progress: The command has been sent to the managed node but hasn't reached a terminal state.</p></li>
    /// <li>
    /// <p>Success: The execution of the command or plugin was successfully completed. This is a terminal state.</p></li>
    /// <li>
    /// <p>Delivery Timed Out: The command wasn't delivered to the managed node before the delivery timeout expired. Delivery timeouts don't count against the parent command's <code>MaxErrors</code> limit, but they do contribute to whether the parent command status is Success or Incomplete. This is a terminal state.</p></li>
    /// <li>
    /// <p>Execution Timed Out: Command execution started on the managed node, but the execution wasn't complete before the execution timeout expired. Execution timeouts count against the <code>MaxErrors</code> limit of the parent command. This is a terminal state.</p></li>
    /// <li>
    /// <p>Failed: The command wasn't successful on the managed node. For a plugin, this indicates that the result code wasn't zero. For a command invocation, this indicates that the result code for one or more plugins wasn't zero. Invocation failures count against the MaxErrors limit of the parent command. This is a terminal state.</p></li>
    /// <li>
    /// <p>Cancelled: The command was terminated before it was completed. This is a terminal state.</p></li>
    /// <li>
    /// <p>Undeliverable: The command can't be delivered to the managed node. The managed node might not exist, or it might not be responding. Undeliverable invocations don't count against the parent command's MaxErrors limit, and they don't contribute to whether the parent command status is Success or Incomplete. This is a terminal state.</p></li>
    /// <li>
    /// <p>Terminated: The parent command exceeded its MaxErrors limit and subsequent command invocations were canceled by the system. This is a terminal state.</p></li>
    /// </ul>
    pub status_details: ::std::option::Option<::std::string::String>,
    /// <p>A numeric response code generated after running the plugin.</p>
    pub response_code: i32,
    /// <p>The time the plugin started running.</p>
    pub response_start_date_time: ::std::option::Option<::aws_smithy_types::DateTime>,
    /// <p>The time the plugin stopped running. Could stop prematurely if, for example, a cancel command was sent.</p>
    pub response_finish_date_time: ::std::option::Option<::aws_smithy_types::DateTime>,
    /// <p>Output of the plugin execution.</p>
    pub output: ::std::option::Option<::std::string::String>,
    /// <p>The URL for the complete text written by the plugin to stdout in Amazon S3. If the S3 bucket for the command wasn't specified, then this string is empty.</p>
    pub standard_output_url: ::std::option::Option<::std::string::String>,
    /// <p>The URL for the complete text written by the plugin to stderr. If execution isn't yet complete, then this string is empty.</p>
    pub standard_error_url: ::std::option::Option<::std::string::String>,
    /// <p>(Deprecated) You can no longer specify this parameter. The system ignores it. Instead, Amazon Web Services Systems Manager automatically determines the S3 bucket region.</p>
    pub output_s3_region: ::std::option::Option<::std::string::String>,
    /// <p>The S3 bucket where the responses to the command executions should be stored. This was requested when issuing the command. For example, in the following response:</p>
    /// <p><code>amzn-s3-demo-bucket/my-prefix/i-02573cafcfEXAMPLE/awsrunShellScript</code></p>
    /// <p><code>amzn-s3-demo-bucket</code> is the name of the S3 bucket;</p>
    /// <p><code>my-prefix</code> is the name of the S3 prefix;</p>
    /// <p><code>i-02573cafcfEXAMPLE</code> is the managed node ID;</p>
    /// <p><code>awsrunShellScript</code> is the name of the plugin.</p>
    pub output_s3_bucket_name: ::std::option::Option<::std::string::String>,
    /// <p>The S3 directory path inside the bucket where the responses to the command executions should be stored. This was requested when issuing the command. For example, in the following response:</p>
    /// <p><code>amzn-s3-demo-bucket/my-prefix/i-02573cafcfEXAMPLE/awsrunShellScript</code></p>
    /// <p><code>amzn-s3-demo-bucket</code> is the name of the S3 bucket;</p>
    /// <p><code>my-prefix</code> is the name of the S3 prefix;</p>
    /// <p><code>i-02573cafcfEXAMPLE</code> is the managed node ID;</p>
    /// <p><code>awsrunShellScript</code> is the name of the plugin.</p>
    pub output_s3_key_prefix: ::std::option::Option<::std::string::String>,
}
impl CommandPlugin {
    /// <p>The name of the plugin. Must be one of the following: <code>aws:updateAgent</code>, <code>aws:domainjoin</code>, <code>aws:applications</code>, <code>aws:runPowerShellScript</code>, <code>aws:psmodule</code>, <code>aws:cloudWatch</code>, <code>aws:runShellScript</code>, or <code>aws:updateSSMAgent</code>.</p>
    pub fn name(&self) -> ::std::option::Option<&str> {
        self.name.as_deref()
    }
    /// <p>The status of this plugin. You can run a document with multiple plugins.</p>
    pub fn status(&self) -> ::std::option::Option<&crate::types::CommandPluginStatus> {
        self.status.as_ref()
    }
    /// <p>A detailed status of the plugin execution. <code>StatusDetails</code> includes more information than Status because it includes states resulting from error and concurrency control parameters. StatusDetails can show different results than Status. For more information about these statuses, see <a href="https://docs.aws.amazon.com/systems-manager/latest/userguide/monitor-commands.html">Understanding command statuses</a> in the <i>Amazon Web Services Systems Manager User Guide</i>. StatusDetails can be one of the following values:</p>
    /// <ul>
    /// <li>
    /// <p>Pending: The command hasn't been sent to the managed node.</p></li>
    /// <li>
    /// <p>In Progress: The command has been sent to the managed node but hasn't reached a terminal state.</p></li>
    /// <li>
    /// <p>Success: The execution of the command or plugin was successfully completed. This is a terminal state.</p></li>
    /// <li>
    /// <p>Delivery Timed Out: The command wasn't delivered to the managed node before the delivery timeout expired. Delivery timeouts don't count against the parent command's <code>MaxErrors</code> limit, but they do contribute to whether the parent command status is Success or Incomplete. This is a terminal state.</p></li>
    /// <li>
    /// <p>Execution Timed Out: Command execution started on the managed node, but the execution wasn't complete before the execution timeout expired. Execution timeouts count against the <code>MaxErrors</code> limit of the parent command. This is a terminal state.</p></li>
    /// <li>
    /// <p>Failed: The command wasn't successful on the managed node. For a plugin, this indicates that the result code wasn't zero. For a command invocation, this indicates that the result code for one or more plugins wasn't zero. Invocation failures count against the MaxErrors limit of the parent command. This is a terminal state.</p></li>
    /// <li>
    /// <p>Cancelled: The command was terminated before it was completed. This is a terminal state.</p></li>
    /// <li>
    /// <p>Undeliverable: The command can't be delivered to the managed node. The managed node might not exist, or it might not be responding. Undeliverable invocations don't count against the parent command's MaxErrors limit, and they don't contribute to whether the parent command status is Success or Incomplete. This is a terminal state.</p></li>
    /// <li>
    /// <p>Terminated: The parent command exceeded its MaxErrors limit and subsequent command invocations were canceled by the system. This is a terminal state.</p></li>
    /// </ul>
    pub fn status_details(&self) -> ::std::option::Option<&str> {
        self.status_details.as_deref()
    }
    /// <p>A numeric response code generated after running the plugin.</p>
    pub fn response_code(&self) -> i32 {
        self.response_code
    }
    /// <p>The time the plugin started running.</p>
    pub fn response_start_date_time(&self) -> ::std::option::Option<&::aws_smithy_types::DateTime> {
        self.response_start_date_time.as_ref()
    }
    /// <p>The time the plugin stopped running. Could stop prematurely if, for example, a cancel command was sent.</p>
    pub fn response_finish_date_time(&self) -> ::std::option::Option<&::aws_smithy_types::DateTime> {
        self.response_finish_date_time.as_ref()
    }
    /// <p>Output of the plugin execution.</p>
    pub fn output(&self) -> ::std::option::Option<&str> {
        self.output.as_deref()
    }
    /// <p>The URL for the complete text written by the plugin to stdout in Amazon S3. If the S3 bucket for the command wasn't specified, then this string is empty.</p>
    pub fn standard_output_url(&self) -> ::std::option::Option<&str> {
        self.standard_output_url.as_deref()
    }
    /// <p>The URL for the complete text written by the plugin to stderr. If execution isn't yet complete, then this string is empty.</p>
    pub fn standard_error_url(&self) -> ::std::option::Option<&str> {
        self.standard_error_url.as_deref()
    }
    /// <p>(Deprecated) You can no longer specify this parameter. The system ignores it. Instead, Amazon Web Services Systems Manager automatically determines the S3 bucket region.</p>
    pub fn output_s3_region(&self) -> ::std::option::Option<&str> {
        self.output_s3_region.as_deref()
    }
    /// <p>The S3 bucket where the responses to the command executions should be stored. This was requested when issuing the command. For example, in the following response:</p>
    /// <p><code>amzn-s3-demo-bucket/my-prefix/i-02573cafcfEXAMPLE/awsrunShellScript</code></p>
    /// <p><code>amzn-s3-demo-bucket</code> is the name of the S3 bucket;</p>
    /// <p><code>my-prefix</code> is the name of the S3 prefix;</p>
    /// <p><code>i-02573cafcfEXAMPLE</code> is the managed node ID;</p>
    /// <p><code>awsrunShellScript</code> is the name of the plugin.</p>
    pub fn output_s3_bucket_name(&self) -> ::std::option::Option<&str> {
        self.output_s3_bucket_name.as_deref()
    }
    /// <p>The S3 directory path inside the bucket where the responses to the command executions should be stored. This was requested when issuing the command. For example, in the following response:</p>
    /// <p><code>amzn-s3-demo-bucket/my-prefix/i-02573cafcfEXAMPLE/awsrunShellScript</code></p>
    /// <p><code>amzn-s3-demo-bucket</code> is the name of the S3 bucket;</p>
    /// <p><code>my-prefix</code> is the name of the S3 prefix;</p>
    /// <p><code>i-02573cafcfEXAMPLE</code> is the managed node ID;</p>
    /// <p><code>awsrunShellScript</code> is the name of the plugin.</p>
    pub fn output_s3_key_prefix(&self) -> ::std::option::Option<&str> {
        self.output_s3_key_prefix.as_deref()
    }
}
static COMMANDPLUGIN_MEMBER_NAME: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin$Name", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::String,
    "Name",
    0,
);
static COMMANDPLUGIN_MEMBER_STATUS: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin$Status", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::String,
    "Status",
    1,
);
static COMMANDPLUGIN_MEMBER_STATUS_DETAILS: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin$StatusDetails", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::String,
    "StatusDetails",
    2,
);
static COMMANDPLUGIN_MEMBER_RESPONSE_CODE: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin$ResponseCode", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::Integer,
    "ResponseCode",
    3,
);
static COMMANDPLUGIN_MEMBER_RESPONSE_START_DATE_TIME: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts(
        "com.amazonaws.ssm#CommandPlugin$ResponseStartDateTime",
        "com.amazonaws.ssm",
        "CommandPlugin",
    ),
    ::aws_smithy_schema::ShapeType::Timestamp,
    "ResponseStartDateTime",
    4,
);
static COMMANDPLUGIN_MEMBER_RESPONSE_FINISH_DATE_TIME: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts(
        "com.amazonaws.ssm#CommandPlugin$ResponseFinishDateTime",
        "com.amazonaws.ssm",
        "CommandPlugin",
    ),
    ::aws_smithy_schema::ShapeType::Timestamp,
    "ResponseFinishDateTime",
    5,
);
static COMMANDPLUGIN_MEMBER_OUTPUT: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin$Output", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::String,
    "Output",
    6,
);
static COMMANDPLUGIN_MEMBER_STANDARD_OUTPUT_URL: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin$StandardOutputUrl", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::String,
    "StandardOutputUrl",
    7,
);
static COMMANDPLUGIN_MEMBER_STANDARD_ERROR_URL: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin$StandardErrorUrl", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::String,
    "StandardErrorUrl",
    8,
);
static COMMANDPLUGIN_MEMBER_OUTPUT_S3_REGION: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin$OutputS3Region", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::String,
    "OutputS3Region",
    9,
);
static COMMANDPLUGIN_MEMBER_OUTPUT_S3_BUCKET_NAME: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin$OutputS3BucketName", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::String,
    "OutputS3BucketName",
    10,
);
static COMMANDPLUGIN_MEMBER_OUTPUT_S3_KEY_PREFIX: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_member(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin$OutputS3KeyPrefix", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::String,
    "OutputS3KeyPrefix",
    11,
);
static COMMANDPLUGIN_SCHEMA: ::aws_smithy_schema::Schema<'static> = ::aws_smithy_schema::Schema::new_struct(
    ::aws_smithy_schema::ShapeId::from_parts("com.amazonaws.ssm#CommandPlugin", "com.amazonaws.ssm", "CommandPlugin"),
    ::aws_smithy_schema::ShapeType::Structure,
    &[
        &COMMANDPLUGIN_MEMBER_NAME,
        &COMMANDPLUGIN_MEMBER_STATUS,
        &COMMANDPLUGIN_MEMBER_STATUS_DETAILS,
        &COMMANDPLUGIN_MEMBER_RESPONSE_CODE,
        &COMMANDPLUGIN_MEMBER_RESPONSE_START_DATE_TIME,
        &COMMANDPLUGIN_MEMBER_RESPONSE_FINISH_DATE_TIME,
        &COMMANDPLUGIN_MEMBER_OUTPUT,
        &COMMANDPLUGIN_MEMBER_STANDARD_OUTPUT_URL,
        &COMMANDPLUGIN_MEMBER_STANDARD_ERROR_URL,
        &COMMANDPLUGIN_MEMBER_OUTPUT_S3_REGION,
        &COMMANDPLUGIN_MEMBER_OUTPUT_S3_BUCKET_NAME,
        &COMMANDPLUGIN_MEMBER_OUTPUT_S3_KEY_PREFIX,
    ],
);
impl CommandPlugin {
    /// The schema for this shape.
    pub const SCHEMA: &'static ::aws_smithy_schema::Schema<'static> = &COMMANDPLUGIN_SCHEMA;
}
impl ::aws_smithy_schema::serde::SerializableStruct for CommandPlugin {
    #[allow(unused_variables, clippy::diverging_sub_expression)]
    fn serialize_members(
        &self,
        ser: &mut dyn ::aws_smithy_schema::serde::ShapeSerializer,
    ) -> ::std::result::Result<(), ::aws_smithy_schema::serde::SerdeError> {
        if let Some(ref val) = self.name {
            ser.write_string(&COMMANDPLUGIN_MEMBER_NAME, val)?;
        }
        if let Some(ref val) = self.status {
            ser.write_string(&COMMANDPLUGIN_MEMBER_STATUS, val.as_str())?;
        }
        if let Some(ref val) = self.status_details {
            ser.write_string(&COMMANDPLUGIN_MEMBER_STATUS_DETAILS, val)?;
        }
        {
            let val = &self.response_code;
            ser.write_integer(&COMMANDPLUGIN_MEMBER_RESPONSE_CODE, *val)?;
        }
        if let Some(ref val) = self.response_start_date_time {
            ser.write_timestamp(&COMMANDPLUGIN_MEMBER_RESPONSE_START_DATE_TIME, val)?;
        }
        if let Some(ref val) = self.response_finish_date_time {
            ser.write_timestamp(&COMMANDPLUGIN_MEMBER_RESPONSE_FINISH_DATE_TIME, val)?;
        }
        if let Some(ref val) = self.output {
            ser.write_string(&COMMANDPLUGIN_MEMBER_OUTPUT, val)?;
        }
        if let Some(ref val) = self.standard_output_url {
            ser.write_string(&COMMANDPLUGIN_MEMBER_STANDARD_OUTPUT_URL, val)?;
        }
        if let Some(ref val) = self.standard_error_url {
            ser.write_string(&COMMANDPLUGIN_MEMBER_STANDARD_ERROR_URL, val)?;
        }
        if let Some(ref val) = self.output_s3_region {
            ser.write_string(&COMMANDPLUGIN_MEMBER_OUTPUT_S3_REGION, val)?;
        }
        if let Some(ref val) = self.output_s3_bucket_name {
            ser.write_string(&COMMANDPLUGIN_MEMBER_OUTPUT_S3_BUCKET_NAME, val)?;
        }
        if let Some(ref val) = self.output_s3_key_prefix {
            ser.write_string(&COMMANDPLUGIN_MEMBER_OUTPUT_S3_KEY_PREFIX, val)?;
        }
        Ok(())
    }
}
impl CommandPlugin {
    /// Deserializes this structure from a [`ShapeDeserializer`].
    pub fn deserialize(
        deserializer: &mut dyn ::aws_smithy_schema::serde::ShapeDeserializer,
    ) -> ::std::result::Result<Self, ::aws_smithy_schema::serde::SerdeError> {
        #[allow(unused_variables, unused_mut)]
        let mut builder = Self::builder();
        #[allow(
            unused_variables,
            unreachable_code,
            clippy::single_match,
            clippy::match_single_binding,
            clippy::diverging_sub_expression
        )]
        deserializer.read_struct(&COMMANDPLUGIN_SCHEMA, &mut |member, deser| {
            match member.member_index() {
                Some(0) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.name = Some(deser.read_string(member)?);
                    }
                }
                Some(1) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.status = Some(crate::types::CommandPluginStatus::from(deser.read_string(member)?.as_str()));
                    }
                }
                Some(2) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.status_details = Some(deser.read_string(member)?);
                    }
                }
                Some(3) => {
                    builder.response_code = Some(deser.read_integer(member)?);
                }
                Some(4) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.response_start_date_time = Some(deser.read_timestamp(member)?);
                    }
                }
                Some(5) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.response_finish_date_time = Some(deser.read_timestamp(member)?);
                    }
                }
                Some(6) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.output = Some(deser.read_string(member)?);
                    }
                }
                Some(7) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.standard_output_url = Some(deser.read_string(member)?);
                    }
                }
                Some(8) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.standard_error_url = Some(deser.read_string(member)?);
                    }
                }
                Some(9) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.output_s3_region = Some(deser.read_string(member)?);
                    }
                }
                Some(10) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.output_s3_bucket_name = Some(deser.read_string(member)?);
                    }
                }
                Some(11) => {
                    if deser.is_null() {
                        deser.read_null()?;
                    } else {
                        builder.output_s3_key_prefix = Some(deser.read_string(member)?);
                    }
                }
                _ => {}
            }
            Ok(())
        })?;
        Ok(builder.build())
    }
}
impl CommandPlugin {
    /// Deserializes this structure from a body deserializer and HTTP response.
    pub fn deserialize_with_response(
        deserializer: &mut dyn ::aws_smithy_schema::serde::ShapeDeserializer,
        _headers: &::aws_smithy_runtime_api::http::Headers,
        _status: u16,
        _body: &[u8],
    ) -> ::std::result::Result<Self, ::aws_smithy_schema::serde::SerdeError> {
        Self::deserialize(deserializer)
    }
}
impl CommandPlugin {
    /// Creates a new builder-style object to manufacture [`CommandPlugin`](crate::types::CommandPlugin).
    pub fn builder() -> crate::types::builders::CommandPluginBuilder {
        crate::types::builders::CommandPluginBuilder::default()
    }
}

/// A builder for [`CommandPlugin`](crate::types::CommandPlugin).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct CommandPluginBuilder {
    pub(crate) name: ::std::option::Option<::std::string::String>,
    pub(crate) status: ::std::option::Option<crate::types::CommandPluginStatus>,
    pub(crate) status_details: ::std::option::Option<::std::string::String>,
    pub(crate) response_code: ::std::option::Option<i32>,
    pub(crate) response_start_date_time: ::std::option::Option<::aws_smithy_types::DateTime>,
    pub(crate) response_finish_date_time: ::std::option::Option<::aws_smithy_types::DateTime>,
    pub(crate) output: ::std::option::Option<::std::string::String>,
    pub(crate) standard_output_url: ::std::option::Option<::std::string::String>,
    pub(crate) standard_error_url: ::std::option::Option<::std::string::String>,
    pub(crate) output_s3_region: ::std::option::Option<::std::string::String>,
    pub(crate) output_s3_bucket_name: ::std::option::Option<::std::string::String>,
    pub(crate) output_s3_key_prefix: ::std::option::Option<::std::string::String>,
}
impl CommandPluginBuilder {
    /// <p>The name of the plugin. Must be one of the following: <code>aws:updateAgent</code>, <code>aws:domainjoin</code>, <code>aws:applications</code>, <code>aws:runPowerShellScript</code>, <code>aws:psmodule</code>, <code>aws:cloudWatch</code>, <code>aws:runShellScript</code>, or <code>aws:updateSSMAgent</code>.</p>
    pub fn name(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.name = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The name of the plugin. Must be one of the following: <code>aws:updateAgent</code>, <code>aws:domainjoin</code>, <code>aws:applications</code>, <code>aws:runPowerShellScript</code>, <code>aws:psmodule</code>, <code>aws:cloudWatch</code>, <code>aws:runShellScript</code>, or <code>aws:updateSSMAgent</code>.</p>
    pub fn set_name(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.name = input;
        self
    }
    /// <p>The name of the plugin. Must be one of the following: <code>aws:updateAgent</code>, <code>aws:domainjoin</code>, <code>aws:applications</code>, <code>aws:runPowerShellScript</code>, <code>aws:psmodule</code>, <code>aws:cloudWatch</code>, <code>aws:runShellScript</code>, or <code>aws:updateSSMAgent</code>.</p>
    pub fn get_name(&self) -> &::std::option::Option<::std::string::String> {
        &self.name
    }
    /// <p>The status of this plugin. You can run a document with multiple plugins.</p>
    pub fn status(mut self, input: crate::types::CommandPluginStatus) -> Self {
        self.status = ::std::option::Option::Some(input);
        self
    }
    /// <p>The status of this plugin. You can run a document with multiple plugins.</p>
    pub fn set_status(mut self, input: ::std::option::Option<crate::types::CommandPluginStatus>) -> Self {
        self.status = input;
        self
    }
    /// <p>The status of this plugin. You can run a document with multiple plugins.</p>
    pub fn get_status(&self) -> &::std::option::Option<crate::types::CommandPluginStatus> {
        &self.status
    }
    /// <p>A detailed status of the plugin execution. <code>StatusDetails</code> includes more information than Status because it includes states resulting from error and concurrency control parameters. StatusDetails can show different results than Status. For more information about these statuses, see <a href="https://docs.aws.amazon.com/systems-manager/latest/userguide/monitor-commands.html">Understanding command statuses</a> in the <i>Amazon Web Services Systems Manager User Guide</i>. StatusDetails can be one of the following values:</p>
    /// <ul>
    /// <li>
    /// <p>Pending: The command hasn't been sent to the managed node.</p></li>
    /// <li>
    /// <p>In Progress: The command has been sent to the managed node but hasn't reached a terminal state.</p></li>
    /// <li>
    /// <p>Success: The execution of the command or plugin was successfully completed. This is a terminal state.</p></li>
    /// <li>
    /// <p>Delivery Timed Out: The command wasn't delivered to the managed node before the delivery timeout expired. Delivery timeouts don't count against the parent command's <code>MaxErrors</code> limit, but they do contribute to whether the parent command status is Success or Incomplete. This is a terminal state.</p></li>
    /// <li>
    /// <p>Execution Timed Out: Command execution started on the managed node, but the execution wasn't complete before the execution timeout expired. Execution timeouts count against the <code>MaxErrors</code> limit of the parent command. This is a terminal state.</p></li>
    /// <li>
    /// <p>Failed: The command wasn't successful on the managed node. For a plugin, this indicates that the result code wasn't zero. For a command invocation, this indicates that the result code for one or more plugins wasn't zero. Invocation failures count against the MaxErrors limit of the parent command. This is a terminal state.</p></li>
    /// <li>
    /// <p>Cancelled: The command was terminated before it was completed. This is a terminal state.</p></li>
    /// <li>
    /// <p>Undeliverable: The command can't be delivered to the managed node. The managed node might not exist, or it might not be responding. Undeliverable invocations don't count against the parent command's MaxErrors limit, and they don't contribute to whether the parent command status is Success or Incomplete. This is a terminal state.</p></li>
    /// <li>
    /// <p>Terminated: The parent command exceeded its MaxErrors limit and subsequent command invocations were canceled by the system. This is a terminal state.</p></li>
    /// </ul>
    pub fn status_details(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.status_details = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>A detailed status of the plugin execution. <code>StatusDetails</code> includes more information than Status because it includes states resulting from error and concurrency control parameters. StatusDetails can show different results than Status. For more information about these statuses, see <a href="https://docs.aws.amazon.com/systems-manager/latest/userguide/monitor-commands.html">Understanding command statuses</a> in the <i>Amazon Web Services Systems Manager User Guide</i>. StatusDetails can be one of the following values:</p>
    /// <ul>
    /// <li>
    /// <p>Pending: The command hasn't been sent to the managed node.</p></li>
    /// <li>
    /// <p>In Progress: The command has been sent to the managed node but hasn't reached a terminal state.</p></li>
    /// <li>
    /// <p>Success: The execution of the command or plugin was successfully completed. This is a terminal state.</p></li>
    /// <li>
    /// <p>Delivery Timed Out: The command wasn't delivered to the managed node before the delivery timeout expired. Delivery timeouts don't count against the parent command's <code>MaxErrors</code> limit, but they do contribute to whether the parent command status is Success or Incomplete. This is a terminal state.</p></li>
    /// <li>
    /// <p>Execution Timed Out: Command execution started on the managed node, but the execution wasn't complete before the execution timeout expired. Execution timeouts count against the <code>MaxErrors</code> limit of the parent command. This is a terminal state.</p></li>
    /// <li>
    /// <p>Failed: The command wasn't successful on the managed node. For a plugin, this indicates that the result code wasn't zero. For a command invocation, this indicates that the result code for one or more plugins wasn't zero. Invocation failures count against the MaxErrors limit of the parent command. This is a terminal state.</p></li>
    /// <li>
    /// <p>Cancelled: The command was terminated before it was completed. This is a terminal state.</p></li>
    /// <li>
    /// <p>Undeliverable: The command can't be delivered to the managed node. The managed node might not exist, or it might not be responding. Undeliverable invocations don't count against the parent command's MaxErrors limit, and they don't contribute to whether the parent command status is Success or Incomplete. This is a terminal state.</p></li>
    /// <li>
    /// <p>Terminated: The parent command exceeded its MaxErrors limit and subsequent command invocations were canceled by the system. This is a terminal state.</p></li>
    /// </ul>
    pub fn set_status_details(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.status_details = input;
        self
    }
    /// <p>A detailed status of the plugin execution. <code>StatusDetails</code> includes more information than Status because it includes states resulting from error and concurrency control parameters. StatusDetails can show different results than Status. For more information about these statuses, see <a href="https://docs.aws.amazon.com/systems-manager/latest/userguide/monitor-commands.html">Understanding command statuses</a> in the <i>Amazon Web Services Systems Manager User Guide</i>. StatusDetails can be one of the following values:</p>
    /// <ul>
    /// <li>
    /// <p>Pending: The command hasn't been sent to the managed node.</p></li>
    /// <li>
    /// <p>In Progress: The command has been sent to the managed node but hasn't reached a terminal state.</p></li>
    /// <li>
    /// <p>Success: The execution of the command or plugin was successfully completed. This is a terminal state.</p></li>
    /// <li>
    /// <p>Delivery Timed Out: The command wasn't delivered to the managed node before the delivery timeout expired. Delivery timeouts don't count against the parent command's <code>MaxErrors</code> limit, but they do contribute to whether the parent command status is Success or Incomplete. This is a terminal state.</p></li>
    /// <li>
    /// <p>Execution Timed Out: Command execution started on the managed node, but the execution wasn't complete before the execution timeout expired. Execution timeouts count against the <code>MaxErrors</code> limit of the parent command. This is a terminal state.</p></li>
    /// <li>
    /// <p>Failed: The command wasn't successful on the managed node. For a plugin, this indicates that the result code wasn't zero. For a command invocation, this indicates that the result code for one or more plugins wasn't zero. Invocation failures count against the MaxErrors limit of the parent command. This is a terminal state.</p></li>
    /// <li>
    /// <p>Cancelled: The command was terminated before it was completed. This is a terminal state.</p></li>
    /// <li>
    /// <p>Undeliverable: The command can't be delivered to the managed node. The managed node might not exist, or it might not be responding. Undeliverable invocations don't count against the parent command's MaxErrors limit, and they don't contribute to whether the parent command status is Success or Incomplete. This is a terminal state.</p></li>
    /// <li>
    /// <p>Terminated: The parent command exceeded its MaxErrors limit and subsequent command invocations were canceled by the system. This is a terminal state.</p></li>
    /// </ul>
    pub fn get_status_details(&self) -> &::std::option::Option<::std::string::String> {
        &self.status_details
    }
    /// <p>A numeric response code generated after running the plugin.</p>
    pub fn response_code(mut self, input: i32) -> Self {
        self.response_code = ::std::option::Option::Some(input);
        self
    }
    /// <p>A numeric response code generated after running the plugin.</p>
    pub fn set_response_code(mut self, input: ::std::option::Option<i32>) -> Self {
        self.response_code = input;
        self
    }
    /// <p>A numeric response code generated after running the plugin.</p>
    pub fn get_response_code(&self) -> &::std::option::Option<i32> {
        &self.response_code
    }
    /// <p>The time the plugin started running.</p>
    pub fn response_start_date_time(mut self, input: ::aws_smithy_types::DateTime) -> Self {
        self.response_start_date_time = ::std::option::Option::Some(input);
        self
    }
    /// <p>The time the plugin started running.</p>
    pub fn set_response_start_date_time(mut self, input: ::std::option::Option<::aws_smithy_types::DateTime>) -> Self {
        self.response_start_date_time = input;
        self
    }
    /// <p>The time the plugin started running.</p>
    pub fn get_response_start_date_time(&self) -> &::std::option::Option<::aws_smithy_types::DateTime> {
        &self.response_start_date_time
    }
    /// <p>The time the plugin stopped running. Could stop prematurely if, for example, a cancel command was sent.</p>
    pub fn response_finish_date_time(mut self, input: ::aws_smithy_types::DateTime) -> Self {
        self.response_finish_date_time = ::std::option::Option::Some(input);
        self
    }
    /// <p>The time the plugin stopped running. Could stop prematurely if, for example, a cancel command was sent.</p>
    pub fn set_response_finish_date_time(mut self, input: ::std::option::Option<::aws_smithy_types::DateTime>) -> Self {
        self.response_finish_date_time = input;
        self
    }
    /// <p>The time the plugin stopped running. Could stop prematurely if, for example, a cancel command was sent.</p>
    pub fn get_response_finish_date_time(&self) -> &::std::option::Option<::aws_smithy_types::DateTime> {
        &self.response_finish_date_time
    }
    /// <p>Output of the plugin execution.</p>
    pub fn output(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.output = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>Output of the plugin execution.</p>
    pub fn set_output(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.output = input;
        self
    }
    /// <p>Output of the plugin execution.</p>
    pub fn get_output(&self) -> &::std::option::Option<::std::string::String> {
        &self.output
    }
    /// <p>The URL for the complete text written by the plugin to stdout in Amazon S3. If the S3 bucket for the command wasn't specified, then this string is empty.</p>
    pub fn standard_output_url(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.standard_output_url = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The URL for the complete text written by the plugin to stdout in Amazon S3. If the S3 bucket for the command wasn't specified, then this string is empty.</p>
    pub fn set_standard_output_url(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.standard_output_url = input;
        self
    }
    /// <p>The URL for the complete text written by the plugin to stdout in Amazon S3. If the S3 bucket for the command wasn't specified, then this string is empty.</p>
    pub fn get_standard_output_url(&self) -> &::std::option::Option<::std::string::String> {
        &self.standard_output_url
    }
    /// <p>The URL for the complete text written by the plugin to stderr. If execution isn't yet complete, then this string is empty.</p>
    pub fn standard_error_url(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.standard_error_url = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The URL for the complete text written by the plugin to stderr. If execution isn't yet complete, then this string is empty.</p>
    pub fn set_standard_error_url(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.standard_error_url = input;
        self
    }
    /// <p>The URL for the complete text written by the plugin to stderr. If execution isn't yet complete, then this string is empty.</p>
    pub fn get_standard_error_url(&self) -> &::std::option::Option<::std::string::String> {
        &self.standard_error_url
    }
    /// <p>(Deprecated) You can no longer specify this parameter. The system ignores it. Instead, Amazon Web Services Systems Manager automatically determines the S3 bucket region.</p>
    pub fn output_s3_region(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.output_s3_region = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>(Deprecated) You can no longer specify this parameter. The system ignores it. Instead, Amazon Web Services Systems Manager automatically determines the S3 bucket region.</p>
    pub fn set_output_s3_region(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.output_s3_region = input;
        self
    }
    /// <p>(Deprecated) You can no longer specify this parameter. The system ignores it. Instead, Amazon Web Services Systems Manager automatically determines the S3 bucket region.</p>
    pub fn get_output_s3_region(&self) -> &::std::option::Option<::std::string::String> {
        &self.output_s3_region
    }
    /// <p>The S3 bucket where the responses to the command executions should be stored. This was requested when issuing the command. For example, in the following response:</p>
    /// <p><code>amzn-s3-demo-bucket/my-prefix/i-02573cafcfEXAMPLE/awsrunShellScript</code></p>
    /// <p><code>amzn-s3-demo-bucket</code> is the name of the S3 bucket;</p>
    /// <p><code>my-prefix</code> is the name of the S3 prefix;</p>
    /// <p><code>i-02573cafcfEXAMPLE</code> is the managed node ID;</p>
    /// <p><code>awsrunShellScript</code> is the name of the plugin.</p>
    pub fn output_s3_bucket_name(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.output_s3_bucket_name = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The S3 bucket where the responses to the command executions should be stored. This was requested when issuing the command. For example, in the following response:</p>
    /// <p><code>amzn-s3-demo-bucket/my-prefix/i-02573cafcfEXAMPLE/awsrunShellScript</code></p>
    /// <p><code>amzn-s3-demo-bucket</code> is the name of the S3 bucket;</p>
    /// <p><code>my-prefix</code> is the name of the S3 prefix;</p>
    /// <p><code>i-02573cafcfEXAMPLE</code> is the managed node ID;</p>
    /// <p><code>awsrunShellScript</code> is the name of the plugin.</p>
    pub fn set_output_s3_bucket_name(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.output_s3_bucket_name = input;
        self
    }
    /// <p>The S3 bucket where the responses to the command executions should be stored. This was requested when issuing the command. For example, in the following response:</p>
    /// <p><code>amzn-s3-demo-bucket/my-prefix/i-02573cafcfEXAMPLE/awsrunShellScript</code></p>
    /// <p><code>amzn-s3-demo-bucket</code> is the name of the S3 bucket;</p>
    /// <p><code>my-prefix</code> is the name of the S3 prefix;</p>
    /// <p><code>i-02573cafcfEXAMPLE</code> is the managed node ID;</p>
    /// <p><code>awsrunShellScript</code> is the name of the plugin.</p>
    pub fn get_output_s3_bucket_name(&self) -> &::std::option::Option<::std::string::String> {
        &self.output_s3_bucket_name
    }
    /// <p>The S3 directory path inside the bucket where the responses to the command executions should be stored. This was requested when issuing the command. For example, in the following response:</p>
    /// <p><code>amzn-s3-demo-bucket/my-prefix/i-02573cafcfEXAMPLE/awsrunShellScript</code></p>
    /// <p><code>amzn-s3-demo-bucket</code> is the name of the S3 bucket;</p>
    /// <p><code>my-prefix</code> is the name of the S3 prefix;</p>
    /// <p><code>i-02573cafcfEXAMPLE</code> is the managed node ID;</p>
    /// <p><code>awsrunShellScript</code> is the name of the plugin.</p>
    pub fn output_s3_key_prefix(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.output_s3_key_prefix = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The S3 directory path inside the bucket where the responses to the command executions should be stored. This was requested when issuing the command. For example, in the following response:</p>
    /// <p><code>amzn-s3-demo-bucket/my-prefix/i-02573cafcfEXAMPLE/awsrunShellScript</code></p>
    /// <p><code>amzn-s3-demo-bucket</code> is the name of the S3 bucket;</p>
    /// <p><code>my-prefix</code> is the name of the S3 prefix;</p>
    /// <p><code>i-02573cafcfEXAMPLE</code> is the managed node ID;</p>
    /// <p><code>awsrunShellScript</code> is the name of the plugin.</p>
    pub fn set_output_s3_key_prefix(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.output_s3_key_prefix = input;
        self
    }
    /// <p>The S3 directory path inside the bucket where the responses to the command executions should be stored. This was requested when issuing the command. For example, in the following response:</p>
    /// <p><code>amzn-s3-demo-bucket/my-prefix/i-02573cafcfEXAMPLE/awsrunShellScript</code></p>
    /// <p><code>amzn-s3-demo-bucket</code> is the name of the S3 bucket;</p>
    /// <p><code>my-prefix</code> is the name of the S3 prefix;</p>
    /// <p><code>i-02573cafcfEXAMPLE</code> is the managed node ID;</p>
    /// <p><code>awsrunShellScript</code> is the name of the plugin.</p>
    pub fn get_output_s3_key_prefix(&self) -> &::std::option::Option<::std::string::String> {
        &self.output_s3_key_prefix
    }
    /// Consumes the builder and constructs a [`CommandPlugin`](crate::types::CommandPlugin).
    pub fn build(self) -> crate::types::CommandPlugin {
        crate::types::CommandPlugin {
            name: self.name,
            status: self.status,
            status_details: self.status_details,
            response_code: self.response_code.unwrap_or_default(),
            response_start_date_time: self.response_start_date_time,
            response_finish_date_time: self.response_finish_date_time,
            output: self.output,
            standard_output_url: self.standard_output_url,
            standard_error_url: self.standard_error_url,
            output_s3_region: self.output_s3_region,
            output_s3_bucket_name: self.output_s3_bucket_name,
            output_s3_key_prefix: self.output_s3_key_prefix,
        }
    }
}
