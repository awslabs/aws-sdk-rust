// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#![cfg(smoketests)]
//!
//! The tests in this module run against live AWS services. As such,
//! they are disabled by default. To enable them, run the tests with
//!
//! ```sh
//! RUSTFLAGS="--cfg smoketests" cargo test.
//! ```
//!
use aws_sdk_iam::{config, Client};
/// Smoke tests for the `get_user` operation
#[::tokio::test]
async fn test_get_user_failure() {
    let config = ::aws_config::load_defaults(config::BehaviorVersion::latest()).await;
    let conf = config::Config::from(&config)
        .to_builder()
        .region(config::Region::new("us-east-1"))
        .use_dual_stack(false)
        .use_fips(false)
        .build();
    let client = Client::from_conf(conf);
    let res = client
        .get_user()
        .set_user_name(::std::option::Option::Some("fake_user".to_owned()))
        .send()
        .await;
    res.expect_err("request should fail");
}
/// Smoke tests for the `list_users` operation
#[::tokio::test]
async fn test_list_users_success() {
    let config = ::aws_config::load_defaults(config::BehaviorVersion::latest()).await;
    let conf = config::Config::from(&config)
        .to_builder()
        .region(config::Region::new("us-east-1"))
        .use_dual_stack(false)
        .use_fips(false)
        .build();
    let client = Client::from_conf(conf);
    let res = client.list_users().send().await;
    res.expect("request should succeed");
}
