// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Specifies which ports are accessible on a MicroVM. Only one of the port specification options can be set.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum PortSpecification {
    /// <p>Indicates that all ports are accessible.</p>
    AllPorts,
    /// <p>A single port number.</p>
    Port(i32),
    /// <p>A range of ports.</p>
    Range(crate::types::PortRange),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl PortSpecification {
    /// Tries to convert the enum instance into [`AllPorts`](crate::types::PortSpecification::AllPorts), extracting the inner `()`.
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_all_ports(&self) -> ::std::result::Result<(), &Self> {
        if let PortSpecification::AllPorts = &self {
            ::std::result::Result::Ok(())
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`AllPorts`](crate::types::PortSpecification::AllPorts).
    pub fn is_all_ports(&self) -> bool {
        self.as_all_ports().is_ok()
    }
    /// Tries to convert the enum instance into [`Port`](crate::types::PortSpecification::Port), extracting the inner [`i32`](i32).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_port(&self) -> ::std::result::Result<&i32, &Self> {
        if let PortSpecification::Port(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Port`](crate::types::PortSpecification::Port).
    pub fn is_port(&self) -> bool {
        self.as_port().is_ok()
    }
    /// Tries to convert the enum instance into [`Range`](crate::types::PortSpecification::Range), extracting the inner [`PortRange`](crate::types::PortRange).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_range(&self) -> ::std::result::Result<&crate::types::PortRange, &Self> {
        if let PortSpecification::Range(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Range`](crate::types::PortSpecification::Range).
    pub fn is_range(&self) -> bool {
        self.as_range().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
