// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#![cfg(smoketests)]
//!
//! The tests in this module run against live AWS services. As such,
//! they are disabled by default. To enable them, run the tests with
//!
//! ```sh
//! RUSTFLAGS="--cfg smoketests" cargo test.
//! ```
use aws_sdk_cloudwatchlogs::{config, Client};
/// Smoke tests for the `describe_log_groups` operation
#[::tokio::test]
async fn test_describe_log_groups_success() {
    let config = ::aws_config::load_defaults(config::BehaviorVersion::latest()).await;
    let conf = config::Config::from(&config)
        .to_builder()
        .region(config::Region::new("us-west-2"))
        .use_dual_stack(false)
        .use_fips(false)
        .build();
    let client = Client::from_conf(conf);
    let res = client.describe_log_groups().send().await;
    res.expect("request should succeed");
}
/// Smoke tests for the `get_log_events` operation
#[::tokio::test]
async fn test_get_log_events_failure() {
    let config = ::aws_config::load_defaults(config::BehaviorVersion::latest()).await;
    let conf = config::Config::from(&config)
        .to_builder()
        .region(config::Region::new("us-west-2"))
        .use_dual_stack(false)
        .use_fips(false)
        .build();
    let client = Client::from_conf(conf);
    let res = client
        .get_log_events()
        .set_log_group_name(::std::option::Option::Some("fakegroup".to_owned()))
        .set_log_stream_name(::std::option::Option::Some("fakestream".to_owned()))
        .send()
        .await;
    res.expect_err("request should fail");
}
