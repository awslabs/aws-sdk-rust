// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Provides information about the current iterator.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct IteratorDescription {
    /// <p>Indicates the current iterator's position within the shard. The possible values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>AT_TIP</code> - No more records are currently available.</p></li>
    /// <li>
    /// <p><code>BEHIND_TIP</code> - Additional records may be available.</p></li>
    /// </ul>
    /// <p>Stream progresses in absence of customer records. <code>BEHIND_TIP</code> with an empty <code>changeRecords</code> list indicates the stream is progressing but no customer records are available at this position. Continue polling normally.</p>
    pub iterator_position: ::std::option::Option<crate::types::IteratorPosition>,
}
impl IteratorDescription {
    /// <p>Indicates the current iterator's position within the shard. The possible values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>AT_TIP</code> - No more records are currently available.</p></li>
    /// <li>
    /// <p><code>BEHIND_TIP</code> - Additional records may be available.</p></li>
    /// </ul>
    /// <p>Stream progresses in absence of customer records. <code>BEHIND_TIP</code> with an empty <code>changeRecords</code> list indicates the stream is progressing but no customer records are available at this position. Continue polling normally.</p>
    pub fn iterator_position(&self) -> ::std::option::Option<&crate::types::IteratorPosition> {
        self.iterator_position.as_ref()
    }
}
impl IteratorDescription {
    /// Creates a new builder-style object to manufacture [`IteratorDescription`](crate::types::IteratorDescription).
    pub fn builder() -> crate::types::builders::IteratorDescriptionBuilder {
        crate::types::builders::IteratorDescriptionBuilder::default()
    }
}

/// A builder for [`IteratorDescription`](crate::types::IteratorDescription).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct IteratorDescriptionBuilder {
    pub(crate) iterator_position: ::std::option::Option<crate::types::IteratorPosition>,
}
impl IteratorDescriptionBuilder {
    /// <p>Indicates the current iterator's position within the shard. The possible values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>AT_TIP</code> - No more records are currently available.</p></li>
    /// <li>
    /// <p><code>BEHIND_TIP</code> - Additional records may be available.</p></li>
    /// </ul>
    /// <p>Stream progresses in absence of customer records. <code>BEHIND_TIP</code> with an empty <code>changeRecords</code> list indicates the stream is progressing but no customer records are available at this position. Continue polling normally.</p>
    pub fn iterator_position(mut self, input: crate::types::IteratorPosition) -> Self {
        self.iterator_position = ::std::option::Option::Some(input);
        self
    }
    /// <p>Indicates the current iterator's position within the shard. The possible values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>AT_TIP</code> - No more records are currently available.</p></li>
    /// <li>
    /// <p><code>BEHIND_TIP</code> - Additional records may be available.</p></li>
    /// </ul>
    /// <p>Stream progresses in absence of customer records. <code>BEHIND_TIP</code> with an empty <code>changeRecords</code> list indicates the stream is progressing but no customer records are available at this position. Continue polling normally.</p>
    pub fn set_iterator_position(mut self, input: ::std::option::Option<crate::types::IteratorPosition>) -> Self {
        self.iterator_position = input;
        self
    }
    /// <p>Indicates the current iterator's position within the shard. The possible values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>AT_TIP</code> - No more records are currently available.</p></li>
    /// <li>
    /// <p><code>BEHIND_TIP</code> - Additional records may be available.</p></li>
    /// </ul>
    /// <p>Stream progresses in absence of customer records. <code>BEHIND_TIP</code> with an empty <code>changeRecords</code> list indicates the stream is progressing but no customer records are available at this position. Continue polling normally.</p>
    pub fn get_iterator_position(&self) -> &::std::option::Option<crate::types::IteratorPosition> {
        &self.iterator_position
    }
    /// Consumes the builder and constructs a [`IteratorDescription`](crate::types::IteratorDescription).
    pub fn build(self) -> crate::types::IteratorDescription {
        crate::types::IteratorDescription {
            iterator_position: self.iterator_position,
        }
    }
}
