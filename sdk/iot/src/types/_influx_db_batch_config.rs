// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The batching configuration of an InfluxDB rule action. IoT closes a batch and writes it to InfluxDB when the first of the configured limits is reached.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct InfluxDbBatchConfig {
    /// <p>The maximum number of data points to collect in a batch.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub max_batch_size: ::std::option::Option<i32>,
    /// <p>The maximum length of time, in milliseconds, to keep a batch open before writing it to InfluxDB.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub max_batch_open_ms: ::std::option::Option<i32>,
    /// <p>The maximum size of a batch, in bytes, before IoT writes it to InfluxDB.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub max_batch_size_bytes: ::std::option::Option<i32>,
    /// <p>Specifies whether to collect data points from different topics into the same batch.</p>
    /// <p>If omitted or <code>false</code>, IoT batches data points for each topic separately.</p>
    pub batch_across_topics: bool,
}
impl InfluxDbBatchConfig {
    /// <p>The maximum number of data points to collect in a batch.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn max_batch_size(&self) -> ::std::option::Option<i32> {
        self.max_batch_size
    }
    /// <p>The maximum length of time, in milliseconds, to keep a batch open before writing it to InfluxDB.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn max_batch_open_ms(&self) -> ::std::option::Option<i32> {
        self.max_batch_open_ms
    }
    /// <p>The maximum size of a batch, in bytes, before IoT writes it to InfluxDB.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn max_batch_size_bytes(&self) -> ::std::option::Option<i32> {
        self.max_batch_size_bytes
    }
    /// <p>Specifies whether to collect data points from different topics into the same batch.</p>
    /// <p>If omitted or <code>false</code>, IoT batches data points for each topic separately.</p>
    pub fn batch_across_topics(&self) -> bool {
        self.batch_across_topics
    }
}
impl InfluxDbBatchConfig {
    /// Creates a new builder-style object to manufacture [`InfluxDbBatchConfig`](crate::types::InfluxDbBatchConfig).
    pub fn builder() -> crate::types::builders::InfluxDbBatchConfigBuilder {
        crate::types::builders::InfluxDbBatchConfigBuilder::default()
    }
}

/// A builder for [`InfluxDbBatchConfig`](crate::types::InfluxDbBatchConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct InfluxDbBatchConfigBuilder {
    pub(crate) max_batch_size: ::std::option::Option<i32>,
    pub(crate) max_batch_open_ms: ::std::option::Option<i32>,
    pub(crate) max_batch_size_bytes: ::std::option::Option<i32>,
    pub(crate) batch_across_topics: ::std::option::Option<bool>,
}
impl InfluxDbBatchConfigBuilder {
    /// <p>The maximum number of data points to collect in a batch.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn max_batch_size(mut self, input: i32) -> Self {
        self.max_batch_size = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum number of data points to collect in a batch.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn set_max_batch_size(mut self, input: ::std::option::Option<i32>) -> Self {
        self.max_batch_size = input;
        self
    }
    /// <p>The maximum number of data points to collect in a batch.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn get_max_batch_size(&self) -> &::std::option::Option<i32> {
        &self.max_batch_size
    }
    /// <p>The maximum length of time, in milliseconds, to keep a batch open before writing it to InfluxDB.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn max_batch_open_ms(mut self, input: i32) -> Self {
        self.max_batch_open_ms = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum length of time, in milliseconds, to keep a batch open before writing it to InfluxDB.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn set_max_batch_open_ms(mut self, input: ::std::option::Option<i32>) -> Self {
        self.max_batch_open_ms = input;
        self
    }
    /// <p>The maximum length of time, in milliseconds, to keep a batch open before writing it to InfluxDB.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn get_max_batch_open_ms(&self) -> &::std::option::Option<i32> {
        &self.max_batch_open_ms
    }
    /// <p>The maximum size of a batch, in bytes, before IoT writes it to InfluxDB.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn max_batch_size_bytes(mut self, input: i32) -> Self {
        self.max_batch_size_bytes = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum size of a batch, in bytes, before IoT writes it to InfluxDB.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn set_max_batch_size_bytes(mut self, input: ::std::option::Option<i32>) -> Self {
        self.max_batch_size_bytes = input;
        self
    }
    /// <p>The maximum size of a batch, in bytes, before IoT writes it to InfluxDB.</p>
    /// <p>If you don't specify a value, this limit doesn't apply. IoT then closes each batch when another configured limit is reached.</p>
    pub fn get_max_batch_size_bytes(&self) -> &::std::option::Option<i32> {
        &self.max_batch_size_bytes
    }
    /// <p>Specifies whether to collect data points from different topics into the same batch.</p>
    /// <p>If omitted or <code>false</code>, IoT batches data points for each topic separately.</p>
    pub fn batch_across_topics(mut self, input: bool) -> Self {
        self.batch_across_topics = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether to collect data points from different topics into the same batch.</p>
    /// <p>If omitted or <code>false</code>, IoT batches data points for each topic separately.</p>
    pub fn set_batch_across_topics(mut self, input: ::std::option::Option<bool>) -> Self {
        self.batch_across_topics = input;
        self
    }
    /// <p>Specifies whether to collect data points from different topics into the same batch.</p>
    /// <p>If omitted or <code>false</code>, IoT batches data points for each topic separately.</p>
    pub fn get_batch_across_topics(&self) -> &::std::option::Option<bool> {
        &self.batch_across_topics
    }
    /// Consumes the builder and constructs a [`InfluxDbBatchConfig`](crate::types::InfluxDbBatchConfig).
    pub fn build(self) -> crate::types::InfluxDbBatchConfig {
        crate::types::InfluxDbBatchConfig {
            max_batch_size: self.max_batch_size,
            max_batch_open_ms: self.max_batch_open_ms,
            max_batch_size_bytes: self.max_batch_size_bytes,
            batch_across_topics: self.batch_across_topics.unwrap_or_default(),
        }
    }
}
