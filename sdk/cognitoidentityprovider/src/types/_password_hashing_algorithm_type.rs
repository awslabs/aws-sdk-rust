// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// When writing a match expression against `PasswordHashingAlgorithmType`, it is important to ensure
/// your code is forward-compatible. That is, if a match arm handles a case for a
/// feature that is supported by the service but has not been represented as an enum
/// variant in a current version of SDK, your code should continue to work when you
/// upgrade SDK to a future version in which the enum does include a variant for that
/// feature.
///
/// Here is an example of how you can make a match expression forward-compatible:
///
/// ```text
/// # let passwordhashingalgorithmtype = unimplemented!();
/// match passwordhashingalgorithmtype {
///     PasswordHashingAlgorithmType::Argon2Id => { /* ... */ },
///     PasswordHashingAlgorithmType::Bcrypt => { /* ... */ },
///     PasswordHashingAlgorithmType::Pbkdf2Sha256 => { /* ... */ },
///     PasswordHashingAlgorithmType::Scrypt => { /* ... */ },
///     other @ _ if other.as_str() == "NewFeature" => { /* handles a case for `NewFeature` */ },
///     _ => { /* ... */ },
/// }
/// ```
/// The above code demonstrates that when `passwordhashingalgorithmtype` represents
/// `NewFeature`, the execution path will lead to the second last match arm,
/// even though the enum does not contain a variant `PasswordHashingAlgorithmType::NewFeature`
/// in the current version of SDK. The reason is that the variable `other`,
/// created by the `@` operator, is bound to
/// `PasswordHashingAlgorithmType::Unknown(UnknownVariantValue("NewFeature".to_owned()))`
/// and calling `as_str` on it yields `"NewFeature"`.
/// This match expression is forward-compatible when executed with a newer
/// version of SDK where the variant `PasswordHashingAlgorithmType::NewFeature` is defined.
/// Specifically, when `passwordhashingalgorithmtype` represents `NewFeature`,
/// the execution path will hit the second last match arm as before by virtue of
/// calling `as_str` on `PasswordHashingAlgorithmType::NewFeature` also yielding `"NewFeature"`.
///
/// Explicitly matching on the `Unknown` variant should
/// be avoided for two reasons:
/// - The inner data `UnknownVariantValue` is opaque, and no further information can be extracted.
/// - It might inadvertently shadow other intended match arms.
///
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(
    ::std::clone::Clone, ::std::cmp::Eq, ::std::cmp::Ord, ::std::cmp::PartialEq, ::std::cmp::PartialOrd, ::std::fmt::Debug, ::std::hash::Hash,
)]
pub enum PasswordHashingAlgorithmType {
    #[allow(missing_docs)] // documentation missing in model
    Argon2Id,
    #[allow(missing_docs)] // documentation missing in model
    Bcrypt,
    #[allow(missing_docs)] // documentation missing in model
    Pbkdf2Sha256,
    #[allow(missing_docs)] // documentation missing in model
    Scrypt,
    /// `Unknown` contains new variants that have been added since this code was generated.
    #[deprecated(note = "Don't directly match on `Unknown`. See the docs on this enum for the correct way to handle unknown variants.")]
    Unknown(crate::primitives::sealed_enum_unknown::UnknownVariantValue),
}
impl ::std::convert::From<&str> for PasswordHashingAlgorithmType {
    fn from(s: &str) -> Self {
        match s {
            "ARGON2ID" => PasswordHashingAlgorithmType::Argon2Id,
            "BCRYPT" => PasswordHashingAlgorithmType::Bcrypt,
            "PBKDF2_SHA256" => PasswordHashingAlgorithmType::Pbkdf2Sha256,
            "SCRYPT" => PasswordHashingAlgorithmType::Scrypt,
            other => PasswordHashingAlgorithmType::Unknown(crate::primitives::sealed_enum_unknown::UnknownVariantValue(other.to_owned())),
        }
    }
}
impl ::std::str::FromStr for PasswordHashingAlgorithmType {
    type Err = ::std::convert::Infallible;

    fn from_str(s: &str) -> ::std::result::Result<Self, <Self as ::std::str::FromStr>::Err> {
        ::std::result::Result::Ok(PasswordHashingAlgorithmType::from(s))
    }
}
impl PasswordHashingAlgorithmType {
    /// Returns the `&str` value of the enum member.
    pub fn as_str(&self) -> &str {
        match self {
            PasswordHashingAlgorithmType::Argon2Id => "ARGON2ID",
            PasswordHashingAlgorithmType::Bcrypt => "BCRYPT",
            PasswordHashingAlgorithmType::Pbkdf2Sha256 => "PBKDF2_SHA256",
            PasswordHashingAlgorithmType::Scrypt => "SCRYPT",
            PasswordHashingAlgorithmType::Unknown(value) => value.as_str(),
        }
    }
    /// Returns all the `&str` representations of the enum members.
    pub const fn values() -> &'static [&'static str] {
        &["ARGON2ID", "BCRYPT", "PBKDF2_SHA256", "SCRYPT"]
    }
}
impl ::std::convert::AsRef<str> for PasswordHashingAlgorithmType {
    fn as_ref(&self) -> &str {
        self.as_str()
    }
}
impl PasswordHashingAlgorithmType {
    /// Parses the enum value while disallowing unknown variants.
    ///
    /// Unknown variants will result in an error.
    pub fn try_parse(value: &str) -> ::std::result::Result<Self, crate::error::UnknownVariantError> {
        match Self::from(value) {
            #[allow(deprecated)]
            Self::Unknown(_) => ::std::result::Result::Err(crate::error::UnknownVariantError::new(value)),
            known => Ok(known),
        }
    }
}
impl ::std::fmt::Display for PasswordHashingAlgorithmType {
    fn fmt(&self, f: &mut ::std::fmt::Formatter) -> ::std::fmt::Result {
        match self {
            PasswordHashingAlgorithmType::Argon2Id => write!(f, "ARGON2ID"),
            PasswordHashingAlgorithmType::Bcrypt => write!(f, "BCRYPT"),
            PasswordHashingAlgorithmType::Pbkdf2Sha256 => write!(f, "PBKDF2_SHA256"),
            PasswordHashingAlgorithmType::Scrypt => write!(f, "SCRYPT"),
            PasswordHashingAlgorithmType::Unknown(value) => write!(f, "{value}"),
        }
    }
}
