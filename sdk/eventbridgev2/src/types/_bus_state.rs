// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// When writing a match expression against `BusState`, it is important to ensure
/// your code is forward-compatible. That is, if a match arm handles a case for a
/// feature that is supported by the service but has not been represented as an enum
/// variant in a current version of SDK, your code should continue to work when you
/// upgrade SDK to a future version in which the enum does include a variant for that
/// feature.
///
/// Here is an example of how you can make a match expression forward-compatible:
///
/// ```text
/// # let busstate = unimplemented!();
/// match busstate {
///     BusState::Active => { /* ... */ },
///     BusState::CreateFailed => { /* ... */ },
///     BusState::Creating => { /* ... */ },
///     BusState::DeleteFailed => { /* ... */ },
///     BusState::Deleting => { /* ... */ },
///     BusState::UpdateFailed => { /* ... */ },
///     BusState::Updating => { /* ... */ },
///     other @ _ if other.as_str() == "NewFeature" => { /* handles a case for `NewFeature` */ },
///     _ => { /* ... */ },
/// }
/// ```
/// The above code demonstrates that when `busstate` represents
/// `NewFeature`, the execution path will lead to the second last match arm,
/// even though the enum does not contain a variant `BusState::NewFeature`
/// in the current version of SDK. The reason is that the variable `other`,
/// created by the `@` operator, is bound to
/// `BusState::Unknown(UnknownVariantValue("NewFeature".to_owned()))`
/// and calling `as_str` on it yields `"NewFeature"`.
/// This match expression is forward-compatible when executed with a newer
/// version of SDK where the variant `BusState::NewFeature` is defined.
/// Specifically, when `busstate` represents `NewFeature`,
/// the execution path will hit the second last match arm as before by virtue of
/// calling `as_str` on `BusState::NewFeature` also yielding `"NewFeature"`.
///
/// Explicitly matching on the `Unknown` variant should
/// be avoided for two reasons:
/// - The inner data `UnknownVariantValue` is opaque, and no further information can be extracted.
/// - It might inadvertently shadow other intended match arms.
///
/// Lifecycle state of an event bus.
/// - CREATING: Bus creation is in progress.
/// - CREATE_FAILED: Bus creation failed; see StateReason. The bus cannot be recovered or
/// recreated in place: delete it with DeleteEventBus, then create a new bus.
/// - ACTIVE: Bus is fully operational and can accept events.
/// - UPDATING: Bus update is in progress.
/// - UPDATE_FAILED: Bus update failed; the bus remains operational with its previous configuration.
/// - DELETING: Bus deletion is actively in progress.
/// - DELETE_FAILED: Bus deletion failed; see StateReason. Retry DeleteEventBus after removing
/// subscribers and event sources.
#[non_exhaustive]
#[derive(
    ::std::clone::Clone, ::std::cmp::Eq, ::std::cmp::Ord, ::std::cmp::PartialEq, ::std::cmp::PartialOrd, ::std::fmt::Debug, ::std::hash::Hash,
)]
pub enum BusState {
    #[allow(missing_docs)] // documentation missing in model
    Active,
    #[allow(missing_docs)] // documentation missing in model
    CreateFailed,
    #[allow(missing_docs)] // documentation missing in model
    Creating,
    #[allow(missing_docs)] // documentation missing in model
    DeleteFailed,
    #[allow(missing_docs)] // documentation missing in model
    Deleting,
    #[allow(missing_docs)] // documentation missing in model
    UpdateFailed,
    #[allow(missing_docs)] // documentation missing in model
    Updating,
    /// `Unknown` contains new variants that have been added since this code was generated.
    #[deprecated(note = "Don't directly match on `Unknown`. See the docs on this enum for the correct way to handle unknown variants.")]
    Unknown(crate::primitives::sealed_enum_unknown::UnknownVariantValue),
}
impl ::std::convert::From<&str> for BusState {
    fn from(s: &str) -> Self {
        match s {
            "ACTIVE" => BusState::Active,
            "CREATE_FAILED" => BusState::CreateFailed,
            "CREATING" => BusState::Creating,
            "DELETE_FAILED" => BusState::DeleteFailed,
            "DELETING" => BusState::Deleting,
            "UPDATE_FAILED" => BusState::UpdateFailed,
            "UPDATING" => BusState::Updating,
            other => BusState::Unknown(crate::primitives::sealed_enum_unknown::UnknownVariantValue(other.to_owned())),
        }
    }
}
impl ::std::str::FromStr for BusState {
    type Err = ::std::convert::Infallible;

    fn from_str(s: &str) -> ::std::result::Result<Self, <Self as ::std::str::FromStr>::Err> {
        ::std::result::Result::Ok(BusState::from(s))
    }
}
impl BusState {
    /// Returns the `&str` value of the enum member.
    pub fn as_str(&self) -> &str {
        match self {
            BusState::Active => "ACTIVE",
            BusState::CreateFailed => "CREATE_FAILED",
            BusState::Creating => "CREATING",
            BusState::DeleteFailed => "DELETE_FAILED",
            BusState::Deleting => "DELETING",
            BusState::UpdateFailed => "UPDATE_FAILED",
            BusState::Updating => "UPDATING",
            BusState::Unknown(value) => value.as_str(),
        }
    }
    /// Returns all the `&str` representations of the enum members.
    pub const fn values() -> &'static [&'static str] {
        &[
            "ACTIVE",
            "CREATE_FAILED",
            "CREATING",
            "DELETE_FAILED",
            "DELETING",
            "UPDATE_FAILED",
            "UPDATING",
        ]
    }
}
impl ::std::convert::AsRef<str> for BusState {
    fn as_ref(&self) -> &str {
        self.as_str()
    }
}
impl BusState {
    /// Parses the enum value while disallowing unknown variants.
    ///
    /// Unknown variants will result in an error.
    pub fn try_parse(value: &str) -> ::std::result::Result<Self, crate::error::UnknownVariantError> {
        match Self::from(value) {
            #[allow(deprecated)]
            Self::Unknown(_) => ::std::result::Result::Err(crate::error::UnknownVariantError::new(value)),
            known => Ok(known),
        }
    }
}
impl ::std::fmt::Display for BusState {
    fn fmt(&self, f: &mut ::std::fmt::Formatter) -> ::std::fmt::Result {
        match self {
            BusState::Active => write!(f, "ACTIVE"),
            BusState::CreateFailed => write!(f, "CREATE_FAILED"),
            BusState::Creating => write!(f, "CREATING"),
            BusState::DeleteFailed => write!(f, "DELETE_FAILED"),
            BusState::Deleting => write!(f, "DELETING"),
            BusState::UpdateFailed => write!(f, "UPDATE_FAILED"),
            BusState::Updating => write!(f, "UPDATING"),
            BusState::Unknown(value) => write!(f, "{value}"),
        }
    }
}
