// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// Batching configuration for a subscriber.
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct BatchConfiguration {
    /// The maximum number of events to include in a single batch delivered to the target. The service delivers up to this many events per batch; fewer may be delivered when the batch window elapses or the target's per-batch limit is smaller. This is a maximum, not a guaranteed count. Valid range is 1-500 (default: 10, or the target API's per-batch maximum). The resolved value applied by the service is returned on read.
    pub max_batch_size: ::std::option::Option<i32>,
    /// The maximum time in seconds to wait for a batch to fill before delivering it to the target. This is a maximum; a batch may be delivered sooner if it reaches MaxBatchSize or another delivery condition is met. Valid range is 0-300 (default: 0, meaning no wait). The resolved value applied by the service is always returned on read.
    pub max_batch_window_in_seconds: ::std::option::Option<i32>,
}
impl BatchConfiguration {
    /// The maximum number of events to include in a single batch delivered to the target. The service delivers up to this many events per batch; fewer may be delivered when the batch window elapses or the target's per-batch limit is smaller. This is a maximum, not a guaranteed count. Valid range is 1-500 (default: 10, or the target API's per-batch maximum). The resolved value applied by the service is returned on read.
    pub fn max_batch_size(&self) -> ::std::option::Option<i32> {
        self.max_batch_size
    }
    /// The maximum time in seconds to wait for a batch to fill before delivering it to the target. This is a maximum; a batch may be delivered sooner if it reaches MaxBatchSize or another delivery condition is met. Valid range is 0-300 (default: 0, meaning no wait). The resolved value applied by the service is always returned on read.
    pub fn max_batch_window_in_seconds(&self) -> ::std::option::Option<i32> {
        self.max_batch_window_in_seconds
    }
}
impl BatchConfiguration {
    /// Creates a new builder-style object to manufacture [`BatchConfiguration`](crate::types::BatchConfiguration).
    pub fn builder() -> crate::types::builders::BatchConfigurationBuilder {
        crate::types::builders::BatchConfigurationBuilder::default()
    }
}

/// A builder for [`BatchConfiguration`](crate::types::BatchConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct BatchConfigurationBuilder {
    pub(crate) max_batch_size: ::std::option::Option<i32>,
    pub(crate) max_batch_window_in_seconds: ::std::option::Option<i32>,
}
impl BatchConfigurationBuilder {
    /// The maximum number of events to include in a single batch delivered to the target. The service delivers up to this many events per batch; fewer may be delivered when the batch window elapses or the target's per-batch limit is smaller. This is a maximum, not a guaranteed count. Valid range is 1-500 (default: 10, or the target API's per-batch maximum). The resolved value applied by the service is returned on read.
    pub fn max_batch_size(mut self, input: i32) -> Self {
        self.max_batch_size = ::std::option::Option::Some(input);
        self
    }
    /// The maximum number of events to include in a single batch delivered to the target. The service delivers up to this many events per batch; fewer may be delivered when the batch window elapses or the target's per-batch limit is smaller. This is a maximum, not a guaranteed count. Valid range is 1-500 (default: 10, or the target API's per-batch maximum). The resolved value applied by the service is returned on read.
    pub fn set_max_batch_size(mut self, input: ::std::option::Option<i32>) -> Self {
        self.max_batch_size = input;
        self
    }
    /// The maximum number of events to include in a single batch delivered to the target. The service delivers up to this many events per batch; fewer may be delivered when the batch window elapses or the target's per-batch limit is smaller. This is a maximum, not a guaranteed count. Valid range is 1-500 (default: 10, or the target API's per-batch maximum). The resolved value applied by the service is returned on read.
    pub fn get_max_batch_size(&self) -> &::std::option::Option<i32> {
        &self.max_batch_size
    }
    /// The maximum time in seconds to wait for a batch to fill before delivering it to the target. This is a maximum; a batch may be delivered sooner if it reaches MaxBatchSize or another delivery condition is met. Valid range is 0-300 (default: 0, meaning no wait). The resolved value applied by the service is always returned on read.
    pub fn max_batch_window_in_seconds(mut self, input: i32) -> Self {
        self.max_batch_window_in_seconds = ::std::option::Option::Some(input);
        self
    }
    /// The maximum time in seconds to wait for a batch to fill before delivering it to the target. This is a maximum; a batch may be delivered sooner if it reaches MaxBatchSize or another delivery condition is met. Valid range is 0-300 (default: 0, meaning no wait). The resolved value applied by the service is always returned on read.
    pub fn set_max_batch_window_in_seconds(mut self, input: ::std::option::Option<i32>) -> Self {
        self.max_batch_window_in_seconds = input;
        self
    }
    /// The maximum time in seconds to wait for a batch to fill before delivering it to the target. This is a maximum; a batch may be delivered sooner if it reaches MaxBatchSize or another delivery condition is met. Valid range is 0-300 (default: 0, meaning no wait). The resolved value applied by the service is always returned on read.
    pub fn get_max_batch_window_in_seconds(&self) -> &::std::option::Option<i32> {
        &self.max_batch_window_in_seconds
    }
    /// Consumes the builder and constructs a [`BatchConfiguration`](crate::types::BatchConfiguration).
    pub fn build(self) -> crate::types::BatchConfiguration {
        crate::types::BatchConfiguration {
            max_batch_size: self.max_batch_size,
            max_batch_window_in_seconds: self.max_batch_window_in_seconds,
        }
    }
}
