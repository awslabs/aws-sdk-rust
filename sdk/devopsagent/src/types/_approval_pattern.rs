// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Tool-invocation pattern primitive used to express both an agent-requested approval and a finalized approval. The primitive is uniform across AWS and third-party tools: a tool identifier plus a map of argument pins that narrow which invocations the pattern matches.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ApprovalPattern {
    /// <p>Identifier of the tool the pattern applies to (e.g. `use_aws` for AWS actions, or a third-party tool name).</p>
    pub tool: ::std::string::String,
    /// <p>Argument constraints that narrow which tool invocations the pattern matches. For AWS tools, the map must include `operation` (the IAM action, e.g. `ec2:AuthorizeSecurityGroupIngress`) and `resource_arn` (the resource ARN or ARN glob); additional narrowing arguments go in further pin keys. The same `{tool, argumentPins}` shape is used uniformly for AWS and third-party tools, with tool-specific keys for third-party tools. Requests whose argument pins are collectively too large are rejected with a ValidationException.</p>
    pub argument_pins: ::std::collections::HashMap<::std::string::String, ::std::string::String>,
}
impl ApprovalPattern {
    /// <p>Identifier of the tool the pattern applies to (e.g. `use_aws` for AWS actions, or a third-party tool name).</p>
    pub fn tool(&self) -> &str {
        use std::ops::Deref;
        self.tool.deref()
    }
    /// <p>Argument constraints that narrow which tool invocations the pattern matches. For AWS tools, the map must include `operation` (the IAM action, e.g. `ec2:AuthorizeSecurityGroupIngress`) and `resource_arn` (the resource ARN or ARN glob); additional narrowing arguments go in further pin keys. The same `{tool, argumentPins}` shape is used uniformly for AWS and third-party tools, with tool-specific keys for third-party tools. Requests whose argument pins are collectively too large are rejected with a ValidationException.</p>
    pub fn argument_pins(&self) -> &::std::collections::HashMap<::std::string::String, ::std::string::String> {
        &self.argument_pins
    }
}
impl ApprovalPattern {
    /// Creates a new builder-style object to manufacture [`ApprovalPattern`](crate::types::ApprovalPattern).
    pub fn builder() -> crate::types::builders::ApprovalPatternBuilder {
        crate::types::builders::ApprovalPatternBuilder::default()
    }
}

/// A builder for [`ApprovalPattern`](crate::types::ApprovalPattern).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ApprovalPatternBuilder {
    pub(crate) tool: ::std::option::Option<::std::string::String>,
    pub(crate) argument_pins: ::std::option::Option<::std::collections::HashMap<::std::string::String, ::std::string::String>>,
}
impl ApprovalPatternBuilder {
    /// <p>Identifier of the tool the pattern applies to (e.g. `use_aws` for AWS actions, or a third-party tool name).</p>
    /// This field is required.
    pub fn tool(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.tool = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>Identifier of the tool the pattern applies to (e.g. `use_aws` for AWS actions, or a third-party tool name).</p>
    pub fn set_tool(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.tool = input;
        self
    }
    /// <p>Identifier of the tool the pattern applies to (e.g. `use_aws` for AWS actions, or a third-party tool name).</p>
    pub fn get_tool(&self) -> &::std::option::Option<::std::string::String> {
        &self.tool
    }
    /// Adds a key-value pair to `argument_pins`.
    ///
    /// To override the contents of this collection use [`set_argument_pins`](Self::set_argument_pins).
    ///
    /// <p>Argument constraints that narrow which tool invocations the pattern matches. For AWS tools, the map must include `operation` (the IAM action, e.g. `ec2:AuthorizeSecurityGroupIngress`) and `resource_arn` (the resource ARN or ARN glob); additional narrowing arguments go in further pin keys. The same `{tool, argumentPins}` shape is used uniformly for AWS and third-party tools, with tool-specific keys for third-party tools. Requests whose argument pins are collectively too large are rejected with a ValidationException.</p>
    pub fn argument_pins(mut self, k: impl ::std::convert::Into<::std::string::String>, v: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut hash_map = self.argument_pins.unwrap_or_default();
        hash_map.insert(k.into(), v.into());
        self.argument_pins = ::std::option::Option::Some(hash_map);
        self
    }
    /// <p>Argument constraints that narrow which tool invocations the pattern matches. For AWS tools, the map must include `operation` (the IAM action, e.g. `ec2:AuthorizeSecurityGroupIngress`) and `resource_arn` (the resource ARN or ARN glob); additional narrowing arguments go in further pin keys. The same `{tool, argumentPins}` shape is used uniformly for AWS and third-party tools, with tool-specific keys for third-party tools. Requests whose argument pins are collectively too large are rejected with a ValidationException.</p>
    pub fn set_argument_pins(
        mut self,
        input: ::std::option::Option<::std::collections::HashMap<::std::string::String, ::std::string::String>>,
    ) -> Self {
        self.argument_pins = input;
        self
    }
    /// <p>Argument constraints that narrow which tool invocations the pattern matches. For AWS tools, the map must include `operation` (the IAM action, e.g. `ec2:AuthorizeSecurityGroupIngress`) and `resource_arn` (the resource ARN or ARN glob); additional narrowing arguments go in further pin keys. The same `{tool, argumentPins}` shape is used uniformly for AWS and third-party tools, with tool-specific keys for third-party tools. Requests whose argument pins are collectively too large are rejected with a ValidationException.</p>
    pub fn get_argument_pins(&self) -> &::std::option::Option<::std::collections::HashMap<::std::string::String, ::std::string::String>> {
        &self.argument_pins
    }
    /// Consumes the builder and constructs a [`ApprovalPattern`](crate::types::ApprovalPattern).
    /// This method will fail if any of the following fields are not set:
    /// - [`tool`](crate::types::builders::ApprovalPatternBuilder::tool)
    /// - [`argument_pins`](crate::types::builders::ApprovalPatternBuilder::argument_pins)
    pub fn build(self) -> ::std::result::Result<crate::types::ApprovalPattern, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::ApprovalPattern {
            tool: self.tool.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "tool",
                    "tool was not specified but it is required when building ApprovalPattern",
                )
            })?,
            argument_pins: self.argument_pins.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "argument_pins",
                    "argument_pins was not specified but it is required when building ApprovalPattern",
                )
            })?,
        })
    }
}
