// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Union of all supported service configuration types. Each service has its own specific configuration structure.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum ServiceConfiguration {
    /// <p>AWS monitor account configuration.</p>
    Aws(crate::types::AwsConfiguration),
    /// <p>Azure subscription integration configuration.</p>
    Azure(crate::types::AzureConfiguration),
    /// <p>Azure DevOps project integration configuration.</p>
    Azuredevops(crate::types::AzureDevOpsConfiguration),
    /// <p>Dynatrace monitoring integration configuration.</p>
    Dynatrace(crate::types::DynatraceConfiguration),
    /// <p>Event Channel instance integration configuration.</p>
    EventChannel(crate::types::EventChannelConfiguration),
    /// <p>GitHub repository integration configuration.</p>
    Github(crate::types::GitHubConfiguration),
    /// <p>GitLab project integration configuration.</p>
    Gitlab(crate::types::GitLabConfiguration),
    /// <p>MCP (Model Context Protocol) server integration configuration.</p>
    Mcpserver(crate::types::McpServerConfiguration),
    /// <p>Datadog MCP server integration configuration.</p>
    Mcpserverdatadog(crate::types::McpServerDatadogConfiguration),
    /// <p>Grafana MCP server integration configuration.</p>
    Mcpservergrafana(crate::types::McpServerGrafanaConfiguration),
    /// <p>NewRelic instance integration configuration.</p>
    Mcpservernewrelic(crate::types::McpServerNewRelicConfiguration),
    /// <p>SigV4-authenticated MCP server integration configuration.</p>
    Mcpserversigv4(crate::types::McpServerSigV4Configuration),
    /// <p>Splunk MCP server integration configuration.</p>
    Mcpserversplunk(crate::types::McpServerSplunkConfiguration),
    /// <p>PagerDuty integration configuration</p>
    Pagerduty(crate::types::PagerDutyConfiguration),
    /// <p>ServiceNow instance integration configuration.</p>
    Servicenow(crate::types::ServiceNowConfiguration),
    /// <p>Slack workspace integration configuration.</p>
    Slack(crate::types::SlackConfiguration),
    /// <p>AWS source account configuration for monitoring resources.</p>
    SourceAws(crate::types::SourceAwsConfiguration),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl ServiceConfiguration {
    /// Tries to convert the enum instance into [`Aws`](crate::types::ServiceConfiguration::Aws), extracting the inner [`AwsConfiguration`](crate::types::AwsConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_aws(&self) -> ::std::result::Result<&crate::types::AwsConfiguration, &Self> {
        if let ServiceConfiguration::Aws(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Aws`](crate::types::ServiceConfiguration::Aws).
    pub fn is_aws(&self) -> bool {
        self.as_aws().is_ok()
    }
    /// Tries to convert the enum instance into [`Azure`](crate::types::ServiceConfiguration::Azure), extracting the inner [`AzureConfiguration`](crate::types::AzureConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_azure(&self) -> ::std::result::Result<&crate::types::AzureConfiguration, &Self> {
        if let ServiceConfiguration::Azure(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Azure`](crate::types::ServiceConfiguration::Azure).
    pub fn is_azure(&self) -> bool {
        self.as_azure().is_ok()
    }
    /// Tries to convert the enum instance into [`Azuredevops`](crate::types::ServiceConfiguration::Azuredevops), extracting the inner [`AzureDevOpsConfiguration`](crate::types::AzureDevOpsConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_azuredevops(&self) -> ::std::result::Result<&crate::types::AzureDevOpsConfiguration, &Self> {
        if let ServiceConfiguration::Azuredevops(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Azuredevops`](crate::types::ServiceConfiguration::Azuredevops).
    pub fn is_azuredevops(&self) -> bool {
        self.as_azuredevops().is_ok()
    }
    /// Tries to convert the enum instance into [`Dynatrace`](crate::types::ServiceConfiguration::Dynatrace), extracting the inner [`DynatraceConfiguration`](crate::types::DynatraceConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_dynatrace(&self) -> ::std::result::Result<&crate::types::DynatraceConfiguration, &Self> {
        if let ServiceConfiguration::Dynatrace(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Dynatrace`](crate::types::ServiceConfiguration::Dynatrace).
    pub fn is_dynatrace(&self) -> bool {
        self.as_dynatrace().is_ok()
    }
    /// Tries to convert the enum instance into [`EventChannel`](crate::types::ServiceConfiguration::EventChannel), extracting the inner [`EventChannelConfiguration`](crate::types::EventChannelConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_event_channel(&self) -> ::std::result::Result<&crate::types::EventChannelConfiguration, &Self> {
        if let ServiceConfiguration::EventChannel(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`EventChannel`](crate::types::ServiceConfiguration::EventChannel).
    pub fn is_event_channel(&self) -> bool {
        self.as_event_channel().is_ok()
    }
    /// Tries to convert the enum instance into [`Github`](crate::types::ServiceConfiguration::Github), extracting the inner [`GitHubConfiguration`](crate::types::GitHubConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_github(&self) -> ::std::result::Result<&crate::types::GitHubConfiguration, &Self> {
        if let ServiceConfiguration::Github(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Github`](crate::types::ServiceConfiguration::Github).
    pub fn is_github(&self) -> bool {
        self.as_github().is_ok()
    }
    /// Tries to convert the enum instance into [`Gitlab`](crate::types::ServiceConfiguration::Gitlab), extracting the inner [`GitLabConfiguration`](crate::types::GitLabConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_gitlab(&self) -> ::std::result::Result<&crate::types::GitLabConfiguration, &Self> {
        if let ServiceConfiguration::Gitlab(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Gitlab`](crate::types::ServiceConfiguration::Gitlab).
    pub fn is_gitlab(&self) -> bool {
        self.as_gitlab().is_ok()
    }
    /// Tries to convert the enum instance into [`Mcpserver`](crate::types::ServiceConfiguration::Mcpserver), extracting the inner [`McpServerConfiguration`](crate::types::McpServerConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_mcpserver(&self) -> ::std::result::Result<&crate::types::McpServerConfiguration, &Self> {
        if let ServiceConfiguration::Mcpserver(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Mcpserver`](crate::types::ServiceConfiguration::Mcpserver).
    pub fn is_mcpserver(&self) -> bool {
        self.as_mcpserver().is_ok()
    }
    /// Tries to convert the enum instance into [`Mcpserverdatadog`](crate::types::ServiceConfiguration::Mcpserverdatadog), extracting the inner [`McpServerDatadogConfiguration`](crate::types::McpServerDatadogConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_mcpserverdatadog(&self) -> ::std::result::Result<&crate::types::McpServerDatadogConfiguration, &Self> {
        if let ServiceConfiguration::Mcpserverdatadog(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Mcpserverdatadog`](crate::types::ServiceConfiguration::Mcpserverdatadog).
    pub fn is_mcpserverdatadog(&self) -> bool {
        self.as_mcpserverdatadog().is_ok()
    }
    /// Tries to convert the enum instance into [`Mcpservergrafana`](crate::types::ServiceConfiguration::Mcpservergrafana), extracting the inner [`McpServerGrafanaConfiguration`](crate::types::McpServerGrafanaConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_mcpservergrafana(&self) -> ::std::result::Result<&crate::types::McpServerGrafanaConfiguration, &Self> {
        if let ServiceConfiguration::Mcpservergrafana(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Mcpservergrafana`](crate::types::ServiceConfiguration::Mcpservergrafana).
    pub fn is_mcpservergrafana(&self) -> bool {
        self.as_mcpservergrafana().is_ok()
    }
    /// Tries to convert the enum instance into [`Mcpservernewrelic`](crate::types::ServiceConfiguration::Mcpservernewrelic), extracting the inner [`McpServerNewRelicConfiguration`](crate::types::McpServerNewRelicConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_mcpservernewrelic(&self) -> ::std::result::Result<&crate::types::McpServerNewRelicConfiguration, &Self> {
        if let ServiceConfiguration::Mcpservernewrelic(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Mcpservernewrelic`](crate::types::ServiceConfiguration::Mcpservernewrelic).
    pub fn is_mcpservernewrelic(&self) -> bool {
        self.as_mcpservernewrelic().is_ok()
    }
    /// Tries to convert the enum instance into [`Mcpserversigv4`](crate::types::ServiceConfiguration::Mcpserversigv4), extracting the inner [`McpServerSigV4Configuration`](crate::types::McpServerSigV4Configuration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_mcpserversigv4(&self) -> ::std::result::Result<&crate::types::McpServerSigV4Configuration, &Self> {
        if let ServiceConfiguration::Mcpserversigv4(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Mcpserversigv4`](crate::types::ServiceConfiguration::Mcpserversigv4).
    pub fn is_mcpserversigv4(&self) -> bool {
        self.as_mcpserversigv4().is_ok()
    }
    /// Tries to convert the enum instance into [`Mcpserversplunk`](crate::types::ServiceConfiguration::Mcpserversplunk), extracting the inner [`McpServerSplunkConfiguration`](crate::types::McpServerSplunkConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_mcpserversplunk(&self) -> ::std::result::Result<&crate::types::McpServerSplunkConfiguration, &Self> {
        if let ServiceConfiguration::Mcpserversplunk(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Mcpserversplunk`](crate::types::ServiceConfiguration::Mcpserversplunk).
    pub fn is_mcpserversplunk(&self) -> bool {
        self.as_mcpserversplunk().is_ok()
    }
    /// Tries to convert the enum instance into [`Pagerduty`](crate::types::ServiceConfiguration::Pagerduty), extracting the inner [`PagerDutyConfiguration`](crate::types::PagerDutyConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_pagerduty(&self) -> ::std::result::Result<&crate::types::PagerDutyConfiguration, &Self> {
        if let ServiceConfiguration::Pagerduty(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Pagerduty`](crate::types::ServiceConfiguration::Pagerduty).
    pub fn is_pagerduty(&self) -> bool {
        self.as_pagerduty().is_ok()
    }
    /// Tries to convert the enum instance into [`Servicenow`](crate::types::ServiceConfiguration::Servicenow), extracting the inner [`ServiceNowConfiguration`](crate::types::ServiceNowConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_servicenow(&self) -> ::std::result::Result<&crate::types::ServiceNowConfiguration, &Self> {
        if let ServiceConfiguration::Servicenow(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Servicenow`](crate::types::ServiceConfiguration::Servicenow).
    pub fn is_servicenow(&self) -> bool {
        self.as_servicenow().is_ok()
    }
    /// Tries to convert the enum instance into [`Slack`](crate::types::ServiceConfiguration::Slack), extracting the inner [`SlackConfiguration`](crate::types::SlackConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_slack(&self) -> ::std::result::Result<&crate::types::SlackConfiguration, &Self> {
        if let ServiceConfiguration::Slack(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Slack`](crate::types::ServiceConfiguration::Slack).
    pub fn is_slack(&self) -> bool {
        self.as_slack().is_ok()
    }
    /// Tries to convert the enum instance into [`SourceAws`](crate::types::ServiceConfiguration::SourceAws), extracting the inner [`SourceAwsConfiguration`](crate::types::SourceAwsConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_source_aws(&self) -> ::std::result::Result<&crate::types::SourceAwsConfiguration, &Self> {
        if let ServiceConfiguration::SourceAws(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`SourceAws`](crate::types::ServiceConfiguration::SourceAws).
    pub fn is_source_aws(&self) -> bool {
        self.as_source_aws().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
