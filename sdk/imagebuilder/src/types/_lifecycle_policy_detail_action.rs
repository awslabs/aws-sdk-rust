// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains the action configuration for a lifecycle policy rule: the action to take, and which underlying resources the action extends to.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct LifecyclePolicyDetailAction {
    /// <p>Specifies the lifecycle action to take. <code>DELETE</code> deletes the image resource and, with <code>includeResources</code>, also removes distributed AMIs, snapshots, or container images. <code>DEPRECATE</code> and <code>DISABLE</code> set the corresponding status on the image resource and, if <code>includeResources.amis</code> is set, on its distributed AMIs.</p>
    pub r#type: crate::types::LifecyclePolicyDetailActionType,
    /// <p>Specifies which underlying resources the action extends to beyond the Image Builder image resource itself: distributed AMIs, their snapshots, or distributed container images. <code>DELETE</code> rules can include all three, <code>DEPRECATE</code> and <code>DISABLE</code> rules can include AMIs only, and you can only include snapshots together with AMIs.</p>
    pub include_resources: ::std::option::Option<crate::types::LifecyclePolicyDetailActionIncludeResources>,
}
impl LifecyclePolicyDetailAction {
    /// <p>Specifies the lifecycle action to take. <code>DELETE</code> deletes the image resource and, with <code>includeResources</code>, also removes distributed AMIs, snapshots, or container images. <code>DEPRECATE</code> and <code>DISABLE</code> set the corresponding status on the image resource and, if <code>includeResources.amis</code> is set, on its distributed AMIs.</p>
    pub fn r#type(&self) -> &crate::types::LifecyclePolicyDetailActionType {
        &self.r#type
    }
    /// <p>Specifies which underlying resources the action extends to beyond the Image Builder image resource itself: distributed AMIs, their snapshots, or distributed container images. <code>DELETE</code> rules can include all three, <code>DEPRECATE</code> and <code>DISABLE</code> rules can include AMIs only, and you can only include snapshots together with AMIs.</p>
    pub fn include_resources(&self) -> ::std::option::Option<&crate::types::LifecyclePolicyDetailActionIncludeResources> {
        self.include_resources.as_ref()
    }
}
impl LifecyclePolicyDetailAction {
    /// Creates a new builder-style object to manufacture [`LifecyclePolicyDetailAction`](crate::types::LifecyclePolicyDetailAction).
    pub fn builder() -> crate::types::builders::LifecyclePolicyDetailActionBuilder {
        crate::types::builders::LifecyclePolicyDetailActionBuilder::default()
    }
}

/// A builder for [`LifecyclePolicyDetailAction`](crate::types::LifecyclePolicyDetailAction).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct LifecyclePolicyDetailActionBuilder {
    pub(crate) r#type: ::std::option::Option<crate::types::LifecyclePolicyDetailActionType>,
    pub(crate) include_resources: ::std::option::Option<crate::types::LifecyclePolicyDetailActionIncludeResources>,
}
impl LifecyclePolicyDetailActionBuilder {
    /// <p>Specifies the lifecycle action to take. <code>DELETE</code> deletes the image resource and, with <code>includeResources</code>, also removes distributed AMIs, snapshots, or container images. <code>DEPRECATE</code> and <code>DISABLE</code> set the corresponding status on the image resource and, if <code>includeResources.amis</code> is set, on its distributed AMIs.</p>
    /// This field is required.
    pub fn r#type(mut self, input: crate::types::LifecyclePolicyDetailActionType) -> Self {
        self.r#type = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies the lifecycle action to take. <code>DELETE</code> deletes the image resource and, with <code>includeResources</code>, also removes distributed AMIs, snapshots, or container images. <code>DEPRECATE</code> and <code>DISABLE</code> set the corresponding status on the image resource and, if <code>includeResources.amis</code> is set, on its distributed AMIs.</p>
    pub fn set_type(mut self, input: ::std::option::Option<crate::types::LifecyclePolicyDetailActionType>) -> Self {
        self.r#type = input;
        self
    }
    /// <p>Specifies the lifecycle action to take. <code>DELETE</code> deletes the image resource and, with <code>includeResources</code>, also removes distributed AMIs, snapshots, or container images. <code>DEPRECATE</code> and <code>DISABLE</code> set the corresponding status on the image resource and, if <code>includeResources.amis</code> is set, on its distributed AMIs.</p>
    pub fn get_type(&self) -> &::std::option::Option<crate::types::LifecyclePolicyDetailActionType> {
        &self.r#type
    }
    /// <p>Specifies which underlying resources the action extends to beyond the Image Builder image resource itself: distributed AMIs, their snapshots, or distributed container images. <code>DELETE</code> rules can include all three, <code>DEPRECATE</code> and <code>DISABLE</code> rules can include AMIs only, and you can only include snapshots together with AMIs.</p>
    pub fn include_resources(mut self, input: crate::types::LifecyclePolicyDetailActionIncludeResources) -> Self {
        self.include_resources = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies which underlying resources the action extends to beyond the Image Builder image resource itself: distributed AMIs, their snapshots, or distributed container images. <code>DELETE</code> rules can include all three, <code>DEPRECATE</code> and <code>DISABLE</code> rules can include AMIs only, and you can only include snapshots together with AMIs.</p>
    pub fn set_include_resources(mut self, input: ::std::option::Option<crate::types::LifecyclePolicyDetailActionIncludeResources>) -> Self {
        self.include_resources = input;
        self
    }
    /// <p>Specifies which underlying resources the action extends to beyond the Image Builder image resource itself: distributed AMIs, their snapshots, or distributed container images. <code>DELETE</code> rules can include all three, <code>DEPRECATE</code> and <code>DISABLE</code> rules can include AMIs only, and you can only include snapshots together with AMIs.</p>
    pub fn get_include_resources(&self) -> &::std::option::Option<crate::types::LifecyclePolicyDetailActionIncludeResources> {
        &self.include_resources
    }
    /// Consumes the builder and constructs a [`LifecyclePolicyDetailAction`](crate::types::LifecyclePolicyDetailAction).
    /// This method will fail if any of the following fields are not set:
    /// - [`r#type`](crate::types::builders::LifecyclePolicyDetailActionBuilder::type)
    pub fn build(self) -> ::std::result::Result<crate::types::LifecyclePolicyDetailAction, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::LifecyclePolicyDetailAction {
            r#type: self.r#type.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "r#type",
                    "r#type was not specified but it is required when building LifecyclePolicyDetailAction",
                )
            })?,
            include_resources: self.include_resources,
        })
    }
}
