// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A filter clause that supports nested boolean logic. Exactly one of <code>andAllFilters</code>, <code>orAnyFilters</code>, <code>attributeFilter</code>, or <code>mapFilter</code> must be specified.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum SearchFilterClause {
    /// <p>A list of filter clauses that must all match (logical AND).</p>
    AndAllFilters(::std::vec::Vec<crate::types::SearchFilterClause>),
    /// <p>A filter on a single attribute value.</p>
    AttributeFilter(crate::types::SearchAttributeFilter),
    /// <p>A filter on a map attribute's key-value pair.</p>
    MapFilter(crate::types::SearchMapFilter),
    /// <p>A list of filter clauses where at least one must match (logical OR).</p>
    OrAnyFilters(::std::vec::Vec<crate::types::SearchFilterClause>),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl SearchFilterClause {
    /// Tries to convert the enum instance into [`AndAllFilters`](crate::types::SearchFilterClause::AndAllFilters), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_and_all_filters(&self) -> ::std::result::Result<&::std::vec::Vec<crate::types::SearchFilterClause>, &Self> {
        if let SearchFilterClause::AndAllFilters(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`AndAllFilters`](crate::types::SearchFilterClause::AndAllFilters).
    pub fn is_and_all_filters(&self) -> bool {
        self.as_and_all_filters().is_ok()
    }
    /// Tries to convert the enum instance into [`AttributeFilter`](crate::types::SearchFilterClause::AttributeFilter), extracting the inner [`SearchAttributeFilter`](crate::types::SearchAttributeFilter).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_attribute_filter(&self) -> ::std::result::Result<&crate::types::SearchAttributeFilter, &Self> {
        if let SearchFilterClause::AttributeFilter(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`AttributeFilter`](crate::types::SearchFilterClause::AttributeFilter).
    pub fn is_attribute_filter(&self) -> bool {
        self.as_attribute_filter().is_ok()
    }
    /// Tries to convert the enum instance into [`MapFilter`](crate::types::SearchFilterClause::MapFilter), extracting the inner [`SearchMapFilter`](crate::types::SearchMapFilter).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_map_filter(&self) -> ::std::result::Result<&crate::types::SearchMapFilter, &Self> {
        if let SearchFilterClause::MapFilter(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`MapFilter`](crate::types::SearchFilterClause::MapFilter).
    pub fn is_map_filter(&self) -> bool {
        self.as_map_filter().is_ok()
    }
    /// Tries to convert the enum instance into [`OrAnyFilters`](crate::types::SearchFilterClause::OrAnyFilters), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_or_any_filters(&self) -> ::std::result::Result<&::std::vec::Vec<crate::types::SearchFilterClause>, &Self> {
        if let SearchFilterClause::OrAnyFilters(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`OrAnyFilters`](crate::types::SearchFilterClause::OrAnyFilters).
    pub fn is_or_any_filters(&self) -> bool {
        self.as_or_any_filters().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
