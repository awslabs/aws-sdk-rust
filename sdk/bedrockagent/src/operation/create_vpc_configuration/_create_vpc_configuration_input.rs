// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct CreateVpcConfigurationInput {
    /// <p>The unique identifier of the knowledge base to associate this VPC configuration with.</p>
    pub knowledge_base_id: ::std::option::Option<::std::string::String>,
    /// <p>A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, the service ignores the request but does not return an error.</p>
    pub client_token: ::std::option::Option<::std::string::String>,
    /// <p>The identifier of the VPC that the knowledge base connects through to reach the resource.</p>
    pub vpc_id: ::std::option::Option<::std::string::String>,
    /// <p>The subnets, in the VPC identified by <code>vpcId</code>, that the knowledge base uses to connect to the resource.</p>
    pub subnet_ids: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    /// <p>The private IPv4 address or DNS name of the resource you want the knowledge base to reach. The target must be privately reachable from inside your VPC, such as an internal load balancer or a private IP. The following are not supported:</p>
    /// <ul>
    /// <li>
    /// <p>Internet-facing endpoints</p></li>
    /// <li>
    /// <p>Loopback addresses</p></li>
    /// <li>
    /// <p>Link-local addresses</p></li>
    /// <li>
    /// <p>Wildcard addresses</p></li>
    /// <li>
    /// <p>Multicast addresses</p></li>
    /// <li>
    /// <p>IPv6 literals</p></li>
    /// </ul>
    pub resource_target: ::std::option::Option<::std::string::String>,
    /// <p>The port on which to reach the resource.</p>
    pub port: ::std::option::Option<i32>,
    /// <p>The protocol used to connect to the resource. Specify <code>HTTP</code> for plaintext or <code>HTTPS</code> for TLS. When you specify <code>HTTPS</code>, you must also provide <code>tlsServerName</code>.</p>
    pub protocol: ::std::option::Option<crate::types::VpcProtocol>,
    /// <p>Controls how a domain-name <code>resourceTarget</code> is resolved. This applies only when the target is a domain name; it has no effect for IP-address targets, which have no name to resolve. In all cases the resolved address must be reachable from inside your VPC. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>IN_VPC</code> (default, recommended) – The target domain name is resolved privately, using the DNS resolvers of the VPC, such as private Route 53 hosted zones or on-premises DNS reachable from the VPC. Use this for targets that are private to your VPC, such as internal load balancers, private hosted-zone names, or on-premises hosts.</p></li>
    /// <li>
    /// <p><code>PUBLIC</code> – The target domain name is resolved against public DNS resolvers. Select this only when the target's domain name must be resolved through public DNS and the resulting address is still reachable from the VPC, an uncommon split-horizon configuration. If you are unsure, use <code>IN_VPC</code>.</p></li>
    /// </ul>
    pub resolution_mode: ::std::option::Option<crate::types::VpcResolutionMode>,
    /// <p>An optional HTTP <code>Host</code> header value to send when invoking the resource. Set this only if your resource (or an upstream router or ingress) routes by the <code>Host</code> header and that host differs from the target. This setting is independent of <code>tlsServerName</code>.</p>
    pub host_header: ::std::option::Option<::std::string::String>,
    /// <p>The expected TLS server name. The service matches this value against the Subject Alternative Names on your resource's TLS certificate during invocation. This field is required when <code>protocol</code> is <code>HTTPS</code>. Set it to a hostname on your certificate, such as <code>app.internal.example.com</code>. You can use a single leftmost wildcard, such as <code>*.example.com</code>. The value must be a hostname without a port.</p>
    pub tls_server_name: ::std::option::Option<::std::string::String>,
    /// <p>An optional human-readable name for the VPC configuration. If you don't specify a name, the VPC configuration has no name.</p>
    pub name: ::std::option::Option<::std::string::String>,
    /// <p>An optional description of the VPC configuration. If you don't specify a description, the VPC configuration has no description.</p>
    pub description: ::std::option::Option<::std::string::String>,
}
impl CreateVpcConfigurationInput {
    /// <p>The unique identifier of the knowledge base to associate this VPC configuration with.</p>
    pub fn knowledge_base_id(&self) -> ::std::option::Option<&str> {
        self.knowledge_base_id.as_deref()
    }
    /// <p>A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, the service ignores the request but does not return an error.</p>
    pub fn client_token(&self) -> ::std::option::Option<&str> {
        self.client_token.as_deref()
    }
    /// <p>The identifier of the VPC that the knowledge base connects through to reach the resource.</p>
    pub fn vpc_id(&self) -> ::std::option::Option<&str> {
        self.vpc_id.as_deref()
    }
    /// <p>The subnets, in the VPC identified by <code>vpcId</code>, that the knowledge base uses to connect to the resource.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.subnet_ids.is_none()`.
    pub fn subnet_ids(&self) -> &[::std::string::String] {
        self.subnet_ids.as_deref().unwrap_or_default()
    }
    /// <p>The private IPv4 address or DNS name of the resource you want the knowledge base to reach. The target must be privately reachable from inside your VPC, such as an internal load balancer or a private IP. The following are not supported:</p>
    /// <ul>
    /// <li>
    /// <p>Internet-facing endpoints</p></li>
    /// <li>
    /// <p>Loopback addresses</p></li>
    /// <li>
    /// <p>Link-local addresses</p></li>
    /// <li>
    /// <p>Wildcard addresses</p></li>
    /// <li>
    /// <p>Multicast addresses</p></li>
    /// <li>
    /// <p>IPv6 literals</p></li>
    /// </ul>
    pub fn resource_target(&self) -> ::std::option::Option<&str> {
        self.resource_target.as_deref()
    }
    /// <p>The port on which to reach the resource.</p>
    pub fn port(&self) -> ::std::option::Option<i32> {
        self.port
    }
    /// <p>The protocol used to connect to the resource. Specify <code>HTTP</code> for plaintext or <code>HTTPS</code> for TLS. When you specify <code>HTTPS</code>, you must also provide <code>tlsServerName</code>.</p>
    pub fn protocol(&self) -> ::std::option::Option<&crate::types::VpcProtocol> {
        self.protocol.as_ref()
    }
    /// <p>Controls how a domain-name <code>resourceTarget</code> is resolved. This applies only when the target is a domain name; it has no effect for IP-address targets, which have no name to resolve. In all cases the resolved address must be reachable from inside your VPC. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>IN_VPC</code> (default, recommended) – The target domain name is resolved privately, using the DNS resolvers of the VPC, such as private Route 53 hosted zones or on-premises DNS reachable from the VPC. Use this for targets that are private to your VPC, such as internal load balancers, private hosted-zone names, or on-premises hosts.</p></li>
    /// <li>
    /// <p><code>PUBLIC</code> – The target domain name is resolved against public DNS resolvers. Select this only when the target's domain name must be resolved through public DNS and the resulting address is still reachable from the VPC, an uncommon split-horizon configuration. If you are unsure, use <code>IN_VPC</code>.</p></li>
    /// </ul>
    pub fn resolution_mode(&self) -> ::std::option::Option<&crate::types::VpcResolutionMode> {
        self.resolution_mode.as_ref()
    }
    /// <p>An optional HTTP <code>Host</code> header value to send when invoking the resource. Set this only if your resource (or an upstream router or ingress) routes by the <code>Host</code> header and that host differs from the target. This setting is independent of <code>tlsServerName</code>.</p>
    pub fn host_header(&self) -> ::std::option::Option<&str> {
        self.host_header.as_deref()
    }
    /// <p>The expected TLS server name. The service matches this value against the Subject Alternative Names on your resource's TLS certificate during invocation. This field is required when <code>protocol</code> is <code>HTTPS</code>. Set it to a hostname on your certificate, such as <code>app.internal.example.com</code>. You can use a single leftmost wildcard, such as <code>*.example.com</code>. The value must be a hostname without a port.</p>
    pub fn tls_server_name(&self) -> ::std::option::Option<&str> {
        self.tls_server_name.as_deref()
    }
    /// <p>An optional human-readable name for the VPC configuration. If you don't specify a name, the VPC configuration has no name.</p>
    pub fn name(&self) -> ::std::option::Option<&str> {
        self.name.as_deref()
    }
    /// <p>An optional description of the VPC configuration. If you don't specify a description, the VPC configuration has no description.</p>
    pub fn description(&self) -> ::std::option::Option<&str> {
        self.description.as_deref()
    }
}
impl CreateVpcConfigurationInput {
    /// Creates a new builder-style object to manufacture [`CreateVpcConfigurationInput`](crate::operation::create_vpc_configuration::CreateVpcConfigurationInput).
    pub fn builder() -> crate::operation::create_vpc_configuration::builders::CreateVpcConfigurationInputBuilder {
        crate::operation::create_vpc_configuration::builders::CreateVpcConfigurationInputBuilder::default()
    }
}

/// A builder for [`CreateVpcConfigurationInput`](crate::operation::create_vpc_configuration::CreateVpcConfigurationInput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct CreateVpcConfigurationInputBuilder {
    pub(crate) knowledge_base_id: ::std::option::Option<::std::string::String>,
    pub(crate) client_token: ::std::option::Option<::std::string::String>,
    pub(crate) vpc_id: ::std::option::Option<::std::string::String>,
    pub(crate) subnet_ids: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    pub(crate) resource_target: ::std::option::Option<::std::string::String>,
    pub(crate) port: ::std::option::Option<i32>,
    pub(crate) protocol: ::std::option::Option<crate::types::VpcProtocol>,
    pub(crate) resolution_mode: ::std::option::Option<crate::types::VpcResolutionMode>,
    pub(crate) host_header: ::std::option::Option<::std::string::String>,
    pub(crate) tls_server_name: ::std::option::Option<::std::string::String>,
    pub(crate) name: ::std::option::Option<::std::string::String>,
    pub(crate) description: ::std::option::Option<::std::string::String>,
}
impl CreateVpcConfigurationInputBuilder {
    /// <p>The unique identifier of the knowledge base to associate this VPC configuration with.</p>
    /// This field is required.
    pub fn knowledge_base_id(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.knowledge_base_id = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The unique identifier of the knowledge base to associate this VPC configuration with.</p>
    pub fn set_knowledge_base_id(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.knowledge_base_id = input;
        self
    }
    /// <p>The unique identifier of the knowledge base to associate this VPC configuration with.</p>
    pub fn get_knowledge_base_id(&self) -> &::std::option::Option<::std::string::String> {
        &self.knowledge_base_id
    }
    /// <p>A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, the service ignores the request but does not return an error.</p>
    pub fn client_token(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.client_token = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, the service ignores the request but does not return an error.</p>
    pub fn set_client_token(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.client_token = input;
        self
    }
    /// <p>A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, the service ignores the request but does not return an error.</p>
    pub fn get_client_token(&self) -> &::std::option::Option<::std::string::String> {
        &self.client_token
    }
    /// <p>The identifier of the VPC that the knowledge base connects through to reach the resource.</p>
    /// This field is required.
    pub fn vpc_id(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.vpc_id = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The identifier of the VPC that the knowledge base connects through to reach the resource.</p>
    pub fn set_vpc_id(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.vpc_id = input;
        self
    }
    /// <p>The identifier of the VPC that the knowledge base connects through to reach the resource.</p>
    pub fn get_vpc_id(&self) -> &::std::option::Option<::std::string::String> {
        &self.vpc_id
    }
    /// Appends an item to `subnet_ids`.
    ///
    /// To override the contents of this collection use [`set_subnet_ids`](Self::set_subnet_ids).
    ///
    /// <p>The subnets, in the VPC identified by <code>vpcId</code>, that the knowledge base uses to connect to the resource.</p>
    pub fn subnet_ids(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.subnet_ids.unwrap_or_default();
        v.push(input.into());
        self.subnet_ids = ::std::option::Option::Some(v);
        self
    }
    /// <p>The subnets, in the VPC identified by <code>vpcId</code>, that the knowledge base uses to connect to the resource.</p>
    pub fn set_subnet_ids(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.subnet_ids = input;
        self
    }
    /// <p>The subnets, in the VPC identified by <code>vpcId</code>, that the knowledge base uses to connect to the resource.</p>
    pub fn get_subnet_ids(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.subnet_ids
    }
    /// <p>The private IPv4 address or DNS name of the resource you want the knowledge base to reach. The target must be privately reachable from inside your VPC, such as an internal load balancer or a private IP. The following are not supported:</p>
    /// <ul>
    /// <li>
    /// <p>Internet-facing endpoints</p></li>
    /// <li>
    /// <p>Loopback addresses</p></li>
    /// <li>
    /// <p>Link-local addresses</p></li>
    /// <li>
    /// <p>Wildcard addresses</p></li>
    /// <li>
    /// <p>Multicast addresses</p></li>
    /// <li>
    /// <p>IPv6 literals</p></li>
    /// </ul>
    /// This field is required.
    pub fn resource_target(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.resource_target = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The private IPv4 address or DNS name of the resource you want the knowledge base to reach. The target must be privately reachable from inside your VPC, such as an internal load balancer or a private IP. The following are not supported:</p>
    /// <ul>
    /// <li>
    /// <p>Internet-facing endpoints</p></li>
    /// <li>
    /// <p>Loopback addresses</p></li>
    /// <li>
    /// <p>Link-local addresses</p></li>
    /// <li>
    /// <p>Wildcard addresses</p></li>
    /// <li>
    /// <p>Multicast addresses</p></li>
    /// <li>
    /// <p>IPv6 literals</p></li>
    /// </ul>
    pub fn set_resource_target(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.resource_target = input;
        self
    }
    /// <p>The private IPv4 address or DNS name of the resource you want the knowledge base to reach. The target must be privately reachable from inside your VPC, such as an internal load balancer or a private IP. The following are not supported:</p>
    /// <ul>
    /// <li>
    /// <p>Internet-facing endpoints</p></li>
    /// <li>
    /// <p>Loopback addresses</p></li>
    /// <li>
    /// <p>Link-local addresses</p></li>
    /// <li>
    /// <p>Wildcard addresses</p></li>
    /// <li>
    /// <p>Multicast addresses</p></li>
    /// <li>
    /// <p>IPv6 literals</p></li>
    /// </ul>
    pub fn get_resource_target(&self) -> &::std::option::Option<::std::string::String> {
        &self.resource_target
    }
    /// <p>The port on which to reach the resource.</p>
    /// This field is required.
    pub fn port(mut self, input: i32) -> Self {
        self.port = ::std::option::Option::Some(input);
        self
    }
    /// <p>The port on which to reach the resource.</p>
    pub fn set_port(mut self, input: ::std::option::Option<i32>) -> Self {
        self.port = input;
        self
    }
    /// <p>The port on which to reach the resource.</p>
    pub fn get_port(&self) -> &::std::option::Option<i32> {
        &self.port
    }
    /// <p>The protocol used to connect to the resource. Specify <code>HTTP</code> for plaintext or <code>HTTPS</code> for TLS. When you specify <code>HTTPS</code>, you must also provide <code>tlsServerName</code>.</p>
    /// This field is required.
    pub fn protocol(mut self, input: crate::types::VpcProtocol) -> Self {
        self.protocol = ::std::option::Option::Some(input);
        self
    }
    /// <p>The protocol used to connect to the resource. Specify <code>HTTP</code> for plaintext or <code>HTTPS</code> for TLS. When you specify <code>HTTPS</code>, you must also provide <code>tlsServerName</code>.</p>
    pub fn set_protocol(mut self, input: ::std::option::Option<crate::types::VpcProtocol>) -> Self {
        self.protocol = input;
        self
    }
    /// <p>The protocol used to connect to the resource. Specify <code>HTTP</code> for plaintext or <code>HTTPS</code> for TLS. When you specify <code>HTTPS</code>, you must also provide <code>tlsServerName</code>.</p>
    pub fn get_protocol(&self) -> &::std::option::Option<crate::types::VpcProtocol> {
        &self.protocol
    }
    /// <p>Controls how a domain-name <code>resourceTarget</code> is resolved. This applies only when the target is a domain name; it has no effect for IP-address targets, which have no name to resolve. In all cases the resolved address must be reachable from inside your VPC. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>IN_VPC</code> (default, recommended) – The target domain name is resolved privately, using the DNS resolvers of the VPC, such as private Route 53 hosted zones or on-premises DNS reachable from the VPC. Use this for targets that are private to your VPC, such as internal load balancers, private hosted-zone names, or on-premises hosts.</p></li>
    /// <li>
    /// <p><code>PUBLIC</code> – The target domain name is resolved against public DNS resolvers. Select this only when the target's domain name must be resolved through public DNS and the resulting address is still reachable from the VPC, an uncommon split-horizon configuration. If you are unsure, use <code>IN_VPC</code>.</p></li>
    /// </ul>
    /// This field is required.
    pub fn resolution_mode(mut self, input: crate::types::VpcResolutionMode) -> Self {
        self.resolution_mode = ::std::option::Option::Some(input);
        self
    }
    /// <p>Controls how a domain-name <code>resourceTarget</code> is resolved. This applies only when the target is a domain name; it has no effect for IP-address targets, which have no name to resolve. In all cases the resolved address must be reachable from inside your VPC. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>IN_VPC</code> (default, recommended) – The target domain name is resolved privately, using the DNS resolvers of the VPC, such as private Route 53 hosted zones or on-premises DNS reachable from the VPC. Use this for targets that are private to your VPC, such as internal load balancers, private hosted-zone names, or on-premises hosts.</p></li>
    /// <li>
    /// <p><code>PUBLIC</code> – The target domain name is resolved against public DNS resolvers. Select this only when the target's domain name must be resolved through public DNS and the resulting address is still reachable from the VPC, an uncommon split-horizon configuration. If you are unsure, use <code>IN_VPC</code>.</p></li>
    /// </ul>
    pub fn set_resolution_mode(mut self, input: ::std::option::Option<crate::types::VpcResolutionMode>) -> Self {
        self.resolution_mode = input;
        self
    }
    /// <p>Controls how a domain-name <code>resourceTarget</code> is resolved. This applies only when the target is a domain name; it has no effect for IP-address targets, which have no name to resolve. In all cases the resolved address must be reachable from inside your VPC. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>IN_VPC</code> (default, recommended) – The target domain name is resolved privately, using the DNS resolvers of the VPC, such as private Route 53 hosted zones or on-premises DNS reachable from the VPC. Use this for targets that are private to your VPC, such as internal load balancers, private hosted-zone names, or on-premises hosts.</p></li>
    /// <li>
    /// <p><code>PUBLIC</code> – The target domain name is resolved against public DNS resolvers. Select this only when the target's domain name must be resolved through public DNS and the resulting address is still reachable from the VPC, an uncommon split-horizon configuration. If you are unsure, use <code>IN_VPC</code>.</p></li>
    /// </ul>
    pub fn get_resolution_mode(&self) -> &::std::option::Option<crate::types::VpcResolutionMode> {
        &self.resolution_mode
    }
    /// <p>An optional HTTP <code>Host</code> header value to send when invoking the resource. Set this only if your resource (or an upstream router or ingress) routes by the <code>Host</code> header and that host differs from the target. This setting is independent of <code>tlsServerName</code>.</p>
    pub fn host_header(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.host_header = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>An optional HTTP <code>Host</code> header value to send when invoking the resource. Set this only if your resource (or an upstream router or ingress) routes by the <code>Host</code> header and that host differs from the target. This setting is independent of <code>tlsServerName</code>.</p>
    pub fn set_host_header(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.host_header = input;
        self
    }
    /// <p>An optional HTTP <code>Host</code> header value to send when invoking the resource. Set this only if your resource (or an upstream router or ingress) routes by the <code>Host</code> header and that host differs from the target. This setting is independent of <code>tlsServerName</code>.</p>
    pub fn get_host_header(&self) -> &::std::option::Option<::std::string::String> {
        &self.host_header
    }
    /// <p>The expected TLS server name. The service matches this value against the Subject Alternative Names on your resource's TLS certificate during invocation. This field is required when <code>protocol</code> is <code>HTTPS</code>. Set it to a hostname on your certificate, such as <code>app.internal.example.com</code>. You can use a single leftmost wildcard, such as <code>*.example.com</code>. The value must be a hostname without a port.</p>
    pub fn tls_server_name(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.tls_server_name = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The expected TLS server name. The service matches this value against the Subject Alternative Names on your resource's TLS certificate during invocation. This field is required when <code>protocol</code> is <code>HTTPS</code>. Set it to a hostname on your certificate, such as <code>app.internal.example.com</code>. You can use a single leftmost wildcard, such as <code>*.example.com</code>. The value must be a hostname without a port.</p>
    pub fn set_tls_server_name(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.tls_server_name = input;
        self
    }
    /// <p>The expected TLS server name. The service matches this value against the Subject Alternative Names on your resource's TLS certificate during invocation. This field is required when <code>protocol</code> is <code>HTTPS</code>. Set it to a hostname on your certificate, such as <code>app.internal.example.com</code>. You can use a single leftmost wildcard, such as <code>*.example.com</code>. The value must be a hostname without a port.</p>
    pub fn get_tls_server_name(&self) -> &::std::option::Option<::std::string::String> {
        &self.tls_server_name
    }
    /// <p>An optional human-readable name for the VPC configuration. If you don't specify a name, the VPC configuration has no name.</p>
    pub fn name(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.name = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>An optional human-readable name for the VPC configuration. If you don't specify a name, the VPC configuration has no name.</p>
    pub fn set_name(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.name = input;
        self
    }
    /// <p>An optional human-readable name for the VPC configuration. If you don't specify a name, the VPC configuration has no name.</p>
    pub fn get_name(&self) -> &::std::option::Option<::std::string::String> {
        &self.name
    }
    /// <p>An optional description of the VPC configuration. If you don't specify a description, the VPC configuration has no description.</p>
    pub fn description(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.description = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>An optional description of the VPC configuration. If you don't specify a description, the VPC configuration has no description.</p>
    pub fn set_description(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.description = input;
        self
    }
    /// <p>An optional description of the VPC configuration. If you don't specify a description, the VPC configuration has no description.</p>
    pub fn get_description(&self) -> &::std::option::Option<::std::string::String> {
        &self.description
    }
    /// Consumes the builder and constructs a [`CreateVpcConfigurationInput`](crate::operation::create_vpc_configuration::CreateVpcConfigurationInput).
    pub fn build(
        self,
    ) -> ::std::result::Result<
        crate::operation::create_vpc_configuration::CreateVpcConfigurationInput,
        ::aws_smithy_types::error::operation::BuildError,
    > {
        ::std::result::Result::Ok(crate::operation::create_vpc_configuration::CreateVpcConfigurationInput {
            knowledge_base_id: self.knowledge_base_id,
            client_token: self.client_token,
            vpc_id: self.vpc_id,
            subnet_ids: self.subnet_ids,
            resource_target: self.resource_target,
            port: self.port,
            protocol: self.protocol,
            resolution_mode: self.resolution_mode,
            host_header: self.host_header,
            tls_server_name: self.tls_server_name,
            name: self.name,
            description: self.description,
        })
    }
}
