// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The recurring schedule on which a managed knowledge base connector automatically syncs its data source. Specify exactly one of <code>daily</code>, <code>weekly</code>, or <code>monthly</code>.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum SyncSchedule {
    /// <p>A daily sync that runs once a day at a system-chosen off-peak time. The run time is not configurable.</p>
    Daily(crate::types::DailySchedule),
    /// <p>A monthly sync that runs once a month on the specified day of the month.</p>
    Monthly(crate::types::MonthlySchedule),
    /// <p>A weekly sync that runs once a week on the specified day of the week.</p>
    Weekly(crate::types::WeeklySchedule),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl SyncSchedule {
    /// Tries to convert the enum instance into [`Daily`](crate::types::SyncSchedule::Daily), extracting the inner [`DailySchedule`](crate::types::DailySchedule).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_daily(&self) -> ::std::result::Result<&crate::types::DailySchedule, &Self> {
        if let SyncSchedule::Daily(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Daily`](crate::types::SyncSchedule::Daily).
    pub fn is_daily(&self) -> bool {
        self.as_daily().is_ok()
    }
    /// Tries to convert the enum instance into [`Monthly`](crate::types::SyncSchedule::Monthly), extracting the inner [`MonthlySchedule`](crate::types::MonthlySchedule).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_monthly(&self) -> ::std::result::Result<&crate::types::MonthlySchedule, &Self> {
        if let SyncSchedule::Monthly(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Monthly`](crate::types::SyncSchedule::Monthly).
    pub fn is_monthly(&self) -> bool {
        self.as_monthly().is_ok()
    }
    /// Tries to convert the enum instance into [`Weekly`](crate::types::SyncSchedule::Weekly), extracting the inner [`WeeklySchedule`](crate::types::WeeklySchedule).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_weekly(&self) -> ::std::result::Result<&crate::types::WeeklySchedule, &Self> {
        if let SyncSchedule::Weekly(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Weekly`](crate::types::SyncSchedule::Weekly).
    pub fn is_weekly(&self) -> bool {
        self.as_weekly().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
