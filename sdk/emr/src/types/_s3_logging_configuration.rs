// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Configuration for S3 logging behavior in EMR clusters. Defines how different types of logs are uploaded to S3 based on the specified upload policies for each log type.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct S3LoggingConfiguration {
    /// <p>A map that specifies the upload policy for each log type. The key is the log type, and the value is the upload policy.</p>
    /// <p>Valid log types:</p>
    /// <ul>
    /// <li>
    /// <p><code>system-logs</code>: EMR Daemon logs.</p></li>
    /// <li>
    /// <p><code>application-logs</code>: Framework logs from Hadoop, Spark, Hive and other applications running on the cluster.</p></li>
    /// <li>
    /// <p><code>persistent-ui-logs</code>: Logs required for persistent application UIs such as Spark History Server and Tez UI.</p></li>
    /// </ul>
    /// <p>Valid upload policies:</p>
    /// <ul>
    /// <li>
    /// <p><code>emr-managed</code>: Standard behavior. Logs are uploaded to S3 bucket as configured in your LogUri, with certain logs retained by the service for operational support and troubleshooting purposes.</p></li>
    /// <li>
    /// <p><code>on-customer-s3only</code>: Logs are uploaded only to the customer-specified S3 bucket. This requires you to specify a LogUri when creating the cluster. Persistent-ui-logs cannot have on-customer-s3only policy. Allowed policies for persistent-ui-logs are emr-managed and disabled.</p></li>
    /// <li>
    /// <p><code>disabled</code>: No S3 upload for this log type.</p></li>
    /// </ul>
    pub log_type_upload_policy: ::std::option::Option<::std::collections::HashMap<crate::types::LogType, crate::types::LogUploadPolicyValue>>,
}
impl S3LoggingConfiguration {
    /// <p>A map that specifies the upload policy for each log type. The key is the log type, and the value is the upload policy.</p>
    /// <p>Valid log types:</p>
    /// <ul>
    /// <li>
    /// <p><code>system-logs</code>: EMR Daemon logs.</p></li>
    /// <li>
    /// <p><code>application-logs</code>: Framework logs from Hadoop, Spark, Hive and other applications running on the cluster.</p></li>
    /// <li>
    /// <p><code>persistent-ui-logs</code>: Logs required for persistent application UIs such as Spark History Server and Tez UI.</p></li>
    /// </ul>
    /// <p>Valid upload policies:</p>
    /// <ul>
    /// <li>
    /// <p><code>emr-managed</code>: Standard behavior. Logs are uploaded to S3 bucket as configured in your LogUri, with certain logs retained by the service for operational support and troubleshooting purposes.</p></li>
    /// <li>
    /// <p><code>on-customer-s3only</code>: Logs are uploaded only to the customer-specified S3 bucket. This requires you to specify a LogUri when creating the cluster. Persistent-ui-logs cannot have on-customer-s3only policy. Allowed policies for persistent-ui-logs are emr-managed and disabled.</p></li>
    /// <li>
    /// <p><code>disabled</code>: No S3 upload for this log type.</p></li>
    /// </ul>
    pub fn log_type_upload_policy(
        &self,
    ) -> ::std::option::Option<&::std::collections::HashMap<crate::types::LogType, crate::types::LogUploadPolicyValue>> {
        self.log_type_upload_policy.as_ref()
    }
}
impl S3LoggingConfiguration {
    /// Creates a new builder-style object to manufacture [`S3LoggingConfiguration`](crate::types::S3LoggingConfiguration).
    pub fn builder() -> crate::types::builders::S3LoggingConfigurationBuilder {
        crate::types::builders::S3LoggingConfigurationBuilder::default()
    }
}

/// A builder for [`S3LoggingConfiguration`](crate::types::S3LoggingConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct S3LoggingConfigurationBuilder {
    pub(crate) log_type_upload_policy: ::std::option::Option<::std::collections::HashMap<crate::types::LogType, crate::types::LogUploadPolicyValue>>,
}
impl S3LoggingConfigurationBuilder {
    /// Adds a key-value pair to `log_type_upload_policy`.
    ///
    /// To override the contents of this collection use [`set_log_type_upload_policy`](Self::set_log_type_upload_policy).
    ///
    /// <p>A map that specifies the upload policy for each log type. The key is the log type, and the value is the upload policy.</p>
    /// <p>Valid log types:</p>
    /// <ul>
    /// <li>
    /// <p><code>system-logs</code>: EMR Daemon logs.</p></li>
    /// <li>
    /// <p><code>application-logs</code>: Framework logs from Hadoop, Spark, Hive and other applications running on the cluster.</p></li>
    /// <li>
    /// <p><code>persistent-ui-logs</code>: Logs required for persistent application UIs such as Spark History Server and Tez UI.</p></li>
    /// </ul>
    /// <p>Valid upload policies:</p>
    /// <ul>
    /// <li>
    /// <p><code>emr-managed</code>: Standard behavior. Logs are uploaded to S3 bucket as configured in your LogUri, with certain logs retained by the service for operational support and troubleshooting purposes.</p></li>
    /// <li>
    /// <p><code>on-customer-s3only</code>: Logs are uploaded only to the customer-specified S3 bucket. This requires you to specify a LogUri when creating the cluster. Persistent-ui-logs cannot have on-customer-s3only policy. Allowed policies for persistent-ui-logs are emr-managed and disabled.</p></li>
    /// <li>
    /// <p><code>disabled</code>: No S3 upload for this log type.</p></li>
    /// </ul>
    pub fn log_type_upload_policy(mut self, k: crate::types::LogType, v: crate::types::LogUploadPolicyValue) -> Self {
        let mut hash_map = self.log_type_upload_policy.unwrap_or_default();
        hash_map.insert(k, v);
        self.log_type_upload_policy = ::std::option::Option::Some(hash_map);
        self
    }
    /// <p>A map that specifies the upload policy for each log type. The key is the log type, and the value is the upload policy.</p>
    /// <p>Valid log types:</p>
    /// <ul>
    /// <li>
    /// <p><code>system-logs</code>: EMR Daemon logs.</p></li>
    /// <li>
    /// <p><code>application-logs</code>: Framework logs from Hadoop, Spark, Hive and other applications running on the cluster.</p></li>
    /// <li>
    /// <p><code>persistent-ui-logs</code>: Logs required for persistent application UIs such as Spark History Server and Tez UI.</p></li>
    /// </ul>
    /// <p>Valid upload policies:</p>
    /// <ul>
    /// <li>
    /// <p><code>emr-managed</code>: Standard behavior. Logs are uploaded to S3 bucket as configured in your LogUri, with certain logs retained by the service for operational support and troubleshooting purposes.</p></li>
    /// <li>
    /// <p><code>on-customer-s3only</code>: Logs are uploaded only to the customer-specified S3 bucket. This requires you to specify a LogUri when creating the cluster. Persistent-ui-logs cannot have on-customer-s3only policy. Allowed policies for persistent-ui-logs are emr-managed and disabled.</p></li>
    /// <li>
    /// <p><code>disabled</code>: No S3 upload for this log type.</p></li>
    /// </ul>
    pub fn set_log_type_upload_policy(
        mut self,
        input: ::std::option::Option<::std::collections::HashMap<crate::types::LogType, crate::types::LogUploadPolicyValue>>,
    ) -> Self {
        self.log_type_upload_policy = input;
        self
    }
    /// <p>A map that specifies the upload policy for each log type. The key is the log type, and the value is the upload policy.</p>
    /// <p>Valid log types:</p>
    /// <ul>
    /// <li>
    /// <p><code>system-logs</code>: EMR Daemon logs.</p></li>
    /// <li>
    /// <p><code>application-logs</code>: Framework logs from Hadoop, Spark, Hive and other applications running on the cluster.</p></li>
    /// <li>
    /// <p><code>persistent-ui-logs</code>: Logs required for persistent application UIs such as Spark History Server and Tez UI.</p></li>
    /// </ul>
    /// <p>Valid upload policies:</p>
    /// <ul>
    /// <li>
    /// <p><code>emr-managed</code>: Standard behavior. Logs are uploaded to S3 bucket as configured in your LogUri, with certain logs retained by the service for operational support and troubleshooting purposes.</p></li>
    /// <li>
    /// <p><code>on-customer-s3only</code>: Logs are uploaded only to the customer-specified S3 bucket. This requires you to specify a LogUri when creating the cluster. Persistent-ui-logs cannot have on-customer-s3only policy. Allowed policies for persistent-ui-logs are emr-managed and disabled.</p></li>
    /// <li>
    /// <p><code>disabled</code>: No S3 upload for this log type.</p></li>
    /// </ul>
    pub fn get_log_type_upload_policy(
        &self,
    ) -> &::std::option::Option<::std::collections::HashMap<crate::types::LogType, crate::types::LogUploadPolicyValue>> {
        &self.log_type_upload_policy
    }
    /// Consumes the builder and constructs a [`S3LoggingConfiguration`](crate::types::S3LoggingConfiguration).
    pub fn build(self) -> crate::types::S3LoggingConfiguration {
        crate::types::S3LoggingConfiguration {
            log_type_upload_policy: self.log_type_upload_policy,
        }
    }
}
