// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The behavior of an agreement when it reaches its end date. For example, whether the agreement renews, and if it doesn't, the reason why.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct EndTimeBehavior {
    /// <p>The behavior of the agreement when it reaches its end date.</p>
    /// <p>Types include:</p>
    /// <ul>
    /// <li>
    /// <p><code>RENEW</code> – A new agreement is created from the accepted terms of this agreement.</p></li>
    /// <li>
    /// <p><code>REPLACE</code> – A new agreement is created from a different offer than the one this agreement was created from. This happens, for example, when a private offer reaches its end date and the acceptor transitions to the public offer for the product.</p></li>
    /// <li>
    /// <p><code>EXPIRE</code> – The agreement ends and isn't renewed or replaced.</p></li>
    /// </ul>
    pub r#type: crate::types::EndTimeBehaviorType,
    /// <p>The reason why the agreement doesn't renew at its end date. The field is <code>null</code> when the agreement renews.</p>
    /// <p>More than one reason can apply to the same agreement. When that happens, the operation returns only one reason code, and <code>PROPOSER_RENEW_OPTED_OUT</code> takes precedence over all others.</p>
    /// <p>The <code>EnableAutoRenew</code> field reflects only the acceptor's preference, and doesn't reflect the other reasons an agreement might not renew.</p>
    /// <p>Reason codes include:</p>
    /// <ul>
    /// <li>
    /// <p><code>PROPOSER_RENEW_OPTED_OUT</code> – The proposer opted out of renewing the agreement.</p></li>
    /// <li>
    /// <p><code>ACCEPTOR_RENEW_OPTED_OUT</code> – The acceptor opted out of renewing the agreement.</p></li>
    /// <li>
    /// <p><code>NO_RENEWAL_TERM</code> – The accepted terms of the agreement don't include a renewal term, which is required for an agreement to renew.</p></li>
    /// <li>
    /// <p><code>RENEWAL_LIMIT_EXHAUSTED</code> – The agreement reached the maximum number of renewals allowed by its renewal term.</p></li>
    /// </ul>
    pub reason_code: ::std::option::Option<crate::types::EndTimeBehaviorReasonCode>,
    /// <p>The details of the renewal that applies at the end date of the agreement. This field is present when <code>Type</code> is <code>RENEW</code>. It is also present when <code>ReasonCode</code> is <code>PROPOSER_RENEW_OPTED_OUT</code> or <code>ACCEPTOR_RENEW_OPTED_OUT</code>. In those cases, it identifies the offer that the agreement would otherwise have renewed from. The field is <code>null</code> in all other cases.</p>
    pub renewal_summary: ::std::option::Option<crate::types::RenewalSummary>,
}
impl EndTimeBehavior {
    /// <p>The behavior of the agreement when it reaches its end date.</p>
    /// <p>Types include:</p>
    /// <ul>
    /// <li>
    /// <p><code>RENEW</code> – A new agreement is created from the accepted terms of this agreement.</p></li>
    /// <li>
    /// <p><code>REPLACE</code> – A new agreement is created from a different offer than the one this agreement was created from. This happens, for example, when a private offer reaches its end date and the acceptor transitions to the public offer for the product.</p></li>
    /// <li>
    /// <p><code>EXPIRE</code> – The agreement ends and isn't renewed or replaced.</p></li>
    /// </ul>
    pub fn r#type(&self) -> &crate::types::EndTimeBehaviorType {
        &self.r#type
    }
    /// <p>The reason why the agreement doesn't renew at its end date. The field is <code>null</code> when the agreement renews.</p>
    /// <p>More than one reason can apply to the same agreement. When that happens, the operation returns only one reason code, and <code>PROPOSER_RENEW_OPTED_OUT</code> takes precedence over all others.</p>
    /// <p>The <code>EnableAutoRenew</code> field reflects only the acceptor's preference, and doesn't reflect the other reasons an agreement might not renew.</p>
    /// <p>Reason codes include:</p>
    /// <ul>
    /// <li>
    /// <p><code>PROPOSER_RENEW_OPTED_OUT</code> – The proposer opted out of renewing the agreement.</p></li>
    /// <li>
    /// <p><code>ACCEPTOR_RENEW_OPTED_OUT</code> – The acceptor opted out of renewing the agreement.</p></li>
    /// <li>
    /// <p><code>NO_RENEWAL_TERM</code> – The accepted terms of the agreement don't include a renewal term, which is required for an agreement to renew.</p></li>
    /// <li>
    /// <p><code>RENEWAL_LIMIT_EXHAUSTED</code> – The agreement reached the maximum number of renewals allowed by its renewal term.</p></li>
    /// </ul>
    pub fn reason_code(&self) -> ::std::option::Option<&crate::types::EndTimeBehaviorReasonCode> {
        self.reason_code.as_ref()
    }
    /// <p>The details of the renewal that applies at the end date of the agreement. This field is present when <code>Type</code> is <code>RENEW</code>. It is also present when <code>ReasonCode</code> is <code>PROPOSER_RENEW_OPTED_OUT</code> or <code>ACCEPTOR_RENEW_OPTED_OUT</code>. In those cases, it identifies the offer that the agreement would otherwise have renewed from. The field is <code>null</code> in all other cases.</p>
    pub fn renewal_summary(&self) -> ::std::option::Option<&crate::types::RenewalSummary> {
        self.renewal_summary.as_ref()
    }
}
impl EndTimeBehavior {
    /// Creates a new builder-style object to manufacture [`EndTimeBehavior`](crate::types::EndTimeBehavior).
    pub fn builder() -> crate::types::builders::EndTimeBehaviorBuilder {
        crate::types::builders::EndTimeBehaviorBuilder::default()
    }
}

/// A builder for [`EndTimeBehavior`](crate::types::EndTimeBehavior).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct EndTimeBehaviorBuilder {
    pub(crate) r#type: ::std::option::Option<crate::types::EndTimeBehaviorType>,
    pub(crate) reason_code: ::std::option::Option<crate::types::EndTimeBehaviorReasonCode>,
    pub(crate) renewal_summary: ::std::option::Option<crate::types::RenewalSummary>,
}
impl EndTimeBehaviorBuilder {
    /// <p>The behavior of the agreement when it reaches its end date.</p>
    /// <p>Types include:</p>
    /// <ul>
    /// <li>
    /// <p><code>RENEW</code> – A new agreement is created from the accepted terms of this agreement.</p></li>
    /// <li>
    /// <p><code>REPLACE</code> – A new agreement is created from a different offer than the one this agreement was created from. This happens, for example, when a private offer reaches its end date and the acceptor transitions to the public offer for the product.</p></li>
    /// <li>
    /// <p><code>EXPIRE</code> – The agreement ends and isn't renewed or replaced.</p></li>
    /// </ul>
    /// This field is required.
    pub fn r#type(mut self, input: crate::types::EndTimeBehaviorType) -> Self {
        self.r#type = ::std::option::Option::Some(input);
        self
    }
    /// <p>The behavior of the agreement when it reaches its end date.</p>
    /// <p>Types include:</p>
    /// <ul>
    /// <li>
    /// <p><code>RENEW</code> – A new agreement is created from the accepted terms of this agreement.</p></li>
    /// <li>
    /// <p><code>REPLACE</code> – A new agreement is created from a different offer than the one this agreement was created from. This happens, for example, when a private offer reaches its end date and the acceptor transitions to the public offer for the product.</p></li>
    /// <li>
    /// <p><code>EXPIRE</code> – The agreement ends and isn't renewed or replaced.</p></li>
    /// </ul>
    pub fn set_type(mut self, input: ::std::option::Option<crate::types::EndTimeBehaviorType>) -> Self {
        self.r#type = input;
        self
    }
    /// <p>The behavior of the agreement when it reaches its end date.</p>
    /// <p>Types include:</p>
    /// <ul>
    /// <li>
    /// <p><code>RENEW</code> – A new agreement is created from the accepted terms of this agreement.</p></li>
    /// <li>
    /// <p><code>REPLACE</code> – A new agreement is created from a different offer than the one this agreement was created from. This happens, for example, when a private offer reaches its end date and the acceptor transitions to the public offer for the product.</p></li>
    /// <li>
    /// <p><code>EXPIRE</code> – The agreement ends and isn't renewed or replaced.</p></li>
    /// </ul>
    pub fn get_type(&self) -> &::std::option::Option<crate::types::EndTimeBehaviorType> {
        &self.r#type
    }
    /// <p>The reason why the agreement doesn't renew at its end date. The field is <code>null</code> when the agreement renews.</p>
    /// <p>More than one reason can apply to the same agreement. When that happens, the operation returns only one reason code, and <code>PROPOSER_RENEW_OPTED_OUT</code> takes precedence over all others.</p>
    /// <p>The <code>EnableAutoRenew</code> field reflects only the acceptor's preference, and doesn't reflect the other reasons an agreement might not renew.</p>
    /// <p>Reason codes include:</p>
    /// <ul>
    /// <li>
    /// <p><code>PROPOSER_RENEW_OPTED_OUT</code> – The proposer opted out of renewing the agreement.</p></li>
    /// <li>
    /// <p><code>ACCEPTOR_RENEW_OPTED_OUT</code> – The acceptor opted out of renewing the agreement.</p></li>
    /// <li>
    /// <p><code>NO_RENEWAL_TERM</code> – The accepted terms of the agreement don't include a renewal term, which is required for an agreement to renew.</p></li>
    /// <li>
    /// <p><code>RENEWAL_LIMIT_EXHAUSTED</code> – The agreement reached the maximum number of renewals allowed by its renewal term.</p></li>
    /// </ul>
    pub fn reason_code(mut self, input: crate::types::EndTimeBehaviorReasonCode) -> Self {
        self.reason_code = ::std::option::Option::Some(input);
        self
    }
    /// <p>The reason why the agreement doesn't renew at its end date. The field is <code>null</code> when the agreement renews.</p>
    /// <p>More than one reason can apply to the same agreement. When that happens, the operation returns only one reason code, and <code>PROPOSER_RENEW_OPTED_OUT</code> takes precedence over all others.</p>
    /// <p>The <code>EnableAutoRenew</code> field reflects only the acceptor's preference, and doesn't reflect the other reasons an agreement might not renew.</p>
    /// <p>Reason codes include:</p>
    /// <ul>
    /// <li>
    /// <p><code>PROPOSER_RENEW_OPTED_OUT</code> – The proposer opted out of renewing the agreement.</p></li>
    /// <li>
    /// <p><code>ACCEPTOR_RENEW_OPTED_OUT</code> – The acceptor opted out of renewing the agreement.</p></li>
    /// <li>
    /// <p><code>NO_RENEWAL_TERM</code> – The accepted terms of the agreement don't include a renewal term, which is required for an agreement to renew.</p></li>
    /// <li>
    /// <p><code>RENEWAL_LIMIT_EXHAUSTED</code> – The agreement reached the maximum number of renewals allowed by its renewal term.</p></li>
    /// </ul>
    pub fn set_reason_code(mut self, input: ::std::option::Option<crate::types::EndTimeBehaviorReasonCode>) -> Self {
        self.reason_code = input;
        self
    }
    /// <p>The reason why the agreement doesn't renew at its end date. The field is <code>null</code> when the agreement renews.</p>
    /// <p>More than one reason can apply to the same agreement. When that happens, the operation returns only one reason code, and <code>PROPOSER_RENEW_OPTED_OUT</code> takes precedence over all others.</p>
    /// <p>The <code>EnableAutoRenew</code> field reflects only the acceptor's preference, and doesn't reflect the other reasons an agreement might not renew.</p>
    /// <p>Reason codes include:</p>
    /// <ul>
    /// <li>
    /// <p><code>PROPOSER_RENEW_OPTED_OUT</code> – The proposer opted out of renewing the agreement.</p></li>
    /// <li>
    /// <p><code>ACCEPTOR_RENEW_OPTED_OUT</code> – The acceptor opted out of renewing the agreement.</p></li>
    /// <li>
    /// <p><code>NO_RENEWAL_TERM</code> – The accepted terms of the agreement don't include a renewal term, which is required for an agreement to renew.</p></li>
    /// <li>
    /// <p><code>RENEWAL_LIMIT_EXHAUSTED</code> – The agreement reached the maximum number of renewals allowed by its renewal term.</p></li>
    /// </ul>
    pub fn get_reason_code(&self) -> &::std::option::Option<crate::types::EndTimeBehaviorReasonCode> {
        &self.reason_code
    }
    /// <p>The details of the renewal that applies at the end date of the agreement. This field is present when <code>Type</code> is <code>RENEW</code>. It is also present when <code>ReasonCode</code> is <code>PROPOSER_RENEW_OPTED_OUT</code> or <code>ACCEPTOR_RENEW_OPTED_OUT</code>. In those cases, it identifies the offer that the agreement would otherwise have renewed from. The field is <code>null</code> in all other cases.</p>
    pub fn renewal_summary(mut self, input: crate::types::RenewalSummary) -> Self {
        self.renewal_summary = ::std::option::Option::Some(input);
        self
    }
    /// <p>The details of the renewal that applies at the end date of the agreement. This field is present when <code>Type</code> is <code>RENEW</code>. It is also present when <code>ReasonCode</code> is <code>PROPOSER_RENEW_OPTED_OUT</code> or <code>ACCEPTOR_RENEW_OPTED_OUT</code>. In those cases, it identifies the offer that the agreement would otherwise have renewed from. The field is <code>null</code> in all other cases.</p>
    pub fn set_renewal_summary(mut self, input: ::std::option::Option<crate::types::RenewalSummary>) -> Self {
        self.renewal_summary = input;
        self
    }
    /// <p>The details of the renewal that applies at the end date of the agreement. This field is present when <code>Type</code> is <code>RENEW</code>. It is also present when <code>ReasonCode</code> is <code>PROPOSER_RENEW_OPTED_OUT</code> or <code>ACCEPTOR_RENEW_OPTED_OUT</code>. In those cases, it identifies the offer that the agreement would otherwise have renewed from. The field is <code>null</code> in all other cases.</p>
    pub fn get_renewal_summary(&self) -> &::std::option::Option<crate::types::RenewalSummary> {
        &self.renewal_summary
    }
    /// Consumes the builder and constructs a [`EndTimeBehavior`](crate::types::EndTimeBehavior).
    /// This method will fail if any of the following fields are not set:
    /// - [`r#type`](crate::types::builders::EndTimeBehaviorBuilder::type)
    pub fn build(self) -> ::std::result::Result<crate::types::EndTimeBehavior, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::EndTimeBehavior {
            r#type: self.r#type.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "r#type",
                    "r#type was not specified but it is required when building EndTimeBehavior",
                )
            })?,
            reason_code: self.reason_code,
            renewal_summary: self.renewal_summary,
        })
    }
}
