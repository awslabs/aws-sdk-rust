// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Defines that on graceful expiration of the agreement (when the agreement ends on its pre-defined end date), a new agreement will be created using the accepted terms on the existing agreement. In other words, the agreement will be renewed. Presence of <code>RenewalTerm</code> in the offer document means that auto-renewal is allowed. The acceptor will have the option to accept or decline auto-renewal at the offer acceptance/agreement creation. The acceptor can also change this flag from <code>True</code> to <code>False</code> or <code>False</code> to <code>True</code>, within the limits set by <code>LockoutPeriod</code> and <code>MaxRenewals</code>. Setting the flag to <code>True</code> doesn't by itself guarantee that the agreement renews, because the proposer can also opt out.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct RenewalTerm {
    /// <p>Category of the term being updated.</p>
    pub r#type: ::std::option::Option<::std::string::String>,
    /// <p>The unique identifier for the term.</p>
    pub id: ::std::option::Option<::std::string::String>,
    /// <p>Additional parameters specified by the acceptor while accepting the term.</p>
    pub configuration: ::std::option::Option<crate::types::RenewalTermConfiguration>,
    /// <p>The renewal decision deadline, measured back from the end date of the agreement. This is the last day either party can opt in to or opt out of the renewal. The duration is represented in the ISO 8601 format in whole days (for example, <code>P30D</code> for 30 days or <code>P60D</code> for 60 days).</p>
    /// <p>The field is <code>null</code> when no renewal decision deadline is set. In that case, either party can change the auto-renewal decision up to the end date of the agreement.</p>
    pub lockout_period: ::std::option::Option<::std::string::String>,
    /// <p>The maximum number of times the agreement can be renewed. The field is <code>null</code> when the number of renewals is unlimited.</p>
    /// <p>After the agreement reaches this limit, it expires on its end date instead of renewing.</p>
    pub max_renewals: ::std::option::Option<i32>,
    /// <p>The date by which the proposer must finalize the price increase for the next renewal, measured back from the end date of the agreement. The duration is represented in the ISO 8601 format in whole days (for example, <code>P30D</code> for 30 days or <code>P60D</code> for 60 days).</p>
    /// <p>This field applies only when <code>PriceIncrease</code> is a <code>PercentageRange</code>. The field is <code>null</code> when <code>PriceIncrease</code> is a <code>FixedPercentage</code>, because the price increase is already fixed and there is nothing for the proposer to finalize. If the proposer doesn't finalize a value by the adjustment deadline, the <code>DefaultValue</code> of the range applies.</p>
    /// <p><code>AdjustmentDeadline</code> must be greater than <code>LockoutPeriod</code>.</p>
    pub adjustment_deadline: ::std::option::Option<::std::string::String>,
    /// <p>The price increase that is applied each time the agreement renews. The field is <code>null</code> when the price doesn't change at renewal.</p>
    pub price_increase: ::std::option::Option<crate::types::PriceIncrease>,
    /// <p>Defines how specific terms change each time the agreement renews. The field is <code>null</code> when no terms change at renewal.</p>
    pub term_templates: ::std::option::Option<::std::vec::Vec<crate::types::TermTemplate>>,
}
impl RenewalTerm {
    /// <p>Category of the term being updated.</p>
    pub fn r#type(&self) -> ::std::option::Option<&str> {
        self.r#type.as_deref()
    }
    /// <p>The unique identifier for the term.</p>
    pub fn id(&self) -> ::std::option::Option<&str> {
        self.id.as_deref()
    }
    /// <p>Additional parameters specified by the acceptor while accepting the term.</p>
    pub fn configuration(&self) -> ::std::option::Option<&crate::types::RenewalTermConfiguration> {
        self.configuration.as_ref()
    }
    /// <p>The renewal decision deadline, measured back from the end date of the agreement. This is the last day either party can opt in to or opt out of the renewal. The duration is represented in the ISO 8601 format in whole days (for example, <code>P30D</code> for 30 days or <code>P60D</code> for 60 days).</p>
    /// <p>The field is <code>null</code> when no renewal decision deadline is set. In that case, either party can change the auto-renewal decision up to the end date of the agreement.</p>
    pub fn lockout_period(&self) -> ::std::option::Option<&str> {
        self.lockout_period.as_deref()
    }
    /// <p>The maximum number of times the agreement can be renewed. The field is <code>null</code> when the number of renewals is unlimited.</p>
    /// <p>After the agreement reaches this limit, it expires on its end date instead of renewing.</p>
    pub fn max_renewals(&self) -> ::std::option::Option<i32> {
        self.max_renewals
    }
    /// <p>The date by which the proposer must finalize the price increase for the next renewal, measured back from the end date of the agreement. The duration is represented in the ISO 8601 format in whole days (for example, <code>P30D</code> for 30 days or <code>P60D</code> for 60 days).</p>
    /// <p>This field applies only when <code>PriceIncrease</code> is a <code>PercentageRange</code>. The field is <code>null</code> when <code>PriceIncrease</code> is a <code>FixedPercentage</code>, because the price increase is already fixed and there is nothing for the proposer to finalize. If the proposer doesn't finalize a value by the adjustment deadline, the <code>DefaultValue</code> of the range applies.</p>
    /// <p><code>AdjustmentDeadline</code> must be greater than <code>LockoutPeriod</code>.</p>
    pub fn adjustment_deadline(&self) -> ::std::option::Option<&str> {
        self.adjustment_deadline.as_deref()
    }
    /// <p>The price increase that is applied each time the agreement renews. The field is <code>null</code> when the price doesn't change at renewal.</p>
    pub fn price_increase(&self) -> ::std::option::Option<&crate::types::PriceIncrease> {
        self.price_increase.as_ref()
    }
    /// <p>Defines how specific terms change each time the agreement renews. The field is <code>null</code> when no terms change at renewal.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.term_templates.is_none()`.
    pub fn term_templates(&self) -> &[crate::types::TermTemplate] {
        self.term_templates.as_deref().unwrap_or_default()
    }
}
impl RenewalTerm {
    /// Creates a new builder-style object to manufacture [`RenewalTerm`](crate::types::RenewalTerm).
    pub fn builder() -> crate::types::builders::RenewalTermBuilder {
        crate::types::builders::RenewalTermBuilder::default()
    }
}

/// A builder for [`RenewalTerm`](crate::types::RenewalTerm).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct RenewalTermBuilder {
    pub(crate) r#type: ::std::option::Option<::std::string::String>,
    pub(crate) id: ::std::option::Option<::std::string::String>,
    pub(crate) configuration: ::std::option::Option<crate::types::RenewalTermConfiguration>,
    pub(crate) lockout_period: ::std::option::Option<::std::string::String>,
    pub(crate) max_renewals: ::std::option::Option<i32>,
    pub(crate) adjustment_deadline: ::std::option::Option<::std::string::String>,
    pub(crate) price_increase: ::std::option::Option<crate::types::PriceIncrease>,
    pub(crate) term_templates: ::std::option::Option<::std::vec::Vec<crate::types::TermTemplate>>,
}
impl RenewalTermBuilder {
    /// <p>Category of the term being updated.</p>
    pub fn r#type(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.r#type = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>Category of the term being updated.</p>
    pub fn set_type(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.r#type = input;
        self
    }
    /// <p>Category of the term being updated.</p>
    pub fn get_type(&self) -> &::std::option::Option<::std::string::String> {
        &self.r#type
    }
    /// <p>The unique identifier for the term.</p>
    pub fn id(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.id = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The unique identifier for the term.</p>
    pub fn set_id(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.id = input;
        self
    }
    /// <p>The unique identifier for the term.</p>
    pub fn get_id(&self) -> &::std::option::Option<::std::string::String> {
        &self.id
    }
    /// <p>Additional parameters specified by the acceptor while accepting the term.</p>
    pub fn configuration(mut self, input: crate::types::RenewalTermConfiguration) -> Self {
        self.configuration = ::std::option::Option::Some(input);
        self
    }
    /// <p>Additional parameters specified by the acceptor while accepting the term.</p>
    pub fn set_configuration(mut self, input: ::std::option::Option<crate::types::RenewalTermConfiguration>) -> Self {
        self.configuration = input;
        self
    }
    /// <p>Additional parameters specified by the acceptor while accepting the term.</p>
    pub fn get_configuration(&self) -> &::std::option::Option<crate::types::RenewalTermConfiguration> {
        &self.configuration
    }
    /// <p>The renewal decision deadline, measured back from the end date of the agreement. This is the last day either party can opt in to or opt out of the renewal. The duration is represented in the ISO 8601 format in whole days (for example, <code>P30D</code> for 30 days or <code>P60D</code> for 60 days).</p>
    /// <p>The field is <code>null</code> when no renewal decision deadline is set. In that case, either party can change the auto-renewal decision up to the end date of the agreement.</p>
    pub fn lockout_period(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.lockout_period = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The renewal decision deadline, measured back from the end date of the agreement. This is the last day either party can opt in to or opt out of the renewal. The duration is represented in the ISO 8601 format in whole days (for example, <code>P30D</code> for 30 days or <code>P60D</code> for 60 days).</p>
    /// <p>The field is <code>null</code> when no renewal decision deadline is set. In that case, either party can change the auto-renewal decision up to the end date of the agreement.</p>
    pub fn set_lockout_period(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.lockout_period = input;
        self
    }
    /// <p>The renewal decision deadline, measured back from the end date of the agreement. This is the last day either party can opt in to or opt out of the renewal. The duration is represented in the ISO 8601 format in whole days (for example, <code>P30D</code> for 30 days or <code>P60D</code> for 60 days).</p>
    /// <p>The field is <code>null</code> when no renewal decision deadline is set. In that case, either party can change the auto-renewal decision up to the end date of the agreement.</p>
    pub fn get_lockout_period(&self) -> &::std::option::Option<::std::string::String> {
        &self.lockout_period
    }
    /// <p>The maximum number of times the agreement can be renewed. The field is <code>null</code> when the number of renewals is unlimited.</p>
    /// <p>After the agreement reaches this limit, it expires on its end date instead of renewing.</p>
    pub fn max_renewals(mut self, input: i32) -> Self {
        self.max_renewals = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum number of times the agreement can be renewed. The field is <code>null</code> when the number of renewals is unlimited.</p>
    /// <p>After the agreement reaches this limit, it expires on its end date instead of renewing.</p>
    pub fn set_max_renewals(mut self, input: ::std::option::Option<i32>) -> Self {
        self.max_renewals = input;
        self
    }
    /// <p>The maximum number of times the agreement can be renewed. The field is <code>null</code> when the number of renewals is unlimited.</p>
    /// <p>After the agreement reaches this limit, it expires on its end date instead of renewing.</p>
    pub fn get_max_renewals(&self) -> &::std::option::Option<i32> {
        &self.max_renewals
    }
    /// <p>The date by which the proposer must finalize the price increase for the next renewal, measured back from the end date of the agreement. The duration is represented in the ISO 8601 format in whole days (for example, <code>P30D</code> for 30 days or <code>P60D</code> for 60 days).</p>
    /// <p>This field applies only when <code>PriceIncrease</code> is a <code>PercentageRange</code>. The field is <code>null</code> when <code>PriceIncrease</code> is a <code>FixedPercentage</code>, because the price increase is already fixed and there is nothing for the proposer to finalize. If the proposer doesn't finalize a value by the adjustment deadline, the <code>DefaultValue</code> of the range applies.</p>
    /// <p><code>AdjustmentDeadline</code> must be greater than <code>LockoutPeriod</code>.</p>
    pub fn adjustment_deadline(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.adjustment_deadline = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The date by which the proposer must finalize the price increase for the next renewal, measured back from the end date of the agreement. The duration is represented in the ISO 8601 format in whole days (for example, <code>P30D</code> for 30 days or <code>P60D</code> for 60 days).</p>
    /// <p>This field applies only when <code>PriceIncrease</code> is a <code>PercentageRange</code>. The field is <code>null</code> when <code>PriceIncrease</code> is a <code>FixedPercentage</code>, because the price increase is already fixed and there is nothing for the proposer to finalize. If the proposer doesn't finalize a value by the adjustment deadline, the <code>DefaultValue</code> of the range applies.</p>
    /// <p><code>AdjustmentDeadline</code> must be greater than <code>LockoutPeriod</code>.</p>
    pub fn set_adjustment_deadline(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.adjustment_deadline = input;
        self
    }
    /// <p>The date by which the proposer must finalize the price increase for the next renewal, measured back from the end date of the agreement. The duration is represented in the ISO 8601 format in whole days (for example, <code>P30D</code> for 30 days or <code>P60D</code> for 60 days).</p>
    /// <p>This field applies only when <code>PriceIncrease</code> is a <code>PercentageRange</code>. The field is <code>null</code> when <code>PriceIncrease</code> is a <code>FixedPercentage</code>, because the price increase is already fixed and there is nothing for the proposer to finalize. If the proposer doesn't finalize a value by the adjustment deadline, the <code>DefaultValue</code> of the range applies.</p>
    /// <p><code>AdjustmentDeadline</code> must be greater than <code>LockoutPeriod</code>.</p>
    pub fn get_adjustment_deadline(&self) -> &::std::option::Option<::std::string::String> {
        &self.adjustment_deadline
    }
    /// <p>The price increase that is applied each time the agreement renews. The field is <code>null</code> when the price doesn't change at renewal.</p>
    pub fn price_increase(mut self, input: crate::types::PriceIncrease) -> Self {
        self.price_increase = ::std::option::Option::Some(input);
        self
    }
    /// <p>The price increase that is applied each time the agreement renews. The field is <code>null</code> when the price doesn't change at renewal.</p>
    pub fn set_price_increase(mut self, input: ::std::option::Option<crate::types::PriceIncrease>) -> Self {
        self.price_increase = input;
        self
    }
    /// <p>The price increase that is applied each time the agreement renews. The field is <code>null</code> when the price doesn't change at renewal.</p>
    pub fn get_price_increase(&self) -> &::std::option::Option<crate::types::PriceIncrease> {
        &self.price_increase
    }
    /// Appends an item to `term_templates`.
    ///
    /// To override the contents of this collection use [`set_term_templates`](Self::set_term_templates).
    ///
    /// <p>Defines how specific terms change each time the agreement renews. The field is <code>null</code> when no terms change at renewal.</p>
    pub fn term_templates(mut self, input: crate::types::TermTemplate) -> Self {
        let mut v = self.term_templates.unwrap_or_default();
        v.push(input);
        self.term_templates = ::std::option::Option::Some(v);
        self
    }
    /// <p>Defines how specific terms change each time the agreement renews. The field is <code>null</code> when no terms change at renewal.</p>
    pub fn set_term_templates(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::TermTemplate>>) -> Self {
        self.term_templates = input;
        self
    }
    /// <p>Defines how specific terms change each time the agreement renews. The field is <code>null</code> when no terms change at renewal.</p>
    pub fn get_term_templates(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::TermTemplate>> {
        &self.term_templates
    }
    /// Consumes the builder and constructs a [`RenewalTerm`](crate::types::RenewalTerm).
    pub fn build(self) -> crate::types::RenewalTerm {
        crate::types::RenewalTerm {
            r#type: self.r#type,
            id: self.id,
            configuration: self.configuration,
            lockout_period: self.lockout_period,
            max_renewals: self.max_renewals,
            adjustment_deadline: self.adjustment_deadline,
            price_increase: self.price_increase,
            term_templates: self.term_templates,
        }
    }
}
