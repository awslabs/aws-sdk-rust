// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration details of the inference model for an evaluation job.</p>
/// <p>For automated model evaluation jobs, only a single model is supported.</p>
/// <p>For human-based model evaluation jobs, your annotator can compare the responses for up to two different models.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum EvaluationInferenceConfig {
    /// <p>Specifies the inference models.</p>
    Models(::std::vec::Vec<crate::types::EvaluationModelConfig>),
    /// <p>Contains the configuration details of the inference for a knowledge base evaluation job, including either the retrieval only configuration or the retrieval with response generation configuration.</p>
    RagConfigs(::std::vec::Vec<crate::types::RagConfig>),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl EvaluationInferenceConfig {
    /// Tries to convert the enum instance into [`Models`](crate::types::EvaluationInferenceConfig::Models), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_models(&self) -> ::std::result::Result<&::std::vec::Vec<crate::types::EvaluationModelConfig>, &Self> {
        if let EvaluationInferenceConfig::Models(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Models`](crate::types::EvaluationInferenceConfig::Models).
    pub fn is_models(&self) -> bool {
        self.as_models().is_ok()
    }
    /// Tries to convert the enum instance into [`RagConfigs`](crate::types::EvaluationInferenceConfig::RagConfigs), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_rag_configs(&self) -> ::std::result::Result<&::std::vec::Vec<crate::types::RagConfig>, &Self> {
        if let EvaluationInferenceConfig::RagConfigs(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`RagConfigs`](crate::types::EvaluationInferenceConfig::RagConfigs).
    pub fn is_rag_configs(&self) -> bool {
        self.as_rag_configs().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
