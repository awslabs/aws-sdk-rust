// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ListStepConsumersOutput {
    /// <p>The consumers on the list.</p>
    pub consumers: ::std::vec::Vec<crate::types::StepConsumer>,
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub next_token: ::std::option::Option<::std::string::String>,
    _request_id: Option<String>,
}
impl ListStepConsumersOutput {
    /// <p>The consumers on the list.</p>
    pub fn consumers(&self) -> &[crate::types::StepConsumer] {
        use std::ops::Deref;
        self.consumers.deref()
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(&self) -> ::std::option::Option<&str> {
        self.next_token.as_deref()
    }
}
impl ::aws_types::request_id::RequestId for ListStepConsumersOutput {
    fn request_id(&self) -> Option<&str> {
        self._request_id.as_deref()
    }
}
impl ListStepConsumersOutput {
    /// Creates a new builder-style object to manufacture [`ListStepConsumersOutput`](crate::operation::list_step_consumers::ListStepConsumersOutput).
    pub fn builder() -> crate::operation::list_step_consumers::builders::ListStepConsumersOutputBuilder {
        crate::operation::list_step_consumers::builders::ListStepConsumersOutputBuilder::default()
    }
}

/// A builder for [`ListStepConsumersOutput`](crate::operation::list_step_consumers::ListStepConsumersOutput).
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
pub struct ListStepConsumersOutputBuilder {
    pub(crate) consumers: ::std::option::Option<::std::vec::Vec<crate::types::StepConsumer>>,
    pub(crate) next_token: ::std::option::Option<::std::string::String>,
    _request_id: Option<String>,
}
impl ListStepConsumersOutputBuilder {
    /// Appends an item to `consumers`.
    ///
    /// To override the contents of this collection use [`set_consumers`](Self::set_consumers).
    ///
    /// <p>The consumers on the list.</p>
    pub fn consumers(mut self, input: crate::types::StepConsumer) -> Self {
        let mut v = self.consumers.unwrap_or_default();
        v.push(input);
        self.consumers = ::std::option::Option::Some(v);
        self
    }
    /// <p>The consumers on the list.</p>
    pub fn set_consumers(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::StepConsumer>>) -> Self {
        self.consumers = input;
        self
    }
    /// <p>The consumers on the list.</p>
    pub fn get_consumers(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::StepConsumer>> {
        &self.consumers
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.next_token = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn set_next_token(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.next_token = input;
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn get_next_token(&self) -> &::std::option::Option<::std::string::String> {
        &self.next_token
    }
    pub(crate) fn _request_id(mut self, request_id: impl Into<String>) -> Self {
        self._request_id = Some(request_id.into());
        self
    }

    pub(crate) fn _set_request_id(&mut self, request_id: Option<String>) -> &mut Self {
        self._request_id = request_id;
        self
    }
    /// Consumes the builder and constructs a [`ListStepConsumersOutput`](crate::operation::list_step_consumers::ListStepConsumersOutput).
    /// This method will fail if any of the following fields are not set:
    /// - [`consumers`](crate::operation::list_step_consumers::builders::ListStepConsumersOutputBuilder::consumers)
    pub fn build(
        self,
    ) -> ::std::result::Result<crate::operation::list_step_consumers::ListStepConsumersOutput, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::operation::list_step_consumers::ListStepConsumersOutput {
            consumers: self.consumers.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "consumers",
                    "consumers was not specified but it is required when building ListStepConsumersOutput",
                )
            })?,
            next_token: self.next_token,
            _request_id: self._request_id,
        })
    }
}
