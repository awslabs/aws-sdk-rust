// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ListFarmsOutput {
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub next_token: ::std::option::Option<::std::string::String>,
    /// <p>Farms on the list.</p>
    pub farms: ::std::vec::Vec<crate::types::FarmSummary>,
    _request_id: Option<String>,
}
impl ListFarmsOutput {
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(&self) -> ::std::option::Option<&str> {
        self.next_token.as_deref()
    }
    /// <p>Farms on the list.</p>
    pub fn farms(&self) -> &[crate::types::FarmSummary] {
        use std::ops::Deref;
        self.farms.deref()
    }
}
impl ::aws_types::request_id::RequestId for ListFarmsOutput {
    fn request_id(&self) -> Option<&str> {
        self._request_id.as_deref()
    }
}
impl ListFarmsOutput {
    /// Creates a new builder-style object to manufacture [`ListFarmsOutput`](crate::operation::list_farms::ListFarmsOutput).
    pub fn builder() -> crate::operation::list_farms::builders::ListFarmsOutputBuilder {
        crate::operation::list_farms::builders::ListFarmsOutputBuilder::default()
    }
}

/// A builder for [`ListFarmsOutput`](crate::operation::list_farms::ListFarmsOutput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ListFarmsOutputBuilder {
    pub(crate) next_token: ::std::option::Option<::std::string::String>,
    pub(crate) farms: ::std::option::Option<::std::vec::Vec<crate::types::FarmSummary>>,
    _request_id: Option<String>,
}
impl ListFarmsOutputBuilder {
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.next_token = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn set_next_token(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.next_token = input;
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn get_next_token(&self) -> &::std::option::Option<::std::string::String> {
        &self.next_token
    }
    /// Appends an item to `farms`.
    ///
    /// To override the contents of this collection use [`set_farms`](Self::set_farms).
    ///
    /// <p>Farms on the list.</p>
    pub fn farms(mut self, input: crate::types::FarmSummary) -> Self {
        let mut v = self.farms.unwrap_or_default();
        v.push(input);
        self.farms = ::std::option::Option::Some(v);
        self
    }
    /// <p>Farms on the list.</p>
    pub fn set_farms(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::FarmSummary>>) -> Self {
        self.farms = input;
        self
    }
    /// <p>Farms on the list.</p>
    pub fn get_farms(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::FarmSummary>> {
        &self.farms
    }
    pub(crate) fn _request_id(mut self, request_id: impl Into<String>) -> Self {
        self._request_id = Some(request_id.into());
        self
    }

    pub(crate) fn _set_request_id(&mut self, request_id: Option<String>) -> &mut Self {
        self._request_id = request_id;
        self
    }
    /// Consumes the builder and constructs a [`ListFarmsOutput`](crate::operation::list_farms::ListFarmsOutput).
    /// This method will fail if any of the following fields are not set:
    /// - [`farms`](crate::operation::list_farms::builders::ListFarmsOutputBuilder::farms)
    pub fn build(self) -> ::std::result::Result<crate::operation::list_farms::ListFarmsOutput, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::operation::list_farms::ListFarmsOutput {
            next_token: self.next_token,
            farms: self.farms.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "farms",
                    "farms was not specified but it is required when building ListFarmsOutput",
                )
            })?,
            _request_id: self._request_id,
        })
    }
}
