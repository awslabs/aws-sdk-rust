// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ListWorkersOutput {
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub next_token: ::std::option::Option<::std::string::String>,
    /// <p>The workers on the list.</p>
    pub workers: ::std::vec::Vec<crate::types::WorkerSummary>,
    _request_id: Option<String>,
}
impl ListWorkersOutput {
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(&self) -> ::std::option::Option<&str> {
        self.next_token.as_deref()
    }
    /// <p>The workers on the list.</p>
    pub fn workers(&self) -> &[crate::types::WorkerSummary] {
        use std::ops::Deref;
        self.workers.deref()
    }
}
impl ::aws_types::request_id::RequestId for ListWorkersOutput {
    fn request_id(&self) -> Option<&str> {
        self._request_id.as_deref()
    }
}
impl ListWorkersOutput {
    /// Creates a new builder-style object to manufacture [`ListWorkersOutput`](crate::operation::list_workers::ListWorkersOutput).
    pub fn builder() -> crate::operation::list_workers::builders::ListWorkersOutputBuilder {
        crate::operation::list_workers::builders::ListWorkersOutputBuilder::default()
    }
}

/// A builder for [`ListWorkersOutput`](crate::operation::list_workers::ListWorkersOutput).
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
pub struct ListWorkersOutputBuilder {
    pub(crate) next_token: ::std::option::Option<::std::string::String>,
    pub(crate) workers: ::std::option::Option<::std::vec::Vec<crate::types::WorkerSummary>>,
    _request_id: Option<String>,
}
impl ListWorkersOutputBuilder {
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.next_token = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn set_next_token(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.next_token = input;
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn get_next_token(&self) -> &::std::option::Option<::std::string::String> {
        &self.next_token
    }
    /// Appends an item to `workers`.
    ///
    /// To override the contents of this collection use [`set_workers`](Self::set_workers).
    ///
    /// <p>The workers on the list.</p>
    pub fn workers(mut self, input: crate::types::WorkerSummary) -> Self {
        let mut v = self.workers.unwrap_or_default();
        v.push(input);
        self.workers = ::std::option::Option::Some(v);
        self
    }
    /// <p>The workers on the list.</p>
    pub fn set_workers(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::WorkerSummary>>) -> Self {
        self.workers = input;
        self
    }
    /// <p>The workers on the list.</p>
    pub fn get_workers(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::WorkerSummary>> {
        &self.workers
    }
    pub(crate) fn _request_id(mut self, request_id: impl Into<String>) -> Self {
        self._request_id = Some(request_id.into());
        self
    }

    pub(crate) fn _set_request_id(&mut self, request_id: Option<String>) -> &mut Self {
        self._request_id = request_id;
        self
    }
    /// Consumes the builder and constructs a [`ListWorkersOutput`](crate::operation::list_workers::ListWorkersOutput).
    /// This method will fail if any of the following fields are not set:
    /// - [`workers`](crate::operation::list_workers::builders::ListWorkersOutputBuilder::workers)
    pub fn build(self) -> ::std::result::Result<crate::operation::list_workers::ListWorkersOutput, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::operation::list_workers::ListWorkersOutput {
            next_token: self.next_token,
            workers: self.workers.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "workers",
                    "workers was not specified but it is required when building ListWorkersOutput",
                )
            })?,
            _request_id: self._request_id,
        })
    }
}
