// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ListQueueEnvironmentsOutput {
    /// <p>The environments to include in the queue environments list.</p>
    pub environments: ::std::vec::Vec<crate::types::QueueEnvironmentSummary>,
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub next_token: ::std::option::Option<::std::string::String>,
    _request_id: Option<String>,
}
impl ListQueueEnvironmentsOutput {
    /// <p>The environments to include in the queue environments list.</p>
    pub fn environments(&self) -> &[crate::types::QueueEnvironmentSummary] {
        use std::ops::Deref;
        self.environments.deref()
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(&self) -> ::std::option::Option<&str> {
        self.next_token.as_deref()
    }
}
impl ::aws_types::request_id::RequestId for ListQueueEnvironmentsOutput {
    fn request_id(&self) -> Option<&str> {
        self._request_id.as_deref()
    }
}
impl ListQueueEnvironmentsOutput {
    /// Creates a new builder-style object to manufacture [`ListQueueEnvironmentsOutput`](crate::operation::list_queue_environments::ListQueueEnvironmentsOutput).
    pub fn builder() -> crate::operation::list_queue_environments::builders::ListQueueEnvironmentsOutputBuilder {
        crate::operation::list_queue_environments::builders::ListQueueEnvironmentsOutputBuilder::default()
    }
}

/// A builder for [`ListQueueEnvironmentsOutput`](crate::operation::list_queue_environments::ListQueueEnvironmentsOutput).
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
pub struct ListQueueEnvironmentsOutputBuilder {
    pub(crate) environments: ::std::option::Option<::std::vec::Vec<crate::types::QueueEnvironmentSummary>>,
    pub(crate) next_token: ::std::option::Option<::std::string::String>,
    _request_id: Option<String>,
}
impl ListQueueEnvironmentsOutputBuilder {
    /// Appends an item to `environments`.
    ///
    /// To override the contents of this collection use [`set_environments`](Self::set_environments).
    ///
    /// <p>The environments to include in the queue environments list.</p>
    pub fn environments(mut self, input: crate::types::QueueEnvironmentSummary) -> Self {
        let mut v = self.environments.unwrap_or_default();
        v.push(input);
        self.environments = ::std::option::Option::Some(v);
        self
    }
    /// <p>The environments to include in the queue environments list.</p>
    pub fn set_environments(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::QueueEnvironmentSummary>>) -> Self {
        self.environments = input;
        self
    }
    /// <p>The environments to include in the queue environments list.</p>
    pub fn get_environments(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::QueueEnvironmentSummary>> {
        &self.environments
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.next_token = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn set_next_token(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.next_token = input;
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn get_next_token(&self) -> &::std::option::Option<::std::string::String> {
        &self.next_token
    }
    pub(crate) fn _request_id(mut self, request_id: impl Into<String>) -> Self {
        self._request_id = Some(request_id.into());
        self
    }

    pub(crate) fn _set_request_id(&mut self, request_id: Option<String>) -> &mut Self {
        self._request_id = request_id;
        self
    }
    /// Consumes the builder and constructs a [`ListQueueEnvironmentsOutput`](crate::operation::list_queue_environments::ListQueueEnvironmentsOutput).
    /// This method will fail if any of the following fields are not set:
    /// - [`environments`](crate::operation::list_queue_environments::builders::ListQueueEnvironmentsOutputBuilder::environments)
    pub fn build(
        self,
    ) -> ::std::result::Result<crate::operation::list_queue_environments::ListQueueEnvironmentsOutput, ::aws_smithy_types::error::operation::BuildError>
    {
        ::std::result::Result::Ok(crate::operation::list_queue_environments::ListQueueEnvironmentsOutput {
            environments: self.environments.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "environments",
                    "environments was not specified but it is required when building ListQueueEnvironmentsOutput",
                )
            })?,
            next_token: self.next_token,
            _request_id: self._request_id,
        })
    }
}
