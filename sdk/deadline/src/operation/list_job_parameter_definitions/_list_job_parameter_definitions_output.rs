// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ListJobParameterDefinitionsOutput {
    /// <p>Lists parameter definitions of a job.</p>
    pub job_parameter_definitions: ::std::vec::Vec<::aws_smithy_types::Document>,
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub next_token: ::std::option::Option<::std::string::String>,
    _request_id: Option<String>,
}
impl ListJobParameterDefinitionsOutput {
    /// <p>Lists parameter definitions of a job.</p>
    pub fn job_parameter_definitions(&self) -> &[::aws_smithy_types::Document] {
        use std::ops::Deref;
        self.job_parameter_definitions.deref()
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(&self) -> ::std::option::Option<&str> {
        self.next_token.as_deref()
    }
}
impl ::aws_types::request_id::RequestId for ListJobParameterDefinitionsOutput {
    fn request_id(&self) -> Option<&str> {
        self._request_id.as_deref()
    }
}
impl ListJobParameterDefinitionsOutput {
    /// Creates a new builder-style object to manufacture [`ListJobParameterDefinitionsOutput`](crate::operation::list_job_parameter_definitions::ListJobParameterDefinitionsOutput).
    pub fn builder() -> crate::operation::list_job_parameter_definitions::builders::ListJobParameterDefinitionsOutputBuilder {
        crate::operation::list_job_parameter_definitions::builders::ListJobParameterDefinitionsOutputBuilder::default()
    }
}

/// A builder for [`ListJobParameterDefinitionsOutput`](crate::operation::list_job_parameter_definitions::ListJobParameterDefinitionsOutput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ListJobParameterDefinitionsOutputBuilder {
    pub(crate) job_parameter_definitions: ::std::option::Option<::std::vec::Vec<::aws_smithy_types::Document>>,
    pub(crate) next_token: ::std::option::Option<::std::string::String>,
    _request_id: Option<String>,
}
impl ListJobParameterDefinitionsOutputBuilder {
    /// Appends an item to `job_parameter_definitions`.
    ///
    /// To override the contents of this collection use [`set_job_parameter_definitions`](Self::set_job_parameter_definitions).
    ///
    /// <p>Lists parameter definitions of a job.</p>
    pub fn job_parameter_definitions(mut self, input: ::aws_smithy_types::Document) -> Self {
        let mut v = self.job_parameter_definitions.unwrap_or_default();
        v.push(input);
        self.job_parameter_definitions = ::std::option::Option::Some(v);
        self
    }
    /// <p>Lists parameter definitions of a job.</p>
    pub fn set_job_parameter_definitions(mut self, input: ::std::option::Option<::std::vec::Vec<::aws_smithy_types::Document>>) -> Self {
        self.job_parameter_definitions = input;
        self
    }
    /// <p>Lists parameter definitions of a job.</p>
    pub fn get_job_parameter_definitions(&self) -> &::std::option::Option<::std::vec::Vec<::aws_smithy_types::Document>> {
        &self.job_parameter_definitions
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.next_token = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn set_next_token(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.next_token = input;
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn get_next_token(&self) -> &::std::option::Option<::std::string::String> {
        &self.next_token
    }
    pub(crate) fn _request_id(mut self, request_id: impl Into<String>) -> Self {
        self._request_id = Some(request_id.into());
        self
    }

    pub(crate) fn _set_request_id(&mut self, request_id: Option<String>) -> &mut Self {
        self._request_id = request_id;
        self
    }
    /// Consumes the builder and constructs a [`ListJobParameterDefinitionsOutput`](crate::operation::list_job_parameter_definitions::ListJobParameterDefinitionsOutput).
    /// This method will fail if any of the following fields are not set:
    /// - [`job_parameter_definitions`](crate::operation::list_job_parameter_definitions::builders::ListJobParameterDefinitionsOutputBuilder::job_parameter_definitions)
    pub fn build(
        self,
    ) -> ::std::result::Result<
        crate::operation::list_job_parameter_definitions::ListJobParameterDefinitionsOutput,
        ::aws_smithy_types::error::operation::BuildError,
    > {
        ::std::result::Result::Ok(crate::operation::list_job_parameter_definitions::ListJobParameterDefinitionsOutput {
            job_parameter_definitions: self.job_parameter_definitions.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "job_parameter_definitions",
                    "job_parameter_definitions was not specified but it is required when building ListJobParameterDefinitionsOutput",
                )
            })?,
            next_token: self.next_token,
            _request_id: self._request_id,
        })
    }
}
