// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ListSessionActionsOutput {
    /// <p>The session actions.</p>
    pub session_actions: ::std::vec::Vec<crate::types::SessionActionSummary>,
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub next_token: ::std::option::Option<::std::string::String>,
    _request_id: Option<String>,
}
impl ListSessionActionsOutput {
    /// <p>The session actions.</p>
    pub fn session_actions(&self) -> &[crate::types::SessionActionSummary] {
        use std::ops::Deref;
        self.session_actions.deref()
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(&self) -> ::std::option::Option<&str> {
        self.next_token.as_deref()
    }
}
impl ::aws_types::request_id::RequestId for ListSessionActionsOutput {
    fn request_id(&self) -> Option<&str> {
        self._request_id.as_deref()
    }
}
impl ListSessionActionsOutput {
    /// Creates a new builder-style object to manufacture [`ListSessionActionsOutput`](crate::operation::list_session_actions::ListSessionActionsOutput).
    pub fn builder() -> crate::operation::list_session_actions::builders::ListSessionActionsOutputBuilder {
        crate::operation::list_session_actions::builders::ListSessionActionsOutputBuilder::default()
    }
}

/// A builder for [`ListSessionActionsOutput`](crate::operation::list_session_actions::ListSessionActionsOutput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ListSessionActionsOutputBuilder {
    pub(crate) session_actions: ::std::option::Option<::std::vec::Vec<crate::types::SessionActionSummary>>,
    pub(crate) next_token: ::std::option::Option<::std::string::String>,
    _request_id: Option<String>,
}
impl ListSessionActionsOutputBuilder {
    /// Appends an item to `session_actions`.
    ///
    /// To override the contents of this collection use [`set_session_actions`](Self::set_session_actions).
    ///
    /// <p>The session actions.</p>
    pub fn session_actions(mut self, input: crate::types::SessionActionSummary) -> Self {
        let mut v = self.session_actions.unwrap_or_default();
        v.push(input);
        self.session_actions = ::std::option::Option::Some(v);
        self
    }
    /// <p>The session actions.</p>
    pub fn set_session_actions(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::SessionActionSummary>>) -> Self {
        self.session_actions = input;
        self
    }
    /// <p>The session actions.</p>
    pub fn get_session_actions(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::SessionActionSummary>> {
        &self.session_actions
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.next_token = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn set_next_token(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.next_token = input;
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn get_next_token(&self) -> &::std::option::Option<::std::string::String> {
        &self.next_token
    }
    pub(crate) fn _request_id(mut self, request_id: impl Into<String>) -> Self {
        self._request_id = Some(request_id.into());
        self
    }

    pub(crate) fn _set_request_id(&mut self, request_id: Option<String>) -> &mut Self {
        self._request_id = request_id;
        self
    }
    /// Consumes the builder and constructs a [`ListSessionActionsOutput`](crate::operation::list_session_actions::ListSessionActionsOutput).
    /// This method will fail if any of the following fields are not set:
    /// - [`session_actions`](crate::operation::list_session_actions::builders::ListSessionActionsOutputBuilder::session_actions)
    pub fn build(
        self,
    ) -> ::std::result::Result<crate::operation::list_session_actions::ListSessionActionsOutput, ::aws_smithy_types::error::operation::BuildError>
    {
        ::std::result::Result::Ok(crate::operation::list_session_actions::ListSessionActionsOutput {
            session_actions: self.session_actions.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "session_actions",
                    "session_actions was not specified but it is required when building ListSessionActionsOutput",
                )
            })?,
            next_token: self.next_token,
            _request_id: self._request_id,
        })
    }
}
