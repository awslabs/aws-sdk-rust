// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ListStorageProfilesOutput {
    /// <p>The storage profiles.</p>
    pub storage_profiles: ::std::vec::Vec<crate::types::StorageProfileSummary>,
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub next_token: ::std::option::Option<::std::string::String>,
    _request_id: Option<String>,
}
impl ListStorageProfilesOutput {
    /// <p>The storage profiles.</p>
    pub fn storage_profiles(&self) -> &[crate::types::StorageProfileSummary] {
        use std::ops::Deref;
        self.storage_profiles.deref()
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(&self) -> ::std::option::Option<&str> {
        self.next_token.as_deref()
    }
}
impl ::aws_types::request_id::RequestId for ListStorageProfilesOutput {
    fn request_id(&self) -> Option<&str> {
        self._request_id.as_deref()
    }
}
impl ListStorageProfilesOutput {
    /// Creates a new builder-style object to manufacture [`ListStorageProfilesOutput`](crate::operation::list_storage_profiles::ListStorageProfilesOutput).
    pub fn builder() -> crate::operation::list_storage_profiles::builders::ListStorageProfilesOutputBuilder {
        crate::operation::list_storage_profiles::builders::ListStorageProfilesOutputBuilder::default()
    }
}

/// A builder for [`ListStorageProfilesOutput`](crate::operation::list_storage_profiles::ListStorageProfilesOutput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ListStorageProfilesOutputBuilder {
    pub(crate) storage_profiles: ::std::option::Option<::std::vec::Vec<crate::types::StorageProfileSummary>>,
    pub(crate) next_token: ::std::option::Option<::std::string::String>,
    _request_id: Option<String>,
}
impl ListStorageProfilesOutputBuilder {
    /// Appends an item to `storage_profiles`.
    ///
    /// To override the contents of this collection use [`set_storage_profiles`](Self::set_storage_profiles).
    ///
    /// <p>The storage profiles.</p>
    pub fn storage_profiles(mut self, input: crate::types::StorageProfileSummary) -> Self {
        let mut v = self.storage_profiles.unwrap_or_default();
        v.push(input);
        self.storage_profiles = ::std::option::Option::Some(v);
        self
    }
    /// <p>The storage profiles.</p>
    pub fn set_storage_profiles(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::StorageProfileSummary>>) -> Self {
        self.storage_profiles = input;
        self
    }
    /// <p>The storage profiles.</p>
    pub fn get_storage_profiles(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::StorageProfileSummary>> {
        &self.storage_profiles
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn next_token(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.next_token = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn set_next_token(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.next_token = input;
        self
    }
    /// <p>If Deadline Cloud returns <code>nextToken</code>, then there are more results available. The value of <code>nextToken</code> is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then <code>nextToken</code> is set to <code>null</code>. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 <code>ValidationException</code> error.</p>
    pub fn get_next_token(&self) -> &::std::option::Option<::std::string::String> {
        &self.next_token
    }
    pub(crate) fn _request_id(mut self, request_id: impl Into<String>) -> Self {
        self._request_id = Some(request_id.into());
        self
    }

    pub(crate) fn _set_request_id(&mut self, request_id: Option<String>) -> &mut Self {
        self._request_id = request_id;
        self
    }
    /// Consumes the builder and constructs a [`ListStorageProfilesOutput`](crate::operation::list_storage_profiles::ListStorageProfilesOutput).
    /// This method will fail if any of the following fields are not set:
    /// - [`storage_profiles`](crate::operation::list_storage_profiles::builders::ListStorageProfilesOutputBuilder::storage_profiles)
    pub fn build(
        self,
    ) -> ::std::result::Result<crate::operation::list_storage_profiles::ListStorageProfilesOutput, ::aws_smithy_types::error::operation::BuildError>
    {
        ::std::result::Result::Ok(crate::operation::list_storage_profiles::ListStorageProfilesOutput {
            storage_profiles: self.storage_profiles.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "storage_profiles",
                    "storage_profiles was not specified but it is required when building ListStorageProfilesOutput",
                )
            })?,
            next_token: self.next_token,
            _request_id: self._request_id,
        })
    }
}
