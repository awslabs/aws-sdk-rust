// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Options for the vehicle leg of the intermodal route.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct RouteIntermodalVehicleOptions {
    /// <p>Allowed vehicle transport modes when calculating the route. By default, all transport modes are allowed. Cannot be used together with <code>ExcludedModes</code>.</p>
    pub allowed_modes: ::std::option::Option<::std::vec::Vec<crate::types::RouteVehicleMode>>,
    /// <p>Specifies the portion of the route for which this leg type is enabled. By default, the leg type is enabled for all legs. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>FirstLeg</code> - Enable this leg type for the first non-pedestrian leg of the route.</p></li>
    /// <li>
    /// <p><code>LastLeg</code> - Enable this leg type for the last non-pedestrian leg of the route.</p></li>
    /// <li>
    /// <p><code>EntireRoute</code> - Enable this leg type for the entire route.</p></li>
    /// <li>
    /// <p><code>None</code> - Disable this leg type entirely.</p></li>
    /// </ul>
    pub enabled_for: ::std::option::Option<::std::vec::Vec<crate::types::RouteIntermodalEnabledLegs>>,
    /// <p>Excluded vehicle transport modes when calculating the route. By default, all transport modes are allowed. Cannot be used together with <code>AllowedModes</code>.</p>
    pub excluded_modes: ::std::option::Option<::std::vec::Vec<crate::types::RouteVehicleMode>>,
}
impl RouteIntermodalVehicleOptions {
    /// <p>Allowed vehicle transport modes when calculating the route. By default, all transport modes are allowed. Cannot be used together with <code>ExcludedModes</code>.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.allowed_modes.is_none()`.
    pub fn allowed_modes(&self) -> &[crate::types::RouteVehicleMode] {
        self.allowed_modes.as_deref().unwrap_or_default()
    }
    /// <p>Specifies the portion of the route for which this leg type is enabled. By default, the leg type is enabled for all legs. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>FirstLeg</code> - Enable this leg type for the first non-pedestrian leg of the route.</p></li>
    /// <li>
    /// <p><code>LastLeg</code> - Enable this leg type for the last non-pedestrian leg of the route.</p></li>
    /// <li>
    /// <p><code>EntireRoute</code> - Enable this leg type for the entire route.</p></li>
    /// <li>
    /// <p><code>None</code> - Disable this leg type entirely.</p></li>
    /// </ul>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.enabled_for.is_none()`.
    pub fn enabled_for(&self) -> &[crate::types::RouteIntermodalEnabledLegs] {
        self.enabled_for.as_deref().unwrap_or_default()
    }
    /// <p>Excluded vehicle transport modes when calculating the route. By default, all transport modes are allowed. Cannot be used together with <code>AllowedModes</code>.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.excluded_modes.is_none()`.
    pub fn excluded_modes(&self) -> &[crate::types::RouteVehicleMode] {
        self.excluded_modes.as_deref().unwrap_or_default()
    }
}
impl RouteIntermodalVehicleOptions {
    /// Creates a new builder-style object to manufacture [`RouteIntermodalVehicleOptions`](crate::types::RouteIntermodalVehicleOptions).
    pub fn builder() -> crate::types::builders::RouteIntermodalVehicleOptionsBuilder {
        crate::types::builders::RouteIntermodalVehicleOptionsBuilder::default()
    }
}

/// A builder for [`RouteIntermodalVehicleOptions`](crate::types::RouteIntermodalVehicleOptions).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct RouteIntermodalVehicleOptionsBuilder {
    pub(crate) allowed_modes: ::std::option::Option<::std::vec::Vec<crate::types::RouteVehicleMode>>,
    pub(crate) enabled_for: ::std::option::Option<::std::vec::Vec<crate::types::RouteIntermodalEnabledLegs>>,
    pub(crate) excluded_modes: ::std::option::Option<::std::vec::Vec<crate::types::RouteVehicleMode>>,
}
impl RouteIntermodalVehicleOptionsBuilder {
    /// Appends an item to `allowed_modes`.
    ///
    /// To override the contents of this collection use [`set_allowed_modes`](Self::set_allowed_modes).
    ///
    /// <p>Allowed vehicle transport modes when calculating the route. By default, all transport modes are allowed. Cannot be used together with <code>ExcludedModes</code>.</p>
    pub fn allowed_modes(mut self, input: crate::types::RouteVehicleMode) -> Self {
        let mut v = self.allowed_modes.unwrap_or_default();
        v.push(input);
        self.allowed_modes = ::std::option::Option::Some(v);
        self
    }
    /// <p>Allowed vehicle transport modes when calculating the route. By default, all transport modes are allowed. Cannot be used together with <code>ExcludedModes</code>.</p>
    pub fn set_allowed_modes(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::RouteVehicleMode>>) -> Self {
        self.allowed_modes = input;
        self
    }
    /// <p>Allowed vehicle transport modes when calculating the route. By default, all transport modes are allowed. Cannot be used together with <code>ExcludedModes</code>.</p>
    pub fn get_allowed_modes(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::RouteVehicleMode>> {
        &self.allowed_modes
    }
    /// Appends an item to `enabled_for`.
    ///
    /// To override the contents of this collection use [`set_enabled_for`](Self::set_enabled_for).
    ///
    /// <p>Specifies the portion of the route for which this leg type is enabled. By default, the leg type is enabled for all legs. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>FirstLeg</code> - Enable this leg type for the first non-pedestrian leg of the route.</p></li>
    /// <li>
    /// <p><code>LastLeg</code> - Enable this leg type for the last non-pedestrian leg of the route.</p></li>
    /// <li>
    /// <p><code>EntireRoute</code> - Enable this leg type for the entire route.</p></li>
    /// <li>
    /// <p><code>None</code> - Disable this leg type entirely.</p></li>
    /// </ul>
    pub fn enabled_for(mut self, input: crate::types::RouteIntermodalEnabledLegs) -> Self {
        let mut v = self.enabled_for.unwrap_or_default();
        v.push(input);
        self.enabled_for = ::std::option::Option::Some(v);
        self
    }
    /// <p>Specifies the portion of the route for which this leg type is enabled. By default, the leg type is enabled for all legs. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>FirstLeg</code> - Enable this leg type for the first non-pedestrian leg of the route.</p></li>
    /// <li>
    /// <p><code>LastLeg</code> - Enable this leg type for the last non-pedestrian leg of the route.</p></li>
    /// <li>
    /// <p><code>EntireRoute</code> - Enable this leg type for the entire route.</p></li>
    /// <li>
    /// <p><code>None</code> - Disable this leg type entirely.</p></li>
    /// </ul>
    pub fn set_enabled_for(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::RouteIntermodalEnabledLegs>>) -> Self {
        self.enabled_for = input;
        self
    }
    /// <p>Specifies the portion of the route for which this leg type is enabled. By default, the leg type is enabled for all legs. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>FirstLeg</code> - Enable this leg type for the first non-pedestrian leg of the route.</p></li>
    /// <li>
    /// <p><code>LastLeg</code> - Enable this leg type for the last non-pedestrian leg of the route.</p></li>
    /// <li>
    /// <p><code>EntireRoute</code> - Enable this leg type for the entire route.</p></li>
    /// <li>
    /// <p><code>None</code> - Disable this leg type entirely.</p></li>
    /// </ul>
    pub fn get_enabled_for(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::RouteIntermodalEnabledLegs>> {
        &self.enabled_for
    }
    /// Appends an item to `excluded_modes`.
    ///
    /// To override the contents of this collection use [`set_excluded_modes`](Self::set_excluded_modes).
    ///
    /// <p>Excluded vehicle transport modes when calculating the route. By default, all transport modes are allowed. Cannot be used together with <code>AllowedModes</code>.</p>
    pub fn excluded_modes(mut self, input: crate::types::RouteVehicleMode) -> Self {
        let mut v = self.excluded_modes.unwrap_or_default();
        v.push(input);
        self.excluded_modes = ::std::option::Option::Some(v);
        self
    }
    /// <p>Excluded vehicle transport modes when calculating the route. By default, all transport modes are allowed. Cannot be used together with <code>AllowedModes</code>.</p>
    pub fn set_excluded_modes(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::RouteVehicleMode>>) -> Self {
        self.excluded_modes = input;
        self
    }
    /// <p>Excluded vehicle transport modes when calculating the route. By default, all transport modes are allowed. Cannot be used together with <code>AllowedModes</code>.</p>
    pub fn get_excluded_modes(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::RouteVehicleMode>> {
        &self.excluded_modes
    }
    /// Consumes the builder and constructs a [`RouteIntermodalVehicleOptions`](crate::types::RouteIntermodalVehicleOptions).
    pub fn build(self) -> crate::types::RouteIntermodalVehicleOptions {
        crate::types::RouteIntermodalVehicleOptions {
            allowed_modes: self.allowed_modes,
            enabled_for: self.enabled_for,
            excluded_modes: self.excluded_modes,
        }
    }
}
