// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains the encryption configuration for a workspace.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct WorkspaceEncryptionConfiguration {
    /// <p>The encryption scheme for the workspace. <code>SITEWISE_DEFAULT_ENCRYPTION</code> encrypts data with the IoT SiteWise default key. <code>KMS_BASED_ENCRYPTION</code> encrypts data with the customer managed KMS key identified by <code>kmsKeyId</code>.</p>
    pub encryption_type: crate::types::EncryptionType,
    /// <p>The customer managed KMS key used when <code>encryptionType</code> is <code>KMS_BASED_ENCRYPTION</code>. Accepts a key ID, key ARN, or key alias. Required for <code>KMS_BASED_ENCRYPTION</code>; must be omitted for <code>SITEWISE_DEFAULT_ENCRYPTION</code>. After a workspace's customer managed key configuration becomes active, the key can't be changed.</p>
    pub kms_key_id: ::std::option::Option<::std::string::String>,
}
impl WorkspaceEncryptionConfiguration {
    /// <p>The encryption scheme for the workspace. <code>SITEWISE_DEFAULT_ENCRYPTION</code> encrypts data with the IoT SiteWise default key. <code>KMS_BASED_ENCRYPTION</code> encrypts data with the customer managed KMS key identified by <code>kmsKeyId</code>.</p>
    pub fn encryption_type(&self) -> &crate::types::EncryptionType {
        &self.encryption_type
    }
    /// <p>The customer managed KMS key used when <code>encryptionType</code> is <code>KMS_BASED_ENCRYPTION</code>. Accepts a key ID, key ARN, or key alias. Required for <code>KMS_BASED_ENCRYPTION</code>; must be omitted for <code>SITEWISE_DEFAULT_ENCRYPTION</code>. After a workspace's customer managed key configuration becomes active, the key can't be changed.</p>
    pub fn kms_key_id(&self) -> ::std::option::Option<&str> {
        self.kms_key_id.as_deref()
    }
}
impl WorkspaceEncryptionConfiguration {
    /// Creates a new builder-style object to manufacture [`WorkspaceEncryptionConfiguration`](crate::types::WorkspaceEncryptionConfiguration).
    pub fn builder() -> crate::types::builders::WorkspaceEncryptionConfigurationBuilder {
        crate::types::builders::WorkspaceEncryptionConfigurationBuilder::default()
    }
}

/// A builder for [`WorkspaceEncryptionConfiguration`](crate::types::WorkspaceEncryptionConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct WorkspaceEncryptionConfigurationBuilder {
    pub(crate) encryption_type: ::std::option::Option<crate::types::EncryptionType>,
    pub(crate) kms_key_id: ::std::option::Option<::std::string::String>,
}
impl WorkspaceEncryptionConfigurationBuilder {
    /// <p>The encryption scheme for the workspace. <code>SITEWISE_DEFAULT_ENCRYPTION</code> encrypts data with the IoT SiteWise default key. <code>KMS_BASED_ENCRYPTION</code> encrypts data with the customer managed KMS key identified by <code>kmsKeyId</code>.</p>
    /// This field is required.
    pub fn encryption_type(mut self, input: crate::types::EncryptionType) -> Self {
        self.encryption_type = ::std::option::Option::Some(input);
        self
    }
    /// <p>The encryption scheme for the workspace. <code>SITEWISE_DEFAULT_ENCRYPTION</code> encrypts data with the IoT SiteWise default key. <code>KMS_BASED_ENCRYPTION</code> encrypts data with the customer managed KMS key identified by <code>kmsKeyId</code>.</p>
    pub fn set_encryption_type(mut self, input: ::std::option::Option<crate::types::EncryptionType>) -> Self {
        self.encryption_type = input;
        self
    }
    /// <p>The encryption scheme for the workspace. <code>SITEWISE_DEFAULT_ENCRYPTION</code> encrypts data with the IoT SiteWise default key. <code>KMS_BASED_ENCRYPTION</code> encrypts data with the customer managed KMS key identified by <code>kmsKeyId</code>.</p>
    pub fn get_encryption_type(&self) -> &::std::option::Option<crate::types::EncryptionType> {
        &self.encryption_type
    }
    /// <p>The customer managed KMS key used when <code>encryptionType</code> is <code>KMS_BASED_ENCRYPTION</code>. Accepts a key ID, key ARN, or key alias. Required for <code>KMS_BASED_ENCRYPTION</code>; must be omitted for <code>SITEWISE_DEFAULT_ENCRYPTION</code>. After a workspace's customer managed key configuration becomes active, the key can't be changed.</p>
    pub fn kms_key_id(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.kms_key_id = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The customer managed KMS key used when <code>encryptionType</code> is <code>KMS_BASED_ENCRYPTION</code>. Accepts a key ID, key ARN, or key alias. Required for <code>KMS_BASED_ENCRYPTION</code>; must be omitted for <code>SITEWISE_DEFAULT_ENCRYPTION</code>. After a workspace's customer managed key configuration becomes active, the key can't be changed.</p>
    pub fn set_kms_key_id(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.kms_key_id = input;
        self
    }
    /// <p>The customer managed KMS key used when <code>encryptionType</code> is <code>KMS_BASED_ENCRYPTION</code>. Accepts a key ID, key ARN, or key alias. Required for <code>KMS_BASED_ENCRYPTION</code>; must be omitted for <code>SITEWISE_DEFAULT_ENCRYPTION</code>. After a workspace's customer managed key configuration becomes active, the key can't be changed.</p>
    pub fn get_kms_key_id(&self) -> &::std::option::Option<::std::string::String> {
        &self.kms_key_id
    }
    /// Consumes the builder and constructs a [`WorkspaceEncryptionConfiguration`](crate::types::WorkspaceEncryptionConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`encryption_type`](crate::types::builders::WorkspaceEncryptionConfigurationBuilder::encryption_type)
    pub fn build(self) -> ::std::result::Result<crate::types::WorkspaceEncryptionConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::WorkspaceEncryptionConfiguration {
            encryption_type: self.encryption_type.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "encryption_type",
                    "encryption_type was not specified but it is required when building WorkspaceEncryptionConfiguration",
                )
            })?,
            kms_key_id: self.kms_key_id,
        })
    }
}
