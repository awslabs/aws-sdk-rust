// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[derive(Debug)]
pub(crate) struct Handle {
    pub(crate) conf: crate::Config,
    #[allow(dead_code)] // unused when a service does not provide any operations
    pub(crate) runtime_plugins: ::aws_smithy_runtime_api::client::runtime_plugin::RuntimePlugins,
}

/// Client for AWS IoT SiteWise
///
/// Client for invoking operations on AWS IoT SiteWise. Each operation on AWS IoT SiteWise is a method on this
/// this struct. `.send()` MUST be invoked on the generated operations to dispatch the request to the service.
/// ## Constructing a `Client`
///
/// A [`Config`] is required to construct a client. For most use cases, the [`aws-config`]
/// crate should be used to automatically resolve this config using
/// [`aws_config::load_from_env()`], since this will resolve an [`SdkConfig`] which can be shared
/// across multiple different AWS SDK clients. This config resolution process can be customized
/// by calling [`aws_config::from_env()`] instead, which returns a [`ConfigLoader`] that uses
/// the [builder pattern] to customize the default config.
///
/// In the simplest case, creating a client looks as follows:
/// ```rust,no_run
/// # async fn wrapper() {
/// let config = aws_config::load_from_env().await;
/// let client = aws_sdk_iotsitewise::Client::new(&config);
/// # }
/// ```
///
/// Occasionally, SDKs may have additional service-specific values that can be set on the [`Config`] that
/// is absent from [`SdkConfig`], or slightly different settings for a specific client may be desired.
/// The [`Builder`](crate::config::Builder) struct implements `From<&SdkConfig>`, so setting these specific settings can be
/// done as follows:
///
/// ```rust,no_run
/// # async fn wrapper() {
/// let sdk_config = ::aws_config::load_from_env().await;
/// let config = aws_sdk_iotsitewise::config::Builder::from(&sdk_config)
/// # /*
///     .some_service_specific_setting("value")
/// # */
///     .build();
/// # }
/// ```
///
/// See the [`aws-config` docs] and [`Config`] for more information on customizing configuration.
///
/// _Note:_ Client construction is expensive due to connection thread pool initialization, and should
/// be done once at application start-up.
///
/// [`Config`]: crate::Config
/// [`ConfigLoader`]: https://docs.rs/aws-config/*/aws_config/struct.ConfigLoader.html
/// [`SdkConfig`]: https://docs.rs/aws-config/*/aws_config/struct.SdkConfig.html
/// [`aws-config` docs]: https://docs.rs/aws-config/*
/// [`aws-config`]: https://crates.io/crates/aws-config
/// [`aws_config::from_env()`]: https://docs.rs/aws-config/*/aws_config/fn.from_env.html
/// [`aws_config::load_from_env()`]: https://docs.rs/aws-config/*/aws_config/fn.load_from_env.html
/// [builder pattern]: https://rust-lang.github.io/api-guidelines/type-safety.html#builders-enable-construction-of-complex-values-c-builder
/// # Using the `Client`
///
/// A client has a function for every operation that can be performed by the service.
/// For example, the [`AssociateAssets`](crate::operation::associate_assets) operation has
/// a [`Client::associate_assets`], function which returns a builder for that operation.
/// The fluent builder ultimately has a `send()` function that returns an async future that
/// returns a result, as illustrated below:
///
/// ```rust,ignore
/// let result = client.associate_assets()
///     .asset_id("example")
///     .send()
///     .await;
/// ```
///
/// The underlying HTTP requests that get made by this can be modified with the `customize_operation`
/// function on the fluent builder. See the [`customize`](crate::client::customize) module for more
/// information.
/// # Waiters
///
/// This client provides `wait_until` methods behind the [`Waiters`](crate::client::Waiters) trait.
/// To use them, simply import the trait, and then call one of the `wait_until` methods. This will
/// return a waiter fluent builder that takes various parameters, which are documented on the builder
/// type. Once parameters have been provided, the `wait` method can be called to initiate waiting.
///
/// For example, if there was a `wait_until_thing` method, it could look like:
/// ```rust,ignore
/// let result = client.wait_until_thing()
///     .thing_id("someId")
///     .wait(Duration::from_secs(120))
///     .await;
/// ```
#[derive(::std::clone::Clone, ::std::fmt::Debug)]
pub struct Client {
    handle: ::std::sync::Arc<Handle>,
}

impl Client {
    /// Creates a new client from the service [`Config`](crate::Config).
    ///
    /// # Panics
    ///
    /// This method will panic in the following cases:
    ///
    /// - Retries or timeouts are enabled without a `sleep_impl` configured.
    /// - Identity caching is enabled without a `sleep_impl` and `time_source` configured.
    /// - No `behavior_version` is provided.
    ///
    /// The panic message for each of these will have instructions on how to resolve them.
    #[track_caller]
    pub fn from_conf(conf: crate::Config) -> Self {
        let handle = Handle {
            conf: conf.clone(),
            runtime_plugins: crate::config::base_client_runtime_plugins(conf),
        };
        if let Err(err) = Self::validate_config(&handle) {
            panic!("Invalid client configuration: {err}");
        }
        Self {
            handle: ::std::sync::Arc::new(handle),
        }
    }

    /// Returns the client's configuration.
    pub fn config(&self) -> &crate::Config {
        &self.handle.conf
    }

    fn validate_config(handle: &Handle) -> ::std::result::Result<(), ::aws_smithy_runtime_api::box_error::BoxError> {
        let mut cfg = ::aws_smithy_types::config_bag::ConfigBag::base();
        handle
            .runtime_plugins
            .apply_client_configuration(&mut cfg)?
            .validate_base_client_config(&cfg)?;
        Ok(())
    }
}

///
/// Waiter functions for the client.
///
/// Import this trait to get `wait_until` methods on the client.
///
pub trait Waiters {
    /// Wait for `asset_active`
    fn wait_until_asset_active(&self) -> crate::waiters::asset_active::AssetActiveFluentBuilder;
    /// Wait for `asset_not_exists`
    fn wait_until_asset_not_exists(&self) -> crate::waiters::asset_not_exists::AssetNotExistsFluentBuilder;
    /// Wait for `asset_model_active`
    fn wait_until_asset_model_active(&self) -> crate::waiters::asset_model_active::AssetModelActiveFluentBuilder;
    /// Wait for `asset_model_not_exists`
    fn wait_until_asset_model_not_exists(&self) -> crate::waiters::asset_model_not_exists::AssetModelNotExistsFluentBuilder;
    /// Wait for `portal_active`
    fn wait_until_portal_active(&self) -> crate::waiters::portal_active::PortalActiveFluentBuilder;
    /// Wait for `portal_not_exists`
    fn wait_until_portal_not_exists(&self) -> crate::waiters::portal_not_exists::PortalNotExistsFluentBuilder;
}
impl Waiters for Client {
    fn wait_until_asset_active(&self) -> crate::waiters::asset_active::AssetActiveFluentBuilder {
        crate::waiters::asset_active::AssetActiveFluentBuilder::new(self.handle.clone())
    }
    fn wait_until_asset_not_exists(&self) -> crate::waiters::asset_not_exists::AssetNotExistsFluentBuilder {
        crate::waiters::asset_not_exists::AssetNotExistsFluentBuilder::new(self.handle.clone())
    }
    fn wait_until_asset_model_active(&self) -> crate::waiters::asset_model_active::AssetModelActiveFluentBuilder {
        crate::waiters::asset_model_active::AssetModelActiveFluentBuilder::new(self.handle.clone())
    }
    fn wait_until_asset_model_not_exists(&self) -> crate::waiters::asset_model_not_exists::AssetModelNotExistsFluentBuilder {
        crate::waiters::asset_model_not_exists::AssetModelNotExistsFluentBuilder::new(self.handle.clone())
    }
    fn wait_until_portal_active(&self) -> crate::waiters::portal_active::PortalActiveFluentBuilder {
        crate::waiters::portal_active::PortalActiveFluentBuilder::new(self.handle.clone())
    }
    fn wait_until_portal_not_exists(&self) -> crate::waiters::portal_not_exists::PortalNotExistsFluentBuilder {
        crate::waiters::portal_not_exists::PortalNotExistsFluentBuilder::new(self.handle.clone())
    }
}

impl Client {
    /// Creates a new client from an [SDK Config](::aws_types::sdk_config::SdkConfig).
    ///
    /// # Panics
    ///
    /// - This method will panic if the `sdk_config` is missing an async sleep implementation. If you experience this panic, set
    ///   the `sleep_impl` on the Config passed into this function to fix it.
    /// - This method will panic if the `sdk_config` is missing an HTTP connector. If you experience this panic, set the
    ///   `http_connector` on the Config passed into this function to fix it.
    /// - This method will panic if no `BehaviorVersion` is provided. If you experience this panic, set `behavior_version` on the Config or enable the `behavior-version-latest` Cargo feature.
    #[track_caller]
    pub fn new(sdk_config: &::aws_types::sdk_config::SdkConfig) -> Self {
        Self::from_conf(sdk_config.into())
    }
}

mod associate_assets;

mod associate_time_series_to_asset_property;

mod batch_associate_project_assets;

mod batch_disassociate_project_assets;

mod batch_get_asset_property_aggregates;

mod batch_get_asset_property_value;

mod batch_get_asset_property_value_history;

mod batch_put_asset_property_value;

mod create_access_policy;

mod create_asset;

mod create_asset_model;

mod create_asset_model_composite_model;

mod create_bulk_import_job;

mod create_dashboard;

mod create_dataset;

mod create_gateway;

mod create_portal;

mod create_project;

/// Operation customization and supporting types.
///
/// The underlying HTTP requests made during an operation can be customized
/// by calling the `customize()` method on the builder returned from a client
/// operation call. For example, this can be used to add an additional HTTP header:
///
/// ```ignore
/// # async fn wrapper() -> ::std::result::Result<(), aws_sdk_iotsitewise::Error> {
/// # let client: aws_sdk_iotsitewise::Client = unimplemented!();
/// use ::http::header::{HeaderName, HeaderValue};
///
/// let result = client.associate_assets()
///     .customize()
///     .mutate_request(|req| {
///         // Add `x-example-header` with value
///         req.headers_mut()
///             .insert(
///                 HeaderName::from_static("x-example-header"),
///                 HeaderValue::from_static("1"),
///             );
///     })
///     .send()
///     .await;
/// # }
/// ```
pub mod customize;

mod delete_access_policy;

mod delete_asset;

mod delete_asset_model;

mod delete_asset_model_composite_model;

mod delete_dashboard;

mod delete_dataset;

mod delete_gateway;

mod delete_portal;

mod delete_project;

mod delete_time_series;

mod describe_access_policy;

mod describe_action;

mod describe_asset;

mod describe_asset_composite_model;

mod describe_asset_model;

mod describe_asset_model_composite_model;

mod describe_asset_property;

mod describe_bulk_import_job;

mod describe_dashboard;

mod describe_dataset;

mod describe_default_encryption_configuration;

mod describe_gateway;

mod describe_gateway_capability_configuration;

mod describe_logging_options;

mod describe_portal;

mod describe_project;

mod describe_storage_configuration;

mod describe_time_series;

mod disassociate_assets;

mod disassociate_time_series_from_asset_property;

mod execute_action;

mod execute_query;

mod get_asset_property_aggregates;

mod get_asset_property_value;

mod get_asset_property_value_history;

mod get_interpolated_asset_property_values;

mod invoke_assistant;

mod list_access_policies;

mod list_actions;

mod list_asset_model_composite_models;

mod list_asset_model_properties;

mod list_asset_models;

mod list_asset_properties;

mod list_asset_relationships;

mod list_assets;

mod list_associated_assets;

mod list_bulk_import_jobs;

mod list_composition_relationships;

mod list_dashboards;

mod list_datasets;

mod list_gateways;

mod list_portals;

mod list_project_assets;

mod list_projects;

mod list_tags_for_resource;

mod list_time_series;

mod put_default_encryption_configuration;

mod put_logging_options;

mod put_storage_configuration;

mod tag_resource;

mod untag_resource;

mod update_access_policy;

mod update_asset;

mod update_asset_model;

mod update_asset_model_composite_model;

mod update_asset_property;

mod update_dashboard;

mod update_dataset;

mod update_gateway;

mod update_gateway_capability_configuration;

mod update_portal;

mod update_project;
