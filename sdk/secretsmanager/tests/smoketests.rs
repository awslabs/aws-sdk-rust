// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#![cfg(smoketests)]
//!
//! The tests in this module run against live AWS services. As such,
//! they are disabled by default. To enable them, run the tests with
//!
//! ```sh
//! RUSTFLAGS="--cfg smoketests" cargo test.
//! ```
use aws_sdk_secretsmanager::{config, Client};
/// Smoke tests for the `describe_secret` operation
#[::tokio::test]
async fn test_describe_secret_failure() {
    let config = ::aws_config::load_defaults(config::BehaviorVersion::latest()).await;
    let conf = config::Config::from(&config)
        .to_builder()
        .region(config::Region::new("us-west-2"))
        .use_dual_stack(false)
        .use_fips(false)
        .build();
    let client = Client::from_conf(conf);
    let res = client
        .describe_secret()
        .set_secret_id(::std::option::Option::Some("fake-secret-id".to_owned()))
        .send()
        .await;
    res.expect_err("request should fail");
}
/// Smoke tests for the `list_secrets` operation
#[::tokio::test]
async fn test_list_secrets_success() {
    let config = ::aws_config::load_defaults(config::BehaviorVersion::latest()).await;
    let conf = config::Config::from(&config)
        .to_builder()
        .region(config::Region::new("us-west-2"))
        .use_dual_stack(false)
        .use_fips(false)
        .build();
    let client = Client::from_conf(conf);
    let res = client.list_secrets().send().await;
    res.expect("request should succeed");
}
