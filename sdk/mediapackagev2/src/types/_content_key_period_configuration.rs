// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration that controls the content key period timing information that MediaPackage signals to your DRM key provider.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ContentKeyPeriodConfiguration {
    /// <p>Specifies what timing information MediaPackage signals in the <code>ContentKeyPeriod</code> to your DRM key provider. If you don't specify a value, the default is <code>INDEX_ONLY</code>. Signaling start and end times (<code>START_END_ONLY</code> or <code>INDEX_WITH_START_END</code>) also requires key rotation to be enabled.</p>
    /// <p>The allowed values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>INDEX_ONLY</code> - Signals only the content key index. This is the default and matches the current behavior. It's supported for both SPEKE Version 2.0 and 2.1.</p></li>
    /// <li>
    /// <p><code>START_END_ONLY</code> - Signals only the start and end times the key is used for. Requires <code>SpekeVersion</code> <code>V2_1</code>.</p></li>
    /// <li>
    /// <p><code>INDEX_WITH_START_END</code> - Signals both the content key index and the start and end times the key is used for. Requires <code>SpekeVersion</code> <code>V2_1</code>.</p></li>
    /// </ul>
    pub content_key_period_timing: ::std::option::Option<crate::types::ContentKeyPeriodTiming>,
}
impl ContentKeyPeriodConfiguration {
    /// <p>Specifies what timing information MediaPackage signals in the <code>ContentKeyPeriod</code> to your DRM key provider. If you don't specify a value, the default is <code>INDEX_ONLY</code>. Signaling start and end times (<code>START_END_ONLY</code> or <code>INDEX_WITH_START_END</code>) also requires key rotation to be enabled.</p>
    /// <p>The allowed values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>INDEX_ONLY</code> - Signals only the content key index. This is the default and matches the current behavior. It's supported for both SPEKE Version 2.0 and 2.1.</p></li>
    /// <li>
    /// <p><code>START_END_ONLY</code> - Signals only the start and end times the key is used for. Requires <code>SpekeVersion</code> <code>V2_1</code>.</p></li>
    /// <li>
    /// <p><code>INDEX_WITH_START_END</code> - Signals both the content key index and the start and end times the key is used for. Requires <code>SpekeVersion</code> <code>V2_1</code>.</p></li>
    /// </ul>
    pub fn content_key_period_timing(&self) -> ::std::option::Option<&crate::types::ContentKeyPeriodTiming> {
        self.content_key_period_timing.as_ref()
    }
}
impl ContentKeyPeriodConfiguration {
    /// Creates a new builder-style object to manufacture [`ContentKeyPeriodConfiguration`](crate::types::ContentKeyPeriodConfiguration).
    pub fn builder() -> crate::types::builders::ContentKeyPeriodConfigurationBuilder {
        crate::types::builders::ContentKeyPeriodConfigurationBuilder::default()
    }
}

/// A builder for [`ContentKeyPeriodConfiguration`](crate::types::ContentKeyPeriodConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ContentKeyPeriodConfigurationBuilder {
    pub(crate) content_key_period_timing: ::std::option::Option<crate::types::ContentKeyPeriodTiming>,
}
impl ContentKeyPeriodConfigurationBuilder {
    /// <p>Specifies what timing information MediaPackage signals in the <code>ContentKeyPeriod</code> to your DRM key provider. If you don't specify a value, the default is <code>INDEX_ONLY</code>. Signaling start and end times (<code>START_END_ONLY</code> or <code>INDEX_WITH_START_END</code>) also requires key rotation to be enabled.</p>
    /// <p>The allowed values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>INDEX_ONLY</code> - Signals only the content key index. This is the default and matches the current behavior. It's supported for both SPEKE Version 2.0 and 2.1.</p></li>
    /// <li>
    /// <p><code>START_END_ONLY</code> - Signals only the start and end times the key is used for. Requires <code>SpekeVersion</code> <code>V2_1</code>.</p></li>
    /// <li>
    /// <p><code>INDEX_WITH_START_END</code> - Signals both the content key index and the start and end times the key is used for. Requires <code>SpekeVersion</code> <code>V2_1</code>.</p></li>
    /// </ul>
    pub fn content_key_period_timing(mut self, input: crate::types::ContentKeyPeriodTiming) -> Self {
        self.content_key_period_timing = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies what timing information MediaPackage signals in the <code>ContentKeyPeriod</code> to your DRM key provider. If you don't specify a value, the default is <code>INDEX_ONLY</code>. Signaling start and end times (<code>START_END_ONLY</code> or <code>INDEX_WITH_START_END</code>) also requires key rotation to be enabled.</p>
    /// <p>The allowed values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>INDEX_ONLY</code> - Signals only the content key index. This is the default and matches the current behavior. It's supported for both SPEKE Version 2.0 and 2.1.</p></li>
    /// <li>
    /// <p><code>START_END_ONLY</code> - Signals only the start and end times the key is used for. Requires <code>SpekeVersion</code> <code>V2_1</code>.</p></li>
    /// <li>
    /// <p><code>INDEX_WITH_START_END</code> - Signals both the content key index and the start and end times the key is used for. Requires <code>SpekeVersion</code> <code>V2_1</code>.</p></li>
    /// </ul>
    pub fn set_content_key_period_timing(mut self, input: ::std::option::Option<crate::types::ContentKeyPeriodTiming>) -> Self {
        self.content_key_period_timing = input;
        self
    }
    /// <p>Specifies what timing information MediaPackage signals in the <code>ContentKeyPeriod</code> to your DRM key provider. If you don't specify a value, the default is <code>INDEX_ONLY</code>. Signaling start and end times (<code>START_END_ONLY</code> or <code>INDEX_WITH_START_END</code>) also requires key rotation to be enabled.</p>
    /// <p>The allowed values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>INDEX_ONLY</code> - Signals only the content key index. This is the default and matches the current behavior. It's supported for both SPEKE Version 2.0 and 2.1.</p></li>
    /// <li>
    /// <p><code>START_END_ONLY</code> - Signals only the start and end times the key is used for. Requires <code>SpekeVersion</code> <code>V2_1</code>.</p></li>
    /// <li>
    /// <p><code>INDEX_WITH_START_END</code> - Signals both the content key index and the start and end times the key is used for. Requires <code>SpekeVersion</code> <code>V2_1</code>.</p></li>
    /// </ul>
    pub fn get_content_key_period_timing(&self) -> &::std::option::Option<crate::types::ContentKeyPeriodTiming> {
        &self.content_key_period_timing
    }
    /// Consumes the builder and constructs a [`ContentKeyPeriodConfiguration`](crate::types::ContentKeyPeriodConfiguration).
    pub fn build(self) -> crate::types::ContentKeyPeriodConfiguration {
        crate::types::ContentKeyPeriodConfiguration {
            content_key_period_timing: self.content_key_period_timing,
        }
    }
}
