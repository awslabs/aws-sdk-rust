// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#![cfg(smoketests)]
//!
//! The tests in this module run against live AWS services. As such,
//! they are disabled by default. To enable them, run the tests with
//!
//! ```sh
//! RUSTFLAGS="--cfg smoketests" cargo test
//! ```
//!
use aws_sdk_neptune::{config, Client};
/// Smoke tests for the `describe_db_engine_versions` operation
#[::tokio::test]
async fn test_describe_db_engine_versions_success() {
    let config = ::aws_config::load_defaults(config::BehaviorVersion::latest()).await;
    let conf = config::Config::from(&config)
        .to_builder()
        .region(::aws_types::region::Region::new("us-west-2"))
        .use_dual_stack(false)
        .use_fips(false)
        .build();
    let client = Client::from_conf(conf);
    let res = client.describe_db_engine_versions().send().await;
    res.expect("request should succeed");
}
/// Smoke tests for the `describe_db_instances` operation
#[::tokio::test]
async fn test_describe_db_instances_failure() {
    let config = ::aws_config::load_defaults(config::BehaviorVersion::latest()).await;
    let conf = config::Config::from(&config)
        .to_builder()
        .region(::aws_types::region::Region::new("us-west-2"))
        .use_dual_stack(false)
        .use_fips(false)
        .build();
    let client = Client::from_conf(conf);
    let res = client
        .describe_db_instances()
        .set_db_instance_identifier(::std::option::Option::Some("fake-id".to_owned()))
        .send()
        .await;
    res.expect_err("request should fail");
}
