// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The File Server Resource Manager (FSRM) configuration that Amazon FSx for Windows File Server uses for the file system. When FSRM is enabled, you can manage and monitor storage quotas, file screening, storage reports, and file classification.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct WindowsFsrmConfiguration {
    /// <p>Specifies whether FSRM is enabled or disabled on the file system. When <code>TRUE</code>, the FSRM service is enabled and monitor file operations according to configured policies. When <code>FALSE</code> or omitted, FSRM is disabled. The default value is <code>FALSE</code>.</p>
    pub fsrm_service_enabled: ::std::option::Option<bool>,
    /// <p>The Amazon Resource Name (ARN) for the destination of the FSRM event logs. The destination can be any Amazon CloudWatch Logs log group ARN or Amazon Kinesis Data Firehose delivery stream ARN.</p>
    /// <p>The name of the Amazon CloudWatch Logs log group must begin with the <code>/aws/fsx</code> prefix. The name of the Amazon Kinesis Data Firehose delivery stream must begin with the <code>aws-fsx</code> prefix.</p>
    /// <p>The destination ARN (either CloudWatch Logs log group or Kinesis Data Firehose delivery stream) must be in the same Amazon Web Services partition, Amazon Web Services Region, and Amazon Web Services account as your Amazon FSx file system.</p>
    pub event_log_destination: ::std::option::Option<::std::string::String>,
}
impl WindowsFsrmConfiguration {
    /// <p>Specifies whether FSRM is enabled or disabled on the file system. When <code>TRUE</code>, the FSRM service is enabled and monitor file operations according to configured policies. When <code>FALSE</code> or omitted, FSRM is disabled. The default value is <code>FALSE</code>.</p>
    pub fn fsrm_service_enabled(&self) -> ::std::option::Option<bool> {
        self.fsrm_service_enabled
    }
    /// <p>The Amazon Resource Name (ARN) for the destination of the FSRM event logs. The destination can be any Amazon CloudWatch Logs log group ARN or Amazon Kinesis Data Firehose delivery stream ARN.</p>
    /// <p>The name of the Amazon CloudWatch Logs log group must begin with the <code>/aws/fsx</code> prefix. The name of the Amazon Kinesis Data Firehose delivery stream must begin with the <code>aws-fsx</code> prefix.</p>
    /// <p>The destination ARN (either CloudWatch Logs log group or Kinesis Data Firehose delivery stream) must be in the same Amazon Web Services partition, Amazon Web Services Region, and Amazon Web Services account as your Amazon FSx file system.</p>
    pub fn event_log_destination(&self) -> ::std::option::Option<&str> {
        self.event_log_destination.as_deref()
    }
}
impl WindowsFsrmConfiguration {
    /// Creates a new builder-style object to manufacture [`WindowsFsrmConfiguration`](crate::types::WindowsFsrmConfiguration).
    pub fn builder() -> crate::types::builders::WindowsFsrmConfigurationBuilder {
        crate::types::builders::WindowsFsrmConfigurationBuilder::default()
    }
}

/// A builder for [`WindowsFsrmConfiguration`](crate::types::WindowsFsrmConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct WindowsFsrmConfigurationBuilder {
    pub(crate) fsrm_service_enabled: ::std::option::Option<bool>,
    pub(crate) event_log_destination: ::std::option::Option<::std::string::String>,
}
impl WindowsFsrmConfigurationBuilder {
    /// <p>Specifies whether FSRM is enabled or disabled on the file system. When <code>TRUE</code>, the FSRM service is enabled and monitor file operations according to configured policies. When <code>FALSE</code> or omitted, FSRM is disabled. The default value is <code>FALSE</code>.</p>
    /// This field is required.
    pub fn fsrm_service_enabled(mut self, input: bool) -> Self {
        self.fsrm_service_enabled = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether FSRM is enabled or disabled on the file system. When <code>TRUE</code>, the FSRM service is enabled and monitor file operations according to configured policies. When <code>FALSE</code> or omitted, FSRM is disabled. The default value is <code>FALSE</code>.</p>
    pub fn set_fsrm_service_enabled(mut self, input: ::std::option::Option<bool>) -> Self {
        self.fsrm_service_enabled = input;
        self
    }
    /// <p>Specifies whether FSRM is enabled or disabled on the file system. When <code>TRUE</code>, the FSRM service is enabled and monitor file operations according to configured policies. When <code>FALSE</code> or omitted, FSRM is disabled. The default value is <code>FALSE</code>.</p>
    pub fn get_fsrm_service_enabled(&self) -> &::std::option::Option<bool> {
        &self.fsrm_service_enabled
    }
    /// <p>The Amazon Resource Name (ARN) for the destination of the FSRM event logs. The destination can be any Amazon CloudWatch Logs log group ARN or Amazon Kinesis Data Firehose delivery stream ARN.</p>
    /// <p>The name of the Amazon CloudWatch Logs log group must begin with the <code>/aws/fsx</code> prefix. The name of the Amazon Kinesis Data Firehose delivery stream must begin with the <code>aws-fsx</code> prefix.</p>
    /// <p>The destination ARN (either CloudWatch Logs log group or Kinesis Data Firehose delivery stream) must be in the same Amazon Web Services partition, Amazon Web Services Region, and Amazon Web Services account as your Amazon FSx file system.</p>
    pub fn event_log_destination(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.event_log_destination = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The Amazon Resource Name (ARN) for the destination of the FSRM event logs. The destination can be any Amazon CloudWatch Logs log group ARN or Amazon Kinesis Data Firehose delivery stream ARN.</p>
    /// <p>The name of the Amazon CloudWatch Logs log group must begin with the <code>/aws/fsx</code> prefix. The name of the Amazon Kinesis Data Firehose delivery stream must begin with the <code>aws-fsx</code> prefix.</p>
    /// <p>The destination ARN (either CloudWatch Logs log group or Kinesis Data Firehose delivery stream) must be in the same Amazon Web Services partition, Amazon Web Services Region, and Amazon Web Services account as your Amazon FSx file system.</p>
    pub fn set_event_log_destination(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.event_log_destination = input;
        self
    }
    /// <p>The Amazon Resource Name (ARN) for the destination of the FSRM event logs. The destination can be any Amazon CloudWatch Logs log group ARN or Amazon Kinesis Data Firehose delivery stream ARN.</p>
    /// <p>The name of the Amazon CloudWatch Logs log group must begin with the <code>/aws/fsx</code> prefix. The name of the Amazon Kinesis Data Firehose delivery stream must begin with the <code>aws-fsx</code> prefix.</p>
    /// <p>The destination ARN (either CloudWatch Logs log group or Kinesis Data Firehose delivery stream) must be in the same Amazon Web Services partition, Amazon Web Services Region, and Amazon Web Services account as your Amazon FSx file system.</p>
    pub fn get_event_log_destination(&self) -> &::std::option::Option<::std::string::String> {
        &self.event_log_destination
    }
    /// Consumes the builder and constructs a [`WindowsFsrmConfiguration`](crate::types::WindowsFsrmConfiguration).
    pub fn build(self) -> crate::types::WindowsFsrmConfiguration {
        crate::types::WindowsFsrmConfiguration {
            fsrm_service_enabled: self.fsrm_service_enabled,
            event_log_destination: self.event_log_destination,
        }
    }
}
