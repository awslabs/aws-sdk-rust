// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration for the optional provisioned SSD read cache on Amazon FSx for Lustre file systems that use the Intelligent-Tiering storage class.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct LustreReadCacheConfiguration {
    /// <p>Specifies how the provisioned SSD read cache is sized, as follows:</p>
    /// <ul>
    /// <li>
    /// <p>Set to <code>NO_CACHE</code> if you do not want to use an SSD read cache with your Intelligent-Tiering file system.</p></li>
    /// <li>
    /// <p>Set to <code>USER_PROVISIONED</code> to specify the exact size of your SSD read cache.</p></li>
    /// <li>
    /// <p>Set to <code>PROPORTIONAL_TO_THROUGHPUT_CAPACITY</code> to have your SSD read cache automatically sized based on your throughput capacity.</p></li>
    /// </ul>
    pub sizing_mode: ::std::option::Option<crate::types::LustreReadCacheSizingMode>,
    /// <p>Required if <code>SizingMode</code> is set to <code>USER_PROVISIONED</code>. Specifies the size of the file system's SSD read cache, in gibibytes (GiB).</p>
    /// <p>The SSD read cache size is distributed across provisioned file servers in your file system. Intelligent-Tiering file systems support a minimum of 32 GiB and maximum of 131072 GiB for SSD read cache size for every 4,000 MB/s of throughput capacity provisioned.</p>
    pub size_gib: ::std::option::Option<i32>,
}
impl LustreReadCacheConfiguration {
    /// <p>Specifies how the provisioned SSD read cache is sized, as follows:</p>
    /// <ul>
    /// <li>
    /// <p>Set to <code>NO_CACHE</code> if you do not want to use an SSD read cache with your Intelligent-Tiering file system.</p></li>
    /// <li>
    /// <p>Set to <code>USER_PROVISIONED</code> to specify the exact size of your SSD read cache.</p></li>
    /// <li>
    /// <p>Set to <code>PROPORTIONAL_TO_THROUGHPUT_CAPACITY</code> to have your SSD read cache automatically sized based on your throughput capacity.</p></li>
    /// </ul>
    pub fn sizing_mode(&self) -> ::std::option::Option<&crate::types::LustreReadCacheSizingMode> {
        self.sizing_mode.as_ref()
    }
    /// <p>Required if <code>SizingMode</code> is set to <code>USER_PROVISIONED</code>. Specifies the size of the file system's SSD read cache, in gibibytes (GiB).</p>
    /// <p>The SSD read cache size is distributed across provisioned file servers in your file system. Intelligent-Tiering file systems support a minimum of 32 GiB and maximum of 131072 GiB for SSD read cache size for every 4,000 MB/s of throughput capacity provisioned.</p>
    pub fn size_gib(&self) -> ::std::option::Option<i32> {
        self.size_gib
    }
}
impl LustreReadCacheConfiguration {
    /// Creates a new builder-style object to manufacture [`LustreReadCacheConfiguration`](crate::types::LustreReadCacheConfiguration).
    pub fn builder() -> crate::types::builders::LustreReadCacheConfigurationBuilder {
        crate::types::builders::LustreReadCacheConfigurationBuilder::default()
    }
}

/// A builder for [`LustreReadCacheConfiguration`](crate::types::LustreReadCacheConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct LustreReadCacheConfigurationBuilder {
    pub(crate) sizing_mode: ::std::option::Option<crate::types::LustreReadCacheSizingMode>,
    pub(crate) size_gib: ::std::option::Option<i32>,
}
impl LustreReadCacheConfigurationBuilder {
    /// <p>Specifies how the provisioned SSD read cache is sized, as follows:</p>
    /// <ul>
    /// <li>
    /// <p>Set to <code>NO_CACHE</code> if you do not want to use an SSD read cache with your Intelligent-Tiering file system.</p></li>
    /// <li>
    /// <p>Set to <code>USER_PROVISIONED</code> to specify the exact size of your SSD read cache.</p></li>
    /// <li>
    /// <p>Set to <code>PROPORTIONAL_TO_THROUGHPUT_CAPACITY</code> to have your SSD read cache automatically sized based on your throughput capacity.</p></li>
    /// </ul>
    pub fn sizing_mode(mut self, input: crate::types::LustreReadCacheSizingMode) -> Self {
        self.sizing_mode = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies how the provisioned SSD read cache is sized, as follows:</p>
    /// <ul>
    /// <li>
    /// <p>Set to <code>NO_CACHE</code> if you do not want to use an SSD read cache with your Intelligent-Tiering file system.</p></li>
    /// <li>
    /// <p>Set to <code>USER_PROVISIONED</code> to specify the exact size of your SSD read cache.</p></li>
    /// <li>
    /// <p>Set to <code>PROPORTIONAL_TO_THROUGHPUT_CAPACITY</code> to have your SSD read cache automatically sized based on your throughput capacity.</p></li>
    /// </ul>
    pub fn set_sizing_mode(mut self, input: ::std::option::Option<crate::types::LustreReadCacheSizingMode>) -> Self {
        self.sizing_mode = input;
        self
    }
    /// <p>Specifies how the provisioned SSD read cache is sized, as follows:</p>
    /// <ul>
    /// <li>
    /// <p>Set to <code>NO_CACHE</code> if you do not want to use an SSD read cache with your Intelligent-Tiering file system.</p></li>
    /// <li>
    /// <p>Set to <code>USER_PROVISIONED</code> to specify the exact size of your SSD read cache.</p></li>
    /// <li>
    /// <p>Set to <code>PROPORTIONAL_TO_THROUGHPUT_CAPACITY</code> to have your SSD read cache automatically sized based on your throughput capacity.</p></li>
    /// </ul>
    pub fn get_sizing_mode(&self) -> &::std::option::Option<crate::types::LustreReadCacheSizingMode> {
        &self.sizing_mode
    }
    /// <p>Required if <code>SizingMode</code> is set to <code>USER_PROVISIONED</code>. Specifies the size of the file system's SSD read cache, in gibibytes (GiB).</p>
    /// <p>The SSD read cache size is distributed across provisioned file servers in your file system. Intelligent-Tiering file systems support a minimum of 32 GiB and maximum of 131072 GiB for SSD read cache size for every 4,000 MB/s of throughput capacity provisioned.</p>
    pub fn size_gib(mut self, input: i32) -> Self {
        self.size_gib = ::std::option::Option::Some(input);
        self
    }
    /// <p>Required if <code>SizingMode</code> is set to <code>USER_PROVISIONED</code>. Specifies the size of the file system's SSD read cache, in gibibytes (GiB).</p>
    /// <p>The SSD read cache size is distributed across provisioned file servers in your file system. Intelligent-Tiering file systems support a minimum of 32 GiB and maximum of 131072 GiB for SSD read cache size for every 4,000 MB/s of throughput capacity provisioned.</p>
    pub fn set_size_gib(mut self, input: ::std::option::Option<i32>) -> Self {
        self.size_gib = input;
        self
    }
    /// <p>Required if <code>SizingMode</code> is set to <code>USER_PROVISIONED</code>. Specifies the size of the file system's SSD read cache, in gibibytes (GiB).</p>
    /// <p>The SSD read cache size is distributed across provisioned file servers in your file system. Intelligent-Tiering file systems support a minimum of 32 GiB and maximum of 131072 GiB for SSD read cache size for every 4,000 MB/s of throughput capacity provisioned.</p>
    pub fn get_size_gib(&self) -> &::std::option::Option<i32> {
        &self.size_gib
    }
    /// Consumes the builder and constructs a [`LustreReadCacheConfiguration`](crate::types::LustreReadCacheConfiguration).
    pub fn build(self) -> crate::types::LustreReadCacheConfiguration {
        crate::types::LustreReadCacheConfiguration {
            sizing_mode: self.sizing_mode,
            size_gib: self.size_gib,
        }
    }
}
