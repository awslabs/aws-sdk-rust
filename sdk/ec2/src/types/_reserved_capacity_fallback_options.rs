// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Describes the fallback behavior for an EC2 Fleet that uses reserved capacity when the reserved capacity is not enough to meet the target capacity. If you don't specify fallback options, EC2 Fleet does not fall back to any other market type after the specified reservation types are exhausted.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ReservedCapacityFallbackOptions {
    /// <p>The instance purchasing options to fall back to when the reserved capacity is not enough to meet the target capacity. The only supported value is <code>on-demand</code>, which launches On-Demand Instances to fulfill the remaining target capacity.</p>
    pub market_types: ::std::option::Option<::std::vec::Vec<crate::types::ReservedCapacityFallbackMarketType>>,
}
impl ReservedCapacityFallbackOptions {
    /// <p>The instance purchasing options to fall back to when the reserved capacity is not enough to meet the target capacity. The only supported value is <code>on-demand</code>, which launches On-Demand Instances to fulfill the remaining target capacity.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.market_types.is_none()`.
    pub fn market_types(&self) -> &[crate::types::ReservedCapacityFallbackMarketType] {
        self.market_types.as_deref().unwrap_or_default()
    }
}
impl ReservedCapacityFallbackOptions {
    /// Creates a new builder-style object to manufacture [`ReservedCapacityFallbackOptions`](crate::types::ReservedCapacityFallbackOptions).
    pub fn builder() -> crate::types::builders::ReservedCapacityFallbackOptionsBuilder {
        crate::types::builders::ReservedCapacityFallbackOptionsBuilder::default()
    }
}

/// A builder for [`ReservedCapacityFallbackOptions`](crate::types::ReservedCapacityFallbackOptions).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ReservedCapacityFallbackOptionsBuilder {
    pub(crate) market_types: ::std::option::Option<::std::vec::Vec<crate::types::ReservedCapacityFallbackMarketType>>,
}
impl ReservedCapacityFallbackOptionsBuilder {
    /// Appends an item to `market_types`.
    ///
    /// To override the contents of this collection use [`set_market_types`](Self::set_market_types).
    ///
    /// <p>The instance purchasing options to fall back to when the reserved capacity is not enough to meet the target capacity. The only supported value is <code>on-demand</code>, which launches On-Demand Instances to fulfill the remaining target capacity.</p>
    pub fn market_types(mut self, input: crate::types::ReservedCapacityFallbackMarketType) -> Self {
        let mut v = self.market_types.unwrap_or_default();
        v.push(input);
        self.market_types = ::std::option::Option::Some(v);
        self
    }
    /// <p>The instance purchasing options to fall back to when the reserved capacity is not enough to meet the target capacity. The only supported value is <code>on-demand</code>, which launches On-Demand Instances to fulfill the remaining target capacity.</p>
    pub fn set_market_types(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::ReservedCapacityFallbackMarketType>>) -> Self {
        self.market_types = input;
        self
    }
    /// <p>The instance purchasing options to fall back to when the reserved capacity is not enough to meet the target capacity. The only supported value is <code>on-demand</code>, which launches On-Demand Instances to fulfill the remaining target capacity.</p>
    pub fn get_market_types(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::ReservedCapacityFallbackMarketType>> {
        &self.market_types
    }
    /// Consumes the builder and constructs a [`ReservedCapacityFallbackOptions`](crate::types::ReservedCapacityFallbackOptions).
    pub fn build(self) -> crate::types::ReservedCapacityFallbackOptions {
        crate::types::ReservedCapacityFallbackOptions {
            market_types: self.market_types,
        }
    }
}
