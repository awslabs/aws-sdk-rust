// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The matching criteria for a transit gateway policy table entry.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct TransitGatewayRequestPolicyRule {
    /// <p>The source CIDR block for the policy rule.</p>
    pub source_cidr_block: ::std::option::Option<::std::string::String>,
    /// <p>The source port or port range for the policy rule. You can specify a port range only when <code>Protocol</code> is <code>6</code> (TCP) or <code>17</code> (UDP); for all other protocols, this value must be <code>*</code>.</p>
    pub source_port_range: ::std::option::Option<::std::string::String>,
    /// <p>The destination CIDR block for the policy rule.</p>
    pub destination_cidr_block: ::std::option::Option<::std::string::String>,
    /// <p>The destination port or port range for the policy rule. You can specify a port range only when <code>Protocol</code> is <code>6</code> (TCP) or <code>17</code> (UDP); for all other protocols, this value must be <code>*</code>.</p>
    pub destination_port_range: ::std::option::Option<::std::string::String>,
    /// <p>The protocol for the policy rule. Valid values are <code>1</code> (ICMP), <code>6</code> (TCP), <code>17</code> (UDP), <code>47</code> (GRE), or <code>*</code> for all protocols.</p>
    pub protocol: ::std::option::Option<::std::string::String>,
    /// <p>The metadata key-value pair for the policy rule.</p>
    pub meta_data: ::std::option::Option<crate::types::TransitGatewayRequestPolicyRuleMetaData>,
}
impl TransitGatewayRequestPolicyRule {
    /// <p>The source CIDR block for the policy rule.</p>
    pub fn source_cidr_block(&self) -> ::std::option::Option<&str> {
        self.source_cidr_block.as_deref()
    }
    /// <p>The source port or port range for the policy rule. You can specify a port range only when <code>Protocol</code> is <code>6</code> (TCP) or <code>17</code> (UDP); for all other protocols, this value must be <code>*</code>.</p>
    pub fn source_port_range(&self) -> ::std::option::Option<&str> {
        self.source_port_range.as_deref()
    }
    /// <p>The destination CIDR block for the policy rule.</p>
    pub fn destination_cidr_block(&self) -> ::std::option::Option<&str> {
        self.destination_cidr_block.as_deref()
    }
    /// <p>The destination port or port range for the policy rule. You can specify a port range only when <code>Protocol</code> is <code>6</code> (TCP) or <code>17</code> (UDP); for all other protocols, this value must be <code>*</code>.</p>
    pub fn destination_port_range(&self) -> ::std::option::Option<&str> {
        self.destination_port_range.as_deref()
    }
    /// <p>The protocol for the policy rule. Valid values are <code>1</code> (ICMP), <code>6</code> (TCP), <code>17</code> (UDP), <code>47</code> (GRE), or <code>*</code> for all protocols.</p>
    pub fn protocol(&self) -> ::std::option::Option<&str> {
        self.protocol.as_deref()
    }
    /// <p>The metadata key-value pair for the policy rule.</p>
    pub fn meta_data(&self) -> ::std::option::Option<&crate::types::TransitGatewayRequestPolicyRuleMetaData> {
        self.meta_data.as_ref()
    }
}
impl TransitGatewayRequestPolicyRule {
    /// Creates a new builder-style object to manufacture [`TransitGatewayRequestPolicyRule`](crate::types::TransitGatewayRequestPolicyRule).
    pub fn builder() -> crate::types::builders::TransitGatewayRequestPolicyRuleBuilder {
        crate::types::builders::TransitGatewayRequestPolicyRuleBuilder::default()
    }
}

/// A builder for [`TransitGatewayRequestPolicyRule`](crate::types::TransitGatewayRequestPolicyRule).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct TransitGatewayRequestPolicyRuleBuilder {
    pub(crate) source_cidr_block: ::std::option::Option<::std::string::String>,
    pub(crate) source_port_range: ::std::option::Option<::std::string::String>,
    pub(crate) destination_cidr_block: ::std::option::Option<::std::string::String>,
    pub(crate) destination_port_range: ::std::option::Option<::std::string::String>,
    pub(crate) protocol: ::std::option::Option<::std::string::String>,
    pub(crate) meta_data: ::std::option::Option<crate::types::TransitGatewayRequestPolicyRuleMetaData>,
}
impl TransitGatewayRequestPolicyRuleBuilder {
    /// <p>The source CIDR block for the policy rule.</p>
    pub fn source_cidr_block(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.source_cidr_block = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The source CIDR block for the policy rule.</p>
    pub fn set_source_cidr_block(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.source_cidr_block = input;
        self
    }
    /// <p>The source CIDR block for the policy rule.</p>
    pub fn get_source_cidr_block(&self) -> &::std::option::Option<::std::string::String> {
        &self.source_cidr_block
    }
    /// <p>The source port or port range for the policy rule. You can specify a port range only when <code>Protocol</code> is <code>6</code> (TCP) or <code>17</code> (UDP); for all other protocols, this value must be <code>*</code>.</p>
    pub fn source_port_range(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.source_port_range = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The source port or port range for the policy rule. You can specify a port range only when <code>Protocol</code> is <code>6</code> (TCP) or <code>17</code> (UDP); for all other protocols, this value must be <code>*</code>.</p>
    pub fn set_source_port_range(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.source_port_range = input;
        self
    }
    /// <p>The source port or port range for the policy rule. You can specify a port range only when <code>Protocol</code> is <code>6</code> (TCP) or <code>17</code> (UDP); for all other protocols, this value must be <code>*</code>.</p>
    pub fn get_source_port_range(&self) -> &::std::option::Option<::std::string::String> {
        &self.source_port_range
    }
    /// <p>The destination CIDR block for the policy rule.</p>
    pub fn destination_cidr_block(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.destination_cidr_block = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The destination CIDR block for the policy rule.</p>
    pub fn set_destination_cidr_block(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.destination_cidr_block = input;
        self
    }
    /// <p>The destination CIDR block for the policy rule.</p>
    pub fn get_destination_cidr_block(&self) -> &::std::option::Option<::std::string::String> {
        &self.destination_cidr_block
    }
    /// <p>The destination port or port range for the policy rule. You can specify a port range only when <code>Protocol</code> is <code>6</code> (TCP) or <code>17</code> (UDP); for all other protocols, this value must be <code>*</code>.</p>
    pub fn destination_port_range(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.destination_port_range = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The destination port or port range for the policy rule. You can specify a port range only when <code>Protocol</code> is <code>6</code> (TCP) or <code>17</code> (UDP); for all other protocols, this value must be <code>*</code>.</p>
    pub fn set_destination_port_range(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.destination_port_range = input;
        self
    }
    /// <p>The destination port or port range for the policy rule. You can specify a port range only when <code>Protocol</code> is <code>6</code> (TCP) or <code>17</code> (UDP); for all other protocols, this value must be <code>*</code>.</p>
    pub fn get_destination_port_range(&self) -> &::std::option::Option<::std::string::String> {
        &self.destination_port_range
    }
    /// <p>The protocol for the policy rule. Valid values are <code>1</code> (ICMP), <code>6</code> (TCP), <code>17</code> (UDP), <code>47</code> (GRE), or <code>*</code> for all protocols.</p>
    pub fn protocol(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.protocol = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The protocol for the policy rule. Valid values are <code>1</code> (ICMP), <code>6</code> (TCP), <code>17</code> (UDP), <code>47</code> (GRE), or <code>*</code> for all protocols.</p>
    pub fn set_protocol(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.protocol = input;
        self
    }
    /// <p>The protocol for the policy rule. Valid values are <code>1</code> (ICMP), <code>6</code> (TCP), <code>17</code> (UDP), <code>47</code> (GRE), or <code>*</code> for all protocols.</p>
    pub fn get_protocol(&self) -> &::std::option::Option<::std::string::String> {
        &self.protocol
    }
    /// <p>The metadata key-value pair for the policy rule.</p>
    pub fn meta_data(mut self, input: crate::types::TransitGatewayRequestPolicyRuleMetaData) -> Self {
        self.meta_data = ::std::option::Option::Some(input);
        self
    }
    /// <p>The metadata key-value pair for the policy rule.</p>
    pub fn set_meta_data(mut self, input: ::std::option::Option<crate::types::TransitGatewayRequestPolicyRuleMetaData>) -> Self {
        self.meta_data = input;
        self
    }
    /// <p>The metadata key-value pair for the policy rule.</p>
    pub fn get_meta_data(&self) -> &::std::option::Option<crate::types::TransitGatewayRequestPolicyRuleMetaData> {
        &self.meta_data
    }
    /// Consumes the builder and constructs a [`TransitGatewayRequestPolicyRule`](crate::types::TransitGatewayRequestPolicyRule).
    pub fn build(self) -> crate::types::TransitGatewayRequestPolicyRule {
        crate::types::TransitGatewayRequestPolicyRule {
            source_cidr_block: self.source_cidr_block,
            source_port_range: self.source_port_range,
            destination_cidr_block: self.destination_cidr_block,
            destination_port_range: self.destination_port_range,
            protocol: self.protocol,
            meta_data: self.meta_data,
        }
    }
}
