// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Describes the reason for an application status check result.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ApplicationStatusReason {
    /// <p>The reason code for the application status check result. Possible values:</p>
    /// <ul>
    /// <li>
    /// <p><code>ResponseCodeMatched</code> – The HTTP status code returned by the health check matched the configured <code>StatusCodeMatcher</code>.</p></li>
    /// <li>
    /// <p><code>ResponseCodeMismatch</code> – The HTTP status code returned by the health check did not match the configured <code>StatusCodeMatcher</code>.</p></li>
    /// <li>
    /// <p><code>ConnectionTimeout</code> – The connection to the target timed out.</p></li>
    /// <li>
    /// <p><code>ResponseTimeout</code> – The health check timed out while waiting for a response from the target.</p></li>
    /// <li>
    /// <p><code>ConnectionRefused</code> – The target refused the health check connection.</p></li>
    /// <li>
    /// <p><code>ConnectionReset</code> – The target reset the health check connection before returning a response.</p></li>
    /// </ul>
    /// <p>Current health check results use the values in the preceding list. Legacy results that do not contain structured reason metadata can instead contain a producer error type, such as <code>Http Status Code</code> or <code>HttpConnectTimeoutException</code>.</p>
    /// <p>For <code>ResponseCodeMatched</code> and <code>ResponseCodeMismatch</code>, the <code>statusCode</code> field contains the returned HTTP status code. The <code>protocol</code> field contains the protocol used for the health check.</p>
    pub code: ::std::option::Option<::std::string::String>,
    /// <p>The HTTP status code returned by the health check.</p>
    pub status_code: ::std::option::Option<i32>,
    /// <p>The protocol used for the health check. Possible values: <code>HTTP</code> and <code>HTTPS</code>.</p>
    pub protocol: ::std::option::Option<::std::string::String>,
}
impl ApplicationStatusReason {
    /// <p>The reason code for the application status check result. Possible values:</p>
    /// <ul>
    /// <li>
    /// <p><code>ResponseCodeMatched</code> – The HTTP status code returned by the health check matched the configured <code>StatusCodeMatcher</code>.</p></li>
    /// <li>
    /// <p><code>ResponseCodeMismatch</code> – The HTTP status code returned by the health check did not match the configured <code>StatusCodeMatcher</code>.</p></li>
    /// <li>
    /// <p><code>ConnectionTimeout</code> – The connection to the target timed out.</p></li>
    /// <li>
    /// <p><code>ResponseTimeout</code> – The health check timed out while waiting for a response from the target.</p></li>
    /// <li>
    /// <p><code>ConnectionRefused</code> – The target refused the health check connection.</p></li>
    /// <li>
    /// <p><code>ConnectionReset</code> – The target reset the health check connection before returning a response.</p></li>
    /// </ul>
    /// <p>Current health check results use the values in the preceding list. Legacy results that do not contain structured reason metadata can instead contain a producer error type, such as <code>Http Status Code</code> or <code>HttpConnectTimeoutException</code>.</p>
    /// <p>For <code>ResponseCodeMatched</code> and <code>ResponseCodeMismatch</code>, the <code>statusCode</code> field contains the returned HTTP status code. The <code>protocol</code> field contains the protocol used for the health check.</p>
    pub fn code(&self) -> ::std::option::Option<&str> {
        self.code.as_deref()
    }
    /// <p>The HTTP status code returned by the health check.</p>
    pub fn status_code(&self) -> ::std::option::Option<i32> {
        self.status_code
    }
    /// <p>The protocol used for the health check. Possible values: <code>HTTP</code> and <code>HTTPS</code>.</p>
    pub fn protocol(&self) -> ::std::option::Option<&str> {
        self.protocol.as_deref()
    }
}
impl ApplicationStatusReason {
    /// Creates a new builder-style object to manufacture [`ApplicationStatusReason`](crate::types::ApplicationStatusReason).
    pub fn builder() -> crate::types::builders::ApplicationStatusReasonBuilder {
        crate::types::builders::ApplicationStatusReasonBuilder::default()
    }
}

/// A builder for [`ApplicationStatusReason`](crate::types::ApplicationStatusReason).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ApplicationStatusReasonBuilder {
    pub(crate) code: ::std::option::Option<::std::string::String>,
    pub(crate) status_code: ::std::option::Option<i32>,
    pub(crate) protocol: ::std::option::Option<::std::string::String>,
}
impl ApplicationStatusReasonBuilder {
    /// <p>The reason code for the application status check result. Possible values:</p>
    /// <ul>
    /// <li>
    /// <p><code>ResponseCodeMatched</code> – The HTTP status code returned by the health check matched the configured <code>StatusCodeMatcher</code>.</p></li>
    /// <li>
    /// <p><code>ResponseCodeMismatch</code> – The HTTP status code returned by the health check did not match the configured <code>StatusCodeMatcher</code>.</p></li>
    /// <li>
    /// <p><code>ConnectionTimeout</code> – The connection to the target timed out.</p></li>
    /// <li>
    /// <p><code>ResponseTimeout</code> – The health check timed out while waiting for a response from the target.</p></li>
    /// <li>
    /// <p><code>ConnectionRefused</code> – The target refused the health check connection.</p></li>
    /// <li>
    /// <p><code>ConnectionReset</code> – The target reset the health check connection before returning a response.</p></li>
    /// </ul>
    /// <p>Current health check results use the values in the preceding list. Legacy results that do not contain structured reason metadata can instead contain a producer error type, such as <code>Http Status Code</code> or <code>HttpConnectTimeoutException</code>.</p>
    /// <p>For <code>ResponseCodeMatched</code> and <code>ResponseCodeMismatch</code>, the <code>statusCode</code> field contains the returned HTTP status code. The <code>protocol</code> field contains the protocol used for the health check.</p>
    pub fn code(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.code = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The reason code for the application status check result. Possible values:</p>
    /// <ul>
    /// <li>
    /// <p><code>ResponseCodeMatched</code> – The HTTP status code returned by the health check matched the configured <code>StatusCodeMatcher</code>.</p></li>
    /// <li>
    /// <p><code>ResponseCodeMismatch</code> – The HTTP status code returned by the health check did not match the configured <code>StatusCodeMatcher</code>.</p></li>
    /// <li>
    /// <p><code>ConnectionTimeout</code> – The connection to the target timed out.</p></li>
    /// <li>
    /// <p><code>ResponseTimeout</code> – The health check timed out while waiting for a response from the target.</p></li>
    /// <li>
    /// <p><code>ConnectionRefused</code> – The target refused the health check connection.</p></li>
    /// <li>
    /// <p><code>ConnectionReset</code> – The target reset the health check connection before returning a response.</p></li>
    /// </ul>
    /// <p>Current health check results use the values in the preceding list. Legacy results that do not contain structured reason metadata can instead contain a producer error type, such as <code>Http Status Code</code> or <code>HttpConnectTimeoutException</code>.</p>
    /// <p>For <code>ResponseCodeMatched</code> and <code>ResponseCodeMismatch</code>, the <code>statusCode</code> field contains the returned HTTP status code. The <code>protocol</code> field contains the protocol used for the health check.</p>
    pub fn set_code(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.code = input;
        self
    }
    /// <p>The reason code for the application status check result. Possible values:</p>
    /// <ul>
    /// <li>
    /// <p><code>ResponseCodeMatched</code> – The HTTP status code returned by the health check matched the configured <code>StatusCodeMatcher</code>.</p></li>
    /// <li>
    /// <p><code>ResponseCodeMismatch</code> – The HTTP status code returned by the health check did not match the configured <code>StatusCodeMatcher</code>.</p></li>
    /// <li>
    /// <p><code>ConnectionTimeout</code> – The connection to the target timed out.</p></li>
    /// <li>
    /// <p><code>ResponseTimeout</code> – The health check timed out while waiting for a response from the target.</p></li>
    /// <li>
    /// <p><code>ConnectionRefused</code> – The target refused the health check connection.</p></li>
    /// <li>
    /// <p><code>ConnectionReset</code> – The target reset the health check connection before returning a response.</p></li>
    /// </ul>
    /// <p>Current health check results use the values in the preceding list. Legacy results that do not contain structured reason metadata can instead contain a producer error type, such as <code>Http Status Code</code> or <code>HttpConnectTimeoutException</code>.</p>
    /// <p>For <code>ResponseCodeMatched</code> and <code>ResponseCodeMismatch</code>, the <code>statusCode</code> field contains the returned HTTP status code. The <code>protocol</code> field contains the protocol used for the health check.</p>
    pub fn get_code(&self) -> &::std::option::Option<::std::string::String> {
        &self.code
    }
    /// <p>The HTTP status code returned by the health check.</p>
    pub fn status_code(mut self, input: i32) -> Self {
        self.status_code = ::std::option::Option::Some(input);
        self
    }
    /// <p>The HTTP status code returned by the health check.</p>
    pub fn set_status_code(mut self, input: ::std::option::Option<i32>) -> Self {
        self.status_code = input;
        self
    }
    /// <p>The HTTP status code returned by the health check.</p>
    pub fn get_status_code(&self) -> &::std::option::Option<i32> {
        &self.status_code
    }
    /// <p>The protocol used for the health check. Possible values: <code>HTTP</code> and <code>HTTPS</code>.</p>
    pub fn protocol(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.protocol = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The protocol used for the health check. Possible values: <code>HTTP</code> and <code>HTTPS</code>.</p>
    pub fn set_protocol(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.protocol = input;
        self
    }
    /// <p>The protocol used for the health check. Possible values: <code>HTTP</code> and <code>HTTPS</code>.</p>
    pub fn get_protocol(&self) -> &::std::option::Option<::std::string::String> {
        &self.protocol
    }
    /// Consumes the builder and constructs a [`ApplicationStatusReason`](crate::types::ApplicationStatusReason).
    pub fn build(self) -> crate::types::ApplicationStatusReason {
        crate::types::ApplicationStatusReason {
            code: self.code,
            status_code: self.status_code,
            protocol: self.protocol,
        }
    }
}
