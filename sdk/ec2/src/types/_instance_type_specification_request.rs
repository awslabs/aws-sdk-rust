// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The instance type specification for an AMI, which contains lists of supported and unsupported instance types that define which instance types are compatible with the AMI.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct InstanceTypeSpecificationRequest {
    /// <p>The instance types that the AMI supports. You can specify instance type names or use wildcard patterns (for example, <code>t3.*</code>).</p>
    /// <p>Constraints: Maximum 100 entries. Each entry must be 1-24 characters and match the pattern <code>^\[A-Za-z0-9_.*-\]+$</code>. Consecutive wildcard characters (<code>**</code>) are not allowed. Entries must be unique within each list and across both lists; duplicate entries cause the request to fail.</p>
    pub supported_instance_types: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    /// <p>The instance types that the AMI does not support. You can specify instance type names or use wildcard patterns (for example, <code>t3.*</code>).</p>
    /// <p>Constraints: Maximum 100 entries. Each entry must be 1-24 characters and match the pattern <code>^\[A-Za-z0-9_.*-\]+$</code>. Consecutive wildcard characters (<code>**</code>) are not allowed. Entries must be unique within each list and across both lists; duplicate entries cause the request to fail.</p>
    pub unsupported_instance_types: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
}
impl InstanceTypeSpecificationRequest {
    /// <p>The instance types that the AMI supports. You can specify instance type names or use wildcard patterns (for example, <code>t3.*</code>).</p>
    /// <p>Constraints: Maximum 100 entries. Each entry must be 1-24 characters and match the pattern <code>^\[A-Za-z0-9_.*-\]+$</code>. Consecutive wildcard characters (<code>**</code>) are not allowed. Entries must be unique within each list and across both lists; duplicate entries cause the request to fail.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.supported_instance_types.is_none()`.
    pub fn supported_instance_types(&self) -> &[::std::string::String] {
        self.supported_instance_types.as_deref().unwrap_or_default()
    }
    /// <p>The instance types that the AMI does not support. You can specify instance type names or use wildcard patterns (for example, <code>t3.*</code>).</p>
    /// <p>Constraints: Maximum 100 entries. Each entry must be 1-24 characters and match the pattern <code>^\[A-Za-z0-9_.*-\]+$</code>. Consecutive wildcard characters (<code>**</code>) are not allowed. Entries must be unique within each list and across both lists; duplicate entries cause the request to fail.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.unsupported_instance_types.is_none()`.
    pub fn unsupported_instance_types(&self) -> &[::std::string::String] {
        self.unsupported_instance_types.as_deref().unwrap_or_default()
    }
}
impl InstanceTypeSpecificationRequest {
    /// Creates a new builder-style object to manufacture [`InstanceTypeSpecificationRequest`](crate::types::InstanceTypeSpecificationRequest).
    pub fn builder() -> crate::types::builders::InstanceTypeSpecificationRequestBuilder {
        crate::types::builders::InstanceTypeSpecificationRequestBuilder::default()
    }
}

/// A builder for [`InstanceTypeSpecificationRequest`](crate::types::InstanceTypeSpecificationRequest).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct InstanceTypeSpecificationRequestBuilder {
    pub(crate) supported_instance_types: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    pub(crate) unsupported_instance_types: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
}
impl InstanceTypeSpecificationRequestBuilder {
    /// Appends an item to `supported_instance_types`.
    ///
    /// To override the contents of this collection use [`set_supported_instance_types`](Self::set_supported_instance_types).
    ///
    /// <p>The instance types that the AMI supports. You can specify instance type names or use wildcard patterns (for example, <code>t3.*</code>).</p>
    /// <p>Constraints: Maximum 100 entries. Each entry must be 1-24 characters and match the pattern <code>^\[A-Za-z0-9_.*-\]+$</code>. Consecutive wildcard characters (<code>**</code>) are not allowed. Entries must be unique within each list and across both lists; duplicate entries cause the request to fail.</p>
    pub fn supported_instance_types(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.supported_instance_types.unwrap_or_default();
        v.push(input.into());
        self.supported_instance_types = ::std::option::Option::Some(v);
        self
    }
    /// <p>The instance types that the AMI supports. You can specify instance type names or use wildcard patterns (for example, <code>t3.*</code>).</p>
    /// <p>Constraints: Maximum 100 entries. Each entry must be 1-24 characters and match the pattern <code>^\[A-Za-z0-9_.*-\]+$</code>. Consecutive wildcard characters (<code>**</code>) are not allowed. Entries must be unique within each list and across both lists; duplicate entries cause the request to fail.</p>
    pub fn set_supported_instance_types(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.supported_instance_types = input;
        self
    }
    /// <p>The instance types that the AMI supports. You can specify instance type names or use wildcard patterns (for example, <code>t3.*</code>).</p>
    /// <p>Constraints: Maximum 100 entries. Each entry must be 1-24 characters and match the pattern <code>^\[A-Za-z0-9_.*-\]+$</code>. Consecutive wildcard characters (<code>**</code>) are not allowed. Entries must be unique within each list and across both lists; duplicate entries cause the request to fail.</p>
    pub fn get_supported_instance_types(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.supported_instance_types
    }
    /// Appends an item to `unsupported_instance_types`.
    ///
    /// To override the contents of this collection use [`set_unsupported_instance_types`](Self::set_unsupported_instance_types).
    ///
    /// <p>The instance types that the AMI does not support. You can specify instance type names or use wildcard patterns (for example, <code>t3.*</code>).</p>
    /// <p>Constraints: Maximum 100 entries. Each entry must be 1-24 characters and match the pattern <code>^\[A-Za-z0-9_.*-\]+$</code>. Consecutive wildcard characters (<code>**</code>) are not allowed. Entries must be unique within each list and across both lists; duplicate entries cause the request to fail.</p>
    pub fn unsupported_instance_types(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.unsupported_instance_types.unwrap_or_default();
        v.push(input.into());
        self.unsupported_instance_types = ::std::option::Option::Some(v);
        self
    }
    /// <p>The instance types that the AMI does not support. You can specify instance type names or use wildcard patterns (for example, <code>t3.*</code>).</p>
    /// <p>Constraints: Maximum 100 entries. Each entry must be 1-24 characters and match the pattern <code>^\[A-Za-z0-9_.*-\]+$</code>. Consecutive wildcard characters (<code>**</code>) are not allowed. Entries must be unique within each list and across both lists; duplicate entries cause the request to fail.</p>
    pub fn set_unsupported_instance_types(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.unsupported_instance_types = input;
        self
    }
    /// <p>The instance types that the AMI does not support. You can specify instance type names or use wildcard patterns (for example, <code>t3.*</code>).</p>
    /// <p>Constraints: Maximum 100 entries. Each entry must be 1-24 characters and match the pattern <code>^\[A-Za-z0-9_.*-\]+$</code>. Consecutive wildcard characters (<code>**</code>) are not allowed. Entries must be unique within each list and across both lists; duplicate entries cause the request to fail.</p>
    pub fn get_unsupported_instance_types(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.unsupported_instance_types
    }
    /// Consumes the builder and constructs a [`InstanceTypeSpecificationRequest`](crate::types::InstanceTypeSpecificationRequest).
    pub fn build(self) -> crate::types::InstanceTypeSpecificationRequest {
        crate::types::InstanceTypeSpecificationRequest {
            supported_instance_types: self.supported_instance_types,
            unsupported_instance_types: self.unsupported_instance_types,
        }
    }
}
