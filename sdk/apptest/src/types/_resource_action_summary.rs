// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Specifies the resource action summary.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum ResourceActionSummary {
    /// <p>The CloudFormation template of the resource action summary.</p>
    CloudFormation(crate::types::CloudFormationStepSummary),
    /// <p>The AWS Mainframe Modernization managed application of the resource action summary.</p>
    M2ManagedApplication(crate::types::M2ManagedApplicationStepSummary),
    /// <p>The AWS Mainframe Modernization non-managed application of the resource action summary.</p>
    M2NonManagedApplication(crate::types::M2NonManagedApplicationStepSummary),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl ResourceActionSummary {
    /// Tries to convert the enum instance into [`CloudFormation`](crate::types::ResourceActionSummary::CloudFormation), extracting the inner [`CloudFormationStepSummary`](crate::types::CloudFormationStepSummary).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_cloud_formation(&self) -> ::std::result::Result<&crate::types::CloudFormationStepSummary, &Self> {
        if let ResourceActionSummary::CloudFormation(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`CloudFormation`](crate::types::ResourceActionSummary::CloudFormation).
    pub fn is_cloud_formation(&self) -> bool {
        self.as_cloud_formation().is_ok()
    }
    /// Tries to convert the enum instance into [`M2ManagedApplication`](crate::types::ResourceActionSummary::M2ManagedApplication), extracting the inner [`M2ManagedApplicationStepSummary`](crate::types::M2ManagedApplicationStepSummary).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_m2_managed_application(&self) -> ::std::result::Result<&crate::types::M2ManagedApplicationStepSummary, &Self> {
        if let ResourceActionSummary::M2ManagedApplication(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`M2ManagedApplication`](crate::types::ResourceActionSummary::M2ManagedApplication).
    pub fn is_m2_managed_application(&self) -> bool {
        self.as_m2_managed_application().is_ok()
    }
    /// Tries to convert the enum instance into [`M2NonManagedApplication`](crate::types::ResourceActionSummary::M2NonManagedApplication), extracting the inner [`M2NonManagedApplicationStepSummary`](crate::types::M2NonManagedApplicationStepSummary).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_m2_non_managed_application(&self) -> ::std::result::Result<&crate::types::M2NonManagedApplicationStepSummary, &Self> {
        if let ResourceActionSummary::M2NonManagedApplication(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`M2NonManagedApplication`](crate::types::ResourceActionSummary::M2NonManagedApplication).
    pub fn is_m2_non_managed_application(&self) -> bool {
        self.as_m2_non_managed_application().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
