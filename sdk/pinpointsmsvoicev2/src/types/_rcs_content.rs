// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The message body of an RCS message. Exactly one content type must be specified.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum RcsContent {
    /// <p>A carousel of 2 to 10 scrollable cards, each with media, title, description, and suggested actions.</p>
    Carousel(crate::types::RcsCarousel),
    /// <p>A file message containing a media file (image, video, audio, or PDF) with an optional thumbnail.</p>
    FileMessage(crate::types::RcsFileMessage),
    /// <p>A standalone rich card with media, title, description, and suggested actions.</p>
    RichCard(crate::types::RcsStandaloneCard),
    /// <p>A plain text RCS message.</p>
    TextMessage(crate::types::RcsTextMessage),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl RcsContent {
    /// Tries to convert the enum instance into [`Carousel`](crate::types::RcsContent::Carousel), extracting the inner [`RcsCarousel`](crate::types::RcsCarousel).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_carousel(&self) -> ::std::result::Result<&crate::types::RcsCarousel, &Self> {
        if let RcsContent::Carousel(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Carousel`](crate::types::RcsContent::Carousel).
    pub fn is_carousel(&self) -> bool {
        self.as_carousel().is_ok()
    }
    /// Tries to convert the enum instance into [`FileMessage`](crate::types::RcsContent::FileMessage), extracting the inner [`RcsFileMessage`](crate::types::RcsFileMessage).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_file_message(&self) -> ::std::result::Result<&crate::types::RcsFileMessage, &Self> {
        if let RcsContent::FileMessage(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`FileMessage`](crate::types::RcsContent::FileMessage).
    pub fn is_file_message(&self) -> bool {
        self.as_file_message().is_ok()
    }
    /// Tries to convert the enum instance into [`RichCard`](crate::types::RcsContent::RichCard), extracting the inner [`RcsStandaloneCard`](crate::types::RcsStandaloneCard).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_rich_card(&self) -> ::std::result::Result<&crate::types::RcsStandaloneCard, &Self> {
        if let RcsContent::RichCard(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`RichCard`](crate::types::RcsContent::RichCard).
    pub fn is_rich_card(&self) -> bool {
        self.as_rich_card().is_ok()
    }
    /// Tries to convert the enum instance into [`TextMessage`](crate::types::RcsContent::TextMessage), extracting the inner [`RcsTextMessage`](crate::types::RcsTextMessage).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_text_message(&self) -> ::std::result::Result<&crate::types::RcsTextMessage, &Self> {
        if let RcsContent::TextMessage(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`TextMessage`](crate::types::RcsContent::TextMessage).
    pub fn is_text_message(&self) -> bool {
        self.as_text_message().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
