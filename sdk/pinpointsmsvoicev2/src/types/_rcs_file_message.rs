// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A file message containing a media file (image, video, audio, or PDF) with an optional thumbnail.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct RcsFileMessage {
    /// <p>The S3 URI of the media file to send, in the format <code>s3://bucket-name/key</code>. The service downloads the file from your S3 bucket, rehosts it, and generates a presigned URL for the aggregator. Maximum 2000 characters.</p>
    pub file_url: ::std::string::String,
    /// <p>The S3 URI of an optional thumbnail image for the media file, in the format <code>s3://bucket-name/key</code>. Maximum 2000 characters.</p>
    pub thumbnail_url: ::std::option::Option<::std::string::String>,
}
impl RcsFileMessage {
    /// <p>The S3 URI of the media file to send, in the format <code>s3://bucket-name/key</code>. The service downloads the file from your S3 bucket, rehosts it, and generates a presigned URL for the aggregator. Maximum 2000 characters.</p>
    pub fn file_url(&self) -> &str {
        use std::ops::Deref;
        self.file_url.deref()
    }
    /// <p>The S3 URI of an optional thumbnail image for the media file, in the format <code>s3://bucket-name/key</code>. Maximum 2000 characters.</p>
    pub fn thumbnail_url(&self) -> ::std::option::Option<&str> {
        self.thumbnail_url.as_deref()
    }
}
impl RcsFileMessage {
    /// Creates a new builder-style object to manufacture [`RcsFileMessage`](crate::types::RcsFileMessage).
    pub fn builder() -> crate::types::builders::RcsFileMessageBuilder {
        crate::types::builders::RcsFileMessageBuilder::default()
    }
}

/// A builder for [`RcsFileMessage`](crate::types::RcsFileMessage).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct RcsFileMessageBuilder {
    pub(crate) file_url: ::std::option::Option<::std::string::String>,
    pub(crate) thumbnail_url: ::std::option::Option<::std::string::String>,
}
impl RcsFileMessageBuilder {
    /// <p>The S3 URI of the media file to send, in the format <code>s3://bucket-name/key</code>. The service downloads the file from your S3 bucket, rehosts it, and generates a presigned URL for the aggregator. Maximum 2000 characters.</p>
    /// This field is required.
    pub fn file_url(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.file_url = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The S3 URI of the media file to send, in the format <code>s3://bucket-name/key</code>. The service downloads the file from your S3 bucket, rehosts it, and generates a presigned URL for the aggregator. Maximum 2000 characters.</p>
    pub fn set_file_url(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.file_url = input;
        self
    }
    /// <p>The S3 URI of the media file to send, in the format <code>s3://bucket-name/key</code>. The service downloads the file from your S3 bucket, rehosts it, and generates a presigned URL for the aggregator. Maximum 2000 characters.</p>
    pub fn get_file_url(&self) -> &::std::option::Option<::std::string::String> {
        &self.file_url
    }
    /// <p>The S3 URI of an optional thumbnail image for the media file, in the format <code>s3://bucket-name/key</code>. Maximum 2000 characters.</p>
    pub fn thumbnail_url(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.thumbnail_url = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The S3 URI of an optional thumbnail image for the media file, in the format <code>s3://bucket-name/key</code>. Maximum 2000 characters.</p>
    pub fn set_thumbnail_url(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.thumbnail_url = input;
        self
    }
    /// <p>The S3 URI of an optional thumbnail image for the media file, in the format <code>s3://bucket-name/key</code>. Maximum 2000 characters.</p>
    pub fn get_thumbnail_url(&self) -> &::std::option::Option<::std::string::String> {
        &self.thumbnail_url
    }
    /// Consumes the builder and constructs a [`RcsFileMessage`](crate::types::RcsFileMessage).
    /// This method will fail if any of the following fields are not set:
    /// - [`file_url`](crate::types::builders::RcsFileMessageBuilder::file_url)
    pub fn build(self) -> ::std::result::Result<crate::types::RcsFileMessage, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::RcsFileMessage {
            file_url: self.file_url.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "file_url",
                    "file_url was not specified but it is required when building RcsFileMessage",
                )
            })?,
            thumbnail_url: self.thumbnail_url,
        })
    }
}
