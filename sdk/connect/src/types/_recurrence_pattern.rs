// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Specifies the detailed pattern for event recurrence. Use this to define complex scheduling rules such as "every 2nd Tuesday of the month" or "every 3 months on the 15th".</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct RecurrencePattern {
    /// <p>Defines how often the pattern repeats. This is the base unit for the recurrence schedule and works in conjunction with the Interval field to determine the exact repetition sequence.</p>
    pub frequency: crate::types::RecurrenceFrequency,
    /// <p>Specifies the number of frequency units between each occurrence. Must be a positive integer.</p>
    /// <p>Examples: To repeat every week, set Interval=1 with WEEKLY frequency. To repeat every two months, set Interval=2 with MONTHLY frequency.</p>
    pub interval: i32,
    /// <p>Specifies which month the event should occur in (1-12, where 1=January, 12=December). Used with YEARLY frequency to schedule events in specific month.</p>
    /// <p>Note: It does not accept multiple values in the same list</p>
    pub by_month: ::std::option::Option<::std::vec::Vec<i32>>,
    /// <p>Specifies which day of the month the event should occur on (1-31). Used with MONTHLY or YEARLY frequency to schedule events on specific date within a month.</p>
    /// <p>Examples: \[15\] for events on the 15th of each month, \[-1\] for events on the last day of month.</p>
    /// <p>Note: It does not accept multiple values in the same list. If a specified day doesn't exist in a particular month (e.g., day 31 in February), the event will be skipped for that month. This field cannot be used simultaneously with ByWeekdayOccurrence as they represent different scheduling approaches (specific dates vs. relative weekday positions).</p>
    pub by_month_day: ::std::option::Option<::std::vec::Vec<i32>>,
    /// <p>Specifies which occurrence of a weekday within the month the event should occur on. Must be used with MONTHLY or YEARLY frequency.</p>
    /// <p>Example: 2 corresponds to second occurrence of the weekday in the month. -1 corresponds to last occurrence of the weekday in the month</p>
    /// <p>The weekday itself is specified separately in the HoursOfOperationConfig. Example: To schedule the recurring event for the 2nd Thursday of April every year, set ByWeekdayOccurrence=\[2\], Day=THURSDAY, ByMonth=\[4\], Frequency: YEARLY and INTERVAL=1.</p>
    pub by_weekday_occurrence: ::std::option::Option<::std::vec::Vec<i32>>,
}
impl RecurrencePattern {
    /// <p>Defines how often the pattern repeats. This is the base unit for the recurrence schedule and works in conjunction with the Interval field to determine the exact repetition sequence.</p>
    pub fn frequency(&self) -> &crate::types::RecurrenceFrequency {
        &self.frequency
    }
    /// <p>Specifies the number of frequency units between each occurrence. Must be a positive integer.</p>
    /// <p>Examples: To repeat every week, set Interval=1 with WEEKLY frequency. To repeat every two months, set Interval=2 with MONTHLY frequency.</p>
    pub fn interval(&self) -> i32 {
        self.interval
    }
    /// <p>Specifies which month the event should occur in (1-12, where 1=January, 12=December). Used with YEARLY frequency to schedule events in specific month.</p>
    /// <p>Note: It does not accept multiple values in the same list</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.by_month.is_none()`.
    pub fn by_month(&self) -> &[i32] {
        self.by_month.as_deref().unwrap_or_default()
    }
    /// <p>Specifies which day of the month the event should occur on (1-31). Used with MONTHLY or YEARLY frequency to schedule events on specific date within a month.</p>
    /// <p>Examples: \[15\] for events on the 15th of each month, \[-1\] for events on the last day of month.</p>
    /// <p>Note: It does not accept multiple values in the same list. If a specified day doesn't exist in a particular month (e.g., day 31 in February), the event will be skipped for that month. This field cannot be used simultaneously with ByWeekdayOccurrence as they represent different scheduling approaches (specific dates vs. relative weekday positions).</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.by_month_day.is_none()`.
    pub fn by_month_day(&self) -> &[i32] {
        self.by_month_day.as_deref().unwrap_or_default()
    }
    /// <p>Specifies which occurrence of a weekday within the month the event should occur on. Must be used with MONTHLY or YEARLY frequency.</p>
    /// <p>Example: 2 corresponds to second occurrence of the weekday in the month. -1 corresponds to last occurrence of the weekday in the month</p>
    /// <p>The weekday itself is specified separately in the HoursOfOperationConfig. Example: To schedule the recurring event for the 2nd Thursday of April every year, set ByWeekdayOccurrence=\[2\], Day=THURSDAY, ByMonth=\[4\], Frequency: YEARLY and INTERVAL=1.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.by_weekday_occurrence.is_none()`.
    pub fn by_weekday_occurrence(&self) -> &[i32] {
        self.by_weekday_occurrence.as_deref().unwrap_or_default()
    }
}
impl RecurrencePattern {
    /// Creates a new builder-style object to manufacture [`RecurrencePattern`](crate::types::RecurrencePattern).
    pub fn builder() -> crate::types::builders::RecurrencePatternBuilder {
        crate::types::builders::RecurrencePatternBuilder::default()
    }
}

/// A builder for [`RecurrencePattern`](crate::types::RecurrencePattern).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct RecurrencePatternBuilder {
    pub(crate) frequency: ::std::option::Option<crate::types::RecurrenceFrequency>,
    pub(crate) interval: ::std::option::Option<i32>,
    pub(crate) by_month: ::std::option::Option<::std::vec::Vec<i32>>,
    pub(crate) by_month_day: ::std::option::Option<::std::vec::Vec<i32>>,
    pub(crate) by_weekday_occurrence: ::std::option::Option<::std::vec::Vec<i32>>,
}
impl RecurrencePatternBuilder {
    /// <p>Defines how often the pattern repeats. This is the base unit for the recurrence schedule and works in conjunction with the Interval field to determine the exact repetition sequence.</p>
    /// This field is required.
    pub fn frequency(mut self, input: crate::types::RecurrenceFrequency) -> Self {
        self.frequency = ::std::option::Option::Some(input);
        self
    }
    /// <p>Defines how often the pattern repeats. This is the base unit for the recurrence schedule and works in conjunction with the Interval field to determine the exact repetition sequence.</p>
    pub fn set_frequency(mut self, input: ::std::option::Option<crate::types::RecurrenceFrequency>) -> Self {
        self.frequency = input;
        self
    }
    /// <p>Defines how often the pattern repeats. This is the base unit for the recurrence schedule and works in conjunction with the Interval field to determine the exact repetition sequence.</p>
    pub fn get_frequency(&self) -> &::std::option::Option<crate::types::RecurrenceFrequency> {
        &self.frequency
    }
    /// <p>Specifies the number of frequency units between each occurrence. Must be a positive integer.</p>
    /// <p>Examples: To repeat every week, set Interval=1 with WEEKLY frequency. To repeat every two months, set Interval=2 with MONTHLY frequency.</p>
    /// This field is required.
    pub fn interval(mut self, input: i32) -> Self {
        self.interval = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies the number of frequency units between each occurrence. Must be a positive integer.</p>
    /// <p>Examples: To repeat every week, set Interval=1 with WEEKLY frequency. To repeat every two months, set Interval=2 with MONTHLY frequency.</p>
    pub fn set_interval(mut self, input: ::std::option::Option<i32>) -> Self {
        self.interval = input;
        self
    }
    /// <p>Specifies the number of frequency units between each occurrence. Must be a positive integer.</p>
    /// <p>Examples: To repeat every week, set Interval=1 with WEEKLY frequency. To repeat every two months, set Interval=2 with MONTHLY frequency.</p>
    pub fn get_interval(&self) -> &::std::option::Option<i32> {
        &self.interval
    }
    /// Appends an item to `by_month`.
    ///
    /// To override the contents of this collection use [`set_by_month`](Self::set_by_month).
    ///
    /// <p>Specifies which month the event should occur in (1-12, where 1=January, 12=December). Used with YEARLY frequency to schedule events in specific month.</p>
    /// <p>Note: It does not accept multiple values in the same list</p>
    pub fn by_month(mut self, input: i32) -> Self {
        let mut v = self.by_month.unwrap_or_default();
        v.push(input);
        self.by_month = ::std::option::Option::Some(v);
        self
    }
    /// <p>Specifies which month the event should occur in (1-12, where 1=January, 12=December). Used with YEARLY frequency to schedule events in specific month.</p>
    /// <p>Note: It does not accept multiple values in the same list</p>
    pub fn set_by_month(mut self, input: ::std::option::Option<::std::vec::Vec<i32>>) -> Self {
        self.by_month = input;
        self
    }
    /// <p>Specifies which month the event should occur in (1-12, where 1=January, 12=December). Used with YEARLY frequency to schedule events in specific month.</p>
    /// <p>Note: It does not accept multiple values in the same list</p>
    pub fn get_by_month(&self) -> &::std::option::Option<::std::vec::Vec<i32>> {
        &self.by_month
    }
    /// Appends an item to `by_month_day`.
    ///
    /// To override the contents of this collection use [`set_by_month_day`](Self::set_by_month_day).
    ///
    /// <p>Specifies which day of the month the event should occur on (1-31). Used with MONTHLY or YEARLY frequency to schedule events on specific date within a month.</p>
    /// <p>Examples: \[15\] for events on the 15th of each month, \[-1\] for events on the last day of month.</p>
    /// <p>Note: It does not accept multiple values in the same list. If a specified day doesn't exist in a particular month (e.g., day 31 in February), the event will be skipped for that month. This field cannot be used simultaneously with ByWeekdayOccurrence as they represent different scheduling approaches (specific dates vs. relative weekday positions).</p>
    pub fn by_month_day(mut self, input: i32) -> Self {
        let mut v = self.by_month_day.unwrap_or_default();
        v.push(input);
        self.by_month_day = ::std::option::Option::Some(v);
        self
    }
    /// <p>Specifies which day of the month the event should occur on (1-31). Used with MONTHLY or YEARLY frequency to schedule events on specific date within a month.</p>
    /// <p>Examples: \[15\] for events on the 15th of each month, \[-1\] for events on the last day of month.</p>
    /// <p>Note: It does not accept multiple values in the same list. If a specified day doesn't exist in a particular month (e.g., day 31 in February), the event will be skipped for that month. This field cannot be used simultaneously with ByWeekdayOccurrence as they represent different scheduling approaches (specific dates vs. relative weekday positions).</p>
    pub fn set_by_month_day(mut self, input: ::std::option::Option<::std::vec::Vec<i32>>) -> Self {
        self.by_month_day = input;
        self
    }
    /// <p>Specifies which day of the month the event should occur on (1-31). Used with MONTHLY or YEARLY frequency to schedule events on specific date within a month.</p>
    /// <p>Examples: \[15\] for events on the 15th of each month, \[-1\] for events on the last day of month.</p>
    /// <p>Note: It does not accept multiple values in the same list. If a specified day doesn't exist in a particular month (e.g., day 31 in February), the event will be skipped for that month. This field cannot be used simultaneously with ByWeekdayOccurrence as they represent different scheduling approaches (specific dates vs. relative weekday positions).</p>
    pub fn get_by_month_day(&self) -> &::std::option::Option<::std::vec::Vec<i32>> {
        &self.by_month_day
    }
    /// Appends an item to `by_weekday_occurrence`.
    ///
    /// To override the contents of this collection use [`set_by_weekday_occurrence`](Self::set_by_weekday_occurrence).
    ///
    /// <p>Specifies which occurrence of a weekday within the month the event should occur on. Must be used with MONTHLY or YEARLY frequency.</p>
    /// <p>Example: 2 corresponds to second occurrence of the weekday in the month. -1 corresponds to last occurrence of the weekday in the month</p>
    /// <p>The weekday itself is specified separately in the HoursOfOperationConfig. Example: To schedule the recurring event for the 2nd Thursday of April every year, set ByWeekdayOccurrence=\[2\], Day=THURSDAY, ByMonth=\[4\], Frequency: YEARLY and INTERVAL=1.</p>
    pub fn by_weekday_occurrence(mut self, input: i32) -> Self {
        let mut v = self.by_weekday_occurrence.unwrap_or_default();
        v.push(input);
        self.by_weekday_occurrence = ::std::option::Option::Some(v);
        self
    }
    /// <p>Specifies which occurrence of a weekday within the month the event should occur on. Must be used with MONTHLY or YEARLY frequency.</p>
    /// <p>Example: 2 corresponds to second occurrence of the weekday in the month. -1 corresponds to last occurrence of the weekday in the month</p>
    /// <p>The weekday itself is specified separately in the HoursOfOperationConfig. Example: To schedule the recurring event for the 2nd Thursday of April every year, set ByWeekdayOccurrence=\[2\], Day=THURSDAY, ByMonth=\[4\], Frequency: YEARLY and INTERVAL=1.</p>
    pub fn set_by_weekday_occurrence(mut self, input: ::std::option::Option<::std::vec::Vec<i32>>) -> Self {
        self.by_weekday_occurrence = input;
        self
    }
    /// <p>Specifies which occurrence of a weekday within the month the event should occur on. Must be used with MONTHLY or YEARLY frequency.</p>
    /// <p>Example: 2 corresponds to second occurrence of the weekday in the month. -1 corresponds to last occurrence of the weekday in the month</p>
    /// <p>The weekday itself is specified separately in the HoursOfOperationConfig. Example: To schedule the recurring event for the 2nd Thursday of April every year, set ByWeekdayOccurrence=\[2\], Day=THURSDAY, ByMonth=\[4\], Frequency: YEARLY and INTERVAL=1.</p>
    pub fn get_by_weekday_occurrence(&self) -> &::std::option::Option<::std::vec::Vec<i32>> {
        &self.by_weekday_occurrence
    }
    /// Consumes the builder and constructs a [`RecurrencePattern`](crate::types::RecurrencePattern).
    /// This method will fail if any of the following fields are not set:
    /// - [`frequency`](crate::types::builders::RecurrencePatternBuilder::frequency)
    /// - [`interval`](crate::types::builders::RecurrencePatternBuilder::interval)
    pub fn build(self) -> ::std::result::Result<crate::types::RecurrencePattern, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::RecurrencePattern {
            frequency: self.frequency.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "frequency",
                    "frequency was not specified but it is required when building RecurrencePattern",
                )
            })?,
            interval: self.interval.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "interval",
                    "interval was not specified but it is required when building RecurrencePattern",
                )
            })?,
            by_month: self.by_month,
            by_month_day: self.by_month_day,
            by_weekday_occurrence: self.by_weekday_occurrence,
        })
    }
}
