// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains the formula and component metrics that define a custom metric calculation.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct MetricCalculation {
    /// <p>The list of component metrics referenced in the calculation formula. Each component has an alias used in the formula expression.</p>
    pub calculation_components: ::std::vec::Vec<crate::types::CalculationComponent>,
    /// <p>The formula expression that defines how the metric is calculated. Uses component aliases (for example, <code>100 * SUM(M1) / SUM(M2)</code>).</p>
    pub calculation: ::std::string::String,
}
impl MetricCalculation {
    /// <p>The list of component metrics referenced in the calculation formula. Each component has an alias used in the formula expression.</p>
    pub fn calculation_components(&self) -> &[crate::types::CalculationComponent] {
        use std::ops::Deref;
        self.calculation_components.deref()
    }
    /// <p>The formula expression that defines how the metric is calculated. Uses component aliases (for example, <code>100 * SUM(M1) / SUM(M2)</code>).</p>
    pub fn calculation(&self) -> &str {
        use std::ops::Deref;
        self.calculation.deref()
    }
}
impl MetricCalculation {
    /// Creates a new builder-style object to manufacture [`MetricCalculation`](crate::types::MetricCalculation).
    pub fn builder() -> crate::types::builders::MetricCalculationBuilder {
        crate::types::builders::MetricCalculationBuilder::default()
    }
}

/// A builder for [`MetricCalculation`](crate::types::MetricCalculation).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct MetricCalculationBuilder {
    pub(crate) calculation_components: ::std::option::Option<::std::vec::Vec<crate::types::CalculationComponent>>,
    pub(crate) calculation: ::std::option::Option<::std::string::String>,
}
impl MetricCalculationBuilder {
    /// Appends an item to `calculation_components`.
    ///
    /// To override the contents of this collection use [`set_calculation_components`](Self::set_calculation_components).
    ///
    /// <p>The list of component metrics referenced in the calculation formula. Each component has an alias used in the formula expression.</p>
    pub fn calculation_components(mut self, input: crate::types::CalculationComponent) -> Self {
        let mut v = self.calculation_components.unwrap_or_default();
        v.push(input);
        self.calculation_components = ::std::option::Option::Some(v);
        self
    }
    /// <p>The list of component metrics referenced in the calculation formula. Each component has an alias used in the formula expression.</p>
    pub fn set_calculation_components(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::CalculationComponent>>) -> Self {
        self.calculation_components = input;
        self
    }
    /// <p>The list of component metrics referenced in the calculation formula. Each component has an alias used in the formula expression.</p>
    pub fn get_calculation_components(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::CalculationComponent>> {
        &self.calculation_components
    }
    /// <p>The formula expression that defines how the metric is calculated. Uses component aliases (for example, <code>100 * SUM(M1) / SUM(M2)</code>).</p>
    /// This field is required.
    pub fn calculation(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.calculation = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The formula expression that defines how the metric is calculated. Uses component aliases (for example, <code>100 * SUM(M1) / SUM(M2)</code>).</p>
    pub fn set_calculation(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.calculation = input;
        self
    }
    /// <p>The formula expression that defines how the metric is calculated. Uses component aliases (for example, <code>100 * SUM(M1) / SUM(M2)</code>).</p>
    pub fn get_calculation(&self) -> &::std::option::Option<::std::string::String> {
        &self.calculation
    }
    /// Consumes the builder and constructs a [`MetricCalculation`](crate::types::MetricCalculation).
    /// This method will fail if any of the following fields are not set:
    /// - [`calculation_components`](crate::types::builders::MetricCalculationBuilder::calculation_components)
    /// - [`calculation`](crate::types::builders::MetricCalculationBuilder::calculation)
    pub fn build(self) -> ::std::result::Result<crate::types::MetricCalculation, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::MetricCalculation {
            calculation_components: self.calculation_components.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "calculation_components",
                    "calculation_components was not specified but it is required when building MetricCalculation",
                )
            })?,
            calculation: self.calculation.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "calculation",
                    "calculation was not specified but it is required when building MetricCalculation",
                )
            })?,
        })
    }
}
