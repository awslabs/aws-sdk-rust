// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct GetCrossRegionRoutingOutput {
    /// <p>The list of Regions for which cross-region routing is currently disabled (isolated). When a Region appears in this list, contacts originating in that Region will not be routed to agents in other Regions, and agents in that Region will not receive contacts from other Regions.</p>
    pub isolated_regions: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    _request_id: Option<String>,
}
impl GetCrossRegionRoutingOutput {
    /// <p>The list of Regions for which cross-region routing is currently disabled (isolated). When a Region appears in this list, contacts originating in that Region will not be routed to agents in other Regions, and agents in that Region will not receive contacts from other Regions.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.isolated_regions.is_none()`.
    pub fn isolated_regions(&self) -> &[::std::string::String] {
        self.isolated_regions.as_deref().unwrap_or_default()
    }
}
impl ::aws_types::request_id::RequestId for GetCrossRegionRoutingOutput {
    fn request_id(&self) -> Option<&str> {
        self._request_id.as_deref()
    }
}
impl GetCrossRegionRoutingOutput {
    /// Creates a new builder-style object to manufacture [`GetCrossRegionRoutingOutput`](crate::operation::get_cross_region_routing::GetCrossRegionRoutingOutput).
    pub fn builder() -> crate::operation::get_cross_region_routing::builders::GetCrossRegionRoutingOutputBuilder {
        crate::operation::get_cross_region_routing::builders::GetCrossRegionRoutingOutputBuilder::default()
    }
}

/// A builder for [`GetCrossRegionRoutingOutput`](crate::operation::get_cross_region_routing::GetCrossRegionRoutingOutput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct GetCrossRegionRoutingOutputBuilder {
    pub(crate) isolated_regions: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    _request_id: Option<String>,
}
impl GetCrossRegionRoutingOutputBuilder {
    /// Appends an item to `isolated_regions`.
    ///
    /// To override the contents of this collection use [`set_isolated_regions`](Self::set_isolated_regions).
    ///
    /// <p>The list of Regions for which cross-region routing is currently disabled (isolated). When a Region appears in this list, contacts originating in that Region will not be routed to agents in other Regions, and agents in that Region will not receive contacts from other Regions.</p>
    pub fn isolated_regions(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.isolated_regions.unwrap_or_default();
        v.push(input.into());
        self.isolated_regions = ::std::option::Option::Some(v);
        self
    }
    /// <p>The list of Regions for which cross-region routing is currently disabled (isolated). When a Region appears in this list, contacts originating in that Region will not be routed to agents in other Regions, and agents in that Region will not receive contacts from other Regions.</p>
    pub fn set_isolated_regions(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.isolated_regions = input;
        self
    }
    /// <p>The list of Regions for which cross-region routing is currently disabled (isolated). When a Region appears in this list, contacts originating in that Region will not be routed to agents in other Regions, and agents in that Region will not receive contacts from other Regions.</p>
    pub fn get_isolated_regions(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.isolated_regions
    }
    pub(crate) fn _request_id(mut self, request_id: impl Into<String>) -> Self {
        self._request_id = Some(request_id.into());
        self
    }

    pub(crate) fn _set_request_id(&mut self, request_id: Option<String>) -> &mut Self {
        self._request_id = request_id;
        self
    }
    /// Consumes the builder and constructs a [`GetCrossRegionRoutingOutput`](crate::operation::get_cross_region_routing::GetCrossRegionRoutingOutput).
    pub fn build(self) -> crate::operation::get_cross_region_routing::GetCrossRegionRoutingOutput {
        crate::operation::get_cross_region_routing::GetCrossRegionRoutingOutput {
            isolated_regions: self.isolated_regions,
            _request_id: self._request_id,
        }
    }
}
