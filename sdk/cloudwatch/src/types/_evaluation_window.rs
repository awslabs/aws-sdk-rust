// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The evaluation window that an alarm uses to select the range of metric data that it evaluates each time it runs. This is a union type. Set exactly one of its members, <code>SlidingWindow</code> or <code>WallClockWindow</code>. If you don't set <code>EvaluationWindow</code>, the alarm uses a <code>SlidingWindow</code> by default.</p>
/// <p>For more information, see <a href="https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/alarm-evaluation-window.html">Alarm evaluation windows</a> in the <i>CloudWatch User Guide</i>.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum EvaluationWindow {
    /// <p>A sliding window, which advances each time the alarm is evaluated, forming a rolling time window. This is the default evaluation window.</p>
    SlidingWindow(crate::types::SlidingWindow),
    /// <p>A wall clock window, which aligns the evaluated range to fixed clock boundaries that match the alarm's period, such as the top of the hour, midnight, or the start of the calendar week.</p>
    WallClockWindow(crate::types::WallClockWindow),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl EvaluationWindow {
    /// Tries to convert the enum instance into [`SlidingWindow`](crate::types::EvaluationWindow::SlidingWindow), extracting the inner [`SlidingWindow`](crate::types::SlidingWindow).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_sliding_window(&self) -> ::std::result::Result<&crate::types::SlidingWindow, &Self> {
        if let EvaluationWindow::SlidingWindow(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`SlidingWindow`](crate::types::EvaluationWindow::SlidingWindow).
    pub fn is_sliding_window(&self) -> bool {
        self.as_sliding_window().is_ok()
    }
    /// Tries to convert the enum instance into [`WallClockWindow`](crate::types::EvaluationWindow::WallClockWindow), extracting the inner [`WallClockWindow`](crate::types::WallClockWindow).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_wall_clock_window(&self) -> ::std::result::Result<&crate::types::WallClockWindow, &Self> {
        if let EvaluationWindow::WallClockWindow(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`WallClockWindow`](crate::types::EvaluationWindow::WallClockWindow).
    pub fn is_wall_clock_window(&self) -> bool {
        self.as_wall_clock_window().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
