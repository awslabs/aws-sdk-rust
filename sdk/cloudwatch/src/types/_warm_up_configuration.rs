// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration settings that define the warm-up behavior for an alarm. Use these settings to delay alarm evaluation after you create or update the alarm, which reduces alarm noise while a new resource or service starts publishing data.</p>
/// <p>During the warm-up period, the alarm stays in <code>INSUFFICIENT_DATA</code> and does not perform alarm actions.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct WarmUpConfiguration {
    /// <p>The length of the warm-up period, in minutes. After you create or update the alarm, the alarm stays in <code>INSUFFICIENT_DATA</code> for this duration. During this time, the alarm does not perform alarm actions.</p>
    /// <p>You can change this value at any time, including after the warm-up period ends. If you change it after the warm-up period ends, the new value does not restart the warm-up period.</p>
    pub warm_up_period_duration_in_minutes: ::std::option::Option<i32>,
    /// <p>Specifies whether the alarm waits for the full warm-up period before it starts to evaluate. The default is <code>false</code>. If <code>true</code>, the alarm waits the entire <code>WarmUpPeriodDurationInMinutes</code> before it starts to evaluate, even if metric data arrives earlier. If <code>false</code>, the alarm ends the warm-up period early. Evaluation begins as soon as the alarm has enough metric data to fill its evaluation window.</p>
    pub only_start_evaluating_after_warm_up_period_ends: ::std::option::Option<bool>,
}
impl WarmUpConfiguration {
    /// <p>The length of the warm-up period, in minutes. After you create or update the alarm, the alarm stays in <code>INSUFFICIENT_DATA</code> for this duration. During this time, the alarm does not perform alarm actions.</p>
    /// <p>You can change this value at any time, including after the warm-up period ends. If you change it after the warm-up period ends, the new value does not restart the warm-up period.</p>
    pub fn warm_up_period_duration_in_minutes(&self) -> ::std::option::Option<i32> {
        self.warm_up_period_duration_in_minutes
    }
    /// <p>Specifies whether the alarm waits for the full warm-up period before it starts to evaluate. The default is <code>false</code>. If <code>true</code>, the alarm waits the entire <code>WarmUpPeriodDurationInMinutes</code> before it starts to evaluate, even if metric data arrives earlier. If <code>false</code>, the alarm ends the warm-up period early. Evaluation begins as soon as the alarm has enough metric data to fill its evaluation window.</p>
    pub fn only_start_evaluating_after_warm_up_period_ends(&self) -> ::std::option::Option<bool> {
        self.only_start_evaluating_after_warm_up_period_ends
    }
}
impl WarmUpConfiguration {
    /// Creates a new builder-style object to manufacture [`WarmUpConfiguration`](crate::types::WarmUpConfiguration).
    pub fn builder() -> crate::types::builders::WarmUpConfigurationBuilder {
        crate::types::builders::WarmUpConfigurationBuilder::default()
    }
}

/// A builder for [`WarmUpConfiguration`](crate::types::WarmUpConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct WarmUpConfigurationBuilder {
    pub(crate) warm_up_period_duration_in_minutes: ::std::option::Option<i32>,
    pub(crate) only_start_evaluating_after_warm_up_period_ends: ::std::option::Option<bool>,
}
impl WarmUpConfigurationBuilder {
    /// <p>The length of the warm-up period, in minutes. After you create or update the alarm, the alarm stays in <code>INSUFFICIENT_DATA</code> for this duration. During this time, the alarm does not perform alarm actions.</p>
    /// <p>You can change this value at any time, including after the warm-up period ends. If you change it after the warm-up period ends, the new value does not restart the warm-up period.</p>
    /// This field is required.
    pub fn warm_up_period_duration_in_minutes(mut self, input: i32) -> Self {
        self.warm_up_period_duration_in_minutes = ::std::option::Option::Some(input);
        self
    }
    /// <p>The length of the warm-up period, in minutes. After you create or update the alarm, the alarm stays in <code>INSUFFICIENT_DATA</code> for this duration. During this time, the alarm does not perform alarm actions.</p>
    /// <p>You can change this value at any time, including after the warm-up period ends. If you change it after the warm-up period ends, the new value does not restart the warm-up period.</p>
    pub fn set_warm_up_period_duration_in_minutes(mut self, input: ::std::option::Option<i32>) -> Self {
        self.warm_up_period_duration_in_minutes = input;
        self
    }
    /// <p>The length of the warm-up period, in minutes. After you create or update the alarm, the alarm stays in <code>INSUFFICIENT_DATA</code> for this duration. During this time, the alarm does not perform alarm actions.</p>
    /// <p>You can change this value at any time, including after the warm-up period ends. If you change it after the warm-up period ends, the new value does not restart the warm-up period.</p>
    pub fn get_warm_up_period_duration_in_minutes(&self) -> &::std::option::Option<i32> {
        &self.warm_up_period_duration_in_minutes
    }
    /// <p>Specifies whether the alarm waits for the full warm-up period before it starts to evaluate. The default is <code>false</code>. If <code>true</code>, the alarm waits the entire <code>WarmUpPeriodDurationInMinutes</code> before it starts to evaluate, even if metric data arrives earlier. If <code>false</code>, the alarm ends the warm-up period early. Evaluation begins as soon as the alarm has enough metric data to fill its evaluation window.</p>
    pub fn only_start_evaluating_after_warm_up_period_ends(mut self, input: bool) -> Self {
        self.only_start_evaluating_after_warm_up_period_ends = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether the alarm waits for the full warm-up period before it starts to evaluate. The default is <code>false</code>. If <code>true</code>, the alarm waits the entire <code>WarmUpPeriodDurationInMinutes</code> before it starts to evaluate, even if metric data arrives earlier. If <code>false</code>, the alarm ends the warm-up period early. Evaluation begins as soon as the alarm has enough metric data to fill its evaluation window.</p>
    pub fn set_only_start_evaluating_after_warm_up_period_ends(mut self, input: ::std::option::Option<bool>) -> Self {
        self.only_start_evaluating_after_warm_up_period_ends = input;
        self
    }
    /// <p>Specifies whether the alarm waits for the full warm-up period before it starts to evaluate. The default is <code>false</code>. If <code>true</code>, the alarm waits the entire <code>WarmUpPeriodDurationInMinutes</code> before it starts to evaluate, even if metric data arrives earlier. If <code>false</code>, the alarm ends the warm-up period early. Evaluation begins as soon as the alarm has enough metric data to fill its evaluation window.</p>
    pub fn get_only_start_evaluating_after_warm_up_period_ends(&self) -> &::std::option::Option<bool> {
        &self.only_start_evaluating_after_warm_up_period_ends
    }
    /// Consumes the builder and constructs a [`WarmUpConfiguration`](crate::types::WarmUpConfiguration).
    pub fn build(self) -> crate::types::WarmUpConfiguration {
        crate::types::WarmUpConfiguration {
            warm_up_period_duration_in_minutes: self.warm_up_period_duration_in_minutes,
            only_start_evaluating_after_warm_up_period_ends: self.only_start_evaluating_after_warm_up_period_ends,
        }
    }
}
