// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
impl super::Client {
    /// Constructs a fluent builder for the [`CreateAttendee`](crate::operation::create_attendee::builders::CreateAttendeeFluentBuilder) operation.
    ///
    /// - The fluent builder is configurable:
    ///   - [`meeting_id(impl Into<String>)`](crate::operation::create_attendee::builders::CreateAttendeeFluentBuilder::meeting_id) / [`set_meeting_id(Option<String>)`](crate::operation::create_attendee::builders::CreateAttendeeFluentBuilder::set_meeting_id):<br>required: **true**<br><p>The unique ID of the meeting.</p><br>
    ///   - [`external_user_id(impl Into<String>)`](crate::operation::create_attendee::builders::CreateAttendeeFluentBuilder::external_user_id) / [`set_external_user_id(Option<String>)`](crate::operation::create_attendee::builders::CreateAttendeeFluentBuilder::set_external_user_id):<br>required: **true**<br><p>The Amazon Chime SDK external user ID. An idempotency token. Links the attendee to an identity managed by a builder application.</p> <p>Pattern: <code>\[-_&amp;@+=,(){}\\[\\]\/«».:|'"#a-zA-Z0-9À-ÿ\s\]*</code></p> <p>Values that begin with <code>aws:</code> are reserved. You can't configure a value that uses this prefix.</p><br>
    ///   - [`capabilities(AttendeeCapabilities)`](crate::operation::create_attendee::builders::CreateAttendeeFluentBuilder::capabilities) / [`set_capabilities(Option<AttendeeCapabilities>)`](crate::operation::create_attendee::builders::CreateAttendeeFluentBuilder::set_capabilities):<br>required: **false**<br><p>The capabilities (<code>audio</code>, <code>video</code>, or <code>content</code>) that you want to grant an attendee. If you don't specify capabilities, all users have send and receive capabilities on all media channels by default.</p><note>  <p>You use the capabilities with a set of values that control what the capabilities can do, such as <code>SendReceive</code> data. For more information about those values, see .</p> </note> <p>When using capabilities, be aware of these corner cases:</p> <ul>  <li>   <p>If you specify <code>MeetingFeatures:Video:MaxResolution:None</code> when you create a meeting, all API requests that include <code>SendReceive</code>, <code>Send</code>, or <code>Receive</code> for <code>AttendeeCapabilities:Video</code> will be rejected with <code>ValidationError 400</code>.</p></li>  <li>   <p>If you specify <code>MeetingFeatures:Content:MaxResolution:None</code> when you create a meeting, all API requests that include <code>SendReceive</code>, <code>Send</code>, or <code>Receive</code> for <code>AttendeeCapabilities:Content</code> will be rejected with <code>ValidationError 400</code>.</p></li>  <li>   <p>You can't set <code>content</code> capabilities to <code>SendReceive</code> or <code>Receive</code> unless you also set <code>video</code> capabilities to <code>SendReceive</code> or <code>Receive</code>. If you don't set the <code>video</code> capability to receive, the response will contain an HTTP 400 Bad Request status code. However, you can set your <code>video</code> capability to receive and you set your <code>content</code> capability to not receive.</p></li>  <li>   <p>When you change an <code>audio</code> capability from <code>None</code> or <code>Receive</code> to <code>Send</code> or <code>SendReceive</code> , and if the attendee left their microphone unmuted, audio will flow from the attendee to the other meeting participants.</p></li>  <li>   <p>When you change a <code>video</code> or <code>content</code> capability from <code>None</code> or <code>Receive</code> to <code>Send</code> or <code>SendReceive</code> , and if the attendee turned on their video or content streams, remote attendees can receive those streams, but only after media renegotiation between the client and the Amazon Chime back-end server.</p></li> </ul><br>
    /// - On success, responds with [`CreateAttendeeOutput`](crate::operation::create_attendee::CreateAttendeeOutput) with field(s):
    ///   - [`attendee(Option<Attendee>)`](crate::operation::create_attendee::CreateAttendeeOutput::attendee): <p>The attendee information, including attendee ID and join token.</p>
    /// - On failure, responds with [`SdkError<CreateAttendeeError>`](crate::operation::create_attendee::CreateAttendeeError)
    pub fn create_attendee(&self) -> crate::operation::create_attendee::builders::CreateAttendeeFluentBuilder {
        crate::operation::create_attendee::builders::CreateAttendeeFluentBuilder::new(self.handle.clone())
    }
}
