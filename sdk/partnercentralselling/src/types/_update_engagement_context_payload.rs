// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Represents the updated payload of an engagement context. The structure of this payload varies based on the context type being updated.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum UpdateEngagementContextPayload {
    /// <p>The CustomerProjects structure in Engagements offers a flexible framework for managing customer-project relationships. It supports multiple customers per Engagement and multiple projects per customer, while also allowing for customers without projects and projects without specific customers.</p>
    /// <p>All Engagement members have full visibility of customers and their associated projects, enabling the capture of relevant context even when project details are not fully defined. This structure also facilitates targeted invitations, allowing partners to focus on specific customers and their business problems when sending Engagement invitations.</p>
    CustomerProject(crate::types::CustomerProjectsContext),
    /// <p>Contains updated information about a lead when the context type is "Lead". This field is present only when updating a lead context within the engagement.</p>
    Lead(crate::types::UpdateLeadContext),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl UpdateEngagementContextPayload {
    /// Tries to convert the enum instance into [`CustomerProject`](crate::types::UpdateEngagementContextPayload::CustomerProject), extracting the inner [`CustomerProjectsContext`](crate::types::CustomerProjectsContext).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_customer_project(&self) -> ::std::result::Result<&crate::types::CustomerProjectsContext, &Self> {
        if let UpdateEngagementContextPayload::CustomerProject(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`CustomerProject`](crate::types::UpdateEngagementContextPayload::CustomerProject).
    pub fn is_customer_project(&self) -> bool {
        self.as_customer_project().is_ok()
    }
    /// Tries to convert the enum instance into [`Lead`](crate::types::UpdateEngagementContextPayload::Lead), extracting the inner [`UpdateLeadContext`](crate::types::UpdateLeadContext).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_lead(&self) -> ::std::result::Result<&crate::types::UpdateLeadContext, &Self> {
        if let UpdateEngagementContextPayload::Lead(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Lead`](crate::types::UpdateEngagementContextPayload::Lead).
    pub fn is_lead(&self) -> bool {
        self.as_lead().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
