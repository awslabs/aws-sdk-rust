// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The connection properties patch.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum ConnectionPropertiesPatch {
    /// <p>The Amazon Athena properties of a connection properties patch.</p>
    AthenaProperties(crate::types::AthenaPropertiesPatch),
    /// <p>The Amazon Web Services Glue properties of a connection properties patch.</p>
    GlueProperties(crate::types::GluePropertiesPatch),
    /// <p>The IAM properties of a connection properties patch.</p>
    IamProperties(crate::types::IamPropertiesPatch),
    /// <p>The Amazon Redshift properties of a connection properties patch.</p>
    RedshiftProperties(crate::types::RedshiftPropertiesPatch),
    /// <p>The Amazon S3 properties of a connection properties patch.</p>
    S3Properties(crate::types::S3PropertiesPatch),
    /// <p>The Spark EMR properties of a connection properties patch.</p>
    SparkEmrProperties(crate::types::SparkEmrPropertiesPatch),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl ConnectionPropertiesPatch {
    /// Tries to convert the enum instance into [`AthenaProperties`](crate::types::ConnectionPropertiesPatch::AthenaProperties), extracting the inner [`AthenaPropertiesPatch`](crate::types::AthenaPropertiesPatch).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_athena_properties(&self) -> ::std::result::Result<&crate::types::AthenaPropertiesPatch, &Self> {
        if let ConnectionPropertiesPatch::AthenaProperties(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`AthenaProperties`](crate::types::ConnectionPropertiesPatch::AthenaProperties).
    pub fn is_athena_properties(&self) -> bool {
        self.as_athena_properties().is_ok()
    }
    /// Tries to convert the enum instance into [`GlueProperties`](crate::types::ConnectionPropertiesPatch::GlueProperties), extracting the inner [`GluePropertiesPatch`](crate::types::GluePropertiesPatch).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_glue_properties(&self) -> ::std::result::Result<&crate::types::GluePropertiesPatch, &Self> {
        if let ConnectionPropertiesPatch::GlueProperties(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`GlueProperties`](crate::types::ConnectionPropertiesPatch::GlueProperties).
    pub fn is_glue_properties(&self) -> bool {
        self.as_glue_properties().is_ok()
    }
    /// Tries to convert the enum instance into [`IamProperties`](crate::types::ConnectionPropertiesPatch::IamProperties), extracting the inner [`IamPropertiesPatch`](crate::types::IamPropertiesPatch).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_iam_properties(&self) -> ::std::result::Result<&crate::types::IamPropertiesPatch, &Self> {
        if let ConnectionPropertiesPatch::IamProperties(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`IamProperties`](crate::types::ConnectionPropertiesPatch::IamProperties).
    pub fn is_iam_properties(&self) -> bool {
        self.as_iam_properties().is_ok()
    }
    /// Tries to convert the enum instance into [`RedshiftProperties`](crate::types::ConnectionPropertiesPatch::RedshiftProperties), extracting the inner [`RedshiftPropertiesPatch`](crate::types::RedshiftPropertiesPatch).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_redshift_properties(&self) -> ::std::result::Result<&crate::types::RedshiftPropertiesPatch, &Self> {
        if let ConnectionPropertiesPatch::RedshiftProperties(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`RedshiftProperties`](crate::types::ConnectionPropertiesPatch::RedshiftProperties).
    pub fn is_redshift_properties(&self) -> bool {
        self.as_redshift_properties().is_ok()
    }
    /// Tries to convert the enum instance into [`S3Properties`](crate::types::ConnectionPropertiesPatch::S3Properties), extracting the inner [`S3PropertiesPatch`](crate::types::S3PropertiesPatch).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_s3_properties(&self) -> ::std::result::Result<&crate::types::S3PropertiesPatch, &Self> {
        if let ConnectionPropertiesPatch::S3Properties(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`S3Properties`](crate::types::ConnectionPropertiesPatch::S3Properties).
    pub fn is_s3_properties(&self) -> bool {
        self.as_s3_properties().is_ok()
    }
    /// Tries to convert the enum instance into [`SparkEmrProperties`](crate::types::ConnectionPropertiesPatch::SparkEmrProperties), extracting the inner [`SparkEmrPropertiesPatch`](crate::types::SparkEmrPropertiesPatch).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_spark_emr_properties(&self) -> ::std::result::Result<&crate::types::SparkEmrPropertiesPatch, &Self> {
        if let ConnectionPropertiesPatch::SparkEmrProperties(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`SparkEmrProperties`](crate::types::ConnectionPropertiesPatch::SparkEmrProperties).
    pub fn is_spark_emr_properties(&self) -> bool {
        self.as_spark_emr_properties().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
