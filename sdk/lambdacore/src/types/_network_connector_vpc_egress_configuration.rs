// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Configuration for a VPC egress network connector. Specifies the VPC subnets, security groups, network protocol, and associated Lambda compute resource types.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct NetworkConnectorVpcEgressConfiguration {
    /// <p>The IDs of the VPC subnets where Lambda provisions elastic network interfaces (ENIs). Specify 1 to 16 subnets. All subnets must be in the same VPC.</p>
    pub subnet_ids: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    /// <p>The IDs of the VPC security groups to attach to the ENIs. Specify 0 to 5 security groups. All security groups must be in the same VPC as the subnets.</p>
    pub security_group_ids: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    /// <p>The network protocol for the connector. Specify <code>IPv4</code> for IPv4-only networking, or <code>DualStack</code> for both IPv4 and IPv6.</p>
    pub network_protocol: ::std::option::Option<crate::types::NetworkProtocol>,
    /// <p>The types of Lambda compute resources that can use this connector. Currently, only <code>MicroVm</code> is supported.</p>
    pub associated_compute_resource_types: ::std::option::Option<::std::vec::Vec<crate::types::ComputeResourceType>>,
}
impl NetworkConnectorVpcEgressConfiguration {
    /// <p>The IDs of the VPC subnets where Lambda provisions elastic network interfaces (ENIs). Specify 1 to 16 subnets. All subnets must be in the same VPC.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.subnet_ids.is_none()`.
    pub fn subnet_ids(&self) -> &[::std::string::String] {
        self.subnet_ids.as_deref().unwrap_or_default()
    }
    /// <p>The IDs of the VPC security groups to attach to the ENIs. Specify 0 to 5 security groups. All security groups must be in the same VPC as the subnets.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.security_group_ids.is_none()`.
    pub fn security_group_ids(&self) -> &[::std::string::String] {
        self.security_group_ids.as_deref().unwrap_or_default()
    }
    /// <p>The network protocol for the connector. Specify <code>IPv4</code> for IPv4-only networking, or <code>DualStack</code> for both IPv4 and IPv6.</p>
    pub fn network_protocol(&self) -> ::std::option::Option<&crate::types::NetworkProtocol> {
        self.network_protocol.as_ref()
    }
    /// <p>The types of Lambda compute resources that can use this connector. Currently, only <code>MicroVm</code> is supported.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.associated_compute_resource_types.is_none()`.
    pub fn associated_compute_resource_types(&self) -> &[crate::types::ComputeResourceType] {
        self.associated_compute_resource_types.as_deref().unwrap_or_default()
    }
}
impl NetworkConnectorVpcEgressConfiguration {
    /// Creates a new builder-style object to manufacture [`NetworkConnectorVpcEgressConfiguration`](crate::types::NetworkConnectorVpcEgressConfiguration).
    pub fn builder() -> crate::types::builders::NetworkConnectorVpcEgressConfigurationBuilder {
        crate::types::builders::NetworkConnectorVpcEgressConfigurationBuilder::default()
    }
}

/// A builder for [`NetworkConnectorVpcEgressConfiguration`](crate::types::NetworkConnectorVpcEgressConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct NetworkConnectorVpcEgressConfigurationBuilder {
    pub(crate) subnet_ids: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    pub(crate) security_group_ids: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    pub(crate) network_protocol: ::std::option::Option<crate::types::NetworkProtocol>,
    pub(crate) associated_compute_resource_types: ::std::option::Option<::std::vec::Vec<crate::types::ComputeResourceType>>,
}
impl NetworkConnectorVpcEgressConfigurationBuilder {
    /// Appends an item to `subnet_ids`.
    ///
    /// To override the contents of this collection use [`set_subnet_ids`](Self::set_subnet_ids).
    ///
    /// <p>The IDs of the VPC subnets where Lambda provisions elastic network interfaces (ENIs). Specify 1 to 16 subnets. All subnets must be in the same VPC.</p>
    pub fn subnet_ids(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.subnet_ids.unwrap_or_default();
        v.push(input.into());
        self.subnet_ids = ::std::option::Option::Some(v);
        self
    }
    /// <p>The IDs of the VPC subnets where Lambda provisions elastic network interfaces (ENIs). Specify 1 to 16 subnets. All subnets must be in the same VPC.</p>
    pub fn set_subnet_ids(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.subnet_ids = input;
        self
    }
    /// <p>The IDs of the VPC subnets where Lambda provisions elastic network interfaces (ENIs). Specify 1 to 16 subnets. All subnets must be in the same VPC.</p>
    pub fn get_subnet_ids(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.subnet_ids
    }
    /// Appends an item to `security_group_ids`.
    ///
    /// To override the contents of this collection use [`set_security_group_ids`](Self::set_security_group_ids).
    ///
    /// <p>The IDs of the VPC security groups to attach to the ENIs. Specify 0 to 5 security groups. All security groups must be in the same VPC as the subnets.</p>
    pub fn security_group_ids(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.security_group_ids.unwrap_or_default();
        v.push(input.into());
        self.security_group_ids = ::std::option::Option::Some(v);
        self
    }
    /// <p>The IDs of the VPC security groups to attach to the ENIs. Specify 0 to 5 security groups. All security groups must be in the same VPC as the subnets.</p>
    pub fn set_security_group_ids(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.security_group_ids = input;
        self
    }
    /// <p>The IDs of the VPC security groups to attach to the ENIs. Specify 0 to 5 security groups. All security groups must be in the same VPC as the subnets.</p>
    pub fn get_security_group_ids(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.security_group_ids
    }
    /// <p>The network protocol for the connector. Specify <code>IPv4</code> for IPv4-only networking, or <code>DualStack</code> for both IPv4 and IPv6.</p>
    pub fn network_protocol(mut self, input: crate::types::NetworkProtocol) -> Self {
        self.network_protocol = ::std::option::Option::Some(input);
        self
    }
    /// <p>The network protocol for the connector. Specify <code>IPv4</code> for IPv4-only networking, or <code>DualStack</code> for both IPv4 and IPv6.</p>
    pub fn set_network_protocol(mut self, input: ::std::option::Option<crate::types::NetworkProtocol>) -> Self {
        self.network_protocol = input;
        self
    }
    /// <p>The network protocol for the connector. Specify <code>IPv4</code> for IPv4-only networking, or <code>DualStack</code> for both IPv4 and IPv6.</p>
    pub fn get_network_protocol(&self) -> &::std::option::Option<crate::types::NetworkProtocol> {
        &self.network_protocol
    }
    /// Appends an item to `associated_compute_resource_types`.
    ///
    /// To override the contents of this collection use [`set_associated_compute_resource_types`](Self::set_associated_compute_resource_types).
    ///
    /// <p>The types of Lambda compute resources that can use this connector. Currently, only <code>MicroVm</code> is supported.</p>
    pub fn associated_compute_resource_types(mut self, input: crate::types::ComputeResourceType) -> Self {
        let mut v = self.associated_compute_resource_types.unwrap_or_default();
        v.push(input);
        self.associated_compute_resource_types = ::std::option::Option::Some(v);
        self
    }
    /// <p>The types of Lambda compute resources that can use this connector. Currently, only <code>MicroVm</code> is supported.</p>
    pub fn set_associated_compute_resource_types(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::ComputeResourceType>>) -> Self {
        self.associated_compute_resource_types = input;
        self
    }
    /// <p>The types of Lambda compute resources that can use this connector. Currently, only <code>MicroVm</code> is supported.</p>
    pub fn get_associated_compute_resource_types(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::ComputeResourceType>> {
        &self.associated_compute_resource_types
    }
    /// Consumes the builder and constructs a [`NetworkConnectorVpcEgressConfiguration`](crate::types::NetworkConnectorVpcEgressConfiguration).
    pub fn build(self) -> crate::types::NetworkConnectorVpcEgressConfiguration {
        crate::types::NetworkConnectorVpcEgressConfiguration {
            subnet_ids: self.subnet_ids,
            security_group_ids: self.security_group_ids,
            network_protocol: self.network_protocol,
            associated_compute_resource_types: self.associated_compute_resource_types,
        }
    }
}
