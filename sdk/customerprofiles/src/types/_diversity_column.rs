// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Defines a diversity constraint for a single item column, specifying a cap type and a target value or placeholder that controls how many recommended items may share the same column value.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct DiversityColumn {
    /// <p>The name of the item catalog column on which to apply the diversity cap. The column must be defined in the recommender schema.</p>
    pub name: ::std::string::String,
    /// <p>The type of diversity cap to apply. Valid values are <code>PERCENTAGE</code> (interpret <code>Target</code> as a percentage of returned items) and <code>VALUE</code> (interpret <code>Target</code> as an absolute count).</p>
    pub cap_type: crate::types::DiversityCapType,
    /// <p>The diversity cap target. Either an integer literal (for example, <code>"25"</code>) or a placeholder expression of the form <code>$name</code> whose value is supplied at inference time through <code>GetProfileRecommendations</code>.</p>
    pub target: ::std::string::String,
}
impl DiversityColumn {
    /// <p>The name of the item catalog column on which to apply the diversity cap. The column must be defined in the recommender schema.</p>
    pub fn name(&self) -> &str {
        use std::ops::Deref;
        self.name.deref()
    }
    /// <p>The type of diversity cap to apply. Valid values are <code>PERCENTAGE</code> (interpret <code>Target</code> as a percentage of returned items) and <code>VALUE</code> (interpret <code>Target</code> as an absolute count).</p>
    pub fn cap_type(&self) -> &crate::types::DiversityCapType {
        &self.cap_type
    }
    /// <p>The diversity cap target. Either an integer literal (for example, <code>"25"</code>) or a placeholder expression of the form <code>$name</code> whose value is supplied at inference time through <code>GetProfileRecommendations</code>.</p>
    pub fn target(&self) -> &str {
        use std::ops::Deref;
        self.target.deref()
    }
}
impl DiversityColumn {
    /// Creates a new builder-style object to manufacture [`DiversityColumn`](crate::types::DiversityColumn).
    pub fn builder() -> crate::types::builders::DiversityColumnBuilder {
        crate::types::builders::DiversityColumnBuilder::default()
    }
}

/// A builder for [`DiversityColumn`](crate::types::DiversityColumn).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct DiversityColumnBuilder {
    pub(crate) name: ::std::option::Option<::std::string::String>,
    pub(crate) cap_type: ::std::option::Option<crate::types::DiversityCapType>,
    pub(crate) target: ::std::option::Option<::std::string::String>,
}
impl DiversityColumnBuilder {
    /// <p>The name of the item catalog column on which to apply the diversity cap. The column must be defined in the recommender schema.</p>
    /// This field is required.
    pub fn name(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.name = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The name of the item catalog column on which to apply the diversity cap. The column must be defined in the recommender schema.</p>
    pub fn set_name(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.name = input;
        self
    }
    /// <p>The name of the item catalog column on which to apply the diversity cap. The column must be defined in the recommender schema.</p>
    pub fn get_name(&self) -> &::std::option::Option<::std::string::String> {
        &self.name
    }
    /// <p>The type of diversity cap to apply. Valid values are <code>PERCENTAGE</code> (interpret <code>Target</code> as a percentage of returned items) and <code>VALUE</code> (interpret <code>Target</code> as an absolute count).</p>
    /// This field is required.
    pub fn cap_type(mut self, input: crate::types::DiversityCapType) -> Self {
        self.cap_type = ::std::option::Option::Some(input);
        self
    }
    /// <p>The type of diversity cap to apply. Valid values are <code>PERCENTAGE</code> (interpret <code>Target</code> as a percentage of returned items) and <code>VALUE</code> (interpret <code>Target</code> as an absolute count).</p>
    pub fn set_cap_type(mut self, input: ::std::option::Option<crate::types::DiversityCapType>) -> Self {
        self.cap_type = input;
        self
    }
    /// <p>The type of diversity cap to apply. Valid values are <code>PERCENTAGE</code> (interpret <code>Target</code> as a percentage of returned items) and <code>VALUE</code> (interpret <code>Target</code> as an absolute count).</p>
    pub fn get_cap_type(&self) -> &::std::option::Option<crate::types::DiversityCapType> {
        &self.cap_type
    }
    /// <p>The diversity cap target. Either an integer literal (for example, <code>"25"</code>) or a placeholder expression of the form <code>$name</code> whose value is supplied at inference time through <code>GetProfileRecommendations</code>.</p>
    /// This field is required.
    pub fn target(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.target = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The diversity cap target. Either an integer literal (for example, <code>"25"</code>) or a placeholder expression of the form <code>$name</code> whose value is supplied at inference time through <code>GetProfileRecommendations</code>.</p>
    pub fn set_target(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.target = input;
        self
    }
    /// <p>The diversity cap target. Either an integer literal (for example, <code>"25"</code>) or a placeholder expression of the form <code>$name</code> whose value is supplied at inference time through <code>GetProfileRecommendations</code>.</p>
    pub fn get_target(&self) -> &::std::option::Option<::std::string::String> {
        &self.target
    }
    /// Consumes the builder and constructs a [`DiversityColumn`](crate::types::DiversityColumn).
    /// This method will fail if any of the following fields are not set:
    /// - [`name`](crate::types::builders::DiversityColumnBuilder::name)
    /// - [`cap_type`](crate::types::builders::DiversityColumnBuilder::cap_type)
    /// - [`target`](crate::types::builders::DiversityColumnBuilder::target)
    pub fn build(self) -> ::std::result::Result<crate::types::DiversityColumn, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::DiversityColumn {
            name: self.name.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "name",
                    "name was not specified but it is required when building DiversityColumn",
                )
            })?,
            cap_type: self.cap_type.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "cap_type",
                    "cap_type was not specified but it is required when building DiversityColumn",
                )
            })?,
            target: self.target.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "target",
                    "target was not specified but it is required when building DiversityColumn",
                )
            })?,
        })
    }
}
