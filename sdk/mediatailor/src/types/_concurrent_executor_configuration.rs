// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration for a <code>CONCURRENT_EXECUTOR</code> function. A <code>CONCURRENT_EXECUTOR</code> runs a set of child functions in parallel, up to a maximum concurrency, and combines their output when all functions complete. For more information about functions, see <a href="https://docs.aws.amazon.com/mediatailor/latest/ug/monetization-functions.html">Working with functions</a> in the <i>MediaTailor User Guide</i>.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ConcurrentExecutorConfiguration {
    /// <p>The expression language used to evaluate expressions in the function configuration. Set this to <code>JSONata</code>.</p>
    pub runtime: crate::types::RuntimeType,
    /// <p>A map of output bindings that controls which bindings the executor commits to the session state after all child functions complete. Each key is a namespaced output path, and each value is an expression that MediaTailor evaluates against the combined results of the child functions.</p>
    pub output: ::std::collections::HashMap<::std::string::String, ::std::string::String>,
    /// <p>The list of child functions that MediaTailor runs in parallel. Each entry specifies a child function to execute and an optional run condition expression that controls whether the function runs.</p>
    pub function_list: ::std::vec::Vec<crate::types::FunctionRef>,
    /// <p>The maximum time, in milliseconds, for all child functions to complete. This timeout covers every function in the list, including any HTTP calls the child functions make. If the executor exceeds this timeout, MediaTailor discards all output from the executor and proceeds with default behavior.</p>
    pub timeout_milliseconds: i32,
    /// <p>The maximum number of child functions that MediaTailor runs simultaneously. When the list contains more functions than <code>MaxConcurrency</code>, MediaTailor starts additional functions as running ones complete, so that no more than <code>MaxConcurrency</code> functions run at the same time.</p>
    pub max_concurrency: i32,
}
impl ConcurrentExecutorConfiguration {
    /// <p>The expression language used to evaluate expressions in the function configuration. Set this to <code>JSONata</code>.</p>
    pub fn runtime(&self) -> &crate::types::RuntimeType {
        &self.runtime
    }
    /// <p>A map of output bindings that controls which bindings the executor commits to the session state after all child functions complete. Each key is a namespaced output path, and each value is an expression that MediaTailor evaluates against the combined results of the child functions.</p>
    pub fn output(&self) -> &::std::collections::HashMap<::std::string::String, ::std::string::String> {
        &self.output
    }
    /// <p>The list of child functions that MediaTailor runs in parallel. Each entry specifies a child function to execute and an optional run condition expression that controls whether the function runs.</p>
    pub fn function_list(&self) -> &[crate::types::FunctionRef] {
        use std::ops::Deref;
        self.function_list.deref()
    }
    /// <p>The maximum time, in milliseconds, for all child functions to complete. This timeout covers every function in the list, including any HTTP calls the child functions make. If the executor exceeds this timeout, MediaTailor discards all output from the executor and proceeds with default behavior.</p>
    pub fn timeout_milliseconds(&self) -> i32 {
        self.timeout_milliseconds
    }
    /// <p>The maximum number of child functions that MediaTailor runs simultaneously. When the list contains more functions than <code>MaxConcurrency</code>, MediaTailor starts additional functions as running ones complete, so that no more than <code>MaxConcurrency</code> functions run at the same time.</p>
    pub fn max_concurrency(&self) -> i32 {
        self.max_concurrency
    }
}
impl ConcurrentExecutorConfiguration {
    /// Creates a new builder-style object to manufacture [`ConcurrentExecutorConfiguration`](crate::types::ConcurrentExecutorConfiguration).
    pub fn builder() -> crate::types::builders::ConcurrentExecutorConfigurationBuilder {
        crate::types::builders::ConcurrentExecutorConfigurationBuilder::default()
    }
}

/// A builder for [`ConcurrentExecutorConfiguration`](crate::types::ConcurrentExecutorConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ConcurrentExecutorConfigurationBuilder {
    pub(crate) runtime: ::std::option::Option<crate::types::RuntimeType>,
    pub(crate) output: ::std::option::Option<::std::collections::HashMap<::std::string::String, ::std::string::String>>,
    pub(crate) function_list: ::std::option::Option<::std::vec::Vec<crate::types::FunctionRef>>,
    pub(crate) timeout_milliseconds: ::std::option::Option<i32>,
    pub(crate) max_concurrency: ::std::option::Option<i32>,
}
impl ConcurrentExecutorConfigurationBuilder {
    /// <p>The expression language used to evaluate expressions in the function configuration. Set this to <code>JSONata</code>.</p>
    /// This field is required.
    pub fn runtime(mut self, input: crate::types::RuntimeType) -> Self {
        self.runtime = ::std::option::Option::Some(input);
        self
    }
    /// <p>The expression language used to evaluate expressions in the function configuration. Set this to <code>JSONata</code>.</p>
    pub fn set_runtime(mut self, input: ::std::option::Option<crate::types::RuntimeType>) -> Self {
        self.runtime = input;
        self
    }
    /// <p>The expression language used to evaluate expressions in the function configuration. Set this to <code>JSONata</code>.</p>
    pub fn get_runtime(&self) -> &::std::option::Option<crate::types::RuntimeType> {
        &self.runtime
    }
    /// Adds a key-value pair to `output`.
    ///
    /// To override the contents of this collection use [`set_output`](Self::set_output).
    ///
    /// <p>A map of output bindings that controls which bindings the executor commits to the session state after all child functions complete. Each key is a namespaced output path, and each value is an expression that MediaTailor evaluates against the combined results of the child functions.</p>
    pub fn output(mut self, k: impl ::std::convert::Into<::std::string::String>, v: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut hash_map = self.output.unwrap_or_default();
        hash_map.insert(k.into(), v.into());
        self.output = ::std::option::Option::Some(hash_map);
        self
    }
    /// <p>A map of output bindings that controls which bindings the executor commits to the session state after all child functions complete. Each key is a namespaced output path, and each value is an expression that MediaTailor evaluates against the combined results of the child functions.</p>
    pub fn set_output(mut self, input: ::std::option::Option<::std::collections::HashMap<::std::string::String, ::std::string::String>>) -> Self {
        self.output = input;
        self
    }
    /// <p>A map of output bindings that controls which bindings the executor commits to the session state after all child functions complete. Each key is a namespaced output path, and each value is an expression that MediaTailor evaluates against the combined results of the child functions.</p>
    pub fn get_output(&self) -> &::std::option::Option<::std::collections::HashMap<::std::string::String, ::std::string::String>> {
        &self.output
    }
    /// Appends an item to `function_list`.
    ///
    /// To override the contents of this collection use [`set_function_list`](Self::set_function_list).
    ///
    /// <p>The list of child functions that MediaTailor runs in parallel. Each entry specifies a child function to execute and an optional run condition expression that controls whether the function runs.</p>
    pub fn function_list(mut self, input: crate::types::FunctionRef) -> Self {
        let mut v = self.function_list.unwrap_or_default();
        v.push(input);
        self.function_list = ::std::option::Option::Some(v);
        self
    }
    /// <p>The list of child functions that MediaTailor runs in parallel. Each entry specifies a child function to execute and an optional run condition expression that controls whether the function runs.</p>
    pub fn set_function_list(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::FunctionRef>>) -> Self {
        self.function_list = input;
        self
    }
    /// <p>The list of child functions that MediaTailor runs in parallel. Each entry specifies a child function to execute and an optional run condition expression that controls whether the function runs.</p>
    pub fn get_function_list(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::FunctionRef>> {
        &self.function_list
    }
    /// <p>The maximum time, in milliseconds, for all child functions to complete. This timeout covers every function in the list, including any HTTP calls the child functions make. If the executor exceeds this timeout, MediaTailor discards all output from the executor and proceeds with default behavior.</p>
    /// This field is required.
    pub fn timeout_milliseconds(mut self, input: i32) -> Self {
        self.timeout_milliseconds = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum time, in milliseconds, for all child functions to complete. This timeout covers every function in the list, including any HTTP calls the child functions make. If the executor exceeds this timeout, MediaTailor discards all output from the executor and proceeds with default behavior.</p>
    pub fn set_timeout_milliseconds(mut self, input: ::std::option::Option<i32>) -> Self {
        self.timeout_milliseconds = input;
        self
    }
    /// <p>The maximum time, in milliseconds, for all child functions to complete. This timeout covers every function in the list, including any HTTP calls the child functions make. If the executor exceeds this timeout, MediaTailor discards all output from the executor and proceeds with default behavior.</p>
    pub fn get_timeout_milliseconds(&self) -> &::std::option::Option<i32> {
        &self.timeout_milliseconds
    }
    /// <p>The maximum number of child functions that MediaTailor runs simultaneously. When the list contains more functions than <code>MaxConcurrency</code>, MediaTailor starts additional functions as running ones complete, so that no more than <code>MaxConcurrency</code> functions run at the same time.</p>
    /// This field is required.
    pub fn max_concurrency(mut self, input: i32) -> Self {
        self.max_concurrency = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum number of child functions that MediaTailor runs simultaneously. When the list contains more functions than <code>MaxConcurrency</code>, MediaTailor starts additional functions as running ones complete, so that no more than <code>MaxConcurrency</code> functions run at the same time.</p>
    pub fn set_max_concurrency(mut self, input: ::std::option::Option<i32>) -> Self {
        self.max_concurrency = input;
        self
    }
    /// <p>The maximum number of child functions that MediaTailor runs simultaneously. When the list contains more functions than <code>MaxConcurrency</code>, MediaTailor starts additional functions as running ones complete, so that no more than <code>MaxConcurrency</code> functions run at the same time.</p>
    pub fn get_max_concurrency(&self) -> &::std::option::Option<i32> {
        &self.max_concurrency
    }
    /// Consumes the builder and constructs a [`ConcurrentExecutorConfiguration`](crate::types::ConcurrentExecutorConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`runtime`](crate::types::builders::ConcurrentExecutorConfigurationBuilder::runtime)
    /// - [`output`](crate::types::builders::ConcurrentExecutorConfigurationBuilder::output)
    /// - [`function_list`](crate::types::builders::ConcurrentExecutorConfigurationBuilder::function_list)
    /// - [`timeout_milliseconds`](crate::types::builders::ConcurrentExecutorConfigurationBuilder::timeout_milliseconds)
    /// - [`max_concurrency`](crate::types::builders::ConcurrentExecutorConfigurationBuilder::max_concurrency)
    pub fn build(self) -> ::std::result::Result<crate::types::ConcurrentExecutorConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::ConcurrentExecutorConfiguration {
            runtime: self.runtime.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "runtime",
                    "runtime was not specified but it is required when building ConcurrentExecutorConfiguration",
                )
            })?,
            output: self.output.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "output",
                    "output was not specified but it is required when building ConcurrentExecutorConfiguration",
                )
            })?,
            function_list: self.function_list.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "function_list",
                    "function_list was not specified but it is required when building ConcurrentExecutorConfiguration",
                )
            })?,
            timeout_milliseconds: self.timeout_milliseconds.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "timeout_milliseconds",
                    "timeout_milliseconds was not specified but it is required when building ConcurrentExecutorConfiguration",
                )
            })?,
            max_concurrency: self.max_concurrency.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "max_concurrency",
                    "max_concurrency was not specified but it is required when building ConcurrentExecutorConfiguration",
                )
            })?,
        })
    }
}
