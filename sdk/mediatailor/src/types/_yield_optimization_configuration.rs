// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Configuration for Yield Optimization, which fills unsold ad inventory in ad breaks with programmatic ads from Amazon Publisher Services (APS).</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct YieldOptimizationConfiguration {
    /// <p>The minimum unfilled duration, in seconds, that must remain in an ad break before MediaTailor requests additional ads from Amazon Publisher Services (APS). For example, if set to 6 seconds, yield optimization triggers only when at least 6 seconds of unfilled time remains after the primary ad server response.</p>
    pub minimum_unfilled_duration: i32,
    /// <p>Publisher ID for an existing Amazon Publisher Services configuration. This ID must be obtained by registering with APS prior to using the Yield Optimization feature. The Publisher ID identifies your account in the APS system and is required for all bid requests.</p>
    pub publisher_id: ::std::string::String,
    /// <p>The Amazon Publisher Services (APS) region that MediaTailor sends bid requests to. Choose the region closest to your primary audience, because the selection affects both latency and the ad inventory available to you. This setting applies to the entire playback configuration, not to individual viewers. If you serve traffic across multiple regions, create a separate playback configuration for each APS region.</p>
    pub region: crate::types::ApsRegion,
    /// <p>The OpenRTB bid request template, in JSON, that MediaTailor sends to Amazon Publisher Services (APS). The template must include an <code>imp</code> array with one impression specifying <code>bidfloor</code>, an <code>app</code> object specifying <code>bundle</code> and <code>storeurl</code>, and a <code>device</code> object specifying <code>ua</code> and <code>ip</code>. Use double curly braces (for example, <code>{{player_params.user_agent}}</code>) to insert session variables and player parameters.</p>
    pub open_rtb_template: ::std::string::String,
}
impl YieldOptimizationConfiguration {
    /// <p>The minimum unfilled duration, in seconds, that must remain in an ad break before MediaTailor requests additional ads from Amazon Publisher Services (APS). For example, if set to 6 seconds, yield optimization triggers only when at least 6 seconds of unfilled time remains after the primary ad server response.</p>
    pub fn minimum_unfilled_duration(&self) -> i32 {
        self.minimum_unfilled_duration
    }
    /// <p>Publisher ID for an existing Amazon Publisher Services configuration. This ID must be obtained by registering with APS prior to using the Yield Optimization feature. The Publisher ID identifies your account in the APS system and is required for all bid requests.</p>
    pub fn publisher_id(&self) -> &str {
        use std::ops::Deref;
        self.publisher_id.deref()
    }
    /// <p>The Amazon Publisher Services (APS) region that MediaTailor sends bid requests to. Choose the region closest to your primary audience, because the selection affects both latency and the ad inventory available to you. This setting applies to the entire playback configuration, not to individual viewers. If you serve traffic across multiple regions, create a separate playback configuration for each APS region.</p>
    pub fn region(&self) -> &crate::types::ApsRegion {
        &self.region
    }
    /// <p>The OpenRTB bid request template, in JSON, that MediaTailor sends to Amazon Publisher Services (APS). The template must include an <code>imp</code> array with one impression specifying <code>bidfloor</code>, an <code>app</code> object specifying <code>bundle</code> and <code>storeurl</code>, and a <code>device</code> object specifying <code>ua</code> and <code>ip</code>. Use double curly braces (for example, <code>{{player_params.user_agent}}</code>) to insert session variables and player parameters.</p>
    pub fn open_rtb_template(&self) -> &str {
        use std::ops::Deref;
        self.open_rtb_template.deref()
    }
}
impl YieldOptimizationConfiguration {
    /// Creates a new builder-style object to manufacture [`YieldOptimizationConfiguration`](crate::types::YieldOptimizationConfiguration).
    pub fn builder() -> crate::types::builders::YieldOptimizationConfigurationBuilder {
        crate::types::builders::YieldOptimizationConfigurationBuilder::default()
    }
}

/// A builder for [`YieldOptimizationConfiguration`](crate::types::YieldOptimizationConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct YieldOptimizationConfigurationBuilder {
    pub(crate) minimum_unfilled_duration: ::std::option::Option<i32>,
    pub(crate) publisher_id: ::std::option::Option<::std::string::String>,
    pub(crate) region: ::std::option::Option<crate::types::ApsRegion>,
    pub(crate) open_rtb_template: ::std::option::Option<::std::string::String>,
}
impl YieldOptimizationConfigurationBuilder {
    /// <p>The minimum unfilled duration, in seconds, that must remain in an ad break before MediaTailor requests additional ads from Amazon Publisher Services (APS). For example, if set to 6 seconds, yield optimization triggers only when at least 6 seconds of unfilled time remains after the primary ad server response.</p>
    /// This field is required.
    pub fn minimum_unfilled_duration(mut self, input: i32) -> Self {
        self.minimum_unfilled_duration = ::std::option::Option::Some(input);
        self
    }
    /// <p>The minimum unfilled duration, in seconds, that must remain in an ad break before MediaTailor requests additional ads from Amazon Publisher Services (APS). For example, if set to 6 seconds, yield optimization triggers only when at least 6 seconds of unfilled time remains after the primary ad server response.</p>
    pub fn set_minimum_unfilled_duration(mut self, input: ::std::option::Option<i32>) -> Self {
        self.minimum_unfilled_duration = input;
        self
    }
    /// <p>The minimum unfilled duration, in seconds, that must remain in an ad break before MediaTailor requests additional ads from Amazon Publisher Services (APS). For example, if set to 6 seconds, yield optimization triggers only when at least 6 seconds of unfilled time remains after the primary ad server response.</p>
    pub fn get_minimum_unfilled_duration(&self) -> &::std::option::Option<i32> {
        &self.minimum_unfilled_duration
    }
    /// <p>Publisher ID for an existing Amazon Publisher Services configuration. This ID must be obtained by registering with APS prior to using the Yield Optimization feature. The Publisher ID identifies your account in the APS system and is required for all bid requests.</p>
    /// This field is required.
    pub fn publisher_id(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.publisher_id = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>Publisher ID for an existing Amazon Publisher Services configuration. This ID must be obtained by registering with APS prior to using the Yield Optimization feature. The Publisher ID identifies your account in the APS system and is required for all bid requests.</p>
    pub fn set_publisher_id(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.publisher_id = input;
        self
    }
    /// <p>Publisher ID for an existing Amazon Publisher Services configuration. This ID must be obtained by registering with APS prior to using the Yield Optimization feature. The Publisher ID identifies your account in the APS system and is required for all bid requests.</p>
    pub fn get_publisher_id(&self) -> &::std::option::Option<::std::string::String> {
        &self.publisher_id
    }
    /// <p>The Amazon Publisher Services (APS) region that MediaTailor sends bid requests to. Choose the region closest to your primary audience, because the selection affects both latency and the ad inventory available to you. This setting applies to the entire playback configuration, not to individual viewers. If you serve traffic across multiple regions, create a separate playback configuration for each APS region.</p>
    /// This field is required.
    pub fn region(mut self, input: crate::types::ApsRegion) -> Self {
        self.region = ::std::option::Option::Some(input);
        self
    }
    /// <p>The Amazon Publisher Services (APS) region that MediaTailor sends bid requests to. Choose the region closest to your primary audience, because the selection affects both latency and the ad inventory available to you. This setting applies to the entire playback configuration, not to individual viewers. If you serve traffic across multiple regions, create a separate playback configuration for each APS region.</p>
    pub fn set_region(mut self, input: ::std::option::Option<crate::types::ApsRegion>) -> Self {
        self.region = input;
        self
    }
    /// <p>The Amazon Publisher Services (APS) region that MediaTailor sends bid requests to. Choose the region closest to your primary audience, because the selection affects both latency and the ad inventory available to you. This setting applies to the entire playback configuration, not to individual viewers. If you serve traffic across multiple regions, create a separate playback configuration for each APS region.</p>
    pub fn get_region(&self) -> &::std::option::Option<crate::types::ApsRegion> {
        &self.region
    }
    /// <p>The OpenRTB bid request template, in JSON, that MediaTailor sends to Amazon Publisher Services (APS). The template must include an <code>imp</code> array with one impression specifying <code>bidfloor</code>, an <code>app</code> object specifying <code>bundle</code> and <code>storeurl</code>, and a <code>device</code> object specifying <code>ua</code> and <code>ip</code>. Use double curly braces (for example, <code>{{player_params.user_agent}}</code>) to insert session variables and player parameters.</p>
    /// This field is required.
    pub fn open_rtb_template(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.open_rtb_template = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The OpenRTB bid request template, in JSON, that MediaTailor sends to Amazon Publisher Services (APS). The template must include an <code>imp</code> array with one impression specifying <code>bidfloor</code>, an <code>app</code> object specifying <code>bundle</code> and <code>storeurl</code>, and a <code>device</code> object specifying <code>ua</code> and <code>ip</code>. Use double curly braces (for example, <code>{{player_params.user_agent}}</code>) to insert session variables and player parameters.</p>
    pub fn set_open_rtb_template(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.open_rtb_template = input;
        self
    }
    /// <p>The OpenRTB bid request template, in JSON, that MediaTailor sends to Amazon Publisher Services (APS). The template must include an <code>imp</code> array with one impression specifying <code>bidfloor</code>, an <code>app</code> object specifying <code>bundle</code> and <code>storeurl</code>, and a <code>device</code> object specifying <code>ua</code> and <code>ip</code>. Use double curly braces (for example, <code>{{player_params.user_agent}}</code>) to insert session variables and player parameters.</p>
    pub fn get_open_rtb_template(&self) -> &::std::option::Option<::std::string::String> {
        &self.open_rtb_template
    }
    /// Consumes the builder and constructs a [`YieldOptimizationConfiguration`](crate::types::YieldOptimizationConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`minimum_unfilled_duration`](crate::types::builders::YieldOptimizationConfigurationBuilder::minimum_unfilled_duration)
    /// - [`publisher_id`](crate::types::builders::YieldOptimizationConfigurationBuilder::publisher_id)
    /// - [`region`](crate::types::builders::YieldOptimizationConfigurationBuilder::region)
    /// - [`open_rtb_template`](crate::types::builders::YieldOptimizationConfigurationBuilder::open_rtb_template)
    pub fn build(self) -> ::std::result::Result<crate::types::YieldOptimizationConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::YieldOptimizationConfiguration {
            minimum_unfilled_duration: self.minimum_unfilled_duration.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "minimum_unfilled_duration",
                    "minimum_unfilled_duration was not specified but it is required when building YieldOptimizationConfiguration",
                )
            })?,
            publisher_id: self.publisher_id.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "publisher_id",
                    "publisher_id was not specified but it is required when building YieldOptimizationConfiguration",
                )
            })?,
            region: self.region.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "region",
                    "region was not specified but it is required when building YieldOptimizationConfiguration",
                )
            })?,
            open_rtb_template: self.open_rtb_template.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "open_rtb_template",
                    "open_rtb_template was not specified but it is required when building YieldOptimizationConfiguration",
                )
            })?,
        })
    }
}
