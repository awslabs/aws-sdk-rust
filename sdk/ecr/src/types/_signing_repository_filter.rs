// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A repository filter used to determine which repositories have their images automatically signed on push. Each filter consists of a filter type and filter value.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct SigningRepositoryFilter {
    /// <p>The filter value used to match repository names. When using <code>WILDCARD_MATCH</code>, the <code>*</code> character matches any sequence of characters.</p>
    /// <p>Examples:</p>
    /// <ul>
    /// <li>
    /// <p><code>myapp/*</code> - Matches all repositories starting with <code>myapp/</code></p></li>
    /// <li>
    /// <p><code>*/production</code> - Matches all repositories ending with <code>/production</code></p></li>
    /// <li>
    /// <p><code>*prod*</code> - Matches all repositories containing <code>prod</code></p></li>
    /// </ul>
    pub filter: ::std::string::String,
    /// <p>The type of filter to apply. Currently, only <code>WILDCARD_MATCH</code> is supported, which uses wildcard patterns to match repository names.</p>
    pub filter_type: crate::types::SigningRepositoryFilterType,
}
impl SigningRepositoryFilter {
    /// <p>The filter value used to match repository names. When using <code>WILDCARD_MATCH</code>, the <code>*</code> character matches any sequence of characters.</p>
    /// <p>Examples:</p>
    /// <ul>
    /// <li>
    /// <p><code>myapp/*</code> - Matches all repositories starting with <code>myapp/</code></p></li>
    /// <li>
    /// <p><code>*/production</code> - Matches all repositories ending with <code>/production</code></p></li>
    /// <li>
    /// <p><code>*prod*</code> - Matches all repositories containing <code>prod</code></p></li>
    /// </ul>
    pub fn filter(&self) -> &str {
        use std::ops::Deref;
        self.filter.deref()
    }
    /// <p>The type of filter to apply. Currently, only <code>WILDCARD_MATCH</code> is supported, which uses wildcard patterns to match repository names.</p>
    pub fn filter_type(&self) -> &crate::types::SigningRepositoryFilterType {
        &self.filter_type
    }
}
impl SigningRepositoryFilter {
    /// Creates a new builder-style object to manufacture [`SigningRepositoryFilter`](crate::types::SigningRepositoryFilter).
    pub fn builder() -> crate::types::builders::SigningRepositoryFilterBuilder {
        crate::types::builders::SigningRepositoryFilterBuilder::default()
    }
}

/// A builder for [`SigningRepositoryFilter`](crate::types::SigningRepositoryFilter).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct SigningRepositoryFilterBuilder {
    pub(crate) filter: ::std::option::Option<::std::string::String>,
    pub(crate) filter_type: ::std::option::Option<crate::types::SigningRepositoryFilterType>,
}
impl SigningRepositoryFilterBuilder {
    /// <p>The filter value used to match repository names. When using <code>WILDCARD_MATCH</code>, the <code>*</code> character matches any sequence of characters.</p>
    /// <p>Examples:</p>
    /// <ul>
    /// <li>
    /// <p><code>myapp/*</code> - Matches all repositories starting with <code>myapp/</code></p></li>
    /// <li>
    /// <p><code>*/production</code> - Matches all repositories ending with <code>/production</code></p></li>
    /// <li>
    /// <p><code>*prod*</code> - Matches all repositories containing <code>prod</code></p></li>
    /// </ul>
    /// This field is required.
    pub fn filter(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.filter = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The filter value used to match repository names. When using <code>WILDCARD_MATCH</code>, the <code>*</code> character matches any sequence of characters.</p>
    /// <p>Examples:</p>
    /// <ul>
    /// <li>
    /// <p><code>myapp/*</code> - Matches all repositories starting with <code>myapp/</code></p></li>
    /// <li>
    /// <p><code>*/production</code> - Matches all repositories ending with <code>/production</code></p></li>
    /// <li>
    /// <p><code>*prod*</code> - Matches all repositories containing <code>prod</code></p></li>
    /// </ul>
    pub fn set_filter(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.filter = input;
        self
    }
    /// <p>The filter value used to match repository names. When using <code>WILDCARD_MATCH</code>, the <code>*</code> character matches any sequence of characters.</p>
    /// <p>Examples:</p>
    /// <ul>
    /// <li>
    /// <p><code>myapp/*</code> - Matches all repositories starting with <code>myapp/</code></p></li>
    /// <li>
    /// <p><code>*/production</code> - Matches all repositories ending with <code>/production</code></p></li>
    /// <li>
    /// <p><code>*prod*</code> - Matches all repositories containing <code>prod</code></p></li>
    /// </ul>
    pub fn get_filter(&self) -> &::std::option::Option<::std::string::String> {
        &self.filter
    }
    /// <p>The type of filter to apply. Currently, only <code>WILDCARD_MATCH</code> is supported, which uses wildcard patterns to match repository names.</p>
    /// This field is required.
    pub fn filter_type(mut self, input: crate::types::SigningRepositoryFilterType) -> Self {
        self.filter_type = ::std::option::Option::Some(input);
        self
    }
    /// <p>The type of filter to apply. Currently, only <code>WILDCARD_MATCH</code> is supported, which uses wildcard patterns to match repository names.</p>
    pub fn set_filter_type(mut self, input: ::std::option::Option<crate::types::SigningRepositoryFilterType>) -> Self {
        self.filter_type = input;
        self
    }
    /// <p>The type of filter to apply. Currently, only <code>WILDCARD_MATCH</code> is supported, which uses wildcard patterns to match repository names.</p>
    pub fn get_filter_type(&self) -> &::std::option::Option<crate::types::SigningRepositoryFilterType> {
        &self.filter_type
    }
    /// Consumes the builder and constructs a [`SigningRepositoryFilter`](crate::types::SigningRepositoryFilter).
    /// This method will fail if any of the following fields are not set:
    /// - [`filter`](crate::types::builders::SigningRepositoryFilterBuilder::filter)
    /// - [`filter_type`](crate::types::builders::SigningRepositoryFilterBuilder::filter_type)
    pub fn build(self) -> ::std::result::Result<crate::types::SigningRepositoryFilter, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::SigningRepositoryFilter {
            filter: self.filter.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "filter",
                    "filter was not specified but it is required when building SigningRepositoryFilter",
                )
            })?,
            filter_type: self.filter_type.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "filter_type",
                    "filter_type was not specified but it is required when building SigningRepositoryFilter",
                )
            })?,
        })
    }
}
