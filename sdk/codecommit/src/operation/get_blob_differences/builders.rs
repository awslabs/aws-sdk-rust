// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
pub use crate::operation::get_blob_differences::_get_blob_differences_input::GetBlobDifferencesInputBuilder;

pub use crate::operation::get_blob_differences::_get_blob_differences_output::GetBlobDifferencesOutputBuilder;

impl crate::operation::get_blob_differences::builders::GetBlobDifferencesInputBuilder {
    /// Sends a request with this input using the given client.
    pub async fn send_with(
        self,
        client: &crate::Client,
    ) -> ::std::result::Result<
        crate::operation::get_blob_differences::GetBlobDifferencesOutput,
        ::aws_smithy_runtime_api::client::result::SdkError<
            crate::operation::get_blob_differences::GetBlobDifferencesError,
            ::aws_smithy_runtime_api::client::orchestrator::HttpResponse,
        >,
    > {
        let mut fluent_builder = client.get_blob_differences();
        fluent_builder.inner = self;
        fluent_builder.send().await
    }
}
/// Fluent builder constructing a request to `GetBlobDifferences`.
///
/// <p>Returns a structured, line-level diff between two blob versions in a repository. The diff is returned as an ordered list of hunks, where each hunk represents a contiguous run of changed lines together with any surrounding unchanged context lines.</p>
/// <p>Results are paginated. Use <code>MaxResults</code> and <code>NextToken</code> to retrieve additional pages.</p>
/// <p>For the typical usage workflow, see <code>GetDifferences</code>.</p>
#[derive(::std::clone::Clone, ::std::fmt::Debug)]
pub struct GetBlobDifferencesFluentBuilder {
    handle: ::std::sync::Arc<crate::client::Handle>,
    inner: crate::operation::get_blob_differences::builders::GetBlobDifferencesInputBuilder,
    config_override: ::std::option::Option<crate::config::Builder>,
}
impl
    crate::client::customize::internal::CustomizableSend<
        crate::operation::get_blob_differences::GetBlobDifferencesOutput,
        crate::operation::get_blob_differences::GetBlobDifferencesError,
    > for GetBlobDifferencesFluentBuilder
{
    fn send(
        self,
        config_override: crate::config::Builder,
    ) -> crate::client::customize::internal::BoxFuture<
        crate::client::customize::internal::SendResult<
            crate::operation::get_blob_differences::GetBlobDifferencesOutput,
            crate::operation::get_blob_differences::GetBlobDifferencesError,
        >,
    > {
        ::std::boxed::Box::pin(async move { self.config_override(config_override).send().await })
    }
}
impl GetBlobDifferencesFluentBuilder {
    /// Creates a new `GetBlobDifferencesFluentBuilder`.
    pub(crate) fn new(handle: ::std::sync::Arc<crate::client::Handle>) -> Self {
        Self {
            handle,
            inner: ::std::default::Default::default(),
            config_override: ::std::option::Option::None,
        }
    }
    /// Access the GetBlobDifferences as a reference.
    pub fn as_input(&self) -> &crate::operation::get_blob_differences::builders::GetBlobDifferencesInputBuilder {
        &self.inner
    }
    /// Sends the request and returns the response.
    ///
    /// If an error occurs, an `SdkError` will be returned with additional details that
    /// can be matched against.
    ///
    /// By default, any retryable failures will be retried twice. Retry behavior
    /// is configurable with the [RetryConfig](aws_smithy_types::retry::RetryConfig), which can be
    /// set when configuring the client.
    pub async fn send(
        self,
    ) -> ::std::result::Result<
        crate::operation::get_blob_differences::GetBlobDifferencesOutput,
        ::aws_smithy_runtime_api::client::result::SdkError<
            crate::operation::get_blob_differences::GetBlobDifferencesError,
            ::aws_smithy_runtime_api::client::orchestrator::HttpResponse,
        >,
    > {
        let input = self
            .inner
            .build()
            .map_err(::aws_smithy_runtime_api::client::result::SdkError::construction_failure)?;
        let runtime_plugins = crate::operation::get_blob_differences::GetBlobDifferences::operation_runtime_plugins(
            self.handle.runtime_plugins.clone(),
            &self.handle.conf,
            self.config_override,
        );
        crate::operation::get_blob_differences::GetBlobDifferences::orchestrate(&runtime_plugins, input).await
    }

    /// Consumes this builder, creating a customizable operation that can be modified before being sent.
    pub fn customize(
        self,
    ) -> crate::client::customize::CustomizableOperation<
        crate::operation::get_blob_differences::GetBlobDifferencesOutput,
        crate::operation::get_blob_differences::GetBlobDifferencesError,
        Self,
    > {
        crate::client::customize::CustomizableOperation::new(self)
    }
    pub(crate) fn config_override(mut self, config_override: impl ::std::convert::Into<crate::config::Builder>) -> Self {
        self.set_config_override(::std::option::Option::Some(config_override.into()));
        self
    }

    pub(crate) fn set_config_override(&mut self, config_override: ::std::option::Option<crate::config::Builder>) -> &mut Self {
        self.config_override = config_override;
        self
    }
    /// Create a paginator for this request
    ///
    /// Paginators are used by calling [`send().await`](crate::operation::get_blob_differences::paginator::GetBlobDifferencesPaginator::send) which returns a [`PaginationStream`](aws_smithy_async::future::pagination_stream::PaginationStream).
    pub fn into_paginator(self) -> crate::operation::get_blob_differences::paginator::GetBlobDifferencesPaginator {
        crate::operation::get_blob_differences::paginator::GetBlobDifferencesPaginator::new(self.handle, self.inner)
    }
    /// <p>The name of the repository that contains the blobs to compare.</p>
    pub fn repository_name(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.inner = self.inner.repository_name(input.into());
        self
    }
    /// <p>The name of the repository that contains the blobs to compare.</p>
    pub fn set_repository_name(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.inner = self.inner.set_repository_name(input);
        self
    }
    /// <p>The name of the repository that contains the blobs to compare.</p>
    pub fn get_repository_name(&self) -> &::std::option::Option<::std::string::String> {
        self.inner.get_repository_name()
    }
    /// <p>The ID of the "after" (destination) blob in the diff. Typically the value of <code>afterBlob.blobId</code> from a <code>Difference</code> object returned by <code>GetDifferences</code>.</p>
    pub fn after_blob_id(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.inner = self.inner.after_blob_id(input.into());
        self
    }
    /// <p>The ID of the "after" (destination) blob in the diff. Typically the value of <code>afterBlob.blobId</code> from a <code>Difference</code> object returned by <code>GetDifferences</code>.</p>
    pub fn set_after_blob_id(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.inner = self.inner.set_after_blob_id(input);
        self
    }
    /// <p>The ID of the "after" (destination) blob in the diff. Typically the value of <code>afterBlob.blobId</code> from a <code>Difference</code> object returned by <code>GetDifferences</code>.</p>
    pub fn get_after_blob_id(&self) -> &::std::option::Option<::std::string::String> {
        self.inner.get_after_blob_id()
    }
    /// <p>The ID of the "before" (source) blob in the diff. Typically the value of <code>beforeBlob.blobId</code> from a <code>Difference</code> object returned by <code>GetDifferences</code>.</p>
    /// <p>If you do not specify a value, the operation returns a diff against an empty before-state. This is equivalent to treating the file as newly added.</p>
    pub fn before_blob_id(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.inner = self.inner.before_blob_id(input.into());
        self
    }
    /// <p>The ID of the "before" (source) blob in the diff. Typically the value of <code>beforeBlob.blobId</code> from a <code>Difference</code> object returned by <code>GetDifferences</code>.</p>
    /// <p>If you do not specify a value, the operation returns a diff against an empty before-state. This is equivalent to treating the file as newly added.</p>
    pub fn set_before_blob_id(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.inner = self.inner.set_before_blob_id(input);
        self
    }
    /// <p>The ID of the "before" (source) blob in the diff. Typically the value of <code>beforeBlob.blobId</code> from a <code>Difference</code> object returned by <code>GetDifferences</code>.</p>
    /// <p>If you do not specify a value, the operation returns a diff against an empty before-state. This is equivalent to treating the file as newly added.</p>
    pub fn get_before_blob_id(&self) -> &::std::option::Option<::std::string::String> {
        self.inner.get_before_blob_id()
    }
    /// <p>The number of unchanged lines of context to include before and after each block of changes in a hunk. Valid values are 0 through 20. Defaults to <code>3</code>.</p>
    pub fn context_lines(mut self, input: i32) -> Self {
        self.inner = self.inner.context_lines(input);
        self
    }
    /// <p>The number of unchanged lines of context to include before and after each block of changes in a hunk. Valid values are 0 through 20. Defaults to <code>3</code>.</p>
    pub fn set_context_lines(mut self, input: ::std::option::Option<i32>) -> Self {
        self.inner = self.inner.set_context_lines(input);
        self
    }
    /// <p>The number of unchanged lines of context to include before and after each block of changes in a hunk. Valid values are 0 through 20. Defaults to <code>3</code>.</p>
    pub fn get_context_lines(&self) -> &::std::option::Option<i32> {
        self.inner.get_context_lines()
    }
    /// <p>Specifies whether to ignore whitespace-only changes when computing the diff. When <code>true</code>, the operation treats lines that differ only in whitespace as unchanged. Defaults to <code>false</code>.</p>
    pub fn ignore_whitespace(mut self, input: bool) -> Self {
        self.inner = self.inner.ignore_whitespace(input);
        self
    }
    /// <p>Specifies whether to ignore whitespace-only changes when computing the diff. When <code>true</code>, the operation treats lines that differ only in whitespace as unchanged. Defaults to <code>false</code>.</p>
    pub fn set_ignore_whitespace(mut self, input: ::std::option::Option<bool>) -> Self {
        self.inner = self.inner.set_ignore_whitespace(input);
        self
    }
    /// <p>Specifies whether to ignore whitespace-only changes when computing the diff. When <code>true</code>, the operation treats lines that differ only in whitespace as unchanged. Defaults to <code>false</code>.</p>
    pub fn get_ignore_whitespace(&self) -> &::std::option::Option<bool> {
        self.inner.get_ignore_whitespace()
    }
    /// <p>The maximum number of <code>DiffHunk</code> entries to return in a single response page. Defaults to <code>100</code>.</p>
    pub fn max_results(mut self, input: i32) -> Self {
        self.inner = self.inner.max_results(input);
        self
    }
    /// <p>The maximum number of <code>DiffHunk</code> entries to return in a single response page. Defaults to <code>100</code>.</p>
    pub fn set_max_results(mut self, input: ::std::option::Option<i32>) -> Self {
        self.inner = self.inner.set_max_results(input);
        self
    }
    /// <p>The maximum number of <code>DiffHunk</code> entries to return in a single response page. Defaults to <code>100</code>.</p>
    pub fn get_max_results(&self) -> &::std::option::Option<i32> {
        self.inner.get_max_results()
    }
    /// <p>An enumeration token that returns the next batch of results when present in a request.</p>
    pub fn next_token(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.inner = self.inner.next_token(input.into());
        self
    }
    /// <p>An enumeration token that returns the next batch of results when present in a request.</p>
    pub fn set_next_token(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.inner = self.inner.set_next_token(input);
        self
    }
    /// <p>An enumeration token that returns the next batch of results when present in a request.</p>
    pub fn get_next_token(&self) -> &::std::option::Option<::std::string::String> {
        self.inner.get_next_token()
    }
}
