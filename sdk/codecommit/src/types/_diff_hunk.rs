// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A contiguous run of changed lines from a blob diff, together with any surrounding unchanged context lines. Hunks are returned in order from the start of the file to the end. Adjacent or overlapping hunks are merged into a single hunk in the response.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct DiffHunk {
    /// <p>The 1-based line number in the before blob where this hunk begins. When the hunk consists entirely of additions, <code>beforeLineCount</code> is <code>0</code>.</p>
    pub before_start_line: ::std::option::Option<i32>,
    /// <p>The number of lines from the before blob covered by this hunk, including any context lines.</p>
    pub before_line_count: ::std::option::Option<i32>,
    /// <p>The 1-based line number in the after blob where this hunk begins. When the hunk consists entirely of deletions, <code>afterLineCount</code> is <code>0</code>.</p>
    pub after_start_line: ::std::option::Option<i32>,
    /// <p>The number of lines from the after blob covered by this hunk, including any context lines.</p>
    pub after_line_count: ::std::option::Option<i32>,
    /// <p>An ordered list of line-level changes that make up this hunk. Each entry indicates whether the line is unchanged context, an addition, or a deletion.</p>
    pub changes: ::std::option::Option<::std::vec::Vec<crate::types::DiffChange>>,
}
impl DiffHunk {
    /// <p>The 1-based line number in the before blob where this hunk begins. When the hunk consists entirely of additions, <code>beforeLineCount</code> is <code>0</code>.</p>
    pub fn before_start_line(&self) -> ::std::option::Option<i32> {
        self.before_start_line
    }
    /// <p>The number of lines from the before blob covered by this hunk, including any context lines.</p>
    pub fn before_line_count(&self) -> ::std::option::Option<i32> {
        self.before_line_count
    }
    /// <p>The 1-based line number in the after blob where this hunk begins. When the hunk consists entirely of deletions, <code>afterLineCount</code> is <code>0</code>.</p>
    pub fn after_start_line(&self) -> ::std::option::Option<i32> {
        self.after_start_line
    }
    /// <p>The number of lines from the after blob covered by this hunk, including any context lines.</p>
    pub fn after_line_count(&self) -> ::std::option::Option<i32> {
        self.after_line_count
    }
    /// <p>An ordered list of line-level changes that make up this hunk. Each entry indicates whether the line is unchanged context, an addition, or a deletion.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.changes.is_none()`.
    pub fn changes(&self) -> &[crate::types::DiffChange] {
        self.changes.as_deref().unwrap_or_default()
    }
}
impl DiffHunk {
    /// Creates a new builder-style object to manufacture [`DiffHunk`](crate::types::DiffHunk).
    pub fn builder() -> crate::types::builders::DiffHunkBuilder {
        crate::types::builders::DiffHunkBuilder::default()
    }
}

/// A builder for [`DiffHunk`](crate::types::DiffHunk).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct DiffHunkBuilder {
    pub(crate) before_start_line: ::std::option::Option<i32>,
    pub(crate) before_line_count: ::std::option::Option<i32>,
    pub(crate) after_start_line: ::std::option::Option<i32>,
    pub(crate) after_line_count: ::std::option::Option<i32>,
    pub(crate) changes: ::std::option::Option<::std::vec::Vec<crate::types::DiffChange>>,
}
impl DiffHunkBuilder {
    /// <p>The 1-based line number in the before blob where this hunk begins. When the hunk consists entirely of additions, <code>beforeLineCount</code> is <code>0</code>.</p>
    pub fn before_start_line(mut self, input: i32) -> Self {
        self.before_start_line = ::std::option::Option::Some(input);
        self
    }
    /// <p>The 1-based line number in the before blob where this hunk begins. When the hunk consists entirely of additions, <code>beforeLineCount</code> is <code>0</code>.</p>
    pub fn set_before_start_line(mut self, input: ::std::option::Option<i32>) -> Self {
        self.before_start_line = input;
        self
    }
    /// <p>The 1-based line number in the before blob where this hunk begins. When the hunk consists entirely of additions, <code>beforeLineCount</code> is <code>0</code>.</p>
    pub fn get_before_start_line(&self) -> &::std::option::Option<i32> {
        &self.before_start_line
    }
    /// <p>The number of lines from the before blob covered by this hunk, including any context lines.</p>
    pub fn before_line_count(mut self, input: i32) -> Self {
        self.before_line_count = ::std::option::Option::Some(input);
        self
    }
    /// <p>The number of lines from the before blob covered by this hunk, including any context lines.</p>
    pub fn set_before_line_count(mut self, input: ::std::option::Option<i32>) -> Self {
        self.before_line_count = input;
        self
    }
    /// <p>The number of lines from the before blob covered by this hunk, including any context lines.</p>
    pub fn get_before_line_count(&self) -> &::std::option::Option<i32> {
        &self.before_line_count
    }
    /// <p>The 1-based line number in the after blob where this hunk begins. When the hunk consists entirely of deletions, <code>afterLineCount</code> is <code>0</code>.</p>
    pub fn after_start_line(mut self, input: i32) -> Self {
        self.after_start_line = ::std::option::Option::Some(input);
        self
    }
    /// <p>The 1-based line number in the after blob where this hunk begins. When the hunk consists entirely of deletions, <code>afterLineCount</code> is <code>0</code>.</p>
    pub fn set_after_start_line(mut self, input: ::std::option::Option<i32>) -> Self {
        self.after_start_line = input;
        self
    }
    /// <p>The 1-based line number in the after blob where this hunk begins. When the hunk consists entirely of deletions, <code>afterLineCount</code> is <code>0</code>.</p>
    pub fn get_after_start_line(&self) -> &::std::option::Option<i32> {
        &self.after_start_line
    }
    /// <p>The number of lines from the after blob covered by this hunk, including any context lines.</p>
    pub fn after_line_count(mut self, input: i32) -> Self {
        self.after_line_count = ::std::option::Option::Some(input);
        self
    }
    /// <p>The number of lines from the after blob covered by this hunk, including any context lines.</p>
    pub fn set_after_line_count(mut self, input: ::std::option::Option<i32>) -> Self {
        self.after_line_count = input;
        self
    }
    /// <p>The number of lines from the after blob covered by this hunk, including any context lines.</p>
    pub fn get_after_line_count(&self) -> &::std::option::Option<i32> {
        &self.after_line_count
    }
    /// Appends an item to `changes`.
    ///
    /// To override the contents of this collection use [`set_changes`](Self::set_changes).
    ///
    /// <p>An ordered list of line-level changes that make up this hunk. Each entry indicates whether the line is unchanged context, an addition, or a deletion.</p>
    pub fn changes(mut self, input: crate::types::DiffChange) -> Self {
        let mut v = self.changes.unwrap_or_default();
        v.push(input);
        self.changes = ::std::option::Option::Some(v);
        self
    }
    /// <p>An ordered list of line-level changes that make up this hunk. Each entry indicates whether the line is unchanged context, an addition, or a deletion.</p>
    pub fn set_changes(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::DiffChange>>) -> Self {
        self.changes = input;
        self
    }
    /// <p>An ordered list of line-level changes that make up this hunk. Each entry indicates whether the line is unchanged context, an addition, or a deletion.</p>
    pub fn get_changes(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::DiffChange>> {
        &self.changes
    }
    /// Consumes the builder and constructs a [`DiffHunk`](crate::types::DiffHunk).
    pub fn build(self) -> crate::types::DiffHunk {
        crate::types::DiffHunk {
            before_start_line: self.before_start_line,
            before_line_count: self.before_line_count,
            after_start_line: self.after_start_line,
            after_line_count: self.after_line_count,
            changes: self.changes,
        }
    }
}
