// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct GetTemporaryDataLocationCredentialsOutput {
    /// <p>A temporary set of credentials for an Lake Formation user. These credentials are scoped down to only access the raw data sources that the user has access to.</p>
    /// <p>The temporary security credentials consist of an access key and a session token. The access key consists of an access key ID and a secret key. When the credentials are created, they are associated with an IAM access control policy that limits what the user can do when using the credentials.</p>
    pub credentials: ::std::option::Option<crate::types::TemporaryCredentials>,
    /// <p>Refers to the Amazon S3 locations that can be accessed through the <code>GetTemporaryCredentialsForLocation</code> API operation.</p>
    pub accessible_data_locations: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    /// <p>The credential scope is determined by the caller's Lake Formation permission on the associated table. Credential scope can be either:</p>
    /// <ul>
    /// <li>
    /// <p>READ - Provides read-only access to the data location.</p></li>
    /// <li>
    /// <p>READ_WRITE - Provides both read and write access to the data location.</p></li>
    /// </ul>
    pub credentials_scope: ::std::option::Option<crate::types::CredentialsScope>,
    _request_id: Option<String>,
}
impl GetTemporaryDataLocationCredentialsOutput {
    /// <p>A temporary set of credentials for an Lake Formation user. These credentials are scoped down to only access the raw data sources that the user has access to.</p>
    /// <p>The temporary security credentials consist of an access key and a session token. The access key consists of an access key ID and a secret key. When the credentials are created, they are associated with an IAM access control policy that limits what the user can do when using the credentials.</p>
    pub fn credentials(&self) -> ::std::option::Option<&crate::types::TemporaryCredentials> {
        self.credentials.as_ref()
    }
    /// <p>Refers to the Amazon S3 locations that can be accessed through the <code>GetTemporaryCredentialsForLocation</code> API operation.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.accessible_data_locations.is_none()`.
    pub fn accessible_data_locations(&self) -> &[::std::string::String] {
        self.accessible_data_locations.as_deref().unwrap_or_default()
    }
    /// <p>The credential scope is determined by the caller's Lake Formation permission on the associated table. Credential scope can be either:</p>
    /// <ul>
    /// <li>
    /// <p>READ - Provides read-only access to the data location.</p></li>
    /// <li>
    /// <p>READ_WRITE - Provides both read and write access to the data location.</p></li>
    /// </ul>
    pub fn credentials_scope(&self) -> ::std::option::Option<&crate::types::CredentialsScope> {
        self.credentials_scope.as_ref()
    }
}
impl ::aws_types::request_id::RequestId for GetTemporaryDataLocationCredentialsOutput {
    fn request_id(&self) -> Option<&str> {
        self._request_id.as_deref()
    }
}
impl GetTemporaryDataLocationCredentialsOutput {
    /// Creates a new builder-style object to manufacture [`GetTemporaryDataLocationCredentialsOutput`](crate::operation::get_temporary_data_location_credentials::GetTemporaryDataLocationCredentialsOutput).
    pub fn builder() -> crate::operation::get_temporary_data_location_credentials::builders::GetTemporaryDataLocationCredentialsOutputBuilder {
        crate::operation::get_temporary_data_location_credentials::builders::GetTemporaryDataLocationCredentialsOutputBuilder::default()
    }
}

/// A builder for [`GetTemporaryDataLocationCredentialsOutput`](crate::operation::get_temporary_data_location_credentials::GetTemporaryDataLocationCredentialsOutput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct GetTemporaryDataLocationCredentialsOutputBuilder {
    pub(crate) credentials: ::std::option::Option<crate::types::TemporaryCredentials>,
    pub(crate) accessible_data_locations: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    pub(crate) credentials_scope: ::std::option::Option<crate::types::CredentialsScope>,
    _request_id: Option<String>,
}
impl GetTemporaryDataLocationCredentialsOutputBuilder {
    /// <p>A temporary set of credentials for an Lake Formation user. These credentials are scoped down to only access the raw data sources that the user has access to.</p>
    /// <p>The temporary security credentials consist of an access key and a session token. The access key consists of an access key ID and a secret key. When the credentials are created, they are associated with an IAM access control policy that limits what the user can do when using the credentials.</p>
    pub fn credentials(mut self, input: crate::types::TemporaryCredentials) -> Self {
        self.credentials = ::std::option::Option::Some(input);
        self
    }
    /// <p>A temporary set of credentials for an Lake Formation user. These credentials are scoped down to only access the raw data sources that the user has access to.</p>
    /// <p>The temporary security credentials consist of an access key and a session token. The access key consists of an access key ID and a secret key. When the credentials are created, they are associated with an IAM access control policy that limits what the user can do when using the credentials.</p>
    pub fn set_credentials(mut self, input: ::std::option::Option<crate::types::TemporaryCredentials>) -> Self {
        self.credentials = input;
        self
    }
    /// <p>A temporary set of credentials for an Lake Formation user. These credentials are scoped down to only access the raw data sources that the user has access to.</p>
    /// <p>The temporary security credentials consist of an access key and a session token. The access key consists of an access key ID and a secret key. When the credentials are created, they are associated with an IAM access control policy that limits what the user can do when using the credentials.</p>
    pub fn get_credentials(&self) -> &::std::option::Option<crate::types::TemporaryCredentials> {
        &self.credentials
    }
    /// Appends an item to `accessible_data_locations`.
    ///
    /// To override the contents of this collection use [`set_accessible_data_locations`](Self::set_accessible_data_locations).
    ///
    /// <p>Refers to the Amazon S3 locations that can be accessed through the <code>GetTemporaryCredentialsForLocation</code> API operation.</p>
    pub fn accessible_data_locations(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.accessible_data_locations.unwrap_or_default();
        v.push(input.into());
        self.accessible_data_locations = ::std::option::Option::Some(v);
        self
    }
    /// <p>Refers to the Amazon S3 locations that can be accessed through the <code>GetTemporaryCredentialsForLocation</code> API operation.</p>
    pub fn set_accessible_data_locations(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.accessible_data_locations = input;
        self
    }
    /// <p>Refers to the Amazon S3 locations that can be accessed through the <code>GetTemporaryCredentialsForLocation</code> API operation.</p>
    pub fn get_accessible_data_locations(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.accessible_data_locations
    }
    /// <p>The credential scope is determined by the caller's Lake Formation permission on the associated table. Credential scope can be either:</p>
    /// <ul>
    /// <li>
    /// <p>READ - Provides read-only access to the data location.</p></li>
    /// <li>
    /// <p>READ_WRITE - Provides both read and write access to the data location.</p></li>
    /// </ul>
    pub fn credentials_scope(mut self, input: crate::types::CredentialsScope) -> Self {
        self.credentials_scope = ::std::option::Option::Some(input);
        self
    }
    /// <p>The credential scope is determined by the caller's Lake Formation permission on the associated table. Credential scope can be either:</p>
    /// <ul>
    /// <li>
    /// <p>READ - Provides read-only access to the data location.</p></li>
    /// <li>
    /// <p>READ_WRITE - Provides both read and write access to the data location.</p></li>
    /// </ul>
    pub fn set_credentials_scope(mut self, input: ::std::option::Option<crate::types::CredentialsScope>) -> Self {
        self.credentials_scope = input;
        self
    }
    /// <p>The credential scope is determined by the caller's Lake Formation permission on the associated table. Credential scope can be either:</p>
    /// <ul>
    /// <li>
    /// <p>READ - Provides read-only access to the data location.</p></li>
    /// <li>
    /// <p>READ_WRITE - Provides both read and write access to the data location.</p></li>
    /// </ul>
    pub fn get_credentials_scope(&self) -> &::std::option::Option<crate::types::CredentialsScope> {
        &self.credentials_scope
    }
    pub(crate) fn _request_id(mut self, request_id: impl Into<String>) -> Self {
        self._request_id = Some(request_id.into());
        self
    }

    pub(crate) fn _set_request_id(&mut self, request_id: Option<String>) -> &mut Self {
        self._request_id = request_id;
        self
    }
    /// Consumes the builder and constructs a [`GetTemporaryDataLocationCredentialsOutput`](crate::operation::get_temporary_data_location_credentials::GetTemporaryDataLocationCredentialsOutput).
    pub fn build(self) -> crate::operation::get_temporary_data_location_credentials::GetTemporaryDataLocationCredentialsOutput {
        crate::operation::get_temporary_data_location_credentials::GetTemporaryDataLocationCredentialsOutput {
            credentials: self.credentials,
            accessible_data_locations: self.accessible_data_locations,
            credentials_scope: self.credentials_scope,
            _request_id: self._request_id,
        }
    }
}
