// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A term attached to an offer. Each element contains exactly one term type, such as a pricing term, legal term, or payment schedule term.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum OfferTerm {
    /// <p>Defines a Bring Your Own License (BYOL) pricing term, where buyers use their existing license for the product.</p>
    ByolPricingTerm(crate::types::ByolPricingTerm),
    /// <p>Defines a configurable upfront pricing term with selectable rate cards, where buyers choose from predefined pricing configurations.</p>
    ConfigurableUpfrontPricingTerm(crate::types::ConfigurableUpfrontPricingTerm),
    /// <p>Defines a fixed upfront pricing term with a pre-paid amount and granted entitlements.</p>
    FixedUpfrontPricingTerm(crate::types::FixedUpfrontPricingTerm),
    /// <p>Defines a free trial pricing term that enables customers to try the product before purchasing.</p>
    FreeTrialPricingTerm(crate::types::FreeTrialPricingTerm),
    /// <p>Defines a legal term containing documents proposed to buyers, such as EULAs and data subscription agreements.</p>
    LegalTerm(crate::types::LegalTerm),
    /// <p>A net payment term.</p>
    NetPaymentTerm(crate::types::NetPaymentTerm),
    /// <p>Defines a payment schedule term with installment payments at specified dates.</p>
    PaymentScheduleTerm(crate::types::PaymentScheduleTerm),
    /// <p>Defines a recurring payment term with fixed charges at regular billing intervals.</p>
    RecurringPaymentTerm(crate::types::RecurringPaymentTerm),
    /// <p>Defines a renewal term that enables automatic agreement renewal.</p>
    RenewalTerm(crate::types::RenewalTerm),
    /// <p>Defines a support term that includes the refund policy for the offer.</p>
    SupportTerm(crate::types::SupportTerm),
    /// <p>Defines a usage-based pricing term (typically pay-as-you-go), where buyers are charged based on product usage.</p>
    UsageBasedPricingTerm(crate::types::UsageBasedPricingTerm),
    /// <p>Defines a validity term that specifies the duration or date range of an agreement.</p>
    ValidityTerm(crate::types::ValidityTerm),
    /// <p>Defines a variable payment term with a maximum total charge amount.</p>
    VariablePaymentTerm(crate::types::VariablePaymentTerm),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl OfferTerm {
    /// Tries to convert the enum instance into [`ByolPricingTerm`](crate::types::OfferTerm::ByolPricingTerm), extracting the inner [`ByolPricingTerm`](crate::types::ByolPricingTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_byol_pricing_term(&self) -> ::std::result::Result<&crate::types::ByolPricingTerm, &Self> {
        if let OfferTerm::ByolPricingTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`ByolPricingTerm`](crate::types::OfferTerm::ByolPricingTerm).
    pub fn is_byol_pricing_term(&self) -> bool {
        self.as_byol_pricing_term().is_ok()
    }
    /// Tries to convert the enum instance into [`ConfigurableUpfrontPricingTerm`](crate::types::OfferTerm::ConfigurableUpfrontPricingTerm), extracting the inner [`ConfigurableUpfrontPricingTerm`](crate::types::ConfigurableUpfrontPricingTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_configurable_upfront_pricing_term(&self) -> ::std::result::Result<&crate::types::ConfigurableUpfrontPricingTerm, &Self> {
        if let OfferTerm::ConfigurableUpfrontPricingTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`ConfigurableUpfrontPricingTerm`](crate::types::OfferTerm::ConfigurableUpfrontPricingTerm).
    pub fn is_configurable_upfront_pricing_term(&self) -> bool {
        self.as_configurable_upfront_pricing_term().is_ok()
    }
    /// Tries to convert the enum instance into [`FixedUpfrontPricingTerm`](crate::types::OfferTerm::FixedUpfrontPricingTerm), extracting the inner [`FixedUpfrontPricingTerm`](crate::types::FixedUpfrontPricingTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_fixed_upfront_pricing_term(&self) -> ::std::result::Result<&crate::types::FixedUpfrontPricingTerm, &Self> {
        if let OfferTerm::FixedUpfrontPricingTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`FixedUpfrontPricingTerm`](crate::types::OfferTerm::FixedUpfrontPricingTerm).
    pub fn is_fixed_upfront_pricing_term(&self) -> bool {
        self.as_fixed_upfront_pricing_term().is_ok()
    }
    /// Tries to convert the enum instance into [`FreeTrialPricingTerm`](crate::types::OfferTerm::FreeTrialPricingTerm), extracting the inner [`FreeTrialPricingTerm`](crate::types::FreeTrialPricingTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_free_trial_pricing_term(&self) -> ::std::result::Result<&crate::types::FreeTrialPricingTerm, &Self> {
        if let OfferTerm::FreeTrialPricingTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`FreeTrialPricingTerm`](crate::types::OfferTerm::FreeTrialPricingTerm).
    pub fn is_free_trial_pricing_term(&self) -> bool {
        self.as_free_trial_pricing_term().is_ok()
    }
    /// Tries to convert the enum instance into [`LegalTerm`](crate::types::OfferTerm::LegalTerm), extracting the inner [`LegalTerm`](crate::types::LegalTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_legal_term(&self) -> ::std::result::Result<&crate::types::LegalTerm, &Self> {
        if let OfferTerm::LegalTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`LegalTerm`](crate::types::OfferTerm::LegalTerm).
    pub fn is_legal_term(&self) -> bool {
        self.as_legal_term().is_ok()
    }
    /// Tries to convert the enum instance into [`NetPaymentTerm`](crate::types::OfferTerm::NetPaymentTerm), extracting the inner [`NetPaymentTerm`](crate::types::NetPaymentTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_net_payment_term(&self) -> ::std::result::Result<&crate::types::NetPaymentTerm, &Self> {
        if let OfferTerm::NetPaymentTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`NetPaymentTerm`](crate::types::OfferTerm::NetPaymentTerm).
    pub fn is_net_payment_term(&self) -> bool {
        self.as_net_payment_term().is_ok()
    }
    /// Tries to convert the enum instance into [`PaymentScheduleTerm`](crate::types::OfferTerm::PaymentScheduleTerm), extracting the inner [`PaymentScheduleTerm`](crate::types::PaymentScheduleTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_payment_schedule_term(&self) -> ::std::result::Result<&crate::types::PaymentScheduleTerm, &Self> {
        if let OfferTerm::PaymentScheduleTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`PaymentScheduleTerm`](crate::types::OfferTerm::PaymentScheduleTerm).
    pub fn is_payment_schedule_term(&self) -> bool {
        self.as_payment_schedule_term().is_ok()
    }
    /// Tries to convert the enum instance into [`RecurringPaymentTerm`](crate::types::OfferTerm::RecurringPaymentTerm), extracting the inner [`RecurringPaymentTerm`](crate::types::RecurringPaymentTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_recurring_payment_term(&self) -> ::std::result::Result<&crate::types::RecurringPaymentTerm, &Self> {
        if let OfferTerm::RecurringPaymentTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`RecurringPaymentTerm`](crate::types::OfferTerm::RecurringPaymentTerm).
    pub fn is_recurring_payment_term(&self) -> bool {
        self.as_recurring_payment_term().is_ok()
    }
    /// Tries to convert the enum instance into [`RenewalTerm`](crate::types::OfferTerm::RenewalTerm), extracting the inner [`RenewalTerm`](crate::types::RenewalTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_renewal_term(&self) -> ::std::result::Result<&crate::types::RenewalTerm, &Self> {
        if let OfferTerm::RenewalTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`RenewalTerm`](crate::types::OfferTerm::RenewalTerm).
    pub fn is_renewal_term(&self) -> bool {
        self.as_renewal_term().is_ok()
    }
    /// Tries to convert the enum instance into [`SupportTerm`](crate::types::OfferTerm::SupportTerm), extracting the inner [`SupportTerm`](crate::types::SupportTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_support_term(&self) -> ::std::result::Result<&crate::types::SupportTerm, &Self> {
        if let OfferTerm::SupportTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`SupportTerm`](crate::types::OfferTerm::SupportTerm).
    pub fn is_support_term(&self) -> bool {
        self.as_support_term().is_ok()
    }
    /// Tries to convert the enum instance into [`UsageBasedPricingTerm`](crate::types::OfferTerm::UsageBasedPricingTerm), extracting the inner [`UsageBasedPricingTerm`](crate::types::UsageBasedPricingTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_usage_based_pricing_term(&self) -> ::std::result::Result<&crate::types::UsageBasedPricingTerm, &Self> {
        if let OfferTerm::UsageBasedPricingTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`UsageBasedPricingTerm`](crate::types::OfferTerm::UsageBasedPricingTerm).
    pub fn is_usage_based_pricing_term(&self) -> bool {
        self.as_usage_based_pricing_term().is_ok()
    }
    /// Tries to convert the enum instance into [`ValidityTerm`](crate::types::OfferTerm::ValidityTerm), extracting the inner [`ValidityTerm`](crate::types::ValidityTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_validity_term(&self) -> ::std::result::Result<&crate::types::ValidityTerm, &Self> {
        if let OfferTerm::ValidityTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`ValidityTerm`](crate::types::OfferTerm::ValidityTerm).
    pub fn is_validity_term(&self) -> bool {
        self.as_validity_term().is_ok()
    }
    /// Tries to convert the enum instance into [`VariablePaymentTerm`](crate::types::OfferTerm::VariablePaymentTerm), extracting the inner [`VariablePaymentTerm`](crate::types::VariablePaymentTerm).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_variable_payment_term(&self) -> ::std::result::Result<&crate::types::VariablePaymentTerm, &Self> {
        if let OfferTerm::VariablePaymentTerm(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`VariablePaymentTerm`](crate::types::OfferTerm::VariablePaymentTerm).
    pub fn is_variable_payment_term(&self) -> bool {
        self.as_variable_payment_term().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
