// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[doc(inline)]
pub use aws_smithy_client::Builder;
#[derive(Debug)]
pub(crate) struct Handle {
    pub(crate) client: aws_smithy_client::Client<
        aws_smithy_client::erase::DynConnector,
        aws_smithy_client::erase::DynMiddleware<aws_smithy_client::erase::DynConnector>,
    >,
    pub(crate) conf: crate::Config,
}

/// Client for Amazon Kinesis Video Streams Archived Media
///
/// Client for invoking operations on Amazon Kinesis Video Streams Archived Media. Each operation on Amazon Kinesis Video Streams Archived Media is a method on this
/// this struct. `.send()` MUST be invoked on the generated operations to dispatch the request to the service.
///
/// # Examples
/// **Constructing a client and invoking an operation**
/// ```rust,no_run
/// # async fn docs() {
///     // create a shared configuration. This can be used & shared between multiple service clients.
///     let shared_config = aws_config::load_from_env().await;
///     let client = aws_sdk_kinesisvideoarchivedmedia::Client::new(&shared_config);
///     // invoke an operation
///     /* let rsp = client
///         .<operation_name>().
///         .<param>("some value")
///         .send().await; */
/// # }
/// ```
/// **Constructing a client with custom configuration**
/// ```rust,no_run
/// use aws_config::retry::RetryConfig;
/// # async fn docs() {
/// let shared_config = aws_config::load_from_env().await;
/// let config = aws_sdk_kinesisvideoarchivedmedia::config::Builder::from(&shared_config)
///   .retry_config(RetryConfig::disabled())
///   .build();
/// let client = aws_sdk_kinesisvideoarchivedmedia::Client::from_conf(config);
/// # }
#[derive(std::fmt::Debug)]
pub struct Client {
    handle: std::sync::Arc<Handle>,
}

impl std::clone::Clone for Client {
    fn clone(&self) -> Self {
        Self {
            handle: self.handle.clone(),
        }
    }
}

impl
    From<
        aws_smithy_client::Client<
            aws_smithy_client::erase::DynConnector,
            aws_smithy_client::erase::DynMiddleware<aws_smithy_client::erase::DynConnector>,
        >,
    > for Client
{
    fn from(
        client: aws_smithy_client::Client<
            aws_smithy_client::erase::DynConnector,
            aws_smithy_client::erase::DynMiddleware<aws_smithy_client::erase::DynConnector>,
        >,
    ) -> Self {
        Self::with_config(client, crate::Config::builder().build())
    }
}

impl Client {
    /// Creates a client with the given service configuration.
    pub fn with_config(
        client: aws_smithy_client::Client<
            aws_smithy_client::erase::DynConnector,
            aws_smithy_client::erase::DynMiddleware<aws_smithy_client::erase::DynConnector>,
        >,
        conf: crate::Config,
    ) -> Self {
        Self {
            handle: std::sync::Arc::new(Handle { client, conf }),
        }
    }

    /// Returns the client's configuration.
    pub fn conf(&self) -> &crate::Config {
        &self.handle.conf
    }
}
impl Client {
    /// Constructs a fluent builder for the [`GetClip`](crate::client::fluent_builders::GetClip) operation.
    ///
    /// - The fluent builder is configurable:
    ///   - [`stream_name(impl Into<String>)`](crate::client::fluent_builders::GetClip::stream_name) / [`set_stream_name(Option<String>)`](crate::client::fluent_builders::GetClip::set_stream_name): <p>The name of the stream for which to retrieve the media clip. </p>  <p>You must specify either the StreamName or the StreamARN. </p>
    ///   - [`stream_arn(impl Into<String>)`](crate::client::fluent_builders::GetClip::stream_arn) / [`set_stream_arn(Option<String>)`](crate::client::fluent_builders::GetClip::set_stream_arn): <p>The Amazon Resource Name (ARN) of the stream for which to retrieve the media clip. </p>  <p>You must specify either the StreamName or the StreamARN. </p>
    ///   - [`clip_fragment_selector(ClipFragmentSelector)`](crate::client::fluent_builders::GetClip::clip_fragment_selector) / [`set_clip_fragment_selector(Option<ClipFragmentSelector>)`](crate::client::fluent_builders::GetClip::set_clip_fragment_selector): <p>The time range of the requested clip and the source of the timestamps.</p>
    /// - On success, responds with [`GetClipOutput`](crate::output::GetClipOutput) with field(s):
    ///   - [`content_type(Option<String>)`](crate::output::GetClipOutput::content_type): <p>The content type of the media in the requested clip.</p>
    ///   - [`payload(ByteStream)`](crate::output::GetClipOutput::payload): <p>Traditional MP4 file that contains the media clip from the specified video stream. The output will contain the first 100 MB or the first 200 fragments from the specified start timestamp. For more information, see <a href="https://docs.aws.amazon.com/kinesisvideostreams/latest/dg/limits.html">Kinesis Video Streams Limits</a>. </p>
    /// - On failure, responds with [`SdkError<GetClipError>`](crate::error::GetClipError)
    pub fn get_clip(&self) -> crate::client::fluent_builders::GetClip {
        crate::client::fluent_builders::GetClip::new(self.handle.clone())
    }
    /// Constructs a fluent builder for the [`GetDASHStreamingSessionURL`](crate::client::fluent_builders::GetDASHStreamingSessionURL) operation.
    ///
    /// - The fluent builder is configurable:
    ///   - [`stream_name(impl Into<String>)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::stream_name) / [`set_stream_name(Option<String>)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::set_stream_name): <p>The name of the stream for which to retrieve the MPEG-DASH manifest URL.</p>  <p>You must specify either the <code>StreamName</code> or the <code>StreamARN</code>.</p>
    ///   - [`stream_arn(impl Into<String>)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::stream_arn) / [`set_stream_arn(Option<String>)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::set_stream_arn): <p>The Amazon Resource Name (ARN) of the stream for which to retrieve the MPEG-DASH manifest URL.</p>  <p>You must specify either the <code>StreamName</code> or the <code>StreamARN</code>.</p>
    ///   - [`playback_mode(DashPlaybackMode)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::playback_mode) / [`set_playback_mode(Option<DashPlaybackMode>)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::set_playback_mode): <p>Whether to retrieve live, live replay, or archived, on-demand data.</p>  <p>Features of the three types of sessions include the following:</p>  <ul>   <li> <p> <b> <code>LIVE</code> </b>: For sessions of this type, the MPEG-DASH manifest is continually updated with the latest fragments as they become available. We recommend that the media player retrieve a new manifest on a one-second interval. When this type of session is played in a media player, the user interface typically displays a "live" notification, with no scrubber control for choosing the position in the playback window to display.</p> <note>     <p>In <code>LIVE</code> mode, the newest available fragments are included in an MPEG-DASH manifest, even if there is a gap between fragments (that is, if a fragment is missing). A gap like this might cause a media player to halt or cause a jump in playback. In this mode, fragments are not added to the MPEG-DASH manifest if they are older than the newest fragment in the playlist. If the missing fragment becomes available after a subsequent fragment is added to the manifest, the older fragment is not added, and the gap is not filled.</p>    </note> </li>   <li> <p> <b> <code>LIVE_REPLAY</code> </b>: For sessions of this type, the MPEG-DASH manifest is updated similarly to how it is updated for <code>LIVE</code> mode except that it starts by including fragments from a given start time. Instead of fragments being added as they are ingested, fragments are added as the duration of the next fragment elapses. For example, if the fragments in the session are two seconds long, then a new fragment is added to the manifest every two seconds. This mode is useful to be able to start playback from when an event is detected and continue live streaming media that has not yet been ingested as of the time of the session creation. This mode is also useful to stream previously archived media without being limited by the 1,000 fragment limit in the <code>ON_DEMAND</code> mode. </p> </li>   <li> <p> <b> <code>ON_DEMAND</code> </b>: For sessions of this type, the MPEG-DASH manifest contains all the fragments for the session, up to the number that is specified in <code>MaxManifestFragmentResults</code>. The manifest must be retrieved only once for each session. When this type of session is played in a media player, the user interface typically displays a scrubber control for choosing the position in the playback window to display.</p> </li>  </ul>  <p>In all playback modes, if <code>FragmentSelectorType</code> is <code>PRODUCER_TIMESTAMP</code>, and if there are multiple fragments with the same start timestamp, the fragment that has the larger fragment number (that is, the newer fragment) is included in the MPEG-DASH manifest. The other fragments are not included. Fragments that have different timestamps but have overlapping durations are still included in the MPEG-DASH manifest. This can lead to unexpected behavior in the media player.</p>  <p>The default is <code>LIVE</code>.</p>
    ///   - [`display_fragment_timestamp(DashDisplayFragmentTimestamp)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::display_fragment_timestamp) / [`set_display_fragment_timestamp(Option<DashDisplayFragmentTimestamp>)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::set_display_fragment_timestamp): <p>Per the MPEG-DASH specification, the wall-clock time of fragments in the manifest file can be derived using attributes in the manifest itself. However, typically, MPEG-DASH compatible media players do not properly handle gaps in the media timeline. Kinesis Video Streams adjusts the media timeline in the manifest file to enable playback of media with discontinuities. Therefore, the wall-clock time derived from the manifest file may be inaccurate. If DisplayFragmentTimestamp is set to <code>ALWAYS</code>, the accurate fragment timestamp is added to each S element in the manifest file with the attribute name “kvs:ts”. A custom MPEG-DASH media player is necessary to leverage this custom attribute.</p>  <p>The default value is <code>NEVER</code>. When <code>DASHFragmentSelector</code> is <code>SERVER_TIMESTAMP</code>, the timestamps will be the server start timestamps. Similarly, when <code>DASHFragmentSelector</code> is <code>PRODUCER_TIMESTAMP</code>, the timestamps will be the producer start timestamps. </p>
    ///   - [`display_fragment_number(DashDisplayFragmentNumber)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::display_fragment_number) / [`set_display_fragment_number(Option<DashDisplayFragmentNumber>)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::set_display_fragment_number): <p>Fragments are identified in the manifest file based on their sequence number in the session. If DisplayFragmentNumber is set to <code>ALWAYS</code>, the Kinesis Video Streams fragment number is added to each S element in the manifest file with the attribute name “kvs:fn”. These fragment numbers can be used for logging or for use with other APIs (e.g. <code>GetMedia</code> and <code>GetMediaForFragmentList</code>). A custom MPEG-DASH media player is necessary to leverage these this custom attribute.</p>  <p>The default value is <code>NEVER</code>.</p>
    ///   - [`dash_fragment_selector(DashFragmentSelector)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::dash_fragment_selector) / [`set_dash_fragment_selector(Option<DashFragmentSelector>)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::set_dash_fragment_selector): <p>The time range of the requested fragment and the source of the timestamps.</p>  <p>This parameter is required if <code>PlaybackMode</code> is <code>ON_DEMAND</code> or <code>LIVE_REPLAY</code>. This parameter is optional if PlaybackMode is<code></code> <code>LIVE</code>. If <code>PlaybackMode</code> is <code>LIVE</code>, the <code>FragmentSelectorType</code> can be set, but the <code>TimestampRange</code> should not be set. If <code>PlaybackMode</code> is <code>ON_DEMAND</code> or <code>LIVE_REPLAY</code>, both <code>FragmentSelectorType</code> and <code>TimestampRange</code> must be set.</p>
    ///   - [`expires(i32)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::expires) / [`set_expires(Option<i32>)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::set_expires): <p>The time in seconds until the requested session expires. This value can be between 300 (5 minutes) and 43200 (12 hours).</p>  <p>When a session expires, no new calls to <code>GetDashManifest</code>, <code>GetMP4InitFragment</code>, or <code>GetMP4MediaFragment</code> can be made for that session.</p>  <p>The default is 300 (5 minutes).</p>
    ///   - [`max_manifest_fragment_results(i64)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::max_manifest_fragment_results) / [`set_max_manifest_fragment_results(Option<i64>)`](crate::client::fluent_builders::GetDASHStreamingSessionURL::set_max_manifest_fragment_results): <p>The maximum number of fragments that are returned in the MPEG-DASH manifest.</p>  <p>When the <code>PlaybackMode</code> is <code>LIVE</code>, the most recent fragments are returned up to this value. When the <code>PlaybackMode</code> is <code>ON_DEMAND</code>, the oldest fragments are returned, up to this maximum number.</p>  <p>When there are a higher number of fragments available in a live MPEG-DASH manifest, video players often buffer content before starting playback. Increasing the buffer size increases the playback latency, but it decreases the likelihood that rebuffering will occur during playback. We recommend that a live MPEG-DASH manifest have a minimum of 3 fragments and a maximum of 10 fragments.</p>  <p>The default is 5 fragments if <code>PlaybackMode</code> is <code>LIVE</code> or <code>LIVE_REPLAY</code>, and 1,000 if <code>PlaybackMode</code> is <code>ON_DEMAND</code>. </p>  <p>The maximum value of 1,000 fragments corresponds to more than 16 minutes of video on streams with 1-second fragments, and more than 2 1/2 hours of video on streams with 10-second fragments.</p>
    /// - On success, responds with [`GetDashStreamingSessionUrlOutput`](crate::output::GetDashStreamingSessionUrlOutput) with field(s):
    ///   - [`dash_streaming_session_url(Option<String>)`](crate::output::GetDashStreamingSessionUrlOutput::dash_streaming_session_url): <p>The URL (containing the session token) that a media player can use to retrieve the MPEG-DASH manifest.</p>
    /// - On failure, responds with [`SdkError<GetDASHStreamingSessionURLError>`](crate::error::GetDASHStreamingSessionURLError)
    pub fn get_dash_streaming_session_url(
        &self,
    ) -> crate::client::fluent_builders::GetDASHStreamingSessionURL {
        crate::client::fluent_builders::GetDASHStreamingSessionURL::new(self.handle.clone())
    }
    /// Constructs a fluent builder for the [`GetHLSStreamingSessionURL`](crate::client::fluent_builders::GetHLSStreamingSessionURL) operation.
    ///
    /// - The fluent builder is configurable:
    ///   - [`stream_name(impl Into<String>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::stream_name) / [`set_stream_name(Option<String>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::set_stream_name): <p>The name of the stream for which to retrieve the HLS master playlist URL.</p>  <p>You must specify either the <code>StreamName</code> or the <code>StreamARN</code>.</p>
    ///   - [`stream_arn(impl Into<String>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::stream_arn) / [`set_stream_arn(Option<String>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::set_stream_arn): <p>The Amazon Resource Name (ARN) of the stream for which to retrieve the HLS master playlist URL.</p>  <p>You must specify either the <code>StreamName</code> or the <code>StreamARN</code>.</p>
    ///   - [`playback_mode(HlsPlaybackMode)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::playback_mode) / [`set_playback_mode(Option<HlsPlaybackMode>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::set_playback_mode): <p>Whether to retrieve live, live replay, or archived, on-demand data.</p>  <p>Features of the three types of sessions include the following:</p>  <ul>   <li> <p> <b> <code>LIVE</code> </b>: For sessions of this type, the HLS media playlist is continually updated with the latest fragments as they become available. We recommend that the media player retrieve a new playlist on a one-second interval. When this type of session is played in a media player, the user interface typically displays a "live" notification, with no scrubber control for choosing the position in the playback window to display.</p> <note>     <p>In <code>LIVE</code> mode, the newest available fragments are included in an HLS media playlist, even if there is a gap between fragments (that is, if a fragment is missing). A gap like this might cause a media player to halt or cause a jump in playback. In this mode, fragments are not added to the HLS media playlist if they are older than the newest fragment in the playlist. If the missing fragment becomes available after a subsequent fragment is added to the playlist, the older fragment is not added, and the gap is not filled.</p>    </note> </li>   <li> <p> <b> <code>LIVE_REPLAY</code> </b>: For sessions of this type, the HLS media playlist is updated similarly to how it is updated for <code>LIVE</code> mode except that it starts by including fragments from a given start time. Instead of fragments being added as they are ingested, fragments are added as the duration of the next fragment elapses. For example, if the fragments in the session are two seconds long, then a new fragment is added to the media playlist every two seconds. This mode is useful to be able to start playback from when an event is detected and continue live streaming media that has not yet been ingested as of the time of the session creation. This mode is also useful to stream previously archived media without being limited by the 1,000 fragment limit in the <code>ON_DEMAND</code> mode. </p> </li>   <li> <p> <b> <code>ON_DEMAND</code> </b>: For sessions of this type, the HLS media playlist contains all the fragments for the session, up to the number that is specified in <code>MaxMediaPlaylistFragmentResults</code>. The playlist must be retrieved only once for each session. When this type of session is played in a media player, the user interface typically displays a scrubber control for choosing the position in the playback window to display.</p> </li>  </ul>  <p>In all playback modes, if <code>FragmentSelectorType</code> is <code>PRODUCER_TIMESTAMP</code>, and if there are multiple fragments with the same start timestamp, the fragment that has the largest fragment number (that is, the newest fragment) is included in the HLS media playlist. The other fragments are not included. Fragments that have different timestamps but have overlapping durations are still included in the HLS media playlist. This can lead to unexpected behavior in the media player.</p>  <p>The default is <code>LIVE</code>.</p>
    ///   - [`hls_fragment_selector(HlsFragmentSelector)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::hls_fragment_selector) / [`set_hls_fragment_selector(Option<HlsFragmentSelector>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::set_hls_fragment_selector): <p>The time range of the requested fragment and the source of the timestamps.</p>  <p>This parameter is required if <code>PlaybackMode</code> is <code>ON_DEMAND</code> or <code>LIVE_REPLAY</code>. This parameter is optional if PlaybackMode is<code></code> <code>LIVE</code>. If <code>PlaybackMode</code> is <code>LIVE</code>, the <code>FragmentSelectorType</code> can be set, but the <code>TimestampRange</code> should not be set. If <code>PlaybackMode</code> is <code>ON_DEMAND</code> or <code>LIVE_REPLAY</code>, both <code>FragmentSelectorType</code> and <code>TimestampRange</code> must be set.</p>
    ///   - [`container_format(ContainerFormat)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::container_format) / [`set_container_format(Option<ContainerFormat>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::set_container_format): <p>Specifies which format should be used for packaging the media. Specifying the <code>FRAGMENTED_MP4</code> container format packages the media into MP4 fragments (fMP4 or CMAF). This is the recommended packaging because there is minimal packaging overhead. The other container format option is <code>MPEG_TS</code>. HLS has supported MPEG TS chunks since it was released and is sometimes the only supported packaging on older HLS players. MPEG TS typically has a 5-25 percent packaging overhead. This means MPEG TS typically requires 5-25 percent more bandwidth and cost than fMP4.</p>  <p>The default is <code>FRAGMENTED_MP4</code>.</p>
    ///   - [`discontinuity_mode(HlsDiscontinuityMode)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::discontinuity_mode) / [`set_discontinuity_mode(Option<HlsDiscontinuityMode>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::set_discontinuity_mode): <p>Specifies when flags marking discontinuities between fragments are added to the media playlists.</p>  <p>Media players typically build a timeline of media content to play, based on the timestamps of each fragment. This means that if there is any overlap or gap between fragments (as is typical if <code>HLSFragmentSelector</code> is set to <code>SERVER_TIMESTAMP</code>), the media player timeline will also have small gaps between fragments in some places, and will overwrite frames in other places. Gaps in the media player timeline can cause playback to stall and overlaps can cause playback to be jittery. When there are discontinuity flags between fragments, the media player is expected to reset the timeline, resulting in the next fragment being played immediately after the previous fragment. </p>  <p>The following modes are supported:</p>  <ul>   <li> <p> <code>ALWAYS</code>: a discontinuity marker is placed between every fragment in the HLS media playlist. It is recommended to use a value of <code>ALWAYS</code> if the fragment timestamps are not accurate.</p> </li>   <li> <p> <code>NEVER</code>: no discontinuity markers are placed anywhere. It is recommended to use a value of <code>NEVER</code> to ensure the media player timeline most accurately maps to the producer timestamps. </p> </li>   <li> <p> <code>ON_DISCONTINUITY</code>: a discontinuity marker is placed between fragments that have a gap or overlap of more than 50 milliseconds. For most playback scenarios, it is recommended to use a value of <code>ON_DISCONTINUITY</code> so that the media player timeline is only reset when there is a significant issue with the media timeline (e.g. a missing fragment).</p> </li>  </ul>  <p>The default is <code>ALWAYS</code> when <code>HLSFragmentSelector</code> is set to <code>SERVER_TIMESTAMP</code>, and <code>NEVER</code> when it is set to <code>PRODUCER_TIMESTAMP</code>.</p>
    ///   - [`display_fragment_timestamp(HlsDisplayFragmentTimestamp)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::display_fragment_timestamp) / [`set_display_fragment_timestamp(Option<HlsDisplayFragmentTimestamp>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::set_display_fragment_timestamp): <p>Specifies when the fragment start timestamps should be included in the HLS media playlist. Typically, media players report the playhead position as a time relative to the start of the first fragment in the playback session. However, when the start timestamps are included in the HLS media playlist, some media players might report the current playhead as an absolute time based on the fragment timestamps. This can be useful for creating a playback experience that shows viewers the wall-clock time of the media.</p>  <p>The default is <code>NEVER</code>. When <code>HLSFragmentSelector</code> is <code>SERVER_TIMESTAMP</code>, the timestamps will be the server start timestamps. Similarly, when <code>HLSFragmentSelector</code> is <code>PRODUCER_TIMESTAMP</code>, the timestamps will be the producer start timestamps. </p>
    ///   - [`expires(i32)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::expires) / [`set_expires(Option<i32>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::set_expires): <p>The time in seconds until the requested session expires. This value can be between 300 (5 minutes) and 43200 (12 hours).</p>  <p>When a session expires, no new calls to <code>GetHLSMasterPlaylist</code>, <code>GetHLSMediaPlaylist</code>, <code>GetMP4InitFragment</code>, <code>GetMP4MediaFragment</code>, or <code>GetTSFragment</code> can be made for that session.</p>  <p>The default is 300 (5 minutes).</p>
    ///   - [`max_media_playlist_fragment_results(i64)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::max_media_playlist_fragment_results) / [`set_max_media_playlist_fragment_results(Option<i64>)`](crate::client::fluent_builders::GetHLSStreamingSessionURL::set_max_media_playlist_fragment_results): <p>The maximum number of fragments that are returned in the HLS media playlists.</p>  <p>When the <code>PlaybackMode</code> is <code>LIVE</code>, the most recent fragments are returned up to this value. When the <code>PlaybackMode</code> is <code>ON_DEMAND</code>, the oldest fragments are returned, up to this maximum number.</p>  <p>When there are a higher number of fragments available in a live HLS media playlist, video players often buffer content before starting playback. Increasing the buffer size increases the playback latency, but it decreases the likelihood that rebuffering will occur during playback. We recommend that a live HLS media playlist have a minimum of 3 fragments and a maximum of 10 fragments.</p>  <p>The default is 5 fragments if <code>PlaybackMode</code> is <code>LIVE</code> or <code>LIVE_REPLAY</code>, and 1,000 if <code>PlaybackMode</code> is <code>ON_DEMAND</code>. </p>  <p>The maximum value of 5,000 fragments corresponds to more than 80 minutes of video on streams with 1-second fragments, and more than 13 hours of video on streams with 10-second fragments.</p>
    /// - On success, responds with [`GetHlsStreamingSessionUrlOutput`](crate::output::GetHlsStreamingSessionUrlOutput) with field(s):
    ///   - [`hls_streaming_session_url(Option<String>)`](crate::output::GetHlsStreamingSessionUrlOutput::hls_streaming_session_url): <p>The URL (containing the session token) that a media player can use to retrieve the HLS master playlist.</p>
    /// - On failure, responds with [`SdkError<GetHLSStreamingSessionURLError>`](crate::error::GetHLSStreamingSessionURLError)
    pub fn get_hls_streaming_session_url(
        &self,
    ) -> crate::client::fluent_builders::GetHLSStreamingSessionURL {
        crate::client::fluent_builders::GetHLSStreamingSessionURL::new(self.handle.clone())
    }
    /// Constructs a fluent builder for the [`GetImages`](crate::client::fluent_builders::GetImages) operation.
    ///
    /// - The fluent builder is configurable:
    ///   - [`stream_name(impl Into<String>)`](crate::client::fluent_builders::GetImages::stream_name) / [`set_stream_name(Option<String>)`](crate::client::fluent_builders::GetImages::set_stream_name): <p>The name of the stream from which to retrieve the images. You must specify either the <code>StreamName</code> or the <code>StreamARN</code>.</p>
    ///   - [`stream_arn(impl Into<String>)`](crate::client::fluent_builders::GetImages::stream_arn) / [`set_stream_arn(Option<String>)`](crate::client::fluent_builders::GetImages::set_stream_arn): <p>The Amazon Resource Name (ARN) of the stream from which to retrieve the images. You must specify either the <code>StreamName</code> or the <code>StreamARN</code>.</p>
    ///   - [`image_selector_type(ImageSelectorType)`](crate::client::fluent_builders::GetImages::image_selector_type) / [`set_image_selector_type(Option<ImageSelectorType>)`](crate::client::fluent_builders::GetImages::set_image_selector_type): <p>The origin of the Server or Producer timestamps to use to generate the images.</p>
    ///   - [`start_timestamp(DateTime)`](crate::client::fluent_builders::GetImages::start_timestamp) / [`set_start_timestamp(Option<DateTime>)`](crate::client::fluent_builders::GetImages::set_start_timestamp): <p>The starting point from which the images should be generated. This <code>StartTimestamp</code> must be within an inclusive range of timestamps for an image to be returned.</p>
    ///   - [`end_timestamp(DateTime)`](crate::client::fluent_builders::GetImages::end_timestamp) / [`set_end_timestamp(Option<DateTime>)`](crate::client::fluent_builders::GetImages::set_end_timestamp): <p>The end timestamp for the range of images to be generated.</p>
    ///   - [`sampling_interval(i32)`](crate::client::fluent_builders::GetImages::sampling_interval) / [`set_sampling_interval(Option<i32>)`](crate::client::fluent_builders::GetImages::set_sampling_interval): <p>The time interval in milliseconds (ms) at which the images need to be generated from the stream. The minimum value that can be provided is 3000 ms. If the timestamp range is less than the sampling interval, the Image from the <code>startTimestamp</code> will be returned if available. </p> <note>   <p>The minimum value of 3000 ms is a soft limit. If needed, a lower sampling frequency can be requested.</p>  </note>
    ///   - [`format(Format)`](crate::client::fluent_builders::GetImages::format) / [`set_format(Option<Format>)`](crate::client::fluent_builders::GetImages::set_format): <p>The format that will be used to encode the image.</p>
    ///   - [`format_config(HashMap<FormatConfigKey, String>)`](crate::client::fluent_builders::GetImages::format_config) / [`set_format_config(Option<HashMap<FormatConfigKey, String>>)`](crate::client::fluent_builders::GetImages::set_format_config): <p>The list of a key-value pair structure that contains extra parameters that can be applied when the image is generated. The <code>FormatConfig</code> key is the <code>JPEGQuality</code>, which indicates the JPEG quality key to be used to generate the image. The <code>FormatConfig</code> value accepts ints from 1 to 100. If the value is 1, the image will be generated with less quality and the best compression. If the value is 100, the image will be generated with the best quality and less compression. If no value is provided, the default value of the <code>JPEGQuality</code> key will be set to 80.</p>
    ///   - [`width_pixels(i32)`](crate::client::fluent_builders::GetImages::width_pixels) / [`set_width_pixels(Option<i32>)`](crate::client::fluent_builders::GetImages::set_width_pixels): <p>The width of the output image that is used in conjunction with the <code>HeightPixels</code> parameter. When both <code>WidthPixels</code> and <code>HeightPixels</code> parameters are provided, the image will be stretched to fit the specified aspect ratio. If only the <code>WidthPixels</code> parameter is provided or if only the <code>HeightPixels</code> is provided, a <code>ValidationException</code> will be thrown. If neither parameter is provided, the original image size from the stream will be returned.</p>
    ///   - [`height_pixels(i32)`](crate::client::fluent_builders::GetImages::height_pixels) / [`set_height_pixels(Option<i32>)`](crate::client::fluent_builders::GetImages::set_height_pixels): <p>The height of the output image that is used in conjunction with the <code>WidthPixels</code> parameter. When both <code>HeightPixels</code> and <code>WidthPixels</code> parameters are provided, the image will be stretched to fit the specified aspect ratio. If only the <code>HeightPixels</code> parameter is provided, its original aspect ratio will be used to calculate the <code>WidthPixels</code> ratio. If neither parameter is provided, the original image size will be returned.</p>
    ///   - [`max_results(i64)`](crate::client::fluent_builders::GetImages::max_results) / [`set_max_results(Option<i64>)`](crate::client::fluent_builders::GetImages::set_max_results): <p>The maximum number of images to be returned by the API. </p> <note>   <p>The default limit is 100 images per API response. The additional results will be paginated. </p>  </note>
    ///   - [`next_token(impl Into<String>)`](crate::client::fluent_builders::GetImages::next_token) / [`set_next_token(Option<String>)`](crate::client::fluent_builders::GetImages::set_next_token): <p>A token that specifies where to start paginating the next set of Images. This is the <code>GetImages:NextToken</code> from a previously truncated response.</p>
    /// - On success, responds with [`GetImagesOutput`](crate::output::GetImagesOutput) with field(s):
    ///   - [`images(Option<Vec<Image>>)`](crate::output::GetImagesOutput::images): <p>The list of images generated from the video stream. If there is no media available for the given timestamp, the <code>NO_MEDIA</code> error will be listed in the output. If an error occurs while the image is being generated, the <code>MEDIA_ERROR</code> will be listed in the output as the cause of the missing image. </p>
    ///   - [`next_token(Option<String>)`](crate::output::GetImagesOutput::next_token): <p>The encrypted token that was used in the request to get more images.</p>
    /// - On failure, responds with [`SdkError<GetImagesError>`](crate::error::GetImagesError)
    pub fn get_images(&self) -> crate::client::fluent_builders::GetImages {
        crate::client::fluent_builders::GetImages::new(self.handle.clone())
    }
    /// Constructs a fluent builder for the [`GetMediaForFragmentList`](crate::client::fluent_builders::GetMediaForFragmentList) operation.
    ///
    /// - The fluent builder is configurable:
    ///   - [`stream_name(impl Into<String>)`](crate::client::fluent_builders::GetMediaForFragmentList::stream_name) / [`set_stream_name(Option<String>)`](crate::client::fluent_builders::GetMediaForFragmentList::set_stream_name): <p>The name of the stream from which to retrieve fragment media. Specify either this parameter or the <code>StreamARN</code> parameter.</p>
    ///   - [`stream_arn(impl Into<String>)`](crate::client::fluent_builders::GetMediaForFragmentList::stream_arn) / [`set_stream_arn(Option<String>)`](crate::client::fluent_builders::GetMediaForFragmentList::set_stream_arn): <p>The Amazon Resource Name (ARN) of the stream from which to retrieve fragment media. Specify either this parameter or the <code>StreamName</code> parameter.</p>
    ///   - [`fragments(Vec<String>)`](crate::client::fluent_builders::GetMediaForFragmentList::fragments) / [`set_fragments(Option<Vec<String>>)`](crate::client::fluent_builders::GetMediaForFragmentList::set_fragments): <p>A list of the numbers of fragments for which to retrieve media. You retrieve these values with <code>ListFragments</code>.</p>
    /// - On success, responds with [`GetMediaForFragmentListOutput`](crate::output::GetMediaForFragmentListOutput) with field(s):
    ///   - [`content_type(Option<String>)`](crate::output::GetMediaForFragmentListOutput::content_type): <p>The content type of the requested media.</p>
    ///   - [`payload(ByteStream)`](crate::output::GetMediaForFragmentListOutput::payload): <p>The payload that Kinesis Video Streams returns is a sequence of chunks from the specified stream. For information about the chunks, see <a href="http://docs.aws.amazon.com/kinesisvideostreams/latest/dg/API_dataplane_PutMedia.html">PutMedia</a>. The chunks that Kinesis Video Streams returns in the <code>GetMediaForFragmentList</code> call also include the following additional Matroska (MKV) tags: </p>  <ul>   <li> <p>AWS_KINESISVIDEO_FRAGMENT_NUMBER - Fragment number returned in the chunk.</p> </li>   <li> <p>AWS_KINESISVIDEO_SERVER_SIDE_TIMESTAMP - Server-side timestamp of the fragment.</p> </li>   <li> <p>AWS_KINESISVIDEO_PRODUCER_SIDE_TIMESTAMP - Producer-side timestamp of the fragment.</p> </li>  </ul>  <p>The following tags will be included if an exception occurs:</p>  <ul>   <li> <p>AWS_KINESISVIDEO_FRAGMENT_NUMBER - The number of the fragment that threw the exception</p> </li>   <li> <p>AWS_KINESISVIDEO_EXCEPTION_ERROR_CODE - The integer code of the exception</p> </li>   <li> <p>AWS_KINESISVIDEO_EXCEPTION_MESSAGE - A text description of the exception</p> </li>  </ul>
    /// - On failure, responds with [`SdkError<GetMediaForFragmentListError>`](crate::error::GetMediaForFragmentListError)
    pub fn get_media_for_fragment_list(
        &self,
    ) -> crate::client::fluent_builders::GetMediaForFragmentList {
        crate::client::fluent_builders::GetMediaForFragmentList::new(self.handle.clone())
    }
    /// Constructs a fluent builder for the [`ListFragments`](crate::client::fluent_builders::ListFragments) operation.
    ///
    /// - The fluent builder is configurable:
    ///   - [`stream_name(impl Into<String>)`](crate::client::fluent_builders::ListFragments::stream_name) / [`set_stream_name(Option<String>)`](crate::client::fluent_builders::ListFragments::set_stream_name): <p>The name of the stream from which to retrieve a fragment list. Specify either this parameter or the <code>StreamARN</code> parameter.</p>
    ///   - [`stream_arn(impl Into<String>)`](crate::client::fluent_builders::ListFragments::stream_arn) / [`set_stream_arn(Option<String>)`](crate::client::fluent_builders::ListFragments::set_stream_arn): <p>The Amazon Resource Name (ARN) of the stream from which to retrieve a fragment list. Specify either this parameter or the <code>StreamName</code> parameter.</p>
    ///   - [`max_results(i64)`](crate::client::fluent_builders::ListFragments::max_results) / [`set_max_results(Option<i64>)`](crate::client::fluent_builders::ListFragments::set_max_results): <p>The total number of fragments to return. If the total number of fragments available is more than the value specified in <code>max-results</code>, then a <code>ListFragmentsOutput$NextToken</code> is provided in the output that you can use to resume pagination.</p>
    ///   - [`next_token(impl Into<String>)`](crate::client::fluent_builders::ListFragments::next_token) / [`set_next_token(Option<String>)`](crate::client::fluent_builders::ListFragments::set_next_token): <p>A token to specify where to start paginating. This is the <code>ListFragmentsOutput$NextToken</code> from a previously truncated response.</p>
    ///   - [`fragment_selector(FragmentSelector)`](crate::client::fluent_builders::ListFragments::fragment_selector) / [`set_fragment_selector(Option<FragmentSelector>)`](crate::client::fluent_builders::ListFragments::set_fragment_selector): <p>Describes the timestamp range and timestamp origin for the range of fragments to return.</p>
    /// - On success, responds with [`ListFragmentsOutput`](crate::output::ListFragmentsOutput) with field(s):
    ///   - [`fragments(Option<Vec<Fragment>>)`](crate::output::ListFragmentsOutput::fragments): <p>A list of archived <code>Fragment</code> objects from the stream that meet the selector criteria. Results are in no specific order, even across pages.</p>
    ///   - [`next_token(Option<String>)`](crate::output::ListFragmentsOutput::next_token): <p>If the returned list is truncated, the operation returns this token to use to retrieve the next page of results. This value is <code>null</code> when there are no more results to return.</p>
    /// - On failure, responds with [`SdkError<ListFragmentsError>`](crate::error::ListFragmentsError)
    pub fn list_fragments(&self) -> crate::client::fluent_builders::ListFragments {
        crate::client::fluent_builders::ListFragments::new(self.handle.clone())
    }
}

impl Client {
    /// Creates a new client from an [SDK Config](aws_types::sdk_config::SdkConfig).
    ///
    /// # Panics
    ///
    /// - This method will panic if the `sdk_config` is missing an async sleep implementation. If you experience this panic, set
    ///     the `sleep_impl` on the Config passed into this function to fix it.
    /// - This method will panic if the `sdk_config` is missing an HTTP connector. If you experience this panic, set the
    ///     `http_connector` on the Config passed into this function to fix it.
    pub fn new(sdk_config: &aws_types::sdk_config::SdkConfig) -> Self {
        Self::from_conf(sdk_config.into())
    }

    /// Creates a new client from the service [`Config`](crate::Config).
    ///
    /// # Panics
    ///
    /// - This method will panic if the `conf` is missing an async sleep implementation. If you experience this panic, set
    ///     the `sleep_impl` on the Config passed into this function to fix it.
    /// - This method will panic if the `conf` is missing an HTTP connector. If you experience this panic, set the
    ///     `http_connector` on the Config passed into this function to fix it.
    pub fn from_conf(conf: crate::Config) -> Self {
        let retry_config = conf
            .retry_config()
            .cloned()
            .unwrap_or_else(aws_smithy_types::retry::RetryConfig::disabled);
        let timeout_config = conf
            .timeout_config()
            .cloned()
            .unwrap_or_else(aws_smithy_types::timeout::TimeoutConfig::disabled);
        let sleep_impl = conf.sleep_impl();
        if (retry_config.has_retry() || timeout_config.has_timeouts()) && sleep_impl.is_none() {
            panic!("An async sleep implementation is required for retries or timeouts to work. \
                                    Set the `sleep_impl` on the Config passed into this function to fix this panic.");
        }

        let connector = conf.http_connector().and_then(|c| {
            let timeout_config = conf
                .timeout_config()
                .cloned()
                .unwrap_or_else(aws_smithy_types::timeout::TimeoutConfig::disabled);
            let connector_settings =
                aws_smithy_client::http_connector::ConnectorSettings::from_timeout_config(
                    &timeout_config,
                );
            c.connector(&connector_settings, conf.sleep_impl())
        });

        let builder = aws_smithy_client::Builder::new();

        let builder = match connector {
            // Use provided connector
            Some(c) => builder.connector(c),
            None => {
                #[cfg(any(feature = "rustls", feature = "native-tls"))]
                {
                    // Use default connector based on enabled features
                    builder.dyn_https_connector(
                        aws_smithy_client::http_connector::ConnectorSettings::from_timeout_config(
                            &timeout_config,
                        ),
                    )
                }
                #[cfg(not(any(feature = "rustls", feature = "native-tls")))]
                {
                    panic!("No HTTP connector was available. Enable the `rustls` or `native-tls` crate feature or set a connector to fix this.");
                }
            }
        };
        let mut builder = builder
            .middleware(aws_smithy_client::erase::DynMiddleware::new(
                crate::middleware::DefaultMiddleware::new(),
            ))
            .retry_config(retry_config.into())
            .operation_timeout_config(timeout_config.into());
        builder.set_sleep_impl(sleep_impl);
        let client = builder.build();

        Self {
            handle: std::sync::Arc::new(Handle { client, conf }),
        }
    }
}

/// Utilities to ergonomically construct a request to the service.
///
/// Fluent builders are created through the [`Client`](crate::client::Client) by calling
/// one if its operation methods. After parameters are set using the builder methods,
/// the `send` method can be called to initiate the request.
pub mod fluent_builders;
