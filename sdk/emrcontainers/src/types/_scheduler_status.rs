// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The current job-run counts for a virtual cluster, reflecting how much of the configured scheduler capacity is in use.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct SchedulerStatus {
    /// <p>The number of job runs currently waiting in the queue (<code>PENDING</code> or <code>SUBMITTED</code>) for the virtual cluster.</p>
    pub current_in_queue_job_runs: i32,
    /// <p>The number of job runs currently in the <code>RUNNING</code> state for the virtual cluster.</p>
    pub current_concurrent_job_runs: i32,
}
impl SchedulerStatus {
    /// <p>The number of job runs currently waiting in the queue (<code>PENDING</code> or <code>SUBMITTED</code>) for the virtual cluster.</p>
    pub fn current_in_queue_job_runs(&self) -> i32 {
        self.current_in_queue_job_runs
    }
    /// <p>The number of job runs currently in the <code>RUNNING</code> state for the virtual cluster.</p>
    pub fn current_concurrent_job_runs(&self) -> i32 {
        self.current_concurrent_job_runs
    }
}
impl SchedulerStatus {
    /// Creates a new builder-style object to manufacture [`SchedulerStatus`](crate::types::SchedulerStatus).
    pub fn builder() -> crate::types::builders::SchedulerStatusBuilder {
        crate::types::builders::SchedulerStatusBuilder::default()
    }
}

/// A builder for [`SchedulerStatus`](crate::types::SchedulerStatus).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct SchedulerStatusBuilder {
    pub(crate) current_in_queue_job_runs: ::std::option::Option<i32>,
    pub(crate) current_concurrent_job_runs: ::std::option::Option<i32>,
}
impl SchedulerStatusBuilder {
    /// <p>The number of job runs currently waiting in the queue (<code>PENDING</code> or <code>SUBMITTED</code>) for the virtual cluster.</p>
    pub fn current_in_queue_job_runs(mut self, input: i32) -> Self {
        self.current_in_queue_job_runs = ::std::option::Option::Some(input);
        self
    }
    /// <p>The number of job runs currently waiting in the queue (<code>PENDING</code> or <code>SUBMITTED</code>) for the virtual cluster.</p>
    pub fn set_current_in_queue_job_runs(mut self, input: ::std::option::Option<i32>) -> Self {
        self.current_in_queue_job_runs = input;
        self
    }
    /// <p>The number of job runs currently waiting in the queue (<code>PENDING</code> or <code>SUBMITTED</code>) for the virtual cluster.</p>
    pub fn get_current_in_queue_job_runs(&self) -> &::std::option::Option<i32> {
        &self.current_in_queue_job_runs
    }
    /// <p>The number of job runs currently in the <code>RUNNING</code> state for the virtual cluster.</p>
    pub fn current_concurrent_job_runs(mut self, input: i32) -> Self {
        self.current_concurrent_job_runs = ::std::option::Option::Some(input);
        self
    }
    /// <p>The number of job runs currently in the <code>RUNNING</code> state for the virtual cluster.</p>
    pub fn set_current_concurrent_job_runs(mut self, input: ::std::option::Option<i32>) -> Self {
        self.current_concurrent_job_runs = input;
        self
    }
    /// <p>The number of job runs currently in the <code>RUNNING</code> state for the virtual cluster.</p>
    pub fn get_current_concurrent_job_runs(&self) -> &::std::option::Option<i32> {
        &self.current_concurrent_job_runs
    }
    /// Consumes the builder and constructs a [`SchedulerStatus`](crate::types::SchedulerStatus).
    pub fn build(self) -> crate::types::SchedulerStatus {
        crate::types::SchedulerStatus {
            current_in_queue_job_runs: self.current_in_queue_job_runs.unwrap_or_default(),
            current_concurrent_job_runs: self.current_concurrent_job_runs.unwrap_or_default(),
        }
    }
}
