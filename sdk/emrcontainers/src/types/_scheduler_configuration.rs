// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The scheduler configuration for a virtual cluster on Amazon EMR on EKS. It controls how many job runs can run concurrently and how many can wait in the queue. When not set, no concurrency or queue limits are applied.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct SchedulerConfiguration {
    /// <p>The maximum number of job runs that can be in the <code>PENDING</code> or <code>SUBMITTED</code> state at any time for the virtual cluster. When the queue is full, the service rejects <code>StartJobRun</code> requests with a <code>ValidationException</code>. If you omit this field, the service applies no queue-depth limit.</p>
    pub max_in_queue_job_runs: ::std::option::Option<i32>,
    /// <p>The maximum number of job runs that can be in the <code>RUNNING</code> state at any time for the virtual cluster. As running slots free up, queued job runs start automatically. If you omit this field, the service applies no concurrency limit.</p>
    pub max_concurrent_job_runs: ::std::option::Option<i32>,
}
impl SchedulerConfiguration {
    /// <p>The maximum number of job runs that can be in the <code>PENDING</code> or <code>SUBMITTED</code> state at any time for the virtual cluster. When the queue is full, the service rejects <code>StartJobRun</code> requests with a <code>ValidationException</code>. If you omit this field, the service applies no queue-depth limit.</p>
    pub fn max_in_queue_job_runs(&self) -> ::std::option::Option<i32> {
        self.max_in_queue_job_runs
    }
    /// <p>The maximum number of job runs that can be in the <code>RUNNING</code> state at any time for the virtual cluster. As running slots free up, queued job runs start automatically. If you omit this field, the service applies no concurrency limit.</p>
    pub fn max_concurrent_job_runs(&self) -> ::std::option::Option<i32> {
        self.max_concurrent_job_runs
    }
}
impl SchedulerConfiguration {
    /// Creates a new builder-style object to manufacture [`SchedulerConfiguration`](crate::types::SchedulerConfiguration).
    pub fn builder() -> crate::types::builders::SchedulerConfigurationBuilder {
        crate::types::builders::SchedulerConfigurationBuilder::default()
    }
}

/// A builder for [`SchedulerConfiguration`](crate::types::SchedulerConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct SchedulerConfigurationBuilder {
    pub(crate) max_in_queue_job_runs: ::std::option::Option<i32>,
    pub(crate) max_concurrent_job_runs: ::std::option::Option<i32>,
}
impl SchedulerConfigurationBuilder {
    /// <p>The maximum number of job runs that can be in the <code>PENDING</code> or <code>SUBMITTED</code> state at any time for the virtual cluster. When the queue is full, the service rejects <code>StartJobRun</code> requests with a <code>ValidationException</code>. If you omit this field, the service applies no queue-depth limit.</p>
    pub fn max_in_queue_job_runs(mut self, input: i32) -> Self {
        self.max_in_queue_job_runs = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum number of job runs that can be in the <code>PENDING</code> or <code>SUBMITTED</code> state at any time for the virtual cluster. When the queue is full, the service rejects <code>StartJobRun</code> requests with a <code>ValidationException</code>. If you omit this field, the service applies no queue-depth limit.</p>
    pub fn set_max_in_queue_job_runs(mut self, input: ::std::option::Option<i32>) -> Self {
        self.max_in_queue_job_runs = input;
        self
    }
    /// <p>The maximum number of job runs that can be in the <code>PENDING</code> or <code>SUBMITTED</code> state at any time for the virtual cluster. When the queue is full, the service rejects <code>StartJobRun</code> requests with a <code>ValidationException</code>. If you omit this field, the service applies no queue-depth limit.</p>
    pub fn get_max_in_queue_job_runs(&self) -> &::std::option::Option<i32> {
        &self.max_in_queue_job_runs
    }
    /// <p>The maximum number of job runs that can be in the <code>RUNNING</code> state at any time for the virtual cluster. As running slots free up, queued job runs start automatically. If you omit this field, the service applies no concurrency limit.</p>
    pub fn max_concurrent_job_runs(mut self, input: i32) -> Self {
        self.max_concurrent_job_runs = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum number of job runs that can be in the <code>RUNNING</code> state at any time for the virtual cluster. As running slots free up, queued job runs start automatically. If you omit this field, the service applies no concurrency limit.</p>
    pub fn set_max_concurrent_job_runs(mut self, input: ::std::option::Option<i32>) -> Self {
        self.max_concurrent_job_runs = input;
        self
    }
    /// <p>The maximum number of job runs that can be in the <code>RUNNING</code> state at any time for the virtual cluster. As running slots free up, queued job runs start automatically. If you omit this field, the service applies no concurrency limit.</p>
    pub fn get_max_concurrent_job_runs(&self) -> &::std::option::Option<i32> {
        &self.max_concurrent_job_runs
    }
    /// Consumes the builder and constructs a [`SchedulerConfiguration`](crate::types::SchedulerConfiguration).
    pub fn build(self) -> crate::types::SchedulerConfiguration {
        crate::types::SchedulerConfiguration {
            max_in_queue_job_runs: self.max_in_queue_job_runs,
            max_concurrent_job_runs: self.max_concurrent_job_runs,
        }
    }
}
