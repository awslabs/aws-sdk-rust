// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The encoding configuration to apply to the NDI® source when transcoding it to a transport stream for downstream distribution. You can choose between several predefined encoding profiles based on common use cases.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct EncodingConfig {
    /// <p>The encoding profile to use when transcoding the NDI source content to a transport stream. You can change this value while the flow is running.</p>
    pub encoding_profile: ::std::option::Option<crate::types::EncodingProfile>,
    /// <p>The maximum video bitrate to use when transcoding the NDI source to a transport stream. This parameter enables you to override the default video bitrate within the encoding profile's supported range.</p>
    /// <p>The supported range is 10,000,000 - 50,000,000 bits per second (bps). If you don't specify a value, MediaConnect uses the default value of 20,000,000 bps.</p>
    pub video_max_bitrate: ::std::option::Option<i32>,
}
impl EncodingConfig {
    /// <p>The encoding profile to use when transcoding the NDI source content to a transport stream. You can change this value while the flow is running.</p>
    pub fn encoding_profile(&self) -> ::std::option::Option<&crate::types::EncodingProfile> {
        self.encoding_profile.as_ref()
    }
    /// <p>The maximum video bitrate to use when transcoding the NDI source to a transport stream. This parameter enables you to override the default video bitrate within the encoding profile's supported range.</p>
    /// <p>The supported range is 10,000,000 - 50,000,000 bits per second (bps). If you don't specify a value, MediaConnect uses the default value of 20,000,000 bps.</p>
    pub fn video_max_bitrate(&self) -> ::std::option::Option<i32> {
        self.video_max_bitrate
    }
}
impl EncodingConfig {
    /// Creates a new builder-style object to manufacture [`EncodingConfig`](crate::types::EncodingConfig).
    pub fn builder() -> crate::types::builders::EncodingConfigBuilder {
        crate::types::builders::EncodingConfigBuilder::default()
    }
}

/// A builder for [`EncodingConfig`](crate::types::EncodingConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct EncodingConfigBuilder {
    pub(crate) encoding_profile: ::std::option::Option<crate::types::EncodingProfile>,
    pub(crate) video_max_bitrate: ::std::option::Option<i32>,
}
impl EncodingConfigBuilder {
    /// <p>The encoding profile to use when transcoding the NDI source content to a transport stream. You can change this value while the flow is running.</p>
    pub fn encoding_profile(mut self, input: crate::types::EncodingProfile) -> Self {
        self.encoding_profile = ::std::option::Option::Some(input);
        self
    }
    /// <p>The encoding profile to use when transcoding the NDI source content to a transport stream. You can change this value while the flow is running.</p>
    pub fn set_encoding_profile(mut self, input: ::std::option::Option<crate::types::EncodingProfile>) -> Self {
        self.encoding_profile = input;
        self
    }
    /// <p>The encoding profile to use when transcoding the NDI source content to a transport stream. You can change this value while the flow is running.</p>
    pub fn get_encoding_profile(&self) -> &::std::option::Option<crate::types::EncodingProfile> {
        &self.encoding_profile
    }
    /// <p>The maximum video bitrate to use when transcoding the NDI source to a transport stream. This parameter enables you to override the default video bitrate within the encoding profile's supported range.</p>
    /// <p>The supported range is 10,000,000 - 50,000,000 bits per second (bps). If you don't specify a value, MediaConnect uses the default value of 20,000,000 bps.</p>
    pub fn video_max_bitrate(mut self, input: i32) -> Self {
        self.video_max_bitrate = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum video bitrate to use when transcoding the NDI source to a transport stream. This parameter enables you to override the default video bitrate within the encoding profile's supported range.</p>
    /// <p>The supported range is 10,000,000 - 50,000,000 bits per second (bps). If you don't specify a value, MediaConnect uses the default value of 20,000,000 bps.</p>
    pub fn set_video_max_bitrate(mut self, input: ::std::option::Option<i32>) -> Self {
        self.video_max_bitrate = input;
        self
    }
    /// <p>The maximum video bitrate to use when transcoding the NDI source to a transport stream. This parameter enables you to override the default video bitrate within the encoding profile's supported range.</p>
    /// <p>The supported range is 10,000,000 - 50,000,000 bits per second (bps). If you don't specify a value, MediaConnect uses the default value of 20,000,000 bps.</p>
    pub fn get_video_max_bitrate(&self) -> &::std::option::Option<i32> {
        &self.video_max_bitrate
    }
    /// Consumes the builder and constructs a [`EncodingConfig`](crate::types::EncodingConfig).
    pub fn build(self) -> crate::types::EncodingConfig {
        crate::types::EncodingConfig {
            encoding_profile: self.encoding_profile,
            video_max_bitrate: self.video_max_bitrate,
        }
    }
}
