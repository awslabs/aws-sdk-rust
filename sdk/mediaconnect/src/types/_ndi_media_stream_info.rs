// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Detailed information about a single media stream that is part of an NDI® source. This includes details about the stream type, codec, resolution, frame rate, audio channels, and sample rate.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct NdiMediaStreamInfo {
    /// <p>The type of media stream (for example, <code>Video</code> or <code>Audio</code>).</p>
    pub stream_type: ::std::option::Option<::std::string::String>,
    /// <p>The codec used for the media stream. For NDI sources, use <code>speed-hq</code>.</p>
    pub codec: ::std::option::Option<::std::string::String>,
    /// <p>A unique identifier for the media stream.</p>
    pub stream_id: ::std::option::Option<i32>,
    /// <p>The method used to display video frames. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub scan_mode: ::std::option::Option<crate::types::ScanMode>,
    /// <p>The width and height dimensions of the video frame in pixels. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub frame_resolution: ::std::option::Option<crate::types::FrameResolution>,
    /// <p>The number of video frames displayed per second. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub frame_rate: ::std::option::Option<::std::string::String>,
    /// <p>The number of audio channels in the stream. Used when the <code>streamType</code> is <code>Audio</code>.</p>
    pub channels: ::std::option::Option<i32>,
    /// <p>The number of audio samples captured per second, measured in kilohertz (kHz). Used when the <code>streamType</code> is <code>Audio</code>.</p>
    pub sample_rate: ::std::option::Option<i32>,
}
impl NdiMediaStreamInfo {
    /// <p>The type of media stream (for example, <code>Video</code> or <code>Audio</code>).</p>
    pub fn stream_type(&self) -> ::std::option::Option<&str> {
        self.stream_type.as_deref()
    }
    /// <p>The codec used for the media stream. For NDI sources, use <code>speed-hq</code>.</p>
    pub fn codec(&self) -> ::std::option::Option<&str> {
        self.codec.as_deref()
    }
    /// <p>A unique identifier for the media stream.</p>
    pub fn stream_id(&self) -> ::std::option::Option<i32> {
        self.stream_id
    }
    /// <p>The method used to display video frames. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn scan_mode(&self) -> ::std::option::Option<&crate::types::ScanMode> {
        self.scan_mode.as_ref()
    }
    /// <p>The width and height dimensions of the video frame in pixels. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn frame_resolution(&self) -> ::std::option::Option<&crate::types::FrameResolution> {
        self.frame_resolution.as_ref()
    }
    /// <p>The number of video frames displayed per second. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn frame_rate(&self) -> ::std::option::Option<&str> {
        self.frame_rate.as_deref()
    }
    /// <p>The number of audio channels in the stream. Used when the <code>streamType</code> is <code>Audio</code>.</p>
    pub fn channels(&self) -> ::std::option::Option<i32> {
        self.channels
    }
    /// <p>The number of audio samples captured per second, measured in kilohertz (kHz). Used when the <code>streamType</code> is <code>Audio</code>.</p>
    pub fn sample_rate(&self) -> ::std::option::Option<i32> {
        self.sample_rate
    }
}
impl NdiMediaStreamInfo {
    /// Creates a new builder-style object to manufacture [`NdiMediaStreamInfo`](crate::types::NdiMediaStreamInfo).
    pub fn builder() -> crate::types::builders::NdiMediaStreamInfoBuilder {
        crate::types::builders::NdiMediaStreamInfoBuilder::default()
    }
}

/// A builder for [`NdiMediaStreamInfo`](crate::types::NdiMediaStreamInfo).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct NdiMediaStreamInfoBuilder {
    pub(crate) stream_type: ::std::option::Option<::std::string::String>,
    pub(crate) codec: ::std::option::Option<::std::string::String>,
    pub(crate) stream_id: ::std::option::Option<i32>,
    pub(crate) scan_mode: ::std::option::Option<crate::types::ScanMode>,
    pub(crate) frame_resolution: ::std::option::Option<crate::types::FrameResolution>,
    pub(crate) frame_rate: ::std::option::Option<::std::string::String>,
    pub(crate) channels: ::std::option::Option<i32>,
    pub(crate) sample_rate: ::std::option::Option<i32>,
}
impl NdiMediaStreamInfoBuilder {
    /// <p>The type of media stream (for example, <code>Video</code> or <code>Audio</code>).</p>
    /// This field is required.
    pub fn stream_type(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.stream_type = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The type of media stream (for example, <code>Video</code> or <code>Audio</code>).</p>
    pub fn set_stream_type(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.stream_type = input;
        self
    }
    /// <p>The type of media stream (for example, <code>Video</code> or <code>Audio</code>).</p>
    pub fn get_stream_type(&self) -> &::std::option::Option<::std::string::String> {
        &self.stream_type
    }
    /// <p>The codec used for the media stream. For NDI sources, use <code>speed-hq</code>.</p>
    /// This field is required.
    pub fn codec(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.codec = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The codec used for the media stream. For NDI sources, use <code>speed-hq</code>.</p>
    pub fn set_codec(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.codec = input;
        self
    }
    /// <p>The codec used for the media stream. For NDI sources, use <code>speed-hq</code>.</p>
    pub fn get_codec(&self) -> &::std::option::Option<::std::string::String> {
        &self.codec
    }
    /// <p>A unique identifier for the media stream.</p>
    /// This field is required.
    pub fn stream_id(mut self, input: i32) -> Self {
        self.stream_id = ::std::option::Option::Some(input);
        self
    }
    /// <p>A unique identifier for the media stream.</p>
    pub fn set_stream_id(mut self, input: ::std::option::Option<i32>) -> Self {
        self.stream_id = input;
        self
    }
    /// <p>A unique identifier for the media stream.</p>
    pub fn get_stream_id(&self) -> &::std::option::Option<i32> {
        &self.stream_id
    }
    /// <p>The method used to display video frames. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn scan_mode(mut self, input: crate::types::ScanMode) -> Self {
        self.scan_mode = ::std::option::Option::Some(input);
        self
    }
    /// <p>The method used to display video frames. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn set_scan_mode(mut self, input: ::std::option::Option<crate::types::ScanMode>) -> Self {
        self.scan_mode = input;
        self
    }
    /// <p>The method used to display video frames. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn get_scan_mode(&self) -> &::std::option::Option<crate::types::ScanMode> {
        &self.scan_mode
    }
    /// <p>The width and height dimensions of the video frame in pixels. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn frame_resolution(mut self, input: crate::types::FrameResolution) -> Self {
        self.frame_resolution = ::std::option::Option::Some(input);
        self
    }
    /// <p>The width and height dimensions of the video frame in pixels. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn set_frame_resolution(mut self, input: ::std::option::Option<crate::types::FrameResolution>) -> Self {
        self.frame_resolution = input;
        self
    }
    /// <p>The width and height dimensions of the video frame in pixels. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn get_frame_resolution(&self) -> &::std::option::Option<crate::types::FrameResolution> {
        &self.frame_resolution
    }
    /// <p>The number of video frames displayed per second. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn frame_rate(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.frame_rate = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The number of video frames displayed per second. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn set_frame_rate(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.frame_rate = input;
        self
    }
    /// <p>The number of video frames displayed per second. Used when the <code>streamType</code> is <code>Video</code>.</p>
    pub fn get_frame_rate(&self) -> &::std::option::Option<::std::string::String> {
        &self.frame_rate
    }
    /// <p>The number of audio channels in the stream. Used when the <code>streamType</code> is <code>Audio</code>.</p>
    pub fn channels(mut self, input: i32) -> Self {
        self.channels = ::std::option::Option::Some(input);
        self
    }
    /// <p>The number of audio channels in the stream. Used when the <code>streamType</code> is <code>Audio</code>.</p>
    pub fn set_channels(mut self, input: ::std::option::Option<i32>) -> Self {
        self.channels = input;
        self
    }
    /// <p>The number of audio channels in the stream. Used when the <code>streamType</code> is <code>Audio</code>.</p>
    pub fn get_channels(&self) -> &::std::option::Option<i32> {
        &self.channels
    }
    /// <p>The number of audio samples captured per second, measured in kilohertz (kHz). Used when the <code>streamType</code> is <code>Audio</code>.</p>
    pub fn sample_rate(mut self, input: i32) -> Self {
        self.sample_rate = ::std::option::Option::Some(input);
        self
    }
    /// <p>The number of audio samples captured per second, measured in kilohertz (kHz). Used when the <code>streamType</code> is <code>Audio</code>.</p>
    pub fn set_sample_rate(mut self, input: ::std::option::Option<i32>) -> Self {
        self.sample_rate = input;
        self
    }
    /// <p>The number of audio samples captured per second, measured in kilohertz (kHz). Used when the <code>streamType</code> is <code>Audio</code>.</p>
    pub fn get_sample_rate(&self) -> &::std::option::Option<i32> {
        &self.sample_rate
    }
    /// Consumes the builder and constructs a [`NdiMediaStreamInfo`](crate::types::NdiMediaStreamInfo).
    pub fn build(self) -> crate::types::NdiMediaStreamInfo {
        crate::types::NdiMediaStreamInfo {
            stream_type: self.stream_type,
            codec: self.codec,
            stream_id: self.stream_id,
            scan_mode: self.scan_mode,
            frame_resolution: self.frame_resolution,
            frame_rate: self.frame_rate,
            channels: self.channels,
            sample_rate: self.sample_rate,
        }
    }
}
