// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The fabric configuration settings for the router output.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct FabricConfiguration {
    /// <p>The recovery latency mode for the router fabric connection. Valid values include the following:</p>
    /// <ul>
    /// <li>
    /// <p><code>BALANCED</code> (default) – Optimizes for stream quality.</p></li>
    /// <li>
    /// <p><code>LOW_LATENCY</code> – Reduces latency at the potential cost of stream quality under adverse network conditions.</p></li>
    /// </ul>
    pub recovery_latency_mode: crate::types::FabricLatencyMode,
}
impl FabricConfiguration {
    /// <p>The recovery latency mode for the router fabric connection. Valid values include the following:</p>
    /// <ul>
    /// <li>
    /// <p><code>BALANCED</code> (default) – Optimizes for stream quality.</p></li>
    /// <li>
    /// <p><code>LOW_LATENCY</code> – Reduces latency at the potential cost of stream quality under adverse network conditions.</p></li>
    /// </ul>
    pub fn recovery_latency_mode(&self) -> &crate::types::FabricLatencyMode {
        &self.recovery_latency_mode
    }
}
impl FabricConfiguration {
    /// Creates a new builder-style object to manufacture [`FabricConfiguration`](crate::types::FabricConfiguration).
    pub fn builder() -> crate::types::builders::FabricConfigurationBuilder {
        crate::types::builders::FabricConfigurationBuilder::default()
    }
}

/// A builder for [`FabricConfiguration`](crate::types::FabricConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct FabricConfigurationBuilder {
    pub(crate) recovery_latency_mode: ::std::option::Option<crate::types::FabricLatencyMode>,
}
impl FabricConfigurationBuilder {
    /// <p>The recovery latency mode for the router fabric connection. Valid values include the following:</p>
    /// <ul>
    /// <li>
    /// <p><code>BALANCED</code> (default) – Optimizes for stream quality.</p></li>
    /// <li>
    /// <p><code>LOW_LATENCY</code> – Reduces latency at the potential cost of stream quality under adverse network conditions.</p></li>
    /// </ul>
    /// This field is required.
    pub fn recovery_latency_mode(mut self, input: crate::types::FabricLatencyMode) -> Self {
        self.recovery_latency_mode = ::std::option::Option::Some(input);
        self
    }
    /// <p>The recovery latency mode for the router fabric connection. Valid values include the following:</p>
    /// <ul>
    /// <li>
    /// <p><code>BALANCED</code> (default) – Optimizes for stream quality.</p></li>
    /// <li>
    /// <p><code>LOW_LATENCY</code> – Reduces latency at the potential cost of stream quality under adverse network conditions.</p></li>
    /// </ul>
    pub fn set_recovery_latency_mode(mut self, input: ::std::option::Option<crate::types::FabricLatencyMode>) -> Self {
        self.recovery_latency_mode = input;
        self
    }
    /// <p>The recovery latency mode for the router fabric connection. Valid values include the following:</p>
    /// <ul>
    /// <li>
    /// <p><code>BALANCED</code> (default) – Optimizes for stream quality.</p></li>
    /// <li>
    /// <p><code>LOW_LATENCY</code> – Reduces latency at the potential cost of stream quality under adverse network conditions.</p></li>
    /// </ul>
    pub fn get_recovery_latency_mode(&self) -> &::std::option::Option<crate::types::FabricLatencyMode> {
        &self.recovery_latency_mode
    }
    /// Consumes the builder and constructs a [`FabricConfiguration`](crate::types::FabricConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`recovery_latency_mode`](crate::types::builders::FabricConfigurationBuilder::recovery_latency_mode)
    pub fn build(self) -> ::std::result::Result<crate::types::FabricConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::FabricConfiguration {
            recovery_latency_mode: self.recovery_latency_mode.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "recovery_latency_mode",
                    "recovery_latency_mode was not specified but it is required when building FabricConfiguration",
                )
            })?,
        })
    }
}
