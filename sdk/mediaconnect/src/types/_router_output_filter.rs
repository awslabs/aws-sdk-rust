// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A filter that can be used to retrieve a list of router outputs.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum RouterOutputFilter {
    /// <p>The names of the router outputs to include in the filter.</p>
    NameContains(::std::vec::Vec<::std::string::String>),
    /// <p>The Amazon Resource Names (ARNs) of the network interfaces associated with the router outputs to include in the filter.</p>
    NetworkInterfaceArns(::std::vec::Vec<::std::string::String>),
    /// <p>The types of router outputs to include in the filter.</p>
    OutputTypes(::std::vec::Vec<crate::types::RouterOutputType>),
    /// <p>The AWS Regions of the router outputs to include in the filter.</p>
    RegionNames(::std::vec::Vec<::std::string::String>),
    /// <p>The ARNs of the router inputs associated with the router outputs to include in the filter.</p>
    RoutedInputArns(::std::vec::Vec<::std::string::String>),
    /// <p>Filter criteria to list router outputs based on their routing scope.</p>
    RoutingScopes(::std::vec::Vec<crate::types::RoutingScope>),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl RouterOutputFilter {
    /// Tries to convert the enum instance into [`NameContains`](crate::types::RouterOutputFilter::NameContains), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_name_contains(&self) -> ::std::result::Result<&::std::vec::Vec<::std::string::String>, &Self> {
        if let RouterOutputFilter::NameContains(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`NameContains`](crate::types::RouterOutputFilter::NameContains).
    pub fn is_name_contains(&self) -> bool {
        self.as_name_contains().is_ok()
    }
    /// Tries to convert the enum instance into [`NetworkInterfaceArns`](crate::types::RouterOutputFilter::NetworkInterfaceArns), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_network_interface_arns(&self) -> ::std::result::Result<&::std::vec::Vec<::std::string::String>, &Self> {
        if let RouterOutputFilter::NetworkInterfaceArns(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`NetworkInterfaceArns`](crate::types::RouterOutputFilter::NetworkInterfaceArns).
    pub fn is_network_interface_arns(&self) -> bool {
        self.as_network_interface_arns().is_ok()
    }
    /// Tries to convert the enum instance into [`OutputTypes`](crate::types::RouterOutputFilter::OutputTypes), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_output_types(&self) -> ::std::result::Result<&::std::vec::Vec<crate::types::RouterOutputType>, &Self> {
        if let RouterOutputFilter::OutputTypes(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`OutputTypes`](crate::types::RouterOutputFilter::OutputTypes).
    pub fn is_output_types(&self) -> bool {
        self.as_output_types().is_ok()
    }
    /// Tries to convert the enum instance into [`RegionNames`](crate::types::RouterOutputFilter::RegionNames), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_region_names(&self) -> ::std::result::Result<&::std::vec::Vec<::std::string::String>, &Self> {
        if let RouterOutputFilter::RegionNames(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`RegionNames`](crate::types::RouterOutputFilter::RegionNames).
    pub fn is_region_names(&self) -> bool {
        self.as_region_names().is_ok()
    }
    /// Tries to convert the enum instance into [`RoutedInputArns`](crate::types::RouterOutputFilter::RoutedInputArns), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_routed_input_arns(&self) -> ::std::result::Result<&::std::vec::Vec<::std::string::String>, &Self> {
        if let RouterOutputFilter::RoutedInputArns(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`RoutedInputArns`](crate::types::RouterOutputFilter::RoutedInputArns).
    pub fn is_routed_input_arns(&self) -> bool {
        self.as_routed_input_arns().is_ok()
    }
    /// Tries to convert the enum instance into [`RoutingScopes`](crate::types::RouterOutputFilter::RoutingScopes), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_routing_scopes(&self) -> ::std::result::Result<&::std::vec::Vec<crate::types::RoutingScope>, &Self> {
        if let RouterOutputFilter::RoutingScopes(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`RoutingScopes`](crate::types::RouterOutputFilter::RoutingScopes).
    pub fn is_routing_scopes(&self) -> bool {
        self.as_routing_scopes().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
