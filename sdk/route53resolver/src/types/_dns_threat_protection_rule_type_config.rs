// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration for a DNS threat protection rule type within the rule type framework.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct DnsThreatProtectionRuleTypeConfig {
    /// <p>The type of DNS threat protection. Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>DGA</code>: Domain generation algorithms detection. DGAs are used by attackers to generate a large number of domains to launch malware attacks.</p></li>
    /// <li>
    /// <p><code>DNS_TUNNELING</code>: DNS tunneling detection. DNS tunneling is used by attackers to exfiltrate data from the client by using the DNS tunnel without making a network connection to the client.</p></li>
    /// <li>
    /// <p><code>DICTIONARY_DGA</code>: Dictionary-based domain generation algorithms detection. Dictionary DGAs use wordlists to generate domains that appear more legitimate, making them harder to detect than traditional DGAs.</p></li>
    /// </ul>
    pub value: ::std::string::String,
    /// <p>The confidence threshold for DNS Firewall Advanced. You must provide this value when you create or update a DNS Firewall Advanced rule. The confidence level values mean:</p>
    /// <ul>
    /// <li>
    /// <p><code>LOW</code>: Provides the highest detection rate for threats, but also increases false positives.</p></li>
    /// <li>
    /// <p><code>MEDIUM</code>: Provides a balance between detecting threats and false positives.</p></li>
    /// <li>
    /// <p><code>HIGH</code>: Detects only the most well corroborated threats with a low rate of false positives.</p></li>
    /// </ul>
    pub confidence_threshold: crate::types::ConfidenceThreshold,
}
impl DnsThreatProtectionRuleTypeConfig {
    /// <p>The type of DNS threat protection. Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>DGA</code>: Domain generation algorithms detection. DGAs are used by attackers to generate a large number of domains to launch malware attacks.</p></li>
    /// <li>
    /// <p><code>DNS_TUNNELING</code>: DNS tunneling detection. DNS tunneling is used by attackers to exfiltrate data from the client by using the DNS tunnel without making a network connection to the client.</p></li>
    /// <li>
    /// <p><code>DICTIONARY_DGA</code>: Dictionary-based domain generation algorithms detection. Dictionary DGAs use wordlists to generate domains that appear more legitimate, making them harder to detect than traditional DGAs.</p></li>
    /// </ul>
    pub fn value(&self) -> &str {
        use std::ops::Deref;
        self.value.deref()
    }
    /// <p>The confidence threshold for DNS Firewall Advanced. You must provide this value when you create or update a DNS Firewall Advanced rule. The confidence level values mean:</p>
    /// <ul>
    /// <li>
    /// <p><code>LOW</code>: Provides the highest detection rate for threats, but also increases false positives.</p></li>
    /// <li>
    /// <p><code>MEDIUM</code>: Provides a balance between detecting threats and false positives.</p></li>
    /// <li>
    /// <p><code>HIGH</code>: Detects only the most well corroborated threats with a low rate of false positives.</p></li>
    /// </ul>
    pub fn confidence_threshold(&self) -> &crate::types::ConfidenceThreshold {
        &self.confidence_threshold
    }
}
impl DnsThreatProtectionRuleTypeConfig {
    /// Creates a new builder-style object to manufacture [`DnsThreatProtectionRuleTypeConfig`](crate::types::DnsThreatProtectionRuleTypeConfig).
    pub fn builder() -> crate::types::builders::DnsThreatProtectionRuleTypeConfigBuilder {
        crate::types::builders::DnsThreatProtectionRuleTypeConfigBuilder::default()
    }
}

/// A builder for [`DnsThreatProtectionRuleTypeConfig`](crate::types::DnsThreatProtectionRuleTypeConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct DnsThreatProtectionRuleTypeConfigBuilder {
    pub(crate) value: ::std::option::Option<::std::string::String>,
    pub(crate) confidence_threshold: ::std::option::Option<crate::types::ConfidenceThreshold>,
}
impl DnsThreatProtectionRuleTypeConfigBuilder {
    /// <p>The type of DNS threat protection. Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>DGA</code>: Domain generation algorithms detection. DGAs are used by attackers to generate a large number of domains to launch malware attacks.</p></li>
    /// <li>
    /// <p><code>DNS_TUNNELING</code>: DNS tunneling detection. DNS tunneling is used by attackers to exfiltrate data from the client by using the DNS tunnel without making a network connection to the client.</p></li>
    /// <li>
    /// <p><code>DICTIONARY_DGA</code>: Dictionary-based domain generation algorithms detection. Dictionary DGAs use wordlists to generate domains that appear more legitimate, making them harder to detect than traditional DGAs.</p></li>
    /// </ul>
    /// This field is required.
    pub fn value(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.value = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The type of DNS threat protection. Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>DGA</code>: Domain generation algorithms detection. DGAs are used by attackers to generate a large number of domains to launch malware attacks.</p></li>
    /// <li>
    /// <p><code>DNS_TUNNELING</code>: DNS tunneling detection. DNS tunneling is used by attackers to exfiltrate data from the client by using the DNS tunnel without making a network connection to the client.</p></li>
    /// <li>
    /// <p><code>DICTIONARY_DGA</code>: Dictionary-based domain generation algorithms detection. Dictionary DGAs use wordlists to generate domains that appear more legitimate, making them harder to detect than traditional DGAs.</p></li>
    /// </ul>
    pub fn set_value(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.value = input;
        self
    }
    /// <p>The type of DNS threat protection. Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>DGA</code>: Domain generation algorithms detection. DGAs are used by attackers to generate a large number of domains to launch malware attacks.</p></li>
    /// <li>
    /// <p><code>DNS_TUNNELING</code>: DNS tunneling detection. DNS tunneling is used by attackers to exfiltrate data from the client by using the DNS tunnel without making a network connection to the client.</p></li>
    /// <li>
    /// <p><code>DICTIONARY_DGA</code>: Dictionary-based domain generation algorithms detection. Dictionary DGAs use wordlists to generate domains that appear more legitimate, making them harder to detect than traditional DGAs.</p></li>
    /// </ul>
    pub fn get_value(&self) -> &::std::option::Option<::std::string::String> {
        &self.value
    }
    /// <p>The confidence threshold for DNS Firewall Advanced. You must provide this value when you create or update a DNS Firewall Advanced rule. The confidence level values mean:</p>
    /// <ul>
    /// <li>
    /// <p><code>LOW</code>: Provides the highest detection rate for threats, but also increases false positives.</p></li>
    /// <li>
    /// <p><code>MEDIUM</code>: Provides a balance between detecting threats and false positives.</p></li>
    /// <li>
    /// <p><code>HIGH</code>: Detects only the most well corroborated threats with a low rate of false positives.</p></li>
    /// </ul>
    /// This field is required.
    pub fn confidence_threshold(mut self, input: crate::types::ConfidenceThreshold) -> Self {
        self.confidence_threshold = ::std::option::Option::Some(input);
        self
    }
    /// <p>The confidence threshold for DNS Firewall Advanced. You must provide this value when you create or update a DNS Firewall Advanced rule. The confidence level values mean:</p>
    /// <ul>
    /// <li>
    /// <p><code>LOW</code>: Provides the highest detection rate for threats, but also increases false positives.</p></li>
    /// <li>
    /// <p><code>MEDIUM</code>: Provides a balance between detecting threats and false positives.</p></li>
    /// <li>
    /// <p><code>HIGH</code>: Detects only the most well corroborated threats with a low rate of false positives.</p></li>
    /// </ul>
    pub fn set_confidence_threshold(mut self, input: ::std::option::Option<crate::types::ConfidenceThreshold>) -> Self {
        self.confidence_threshold = input;
        self
    }
    /// <p>The confidence threshold for DNS Firewall Advanced. You must provide this value when you create or update a DNS Firewall Advanced rule. The confidence level values mean:</p>
    /// <ul>
    /// <li>
    /// <p><code>LOW</code>: Provides the highest detection rate for threats, but also increases false positives.</p></li>
    /// <li>
    /// <p><code>MEDIUM</code>: Provides a balance between detecting threats and false positives.</p></li>
    /// <li>
    /// <p><code>HIGH</code>: Detects only the most well corroborated threats with a low rate of false positives.</p></li>
    /// </ul>
    pub fn get_confidence_threshold(&self) -> &::std::option::Option<crate::types::ConfidenceThreshold> {
        &self.confidence_threshold
    }
    /// Consumes the builder and constructs a [`DnsThreatProtectionRuleTypeConfig`](crate::types::DnsThreatProtectionRuleTypeConfig).
    /// This method will fail if any of the following fields are not set:
    /// - [`value`](crate::types::builders::DnsThreatProtectionRuleTypeConfigBuilder::value)
    /// - [`confidence_threshold`](crate::types::builders::DnsThreatProtectionRuleTypeConfigBuilder::confidence_threshold)
    pub fn build(self) -> ::std::result::Result<crate::types::DnsThreatProtectionRuleTypeConfig, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::DnsThreatProtectionRuleTypeConfig {
            value: self.value.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "value",
                    "value was not specified but it is required when building DnsThreatProtectionRuleTypeConfig",
                )
            })?,
            confidence_threshold: self.confidence_threshold.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "confidence_threshold",
                    "confidence_threshold was not specified but it is required when building DnsThreatProtectionRuleTypeConfig",
                )
            })?,
        })
    }
}
