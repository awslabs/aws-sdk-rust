// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A logical expression that selects resources. Exactly one of <code>criteria</code>, <code>and</code>, <code>or</code>, or <code>not</code> is set.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum ResourceLogicalExpression {
    /// <p>A list of subexpressions that must all match.</p>
    And(::std::vec::Vec<crate::types::ResourceLogicalExpression>),
    /// <p>A leaf condition that matches resources by tag or by resource-type-specific configuration.</p>
    Criteria(crate::types::ResourceCriteria),
    /// <p>A subexpression that must not match.</p>
    Not(::std::boxed::Box<crate::types::ResourceLogicalExpression>),
    /// <p>A list of subexpressions of which at least one must match.</p>
    Or(::std::vec::Vec<crate::types::ResourceLogicalExpression>),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl ResourceLogicalExpression {
    /// Tries to convert the enum instance into [`And`](crate::types::ResourceLogicalExpression::And), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_and(&self) -> ::std::result::Result<&::std::vec::Vec<crate::types::ResourceLogicalExpression>, &Self> {
        if let ResourceLogicalExpression::And(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`And`](crate::types::ResourceLogicalExpression::And).
    pub fn is_and(&self) -> bool {
        self.as_and().is_ok()
    }
    /// Tries to convert the enum instance into [`Criteria`](crate::types::ResourceLogicalExpression::Criteria), extracting the inner [`ResourceCriteria`](crate::types::ResourceCriteria).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_criteria(&self) -> ::std::result::Result<&crate::types::ResourceCriteria, &Self> {
        if let ResourceLogicalExpression::Criteria(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Criteria`](crate::types::ResourceLogicalExpression::Criteria).
    pub fn is_criteria(&self) -> bool {
        self.as_criteria().is_ok()
    }
    /// Tries to convert the enum instance into [`Not`](crate::types::ResourceLogicalExpression::Not), extracting the inner [`ResourceLogicalExpression`](crate::types::ResourceLogicalExpression).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_not(&self) -> ::std::result::Result<&::std::boxed::Box<crate::types::ResourceLogicalExpression>, &Self> {
        if let ResourceLogicalExpression::Not(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Not`](crate::types::ResourceLogicalExpression::Not).
    pub fn is_not(&self) -> bool {
        self.as_not().is_ok()
    }
    /// Tries to convert the enum instance into [`Or`](crate::types::ResourceLogicalExpression::Or), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_or(&self) -> ::std::result::Result<&::std::vec::Vec<crate::types::ResourceLogicalExpression>, &Self> {
        if let ResourceLogicalExpression::Or(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Or`](crate::types::ResourceLogicalExpression::Or).
    pub fn is_or(&self) -> bool {
        self.as_or().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
