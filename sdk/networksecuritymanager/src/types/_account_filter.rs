// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Determines which accounts are in scope. Exactly one of <code>includeAll</code>, <code>include</code>, or <code>exclude</code> is set.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum AccountFilter {
    /// <p>Excludes the specified accounts and organizational units. All others are in scope.</p>
    Exclude(crate::types::AccountSet),
    /// <p>Includes only the specified accounts and organizational units.</p>
    Include(crate::types::AccountSet),
    /// <p>Includes all accounts. No account filtering is applied.</p>
    IncludeAll,
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl AccountFilter {
    /// Tries to convert the enum instance into [`Exclude`](crate::types::AccountFilter::Exclude), extracting the inner [`AccountSet`](crate::types::AccountSet).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_exclude(&self) -> ::std::result::Result<&crate::types::AccountSet, &Self> {
        if let AccountFilter::Exclude(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Exclude`](crate::types::AccountFilter::Exclude).
    pub fn is_exclude(&self) -> bool {
        self.as_exclude().is_ok()
    }
    /// Tries to convert the enum instance into [`Include`](crate::types::AccountFilter::Include), extracting the inner [`AccountSet`](crate::types::AccountSet).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_include(&self) -> ::std::result::Result<&crate::types::AccountSet, &Self> {
        if let AccountFilter::Include(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Include`](crate::types::AccountFilter::Include).
    pub fn is_include(&self) -> bool {
        self.as_include().is_ok()
    }
    /// Tries to convert the enum instance into [`IncludeAll`](crate::types::AccountFilter::IncludeAll), extracting the inner `()`.
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_include_all(&self) -> ::std::result::Result<(), &Self> {
        if let AccountFilter::IncludeAll = &self {
            ::std::result::Result::Ok(())
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`IncludeAll`](crate::types::AccountFilter::IncludeAll).
    pub fn is_include_all(&self) -> bool {
        self.as_include_all().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
