// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Defines which accounts and resources are in scope.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ScopeConfiguration {
    /// <p>The account filter that determines which accounts are in scope. When set, exactly one of <code>includeAll</code>, <code>include</code>, or <code>exclude</code> is set.</p>
    /// <p>Organization administrators must include an account filter in every scope configuration. Single-account administrators must omit it: a scope without an account filter applies only to the administrator's own account. The presence of an account filter is fixed when the scope is created: an update can't add an account filter to a scope that was created without one, or remove the account filter from a scope that was created with one.</p>
    pub account_filter: ::std::option::Option<crate::types::AccountFilter>,
    /// <p>The resource-level scoping configuration, keyed by resource type, that defines which resources within the selected accounts are in scope.</p>
    pub resource_scopes: ::std::collections::HashMap<crate::types::ScopeResourceType, crate::types::ResourceScope>,
}
impl ScopeConfiguration {
    /// <p>The account filter that determines which accounts are in scope. When set, exactly one of <code>includeAll</code>, <code>include</code>, or <code>exclude</code> is set.</p>
    /// <p>Organization administrators must include an account filter in every scope configuration. Single-account administrators must omit it: a scope without an account filter applies only to the administrator's own account. The presence of an account filter is fixed when the scope is created: an update can't add an account filter to a scope that was created without one, or remove the account filter from a scope that was created with one.</p>
    pub fn account_filter(&self) -> ::std::option::Option<&crate::types::AccountFilter> {
        self.account_filter.as_ref()
    }
    /// <p>The resource-level scoping configuration, keyed by resource type, that defines which resources within the selected accounts are in scope.</p>
    pub fn resource_scopes(&self) -> &::std::collections::HashMap<crate::types::ScopeResourceType, crate::types::ResourceScope> {
        &self.resource_scopes
    }
}
impl ScopeConfiguration {
    /// Creates a new builder-style object to manufacture [`ScopeConfiguration`](crate::types::ScopeConfiguration).
    pub fn builder() -> crate::types::builders::ScopeConfigurationBuilder {
        crate::types::builders::ScopeConfigurationBuilder::default()
    }
}

/// A builder for [`ScopeConfiguration`](crate::types::ScopeConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ScopeConfigurationBuilder {
    pub(crate) account_filter: ::std::option::Option<crate::types::AccountFilter>,
    pub(crate) resource_scopes: ::std::option::Option<::std::collections::HashMap<crate::types::ScopeResourceType, crate::types::ResourceScope>>,
}
impl ScopeConfigurationBuilder {
    /// <p>The account filter that determines which accounts are in scope. When set, exactly one of <code>includeAll</code>, <code>include</code>, or <code>exclude</code> is set.</p>
    /// <p>Organization administrators must include an account filter in every scope configuration. Single-account administrators must omit it: a scope without an account filter applies only to the administrator's own account. The presence of an account filter is fixed when the scope is created: an update can't add an account filter to a scope that was created without one, or remove the account filter from a scope that was created with one.</p>
    pub fn account_filter(mut self, input: crate::types::AccountFilter) -> Self {
        self.account_filter = ::std::option::Option::Some(input);
        self
    }
    /// <p>The account filter that determines which accounts are in scope. When set, exactly one of <code>includeAll</code>, <code>include</code>, or <code>exclude</code> is set.</p>
    /// <p>Organization administrators must include an account filter in every scope configuration. Single-account administrators must omit it: a scope without an account filter applies only to the administrator's own account. The presence of an account filter is fixed when the scope is created: an update can't add an account filter to a scope that was created without one, or remove the account filter from a scope that was created with one.</p>
    pub fn set_account_filter(mut self, input: ::std::option::Option<crate::types::AccountFilter>) -> Self {
        self.account_filter = input;
        self
    }
    /// <p>The account filter that determines which accounts are in scope. When set, exactly one of <code>includeAll</code>, <code>include</code>, or <code>exclude</code> is set.</p>
    /// <p>Organization administrators must include an account filter in every scope configuration. Single-account administrators must omit it: a scope without an account filter applies only to the administrator's own account. The presence of an account filter is fixed when the scope is created: an update can't add an account filter to a scope that was created without one, or remove the account filter from a scope that was created with one.</p>
    pub fn get_account_filter(&self) -> &::std::option::Option<crate::types::AccountFilter> {
        &self.account_filter
    }
    /// Adds a key-value pair to `resource_scopes`.
    ///
    /// To override the contents of this collection use [`set_resource_scopes`](Self::set_resource_scopes).
    ///
    /// <p>The resource-level scoping configuration, keyed by resource type, that defines which resources within the selected accounts are in scope.</p>
    pub fn resource_scopes(mut self, k: crate::types::ScopeResourceType, v: crate::types::ResourceScope) -> Self {
        let mut hash_map = self.resource_scopes.unwrap_or_default();
        hash_map.insert(k, v);
        self.resource_scopes = ::std::option::Option::Some(hash_map);
        self
    }
    /// <p>The resource-level scoping configuration, keyed by resource type, that defines which resources within the selected accounts are in scope.</p>
    pub fn set_resource_scopes(
        mut self,
        input: ::std::option::Option<::std::collections::HashMap<crate::types::ScopeResourceType, crate::types::ResourceScope>>,
    ) -> Self {
        self.resource_scopes = input;
        self
    }
    /// <p>The resource-level scoping configuration, keyed by resource type, that defines which resources within the selected accounts are in scope.</p>
    pub fn get_resource_scopes(
        &self,
    ) -> &::std::option::Option<::std::collections::HashMap<crate::types::ScopeResourceType, crate::types::ResourceScope>> {
        &self.resource_scopes
    }
    /// Consumes the builder and constructs a [`ScopeConfiguration`](crate::types::ScopeConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`resource_scopes`](crate::types::builders::ScopeConfigurationBuilder::resource_scopes)
    pub fn build(self) -> ::std::result::Result<crate::types::ScopeConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::ScopeConfiguration {
            account_filter: self.account_filter,
            resource_scopes: self.resource_scopes.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "resource_scopes",
                    "resource_scopes was not specified but it is required when building ScopeConfiguration",
                )
            })?,
        })
    }
}
