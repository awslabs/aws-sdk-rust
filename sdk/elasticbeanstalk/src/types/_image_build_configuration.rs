// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Settings that Elastic Beanstalk uses to build a container image from the source bundle of an application version.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ImageBuildConfiguration {
    /// <p>How Elastic Beanstalk builds the container image. Elastic Beanstalk rejects a <code>Build</code> that doesn't specify it.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>docker</code> – Elastic Beanstalk builds the image from a Dockerfile in your source bundle. Specify the Dockerfile with <code>DockerfileLocation</code>.</p></li>
    /// <li>
    /// <p><code>buildpack</code> – Elastic Beanstalk builds the image with a Cloud Native Buildpacks builder. Specify the builder with <code>Buildpack</code>.</p></li>
    /// </ul>
    pub r#type: ::std::option::Option<crate::types::ImageBuildType>,
    /// <p>The path to the Dockerfile within the source bundle, relative to the root of the source bundle. For example, <code>backend/Dockerfile</code>.</p>
    /// <p>Elastic Beanstalk uses this member only when <code>Type</code> is <code>docker</code>. If you don't specify it, Elastic Beanstalk uses the Dockerfile at the root of the source bundle.</p>
    pub dockerfile_location: ::std::option::Option<::std::string::String>,
    /// <p>The Cloud Native Buildpacks builder image that Elastic Beanstalk uses to build the container image. For example, <code>paketobuildpacks/builder-jammy-base</code>.</p>
    /// <p>This member is required when <code>Type</code> is <code>buildpack</code>. Elastic Beanstalk doesn't provide a default builder.</p>
    pub buildpack: ::std::option::Option<::std::string::String>,
    /// <p>The processor architecture that Elastic Beanstalk builds the container image for. The architecture must match the architecture of the instances in the environment that you deploy the application version to.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>amd64</code> – x86-64 instances. This is the default.</p></li>
    /// <li>
    /// <p><code>arm64</code> – Amazon Web Services Graviton instances.</p></li>
    /// </ul>
    pub architecture: ::std::option::Option<crate::types::ArchitectureType>,
    /// <p>The Amazon Resource Name (ARN) of the Identity and Access Management (IAM) role that CodeBuild assumes to run the build in your Amazon Web Services account. Elastic Beanstalk rejects a <code>Build</code> that doesn't specify this role.</p>
    pub code_build_service_role: ::std::option::Option<::std::string::String>,
    /// <p>The size of the compute resources that run the build. If you don't specify it, Elastic Beanstalk uses <code>BUILD_GENERAL1_MEDIUM</code>.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>BUILD_GENERAL1_SMALL</code> – Use up to 3 GB memory and 2 vCPUs for builds.</p></li>
    /// <li>
    /// <p><code>BUILD_GENERAL1_MEDIUM</code> – Use up to 7 GB memory and 4 vCPUs for builds.</p></li>
    /// <li>
    /// <p><code>BUILD_GENERAL1_LARGE</code> – Use up to 15 GB memory and 8 vCPUs for builds.</p></li>
    /// </ul>
    pub compute_type: ::std::option::Option<crate::types::ComputeType>,
    /// <p>How long, in minutes from 5 to 480 (8 hours), Elastic Beanstalk waits before stopping a build that hasn't completed. The default is 60 minutes.</p>
    pub timeout_in_minutes: ::std::option::Option<i32>,
}
impl ImageBuildConfiguration {
    /// <p>How Elastic Beanstalk builds the container image. Elastic Beanstalk rejects a <code>Build</code> that doesn't specify it.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>docker</code> – Elastic Beanstalk builds the image from a Dockerfile in your source bundle. Specify the Dockerfile with <code>DockerfileLocation</code>.</p></li>
    /// <li>
    /// <p><code>buildpack</code> – Elastic Beanstalk builds the image with a Cloud Native Buildpacks builder. Specify the builder with <code>Buildpack</code>.</p></li>
    /// </ul>
    pub fn r#type(&self) -> ::std::option::Option<&crate::types::ImageBuildType> {
        self.r#type.as_ref()
    }
    /// <p>The path to the Dockerfile within the source bundle, relative to the root of the source bundle. For example, <code>backend/Dockerfile</code>.</p>
    /// <p>Elastic Beanstalk uses this member only when <code>Type</code> is <code>docker</code>. If you don't specify it, Elastic Beanstalk uses the Dockerfile at the root of the source bundle.</p>
    pub fn dockerfile_location(&self) -> ::std::option::Option<&str> {
        self.dockerfile_location.as_deref()
    }
    /// <p>The Cloud Native Buildpacks builder image that Elastic Beanstalk uses to build the container image. For example, <code>paketobuildpacks/builder-jammy-base</code>.</p>
    /// <p>This member is required when <code>Type</code> is <code>buildpack</code>. Elastic Beanstalk doesn't provide a default builder.</p>
    pub fn buildpack(&self) -> ::std::option::Option<&str> {
        self.buildpack.as_deref()
    }
    /// <p>The processor architecture that Elastic Beanstalk builds the container image for. The architecture must match the architecture of the instances in the environment that you deploy the application version to.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>amd64</code> – x86-64 instances. This is the default.</p></li>
    /// <li>
    /// <p><code>arm64</code> – Amazon Web Services Graviton instances.</p></li>
    /// </ul>
    pub fn architecture(&self) -> ::std::option::Option<&crate::types::ArchitectureType> {
        self.architecture.as_ref()
    }
    /// <p>The Amazon Resource Name (ARN) of the Identity and Access Management (IAM) role that CodeBuild assumes to run the build in your Amazon Web Services account. Elastic Beanstalk rejects a <code>Build</code> that doesn't specify this role.</p>
    pub fn code_build_service_role(&self) -> ::std::option::Option<&str> {
        self.code_build_service_role.as_deref()
    }
    /// <p>The size of the compute resources that run the build. If you don't specify it, Elastic Beanstalk uses <code>BUILD_GENERAL1_MEDIUM</code>.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>BUILD_GENERAL1_SMALL</code> – Use up to 3 GB memory and 2 vCPUs for builds.</p></li>
    /// <li>
    /// <p><code>BUILD_GENERAL1_MEDIUM</code> – Use up to 7 GB memory and 4 vCPUs for builds.</p></li>
    /// <li>
    /// <p><code>BUILD_GENERAL1_LARGE</code> – Use up to 15 GB memory and 8 vCPUs for builds.</p></li>
    /// </ul>
    pub fn compute_type(&self) -> ::std::option::Option<&crate::types::ComputeType> {
        self.compute_type.as_ref()
    }
    /// <p>How long, in minutes from 5 to 480 (8 hours), Elastic Beanstalk waits before stopping a build that hasn't completed. The default is 60 minutes.</p>
    pub fn timeout_in_minutes(&self) -> ::std::option::Option<i32> {
        self.timeout_in_minutes
    }
}
impl ImageBuildConfiguration {
    /// Creates a new builder-style object to manufacture [`ImageBuildConfiguration`](crate::types::ImageBuildConfiguration).
    pub fn builder() -> crate::types::builders::ImageBuildConfigurationBuilder {
        crate::types::builders::ImageBuildConfigurationBuilder::default()
    }
}

/// A builder for [`ImageBuildConfiguration`](crate::types::ImageBuildConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ImageBuildConfigurationBuilder {
    pub(crate) r#type: ::std::option::Option<crate::types::ImageBuildType>,
    pub(crate) dockerfile_location: ::std::option::Option<::std::string::String>,
    pub(crate) buildpack: ::std::option::Option<::std::string::String>,
    pub(crate) architecture: ::std::option::Option<crate::types::ArchitectureType>,
    pub(crate) code_build_service_role: ::std::option::Option<::std::string::String>,
    pub(crate) compute_type: ::std::option::Option<crate::types::ComputeType>,
    pub(crate) timeout_in_minutes: ::std::option::Option<i32>,
}
impl ImageBuildConfigurationBuilder {
    /// <p>How Elastic Beanstalk builds the container image. Elastic Beanstalk rejects a <code>Build</code> that doesn't specify it.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>docker</code> – Elastic Beanstalk builds the image from a Dockerfile in your source bundle. Specify the Dockerfile with <code>DockerfileLocation</code>.</p></li>
    /// <li>
    /// <p><code>buildpack</code> – Elastic Beanstalk builds the image with a Cloud Native Buildpacks builder. Specify the builder with <code>Buildpack</code>.</p></li>
    /// </ul>
    pub fn r#type(mut self, input: crate::types::ImageBuildType) -> Self {
        self.r#type = ::std::option::Option::Some(input);
        self
    }
    /// <p>How Elastic Beanstalk builds the container image. Elastic Beanstalk rejects a <code>Build</code> that doesn't specify it.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>docker</code> – Elastic Beanstalk builds the image from a Dockerfile in your source bundle. Specify the Dockerfile with <code>DockerfileLocation</code>.</p></li>
    /// <li>
    /// <p><code>buildpack</code> – Elastic Beanstalk builds the image with a Cloud Native Buildpacks builder. Specify the builder with <code>Buildpack</code>.</p></li>
    /// </ul>
    pub fn set_type(mut self, input: ::std::option::Option<crate::types::ImageBuildType>) -> Self {
        self.r#type = input;
        self
    }
    /// <p>How Elastic Beanstalk builds the container image. Elastic Beanstalk rejects a <code>Build</code> that doesn't specify it.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>docker</code> – Elastic Beanstalk builds the image from a Dockerfile in your source bundle. Specify the Dockerfile with <code>DockerfileLocation</code>.</p></li>
    /// <li>
    /// <p><code>buildpack</code> – Elastic Beanstalk builds the image with a Cloud Native Buildpacks builder. Specify the builder with <code>Buildpack</code>.</p></li>
    /// </ul>
    pub fn get_type(&self) -> &::std::option::Option<crate::types::ImageBuildType> {
        &self.r#type
    }
    /// <p>The path to the Dockerfile within the source bundle, relative to the root of the source bundle. For example, <code>backend/Dockerfile</code>.</p>
    /// <p>Elastic Beanstalk uses this member only when <code>Type</code> is <code>docker</code>. If you don't specify it, Elastic Beanstalk uses the Dockerfile at the root of the source bundle.</p>
    pub fn dockerfile_location(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.dockerfile_location = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The path to the Dockerfile within the source bundle, relative to the root of the source bundle. For example, <code>backend/Dockerfile</code>.</p>
    /// <p>Elastic Beanstalk uses this member only when <code>Type</code> is <code>docker</code>. If you don't specify it, Elastic Beanstalk uses the Dockerfile at the root of the source bundle.</p>
    pub fn set_dockerfile_location(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.dockerfile_location = input;
        self
    }
    /// <p>The path to the Dockerfile within the source bundle, relative to the root of the source bundle. For example, <code>backend/Dockerfile</code>.</p>
    /// <p>Elastic Beanstalk uses this member only when <code>Type</code> is <code>docker</code>. If you don't specify it, Elastic Beanstalk uses the Dockerfile at the root of the source bundle.</p>
    pub fn get_dockerfile_location(&self) -> &::std::option::Option<::std::string::String> {
        &self.dockerfile_location
    }
    /// <p>The Cloud Native Buildpacks builder image that Elastic Beanstalk uses to build the container image. For example, <code>paketobuildpacks/builder-jammy-base</code>.</p>
    /// <p>This member is required when <code>Type</code> is <code>buildpack</code>. Elastic Beanstalk doesn't provide a default builder.</p>
    pub fn buildpack(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.buildpack = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The Cloud Native Buildpacks builder image that Elastic Beanstalk uses to build the container image. For example, <code>paketobuildpacks/builder-jammy-base</code>.</p>
    /// <p>This member is required when <code>Type</code> is <code>buildpack</code>. Elastic Beanstalk doesn't provide a default builder.</p>
    pub fn set_buildpack(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.buildpack = input;
        self
    }
    /// <p>The Cloud Native Buildpacks builder image that Elastic Beanstalk uses to build the container image. For example, <code>paketobuildpacks/builder-jammy-base</code>.</p>
    /// <p>This member is required when <code>Type</code> is <code>buildpack</code>. Elastic Beanstalk doesn't provide a default builder.</p>
    pub fn get_buildpack(&self) -> &::std::option::Option<::std::string::String> {
        &self.buildpack
    }
    /// <p>The processor architecture that Elastic Beanstalk builds the container image for. The architecture must match the architecture of the instances in the environment that you deploy the application version to.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>amd64</code> – x86-64 instances. This is the default.</p></li>
    /// <li>
    /// <p><code>arm64</code> – Amazon Web Services Graviton instances.</p></li>
    /// </ul>
    pub fn architecture(mut self, input: crate::types::ArchitectureType) -> Self {
        self.architecture = ::std::option::Option::Some(input);
        self
    }
    /// <p>The processor architecture that Elastic Beanstalk builds the container image for. The architecture must match the architecture of the instances in the environment that you deploy the application version to.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>amd64</code> – x86-64 instances. This is the default.</p></li>
    /// <li>
    /// <p><code>arm64</code> – Amazon Web Services Graviton instances.</p></li>
    /// </ul>
    pub fn set_architecture(mut self, input: ::std::option::Option<crate::types::ArchitectureType>) -> Self {
        self.architecture = input;
        self
    }
    /// <p>The processor architecture that Elastic Beanstalk builds the container image for. The architecture must match the architecture of the instances in the environment that you deploy the application version to.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>amd64</code> – x86-64 instances. This is the default.</p></li>
    /// <li>
    /// <p><code>arm64</code> – Amazon Web Services Graviton instances.</p></li>
    /// </ul>
    pub fn get_architecture(&self) -> &::std::option::Option<crate::types::ArchitectureType> {
        &self.architecture
    }
    /// <p>The Amazon Resource Name (ARN) of the Identity and Access Management (IAM) role that CodeBuild assumes to run the build in your Amazon Web Services account. Elastic Beanstalk rejects a <code>Build</code> that doesn't specify this role.</p>
    pub fn code_build_service_role(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.code_build_service_role = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The Amazon Resource Name (ARN) of the Identity and Access Management (IAM) role that CodeBuild assumes to run the build in your Amazon Web Services account. Elastic Beanstalk rejects a <code>Build</code> that doesn't specify this role.</p>
    pub fn set_code_build_service_role(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.code_build_service_role = input;
        self
    }
    /// <p>The Amazon Resource Name (ARN) of the Identity and Access Management (IAM) role that CodeBuild assumes to run the build in your Amazon Web Services account. Elastic Beanstalk rejects a <code>Build</code> that doesn't specify this role.</p>
    pub fn get_code_build_service_role(&self) -> &::std::option::Option<::std::string::String> {
        &self.code_build_service_role
    }
    /// <p>The size of the compute resources that run the build. If you don't specify it, Elastic Beanstalk uses <code>BUILD_GENERAL1_MEDIUM</code>.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>BUILD_GENERAL1_SMALL</code> – Use up to 3 GB memory and 2 vCPUs for builds.</p></li>
    /// <li>
    /// <p><code>BUILD_GENERAL1_MEDIUM</code> – Use up to 7 GB memory and 4 vCPUs for builds.</p></li>
    /// <li>
    /// <p><code>BUILD_GENERAL1_LARGE</code> – Use up to 15 GB memory and 8 vCPUs for builds.</p></li>
    /// </ul>
    pub fn compute_type(mut self, input: crate::types::ComputeType) -> Self {
        self.compute_type = ::std::option::Option::Some(input);
        self
    }
    /// <p>The size of the compute resources that run the build. If you don't specify it, Elastic Beanstalk uses <code>BUILD_GENERAL1_MEDIUM</code>.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>BUILD_GENERAL1_SMALL</code> – Use up to 3 GB memory and 2 vCPUs for builds.</p></li>
    /// <li>
    /// <p><code>BUILD_GENERAL1_MEDIUM</code> – Use up to 7 GB memory and 4 vCPUs for builds.</p></li>
    /// <li>
    /// <p><code>BUILD_GENERAL1_LARGE</code> – Use up to 15 GB memory and 8 vCPUs for builds.</p></li>
    /// </ul>
    pub fn set_compute_type(mut self, input: ::std::option::Option<crate::types::ComputeType>) -> Self {
        self.compute_type = input;
        self
    }
    /// <p>The size of the compute resources that run the build. If you don't specify it, Elastic Beanstalk uses <code>BUILD_GENERAL1_MEDIUM</code>.</p>
    /// <p>Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>BUILD_GENERAL1_SMALL</code> – Use up to 3 GB memory and 2 vCPUs for builds.</p></li>
    /// <li>
    /// <p><code>BUILD_GENERAL1_MEDIUM</code> – Use up to 7 GB memory and 4 vCPUs for builds.</p></li>
    /// <li>
    /// <p><code>BUILD_GENERAL1_LARGE</code> – Use up to 15 GB memory and 8 vCPUs for builds.</p></li>
    /// </ul>
    pub fn get_compute_type(&self) -> &::std::option::Option<crate::types::ComputeType> {
        &self.compute_type
    }
    /// <p>How long, in minutes from 5 to 480 (8 hours), Elastic Beanstalk waits before stopping a build that hasn't completed. The default is 60 minutes.</p>
    pub fn timeout_in_minutes(mut self, input: i32) -> Self {
        self.timeout_in_minutes = ::std::option::Option::Some(input);
        self
    }
    /// <p>How long, in minutes from 5 to 480 (8 hours), Elastic Beanstalk waits before stopping a build that hasn't completed. The default is 60 minutes.</p>
    pub fn set_timeout_in_minutes(mut self, input: ::std::option::Option<i32>) -> Self {
        self.timeout_in_minutes = input;
        self
    }
    /// <p>How long, in minutes from 5 to 480 (8 hours), Elastic Beanstalk waits before stopping a build that hasn't completed. The default is 60 minutes.</p>
    pub fn get_timeout_in_minutes(&self) -> &::std::option::Option<i32> {
        &self.timeout_in_minutes
    }
    /// Consumes the builder and constructs a [`ImageBuildConfiguration`](crate::types::ImageBuildConfiguration).
    pub fn build(self) -> crate::types::ImageBuildConfiguration {
        crate::types::ImageBuildConfiguration {
            r#type: self.r#type,
            dockerfile_location: self.dockerfile_location,
            buildpack: self.buildpack,
            architecture: self.architecture,
            code_build_service_role: self.code_build_service_role,
            compute_type: self.compute_type,
            timeout_in_minutes: self.timeout_in_minutes,
        }
    }
}
