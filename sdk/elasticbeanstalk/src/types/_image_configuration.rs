// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The source of the container image for an application version: an image that you built and pushed to a container registry yourself, or settings for Elastic Beanstalk to build one from your source bundle.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ImageConfiguration {
    /// <p>The location of a container image that you built and pushed to a container registry yourself. Elastic Beanstalk deploys the image without a build step.</p>
    /// <p>If you specify <code>Source</code>, don't specify <code>Build</code> or the request's <code>SourceBundle</code> parameter.</p>
    pub source: ::std::option::Option<crate::types::ImageSource>,
    /// <p>Settings that Elastic Beanstalk uses to build a container image from the source bundle of the application version.</p>
    /// <p>If you specify <code>Build</code>, also specify the request's <code>SourceBundle</code> parameter, and don't specify <code>Source</code>.</p>
    ///
    /// _Note: This member has been renamed from `build`._
    pub build_value: ::std::option::Option<crate::types::ImageBuildConfiguration>,
}
impl ImageConfiguration {
    /// <p>The location of a container image that you built and pushed to a container registry yourself. Elastic Beanstalk deploys the image without a build step.</p>
    /// <p>If you specify <code>Source</code>, don't specify <code>Build</code> or the request's <code>SourceBundle</code> parameter.</p>
    pub fn source(&self) -> ::std::option::Option<&crate::types::ImageSource> {
        self.source.as_ref()
    }
    /// <p>Settings that Elastic Beanstalk uses to build a container image from the source bundle of the application version.</p>
    /// <p>If you specify <code>Build</code>, also specify the request's <code>SourceBundle</code> parameter, and don't specify <code>Source</code>.</p>
    ///
    /// _Note: This member has been renamed from `build`._
    pub fn build_value(&self) -> ::std::option::Option<&crate::types::ImageBuildConfiguration> {
        self.build_value.as_ref()
    }
}
impl ImageConfiguration {
    /// Creates a new builder-style object to manufacture [`ImageConfiguration`](crate::types::ImageConfiguration).
    pub fn builder() -> crate::types::builders::ImageConfigurationBuilder {
        crate::types::builders::ImageConfigurationBuilder::default()
    }
}

/// A builder for [`ImageConfiguration`](crate::types::ImageConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ImageConfigurationBuilder {
    pub(crate) source: ::std::option::Option<crate::types::ImageSource>,
    pub(crate) build_value: ::std::option::Option<crate::types::ImageBuildConfiguration>,
}
impl ImageConfigurationBuilder {
    /// <p>The location of a container image that you built and pushed to a container registry yourself. Elastic Beanstalk deploys the image without a build step.</p>
    /// <p>If you specify <code>Source</code>, don't specify <code>Build</code> or the request's <code>SourceBundle</code> parameter.</p>
    pub fn source(mut self, input: crate::types::ImageSource) -> Self {
        self.source = ::std::option::Option::Some(input);
        self
    }
    /// <p>The location of a container image that you built and pushed to a container registry yourself. Elastic Beanstalk deploys the image without a build step.</p>
    /// <p>If you specify <code>Source</code>, don't specify <code>Build</code> or the request's <code>SourceBundle</code> parameter.</p>
    pub fn set_source(mut self, input: ::std::option::Option<crate::types::ImageSource>) -> Self {
        self.source = input;
        self
    }
    /// <p>The location of a container image that you built and pushed to a container registry yourself. Elastic Beanstalk deploys the image without a build step.</p>
    /// <p>If you specify <code>Source</code>, don't specify <code>Build</code> or the request's <code>SourceBundle</code> parameter.</p>
    pub fn get_source(&self) -> &::std::option::Option<crate::types::ImageSource> {
        &self.source
    }
    /// <p>Settings that Elastic Beanstalk uses to build a container image from the source bundle of the application version.</p>
    /// <p>If you specify <code>Build</code>, also specify the request's <code>SourceBundle</code> parameter, and don't specify <code>Source</code>.</p>
    pub fn build_value(mut self, input: crate::types::ImageBuildConfiguration) -> Self {
        self.build_value = ::std::option::Option::Some(input);
        self
    }
    /// <p>Settings that Elastic Beanstalk uses to build a container image from the source bundle of the application version.</p>
    /// <p>If you specify <code>Build</code>, also specify the request's <code>SourceBundle</code> parameter, and don't specify <code>Source</code>.</p>
    pub fn set_build(mut self, input: ::std::option::Option<crate::types::ImageBuildConfiguration>) -> Self {
        self.build_value = input;
        self
    }
    /// <p>Settings that Elastic Beanstalk uses to build a container image from the source bundle of the application version.</p>
    /// <p>If you specify <code>Build</code>, also specify the request's <code>SourceBundle</code> parameter, and don't specify <code>Source</code>.</p>
    pub fn get_build(&self) -> &::std::option::Option<crate::types::ImageBuildConfiguration> {
        &self.build_value
    }
    /// Consumes the builder and constructs a [`ImageConfiguration`](crate::types::ImageConfiguration).
    pub fn build(self) -> crate::types::ImageConfiguration {
        crate::types::ImageConfiguration {
            source: self.source,
            build_value: self.build_value,
        }
    }
}
