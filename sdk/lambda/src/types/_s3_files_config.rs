// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Setting controls how your function accesses data from an Amazon S3 file system.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct S3FilesConfig {
    /// <p>Specifies if a function reads from the file system for the lowest latency, or through Amazon S3 Files feature "direct Amazon S3 bucket reads" for the highest throughput. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>AUTO</code> (default) – Direct reads are active for functions you configure with 512 MB or more of memory.</p></li>
    /// <li>
    /// <p><code>ENABLED</code> – Enforces all reads are directly from the Amazon S3 bucket, regardless of available memory (less than 512 MB).</p></li>
    /// <li>
    /// <p><code>DISABLED</code> – Routes all reads through the file system, regardless of memory configuration.</p></li>
    /// </ul>
    /// <p>To use direct reads, you must grant the execution role the <code>s3:GetObject</code> and <code>s3:GetObjectVersion</code> permissions. If a direct read fails, Lambda automatically falls back to reading through the file system.</p>
    pub direct_s3_read: ::std::option::Option<crate::types::DirectS3Read>,
}
impl S3FilesConfig {
    /// <p>Specifies if a function reads from the file system for the lowest latency, or through Amazon S3 Files feature "direct Amazon S3 bucket reads" for the highest throughput. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>AUTO</code> (default) – Direct reads are active for functions you configure with 512 MB or more of memory.</p></li>
    /// <li>
    /// <p><code>ENABLED</code> – Enforces all reads are directly from the Amazon S3 bucket, regardless of available memory (less than 512 MB).</p></li>
    /// <li>
    /// <p><code>DISABLED</code> – Routes all reads through the file system, regardless of memory configuration.</p></li>
    /// </ul>
    /// <p>To use direct reads, you must grant the execution role the <code>s3:GetObject</code> and <code>s3:GetObjectVersion</code> permissions. If a direct read fails, Lambda automatically falls back to reading through the file system.</p>
    pub fn direct_s3_read(&self) -> ::std::option::Option<&crate::types::DirectS3Read> {
        self.direct_s3_read.as_ref()
    }
}
impl S3FilesConfig {
    /// Creates a new builder-style object to manufacture [`S3FilesConfig`](crate::types::S3FilesConfig).
    pub fn builder() -> crate::types::builders::S3FilesConfigBuilder {
        crate::types::builders::S3FilesConfigBuilder::default()
    }
}

/// A builder for [`S3FilesConfig`](crate::types::S3FilesConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct S3FilesConfigBuilder {
    pub(crate) direct_s3_read: ::std::option::Option<crate::types::DirectS3Read>,
}
impl S3FilesConfigBuilder {
    /// <p>Specifies if a function reads from the file system for the lowest latency, or through Amazon S3 Files feature "direct Amazon S3 bucket reads" for the highest throughput. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>AUTO</code> (default) – Direct reads are active for functions you configure with 512 MB or more of memory.</p></li>
    /// <li>
    /// <p><code>ENABLED</code> – Enforces all reads are directly from the Amazon S3 bucket, regardless of available memory (less than 512 MB).</p></li>
    /// <li>
    /// <p><code>DISABLED</code> – Routes all reads through the file system, regardless of memory configuration.</p></li>
    /// </ul>
    /// <p>To use direct reads, you must grant the execution role the <code>s3:GetObject</code> and <code>s3:GetObjectVersion</code> permissions. If a direct read fails, Lambda automatically falls back to reading through the file system.</p>
    pub fn direct_s3_read(mut self, input: crate::types::DirectS3Read) -> Self {
        self.direct_s3_read = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies if a function reads from the file system for the lowest latency, or through Amazon S3 Files feature "direct Amazon S3 bucket reads" for the highest throughput. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>AUTO</code> (default) – Direct reads are active for functions you configure with 512 MB or more of memory.</p></li>
    /// <li>
    /// <p><code>ENABLED</code> – Enforces all reads are directly from the Amazon S3 bucket, regardless of available memory (less than 512 MB).</p></li>
    /// <li>
    /// <p><code>DISABLED</code> – Routes all reads through the file system, regardless of memory configuration.</p></li>
    /// </ul>
    /// <p>To use direct reads, you must grant the execution role the <code>s3:GetObject</code> and <code>s3:GetObjectVersion</code> permissions. If a direct read fails, Lambda automatically falls back to reading through the file system.</p>
    pub fn set_direct_s3_read(mut self, input: ::std::option::Option<crate::types::DirectS3Read>) -> Self {
        self.direct_s3_read = input;
        self
    }
    /// <p>Specifies if a function reads from the file system for the lowest latency, or through Amazon S3 Files feature "direct Amazon S3 bucket reads" for the highest throughput. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>AUTO</code> (default) – Direct reads are active for functions you configure with 512 MB or more of memory.</p></li>
    /// <li>
    /// <p><code>ENABLED</code> – Enforces all reads are directly from the Amazon S3 bucket, regardless of available memory (less than 512 MB).</p></li>
    /// <li>
    /// <p><code>DISABLED</code> – Routes all reads through the file system, regardless of memory configuration.</p></li>
    /// </ul>
    /// <p>To use direct reads, you must grant the execution role the <code>s3:GetObject</code> and <code>s3:GetObjectVersion</code> permissions. If a direct read fails, Lambda automatically falls back to reading through the file system.</p>
    pub fn get_direct_s3_read(&self) -> &::std::option::Option<crate::types::DirectS3Read> {
        &self.direct_s3_read
    }
    /// Consumes the builder and constructs a [`S3FilesConfig`](crate::types::S3FilesConfig).
    pub fn build(self) -> crate::types::S3FilesConfig {
        crate::types::S3FilesConfig {
            direct_s3_read: self.direct_s3_read,
        }
    }
}
