// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#![cfg(smoketests)]
//!
//! The tests in this module run against live AWS services. As such,
//! they are disabled by default. To enable them, run the tests with
//!
//! ```sh
//! RUSTFLAGS="--cfg smoketests" cargo test.
//! ```
//!
use aws_sdk_acm::{config, Client};
/// Smoke tests for the `get_certificate` operation
#[::tokio::test]
async fn test_get_certificate_failure() {
    let config = ::aws_config::load_defaults(config::BehaviorVersion::latest()).await;
    let conf = config::Config::from(&config)
        .to_builder()
        .region(config::Region::new("us-west-2"))
        .use_dual_stack(false)
        .use_fips(false)
        .build();
    let client = Client::from_conf(conf);
    let res = client
        .get_certificate()
        .set_certificate_arn(::std::option::Option::Some(
            "arn:aws:acm:region:123456789012:certificate/12345678-1234-1234-1234-123456789012".to_owned(),
        ))
        .send()
        .await;
    res.expect_err("request should fail");
}
/// Smoke tests for the `list_certificates` operation
#[::tokio::test]
async fn test_list_certificates_success() {
    let config = ::aws_config::load_defaults(config::BehaviorVersion::latest()).await;
    let conf = config::Config::from(&config)
        .to_builder()
        .region(config::Region::new("us-west-2"))
        .use_dual_stack(false)
        .use_fips(false)
        .build();
    let client = Client::from_conf(conf);
    let res = client.list_certificates().send().await;
    res.expect("request should succeed");
}
