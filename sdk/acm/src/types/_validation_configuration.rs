// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains the validation method, validation status, and validation challenge details for a domain. This structure appears in <code>DomainValidationSummary</code> as both the active and requested validation configuration.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ValidationConfiguration {
    /// <p>The validation method for this configuration. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>DNS</code> – Validation using a CNAME record added to your DNS configuration.</p></li>
    /// <li>
    /// <p><code>EMAIL</code> – Validation using an approval email sent to domain contacts.</p></li>
    /// <li>
    /// <p><code>HTTP</code> – Validation using an HTTP resource placed on your web server.</p></li>
    /// </ul>
    pub validation_method: ::std::option::Option<crate::types::ValidationMethod>,
    /// <p>The validation challenge details for this configuration. The structure varies by validation method: for DNS validation, contains a <code>DnsValidationChallenge</code> with the CNAME record to add; for email validation, contains an <code>EmailValidationChallenge</code> with the validation email addresses.</p>
    pub validation_challenge: ::std::option::Option<crate::types::ValidationChallenge>,
    /// <p>The validation status for this domain. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>PENDING_VALIDATION</code> – The domain is waiting for validation to complete.</p></li>
    /// <li>
    /// <p><code>SUCCESS</code> – Validation completed successfully.</p></li>
    /// <li>
    /// <p><code>FAILED</code> – Validation failed.</p></li>
    /// </ul>
    pub validation_status: ::std::option::Option<crate::types::DomainStatus>,
}
impl ValidationConfiguration {
    /// <p>The validation method for this configuration. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>DNS</code> – Validation using a CNAME record added to your DNS configuration.</p></li>
    /// <li>
    /// <p><code>EMAIL</code> – Validation using an approval email sent to domain contacts.</p></li>
    /// <li>
    /// <p><code>HTTP</code> – Validation using an HTTP resource placed on your web server.</p></li>
    /// </ul>
    pub fn validation_method(&self) -> ::std::option::Option<&crate::types::ValidationMethod> {
        self.validation_method.as_ref()
    }
    /// <p>The validation challenge details for this configuration. The structure varies by validation method: for DNS validation, contains a <code>DnsValidationChallenge</code> with the CNAME record to add; for email validation, contains an <code>EmailValidationChallenge</code> with the validation email addresses.</p>
    pub fn validation_challenge(&self) -> ::std::option::Option<&crate::types::ValidationChallenge> {
        self.validation_challenge.as_ref()
    }
    /// <p>The validation status for this domain. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>PENDING_VALIDATION</code> – The domain is waiting for validation to complete.</p></li>
    /// <li>
    /// <p><code>SUCCESS</code> – Validation completed successfully.</p></li>
    /// <li>
    /// <p><code>FAILED</code> – Validation failed.</p></li>
    /// </ul>
    pub fn validation_status(&self) -> ::std::option::Option<&crate::types::DomainStatus> {
        self.validation_status.as_ref()
    }
}
impl ValidationConfiguration {
    /// Creates a new builder-style object to manufacture [`ValidationConfiguration`](crate::types::ValidationConfiguration).
    pub fn builder() -> crate::types::builders::ValidationConfigurationBuilder {
        crate::types::builders::ValidationConfigurationBuilder::default()
    }
}

/// A builder for [`ValidationConfiguration`](crate::types::ValidationConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ValidationConfigurationBuilder {
    pub(crate) validation_method: ::std::option::Option<crate::types::ValidationMethod>,
    pub(crate) validation_challenge: ::std::option::Option<crate::types::ValidationChallenge>,
    pub(crate) validation_status: ::std::option::Option<crate::types::DomainStatus>,
}
impl ValidationConfigurationBuilder {
    /// <p>The validation method for this configuration. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>DNS</code> – Validation using a CNAME record added to your DNS configuration.</p></li>
    /// <li>
    /// <p><code>EMAIL</code> – Validation using an approval email sent to domain contacts.</p></li>
    /// <li>
    /// <p><code>HTTP</code> – Validation using an HTTP resource placed on your web server.</p></li>
    /// </ul>
    pub fn validation_method(mut self, input: crate::types::ValidationMethod) -> Self {
        self.validation_method = ::std::option::Option::Some(input);
        self
    }
    /// <p>The validation method for this configuration. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>DNS</code> – Validation using a CNAME record added to your DNS configuration.</p></li>
    /// <li>
    /// <p><code>EMAIL</code> – Validation using an approval email sent to domain contacts.</p></li>
    /// <li>
    /// <p><code>HTTP</code> – Validation using an HTTP resource placed on your web server.</p></li>
    /// </ul>
    pub fn set_validation_method(mut self, input: ::std::option::Option<crate::types::ValidationMethod>) -> Self {
        self.validation_method = input;
        self
    }
    /// <p>The validation method for this configuration. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>DNS</code> – Validation using a CNAME record added to your DNS configuration.</p></li>
    /// <li>
    /// <p><code>EMAIL</code> – Validation using an approval email sent to domain contacts.</p></li>
    /// <li>
    /// <p><code>HTTP</code> – Validation using an HTTP resource placed on your web server.</p></li>
    /// </ul>
    pub fn get_validation_method(&self) -> &::std::option::Option<crate::types::ValidationMethod> {
        &self.validation_method
    }
    /// <p>The validation challenge details for this configuration. The structure varies by validation method: for DNS validation, contains a <code>DnsValidationChallenge</code> with the CNAME record to add; for email validation, contains an <code>EmailValidationChallenge</code> with the validation email addresses.</p>
    pub fn validation_challenge(mut self, input: crate::types::ValidationChallenge) -> Self {
        self.validation_challenge = ::std::option::Option::Some(input);
        self
    }
    /// <p>The validation challenge details for this configuration. The structure varies by validation method: for DNS validation, contains a <code>DnsValidationChallenge</code> with the CNAME record to add; for email validation, contains an <code>EmailValidationChallenge</code> with the validation email addresses.</p>
    pub fn set_validation_challenge(mut self, input: ::std::option::Option<crate::types::ValidationChallenge>) -> Self {
        self.validation_challenge = input;
        self
    }
    /// <p>The validation challenge details for this configuration. The structure varies by validation method: for DNS validation, contains a <code>DnsValidationChallenge</code> with the CNAME record to add; for email validation, contains an <code>EmailValidationChallenge</code> with the validation email addresses.</p>
    pub fn get_validation_challenge(&self) -> &::std::option::Option<crate::types::ValidationChallenge> {
        &self.validation_challenge
    }
    /// <p>The validation status for this domain. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>PENDING_VALIDATION</code> – The domain is waiting for validation to complete.</p></li>
    /// <li>
    /// <p><code>SUCCESS</code> – Validation completed successfully.</p></li>
    /// <li>
    /// <p><code>FAILED</code> – Validation failed.</p></li>
    /// </ul>
    pub fn validation_status(mut self, input: crate::types::DomainStatus) -> Self {
        self.validation_status = ::std::option::Option::Some(input);
        self
    }
    /// <p>The validation status for this domain. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>PENDING_VALIDATION</code> – The domain is waiting for validation to complete.</p></li>
    /// <li>
    /// <p><code>SUCCESS</code> – Validation completed successfully.</p></li>
    /// <li>
    /// <p><code>FAILED</code> – Validation failed.</p></li>
    /// </ul>
    pub fn set_validation_status(mut self, input: ::std::option::Option<crate::types::DomainStatus>) -> Self {
        self.validation_status = input;
        self
    }
    /// <p>The validation status for this domain. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>PENDING_VALIDATION</code> – The domain is waiting for validation to complete.</p></li>
    /// <li>
    /// <p><code>SUCCESS</code> – Validation completed successfully.</p></li>
    /// <li>
    /// <p><code>FAILED</code> – Validation failed.</p></li>
    /// </ul>
    pub fn get_validation_status(&self) -> &::std::option::Option<crate::types::DomainStatus> {
        &self.validation_status
    }
    /// Consumes the builder and constructs a [`ValidationConfiguration`](crate::types::ValidationConfiguration).
    pub fn build(self) -> crate::types::ValidationConfiguration {
        crate::types::ValidationConfiguration {
            validation_method: self.validation_method,
            validation_challenge: self.validation_challenge,
            validation_status: self.validation_status,
        }
    }
}
