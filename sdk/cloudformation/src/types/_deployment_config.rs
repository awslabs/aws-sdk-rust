// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The deployment configuration for a stack operation, including the deployment mode.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct DeploymentConfig {
    /// <p>Specifies the deployment mode for the stack operation. Possible values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>STANDARD</code> - Use the standard deployment behavior, ensuring resources are ready to serve traffic before completing the operation. This is the default. You do not need to specify this value explicitly.</p></li>
    /// <li>
    /// <p><code>EXPRESS</code> - Complete the stack operation when resource configuration is applied, without waiting for resources to become ready to serve traffic. Resources continue becoming ready in the background.</p></li>
    /// </ul>
    pub mode: ::std::option::Option<crate::types::DeploymentConfigMode>,
    /// <p>Specifies whether to disable rollback of the stack if the stack operation fails.</p>
    /// <p>Default: <code>false</code></p>
    pub disable_rollback: ::std::option::Option<bool>,
}
impl DeploymentConfig {
    /// <p>Specifies the deployment mode for the stack operation. Possible values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>STANDARD</code> - Use the standard deployment behavior, ensuring resources are ready to serve traffic before completing the operation. This is the default. You do not need to specify this value explicitly.</p></li>
    /// <li>
    /// <p><code>EXPRESS</code> - Complete the stack operation when resource configuration is applied, without waiting for resources to become ready to serve traffic. Resources continue becoming ready in the background.</p></li>
    /// </ul>
    pub fn mode(&self) -> ::std::option::Option<&crate::types::DeploymentConfigMode> {
        self.mode.as_ref()
    }
    /// <p>Specifies whether to disable rollback of the stack if the stack operation fails.</p>
    /// <p>Default: <code>false</code></p>
    pub fn disable_rollback(&self) -> ::std::option::Option<bool> {
        self.disable_rollback
    }
}
impl DeploymentConfig {
    /// Creates a new builder-style object to manufacture [`DeploymentConfig`](crate::types::DeploymentConfig).
    pub fn builder() -> crate::types::builders::DeploymentConfigBuilder {
        crate::types::builders::DeploymentConfigBuilder::default()
    }
}

/// A builder for [`DeploymentConfig`](crate::types::DeploymentConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct DeploymentConfigBuilder {
    pub(crate) mode: ::std::option::Option<crate::types::DeploymentConfigMode>,
    pub(crate) disable_rollback: ::std::option::Option<bool>,
}
impl DeploymentConfigBuilder {
    /// <p>Specifies the deployment mode for the stack operation. Possible values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>STANDARD</code> - Use the standard deployment behavior, ensuring resources are ready to serve traffic before completing the operation. This is the default. You do not need to specify this value explicitly.</p></li>
    /// <li>
    /// <p><code>EXPRESS</code> - Complete the stack operation when resource configuration is applied, without waiting for resources to become ready to serve traffic. Resources continue becoming ready in the background.</p></li>
    /// </ul>
    pub fn mode(mut self, input: crate::types::DeploymentConfigMode) -> Self {
        self.mode = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies the deployment mode for the stack operation. Possible values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>STANDARD</code> - Use the standard deployment behavior, ensuring resources are ready to serve traffic before completing the operation. This is the default. You do not need to specify this value explicitly.</p></li>
    /// <li>
    /// <p><code>EXPRESS</code> - Complete the stack operation when resource configuration is applied, without waiting for resources to become ready to serve traffic. Resources continue becoming ready in the background.</p></li>
    /// </ul>
    pub fn set_mode(mut self, input: ::std::option::Option<crate::types::DeploymentConfigMode>) -> Self {
        self.mode = input;
        self
    }
    /// <p>Specifies the deployment mode for the stack operation. Possible values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>STANDARD</code> - Use the standard deployment behavior, ensuring resources are ready to serve traffic before completing the operation. This is the default. You do not need to specify this value explicitly.</p></li>
    /// <li>
    /// <p><code>EXPRESS</code> - Complete the stack operation when resource configuration is applied, without waiting for resources to become ready to serve traffic. Resources continue becoming ready in the background.</p></li>
    /// </ul>
    pub fn get_mode(&self) -> &::std::option::Option<crate::types::DeploymentConfigMode> {
        &self.mode
    }
    /// <p>Specifies whether to disable rollback of the stack if the stack operation fails.</p>
    /// <p>Default: <code>false</code></p>
    pub fn disable_rollback(mut self, input: bool) -> Self {
        self.disable_rollback = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether to disable rollback of the stack if the stack operation fails.</p>
    /// <p>Default: <code>false</code></p>
    pub fn set_disable_rollback(mut self, input: ::std::option::Option<bool>) -> Self {
        self.disable_rollback = input;
        self
    }
    /// <p>Specifies whether to disable rollback of the stack if the stack operation fails.</p>
    /// <p>Default: <code>false</code></p>
    pub fn get_disable_rollback(&self) -> &::std::option::Option<bool> {
        &self.disable_rollback
    }
    /// Consumes the builder and constructs a [`DeploymentConfig`](crate::types::DeploymentConfig).
    pub fn build(self) -> crate::types::DeploymentConfig {
        crate::types::DeploymentConfig {
            mode: self.mode,
            disable_rollback: self.disable_rollback,
        }
    }
}
