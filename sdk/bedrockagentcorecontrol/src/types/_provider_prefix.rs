// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration that controls how a provider prefix is applied to model IDs during translation.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ProviderPrefix {
    /// <p>Whether clients can omit the provider prefix from model IDs. If <code>true</code>, the gateway accepts model IDs without the prefix and restores the full prefixed form before forwarding to the provider. The default is <code>false</code>.</p>
    pub strip: bool,
    /// <p>The single character that separates the provider prefix from the model name (for example, <code>.</code>). The default is <code>.</code>.</p>
    pub separator: ::std::string::String,
}
impl ProviderPrefix {
    /// <p>Whether clients can omit the provider prefix from model IDs. If <code>true</code>, the gateway accepts model IDs without the prefix and restores the full prefixed form before forwarding to the provider. The default is <code>false</code>.</p>
    pub fn strip(&self) -> bool {
        self.strip
    }
    /// <p>The single character that separates the provider prefix from the model name (for example, <code>.</code>). The default is <code>.</code>.</p>
    pub fn separator(&self) -> &str {
        use std::ops::Deref;
        self.separator.deref()
    }
}
impl ProviderPrefix {
    /// Creates a new builder-style object to manufacture [`ProviderPrefix`](crate::types::ProviderPrefix).
    pub fn builder() -> crate::types::builders::ProviderPrefixBuilder {
        crate::types::builders::ProviderPrefixBuilder::default()
    }
}

/// A builder for [`ProviderPrefix`](crate::types::ProviderPrefix).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ProviderPrefixBuilder {
    pub(crate) strip: ::std::option::Option<bool>,
    pub(crate) separator: ::std::option::Option<::std::string::String>,
}
impl ProviderPrefixBuilder {
    /// <p>Whether clients can omit the provider prefix from model IDs. If <code>true</code>, the gateway accepts model IDs without the prefix and restores the full prefixed form before forwarding to the provider. The default is <code>false</code>.</p>
    pub fn strip(mut self, input: bool) -> Self {
        self.strip = ::std::option::Option::Some(input);
        self
    }
    /// <p>Whether clients can omit the provider prefix from model IDs. If <code>true</code>, the gateway accepts model IDs without the prefix and restores the full prefixed form before forwarding to the provider. The default is <code>false</code>.</p>
    pub fn set_strip(mut self, input: ::std::option::Option<bool>) -> Self {
        self.strip = input;
        self
    }
    /// <p>Whether clients can omit the provider prefix from model IDs. If <code>true</code>, the gateway accepts model IDs without the prefix and restores the full prefixed form before forwarding to the provider. The default is <code>false</code>.</p>
    pub fn get_strip(&self) -> &::std::option::Option<bool> {
        &self.strip
    }
    /// <p>The single character that separates the provider prefix from the model name (for example, <code>.</code>). The default is <code>.</code>.</p>
    pub fn separator(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.separator = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The single character that separates the provider prefix from the model name (for example, <code>.</code>). The default is <code>.</code>.</p>
    pub fn set_separator(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.separator = input;
        self
    }
    /// <p>The single character that separates the provider prefix from the model name (for example, <code>.</code>). The default is <code>.</code>.</p>
    pub fn get_separator(&self) -> &::std::option::Option<::std::string::String> {
        &self.separator
    }
    /// Consumes the builder and constructs a [`ProviderPrefix`](crate::types::ProviderPrefix).
    pub fn build(self) -> crate::types::ProviderPrefix {
        crate::types::ProviderPrefix {
            strip: self.strip.unwrap_or_default(),
            separator: self.separator.unwrap_or_else(|| ".".to_owned()),
        }
    }
}
