// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains the rate configuration for a rate limit metric, specifying the allowed rate and time period.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct RateConfig {
    /// <p>The rate value for the limit. For request limits, this is the number of requests allowed per period. For token limits, this is the number of tokens allowed per period. For connection limits, this is the number of concurrent connections allowed.</p>
    pub rate: f64,
    /// <p>The time period for the rate limit. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>second</code>—Measures the rate limit over a one-second window.</p></li>
    /// <li>
    /// <p><code>minute</code>—Measures the rate limit over a one-minute window.</p></li>
    /// </ul>
    pub period: crate::types::Period,
}
impl RateConfig {
    /// <p>The rate value for the limit. For request limits, this is the number of requests allowed per period. For token limits, this is the number of tokens allowed per period. For connection limits, this is the number of concurrent connections allowed.</p>
    pub fn rate(&self) -> f64 {
        self.rate
    }
    /// <p>The time period for the rate limit. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>second</code>—Measures the rate limit over a one-second window.</p></li>
    /// <li>
    /// <p><code>minute</code>—Measures the rate limit over a one-minute window.</p></li>
    /// </ul>
    pub fn period(&self) -> &crate::types::Period {
        &self.period
    }
}
impl RateConfig {
    /// Creates a new builder-style object to manufacture [`RateConfig`](crate::types::RateConfig).
    pub fn builder() -> crate::types::builders::RateConfigBuilder {
        crate::types::builders::RateConfigBuilder::default()
    }
}

/// A builder for [`RateConfig`](crate::types::RateConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct RateConfigBuilder {
    pub(crate) rate: ::std::option::Option<f64>,
    pub(crate) period: ::std::option::Option<crate::types::Period>,
}
impl RateConfigBuilder {
    /// <p>The rate value for the limit. For request limits, this is the number of requests allowed per period. For token limits, this is the number of tokens allowed per period. For connection limits, this is the number of concurrent connections allowed.</p>
    /// This field is required.
    pub fn rate(mut self, input: f64) -> Self {
        self.rate = ::std::option::Option::Some(input);
        self
    }
    /// <p>The rate value for the limit. For request limits, this is the number of requests allowed per period. For token limits, this is the number of tokens allowed per period. For connection limits, this is the number of concurrent connections allowed.</p>
    pub fn set_rate(mut self, input: ::std::option::Option<f64>) -> Self {
        self.rate = input;
        self
    }
    /// <p>The rate value for the limit. For request limits, this is the number of requests allowed per period. For token limits, this is the number of tokens allowed per period. For connection limits, this is the number of concurrent connections allowed.</p>
    pub fn get_rate(&self) -> &::std::option::Option<f64> {
        &self.rate
    }
    /// <p>The time period for the rate limit. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>second</code>—Measures the rate limit over a one-second window.</p></li>
    /// <li>
    /// <p><code>minute</code>—Measures the rate limit over a one-minute window.</p></li>
    /// </ul>
    /// This field is required.
    pub fn period(mut self, input: crate::types::Period) -> Self {
        self.period = ::std::option::Option::Some(input);
        self
    }
    /// <p>The time period for the rate limit. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>second</code>—Measures the rate limit over a one-second window.</p></li>
    /// <li>
    /// <p><code>minute</code>—Measures the rate limit over a one-minute window.</p></li>
    /// </ul>
    pub fn set_period(mut self, input: ::std::option::Option<crate::types::Period>) -> Self {
        self.period = input;
        self
    }
    /// <p>The time period for the rate limit. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>second</code>—Measures the rate limit over a one-second window.</p></li>
    /// <li>
    /// <p><code>minute</code>—Measures the rate limit over a one-minute window.</p></li>
    /// </ul>
    pub fn get_period(&self) -> &::std::option::Option<crate::types::Period> {
        &self.period
    }
    /// Consumes the builder and constructs a [`RateConfig`](crate::types::RateConfig).
    /// This method will fail if any of the following fields are not set:
    /// - [`rate`](crate::types::builders::RateConfigBuilder::rate)
    /// - [`period`](crate::types::builders::RateConfigBuilder::period)
    pub fn build(self) -> ::std::result::Result<crate::types::RateConfig, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::RateConfig {
            rate: self.rate.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "rate",
                    "rate was not specified but it is required when building RateConfig",
                )
            })?,
            period: self.period.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "period",
                    "period was not specified but it is required when building RateConfig",
                )
            })?,
        })
    }
}
