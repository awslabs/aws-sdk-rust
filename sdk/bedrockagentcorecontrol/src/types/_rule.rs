// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The evaluation rule that defines sampling configuration, filtering criteria, and session detection settings for online evaluation.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct Rule {
    /// <p>The sampling configuration that determines what percentage of agent traces to evaluate.</p>
    pub sampling_config: ::std::option::Option<crate::types::SamplingConfig>,
    /// <p>The list of filters that determine which agent traces should be included in the evaluation based on trace properties.</p>
    pub filters: ::std::option::Option<::std::vec::Vec<crate::types::Filter>>,
    /// <p>The session configuration that defines timeout settings for detecting when agent sessions are complete and ready for evaluation.</p>
    pub session_config: ::std::option::Option<crate::types::SessionConfig>,
}
impl Rule {
    /// <p>The sampling configuration that determines what percentage of agent traces to evaluate.</p>
    pub fn sampling_config(&self) -> ::std::option::Option<&crate::types::SamplingConfig> {
        self.sampling_config.as_ref()
    }
    /// <p>The list of filters that determine which agent traces should be included in the evaluation based on trace properties.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.filters.is_none()`.
    pub fn filters(&self) -> &[crate::types::Filter] {
        self.filters.as_deref().unwrap_or_default()
    }
    /// <p>The session configuration that defines timeout settings for detecting when agent sessions are complete and ready for evaluation.</p>
    pub fn session_config(&self) -> ::std::option::Option<&crate::types::SessionConfig> {
        self.session_config.as_ref()
    }
}
impl Rule {
    /// Creates a new builder-style object to manufacture [`Rule`](crate::types::Rule).
    pub fn builder() -> crate::types::builders::RuleBuilder {
        crate::types::builders::RuleBuilder::default()
    }
}

/// A builder for [`Rule`](crate::types::Rule).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct RuleBuilder {
    pub(crate) sampling_config: ::std::option::Option<crate::types::SamplingConfig>,
    pub(crate) filters: ::std::option::Option<::std::vec::Vec<crate::types::Filter>>,
    pub(crate) session_config: ::std::option::Option<crate::types::SessionConfig>,
}
impl RuleBuilder {
    /// <p>The sampling configuration that determines what percentage of agent traces to evaluate.</p>
    /// This field is required.
    pub fn sampling_config(mut self, input: crate::types::SamplingConfig) -> Self {
        self.sampling_config = ::std::option::Option::Some(input);
        self
    }
    /// <p>The sampling configuration that determines what percentage of agent traces to evaluate.</p>
    pub fn set_sampling_config(mut self, input: ::std::option::Option<crate::types::SamplingConfig>) -> Self {
        self.sampling_config = input;
        self
    }
    /// <p>The sampling configuration that determines what percentage of agent traces to evaluate.</p>
    pub fn get_sampling_config(&self) -> &::std::option::Option<crate::types::SamplingConfig> {
        &self.sampling_config
    }
    /// Appends an item to `filters`.
    ///
    /// To override the contents of this collection use [`set_filters`](Self::set_filters).
    ///
    /// <p>The list of filters that determine which agent traces should be included in the evaluation based on trace properties.</p>
    pub fn filters(mut self, input: crate::types::Filter) -> Self {
        let mut v = self.filters.unwrap_or_default();
        v.push(input);
        self.filters = ::std::option::Option::Some(v);
        self
    }
    /// <p>The list of filters that determine which agent traces should be included in the evaluation based on trace properties.</p>
    pub fn set_filters(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::Filter>>) -> Self {
        self.filters = input;
        self
    }
    /// <p>The list of filters that determine which agent traces should be included in the evaluation based on trace properties.</p>
    pub fn get_filters(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::Filter>> {
        &self.filters
    }
    /// <p>The session configuration that defines timeout settings for detecting when agent sessions are complete and ready for evaluation.</p>
    pub fn session_config(mut self, input: crate::types::SessionConfig) -> Self {
        self.session_config = ::std::option::Option::Some(input);
        self
    }
    /// <p>The session configuration that defines timeout settings for detecting when agent sessions are complete and ready for evaluation.</p>
    pub fn set_session_config(mut self, input: ::std::option::Option<crate::types::SessionConfig>) -> Self {
        self.session_config = input;
        self
    }
    /// <p>The session configuration that defines timeout settings for detecting when agent sessions are complete and ready for evaluation.</p>
    pub fn get_session_config(&self) -> &::std::option::Option<crate::types::SessionConfig> {
        &self.session_config
    }
    /// Consumes the builder and constructs a [`Rule`](crate::types::Rule).
    pub fn build(self) -> crate::types::Rule {
        crate::types::Rule {
            sampling_config: self.sampling_config,
            filters: self.filters,
            session_config: self.session_config,
        }
    }
}
