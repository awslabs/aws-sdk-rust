// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A lifecycle hook configuration. Specify one hook type.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum HarnessHook {
    /// <p>A hook that runs after an invocation completes.</p>
    AfterInvocation(crate::types::HarnessAfterInvocationHook),
    /// <p>A hook that runs after a tool call completes.</p>
    AfterToolCall(crate::types::HarnessAfterToolCallHook),
    /// <p>A hook that runs before an invocation begins.</p>
    BeforeInvocation(crate::types::HarnessBeforeInvocationHook),
    /// <p>A hook that runs before the agent calls a tool.</p>
    BeforeToolCall(crate::types::HarnessBeforeToolCallHook),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl HarnessHook {
    /// Tries to convert the enum instance into [`AfterInvocation`](crate::types::HarnessHook::AfterInvocation), extracting the inner [`HarnessAfterInvocationHook`](crate::types::HarnessAfterInvocationHook).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_after_invocation(&self) -> ::std::result::Result<&crate::types::HarnessAfterInvocationHook, &Self> {
        if let HarnessHook::AfterInvocation(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`AfterInvocation`](crate::types::HarnessHook::AfterInvocation).
    pub fn is_after_invocation(&self) -> bool {
        self.as_after_invocation().is_ok()
    }
    /// Tries to convert the enum instance into [`AfterToolCall`](crate::types::HarnessHook::AfterToolCall), extracting the inner [`HarnessAfterToolCallHook`](crate::types::HarnessAfterToolCallHook).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_after_tool_call(&self) -> ::std::result::Result<&crate::types::HarnessAfterToolCallHook, &Self> {
        if let HarnessHook::AfterToolCall(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`AfterToolCall`](crate::types::HarnessHook::AfterToolCall).
    pub fn is_after_tool_call(&self) -> bool {
        self.as_after_tool_call().is_ok()
    }
    /// Tries to convert the enum instance into [`BeforeInvocation`](crate::types::HarnessHook::BeforeInvocation), extracting the inner [`HarnessBeforeInvocationHook`](crate::types::HarnessBeforeInvocationHook).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_before_invocation(&self) -> ::std::result::Result<&crate::types::HarnessBeforeInvocationHook, &Self> {
        if let HarnessHook::BeforeInvocation(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`BeforeInvocation`](crate::types::HarnessHook::BeforeInvocation).
    pub fn is_before_invocation(&self) -> bool {
        self.as_before_invocation().is_ok()
    }
    /// Tries to convert the enum instance into [`BeforeToolCall`](crate::types::HarnessHook::BeforeToolCall), extracting the inner [`HarnessBeforeToolCallHook`](crate::types::HarnessBeforeToolCallHook).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_before_tool_call(&self) -> ::std::result::Result<&crate::types::HarnessBeforeToolCallHook, &Self> {
        if let HarnessHook::BeforeToolCall(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`BeforeToolCall`](crate::types::HarnessHook::BeforeToolCall).
    pub fn is_before_tool_call(&self) -> bool {
        self.as_before_tool_call().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
