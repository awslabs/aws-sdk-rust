// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Configuration for a capacity provider volume mounted into the AgentCore Runtime. This references a persistent volume by its logical name, as defined in the capacity provider's list of volumes.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct CapacityProviderVolumeConfiguration {
    /// <p>The logical name of the capacity provider volume to mount. This name must match a volume that is defined in the capacity provider's list of volumes.</p>
    pub volume_name: ::std::string::String,
    /// <p>The mount path for the capacity provider volume inside the AgentCore Runtime. The path must be under <code>/mnt</code> with exactly one subdirectory level (for example, <code>/mnt/data</code>).</p>
    pub mount_path: ::std::string::String,
}
impl CapacityProviderVolumeConfiguration {
    /// <p>The logical name of the capacity provider volume to mount. This name must match a volume that is defined in the capacity provider's list of volumes.</p>
    pub fn volume_name(&self) -> &str {
        use std::ops::Deref;
        self.volume_name.deref()
    }
    /// <p>The mount path for the capacity provider volume inside the AgentCore Runtime. The path must be under <code>/mnt</code> with exactly one subdirectory level (for example, <code>/mnt/data</code>).</p>
    pub fn mount_path(&self) -> &str {
        use std::ops::Deref;
        self.mount_path.deref()
    }
}
impl CapacityProviderVolumeConfiguration {
    /// Creates a new builder-style object to manufacture [`CapacityProviderVolumeConfiguration`](crate::types::CapacityProviderVolumeConfiguration).
    pub fn builder() -> crate::types::builders::CapacityProviderVolumeConfigurationBuilder {
        crate::types::builders::CapacityProviderVolumeConfigurationBuilder::default()
    }
}

/// A builder for [`CapacityProviderVolumeConfiguration`](crate::types::CapacityProviderVolumeConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct CapacityProviderVolumeConfigurationBuilder {
    pub(crate) volume_name: ::std::option::Option<::std::string::String>,
    pub(crate) mount_path: ::std::option::Option<::std::string::String>,
}
impl CapacityProviderVolumeConfigurationBuilder {
    /// <p>The logical name of the capacity provider volume to mount. This name must match a volume that is defined in the capacity provider's list of volumes.</p>
    /// This field is required.
    pub fn volume_name(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.volume_name = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The logical name of the capacity provider volume to mount. This name must match a volume that is defined in the capacity provider's list of volumes.</p>
    pub fn set_volume_name(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.volume_name = input;
        self
    }
    /// <p>The logical name of the capacity provider volume to mount. This name must match a volume that is defined in the capacity provider's list of volumes.</p>
    pub fn get_volume_name(&self) -> &::std::option::Option<::std::string::String> {
        &self.volume_name
    }
    /// <p>The mount path for the capacity provider volume inside the AgentCore Runtime. The path must be under <code>/mnt</code> with exactly one subdirectory level (for example, <code>/mnt/data</code>).</p>
    /// This field is required.
    pub fn mount_path(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.mount_path = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The mount path for the capacity provider volume inside the AgentCore Runtime. The path must be under <code>/mnt</code> with exactly one subdirectory level (for example, <code>/mnt/data</code>).</p>
    pub fn set_mount_path(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.mount_path = input;
        self
    }
    /// <p>The mount path for the capacity provider volume inside the AgentCore Runtime. The path must be under <code>/mnt</code> with exactly one subdirectory level (for example, <code>/mnt/data</code>).</p>
    pub fn get_mount_path(&self) -> &::std::option::Option<::std::string::String> {
        &self.mount_path
    }
    /// Consumes the builder and constructs a [`CapacityProviderVolumeConfiguration`](crate::types::CapacityProviderVolumeConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`volume_name`](crate::types::builders::CapacityProviderVolumeConfigurationBuilder::volume_name)
    /// - [`mount_path`](crate::types::builders::CapacityProviderVolumeConfigurationBuilder::mount_path)
    pub fn build(self) -> ::std::result::Result<crate::types::CapacityProviderVolumeConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::CapacityProviderVolumeConfiguration {
            volume_name: self.volume_name.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "volume_name",
                    "volume_name was not specified but it is required when building CapacityProviderVolumeConfiguration",
                )
            })?,
            mount_path: self.mount_path.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "mount_path",
                    "mount_path was not specified but it is required when building CapacityProviderVolumeConfiguration",
                )
            })?,
        })
    }
}
