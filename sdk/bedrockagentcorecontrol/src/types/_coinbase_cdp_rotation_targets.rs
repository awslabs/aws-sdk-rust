// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Specifies the service-managed Coinbase CDP secrets to rotate.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct CoinbaseCdpRotationTargets {
    /// <p>The secrets to rotate. Specify at least one value. Each secret that you specify is rotated independently.</p>
    /// <ul>
    /// <li>
    /// <p><code>API_KEY</code> - The API key that the payment connector uses to call Coinbase CDP. Rotate it as routine maintenance, or if you suspect that it is compromised.</p></li>
    /// <li>
    /// <p><code>WALLET_SECRET</code> - The wallet secret that signs transactions. Rotate it only if it is lost or compromised. Coinbase CDP allows one wallet secret per project, so it is replaced in place and signing can be briefly interrupted.</p></li>
    /// </ul>
    pub secrets: ::std::vec::Vec<crate::types::CoinbaseCdpSecret>,
}
impl CoinbaseCdpRotationTargets {
    /// <p>The secrets to rotate. Specify at least one value. Each secret that you specify is rotated independently.</p>
    /// <ul>
    /// <li>
    /// <p><code>API_KEY</code> - The API key that the payment connector uses to call Coinbase CDP. Rotate it as routine maintenance, or if you suspect that it is compromised.</p></li>
    /// <li>
    /// <p><code>WALLET_SECRET</code> - The wallet secret that signs transactions. Rotate it only if it is lost or compromised. Coinbase CDP allows one wallet secret per project, so it is replaced in place and signing can be briefly interrupted.</p></li>
    /// </ul>
    pub fn secrets(&self) -> &[crate::types::CoinbaseCdpSecret] {
        use std::ops::Deref;
        self.secrets.deref()
    }
}
impl CoinbaseCdpRotationTargets {
    /// Creates a new builder-style object to manufacture [`CoinbaseCdpRotationTargets`](crate::types::CoinbaseCdpRotationTargets).
    pub fn builder() -> crate::types::builders::CoinbaseCdpRotationTargetsBuilder {
        crate::types::builders::CoinbaseCdpRotationTargetsBuilder::default()
    }
}

/// A builder for [`CoinbaseCdpRotationTargets`](crate::types::CoinbaseCdpRotationTargets).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct CoinbaseCdpRotationTargetsBuilder {
    pub(crate) secrets: ::std::option::Option<::std::vec::Vec<crate::types::CoinbaseCdpSecret>>,
}
impl CoinbaseCdpRotationTargetsBuilder {
    /// Appends an item to `secrets`.
    ///
    /// To override the contents of this collection use [`set_secrets`](Self::set_secrets).
    ///
    /// <p>The secrets to rotate. Specify at least one value. Each secret that you specify is rotated independently.</p>
    /// <ul>
    /// <li>
    /// <p><code>API_KEY</code> - The API key that the payment connector uses to call Coinbase CDP. Rotate it as routine maintenance, or if you suspect that it is compromised.</p></li>
    /// <li>
    /// <p><code>WALLET_SECRET</code> - The wallet secret that signs transactions. Rotate it only if it is lost or compromised. Coinbase CDP allows one wallet secret per project, so it is replaced in place and signing can be briefly interrupted.</p></li>
    /// </ul>
    pub fn secrets(mut self, input: crate::types::CoinbaseCdpSecret) -> Self {
        let mut v = self.secrets.unwrap_or_default();
        v.push(input);
        self.secrets = ::std::option::Option::Some(v);
        self
    }
    /// <p>The secrets to rotate. Specify at least one value. Each secret that you specify is rotated independently.</p>
    /// <ul>
    /// <li>
    /// <p><code>API_KEY</code> - The API key that the payment connector uses to call Coinbase CDP. Rotate it as routine maintenance, or if you suspect that it is compromised.</p></li>
    /// <li>
    /// <p><code>WALLET_SECRET</code> - The wallet secret that signs transactions. Rotate it only if it is lost or compromised. Coinbase CDP allows one wallet secret per project, so it is replaced in place and signing can be briefly interrupted.</p></li>
    /// </ul>
    pub fn set_secrets(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::CoinbaseCdpSecret>>) -> Self {
        self.secrets = input;
        self
    }
    /// <p>The secrets to rotate. Specify at least one value. Each secret that you specify is rotated independently.</p>
    /// <ul>
    /// <li>
    /// <p><code>API_KEY</code> - The API key that the payment connector uses to call Coinbase CDP. Rotate it as routine maintenance, or if you suspect that it is compromised.</p></li>
    /// <li>
    /// <p><code>WALLET_SECRET</code> - The wallet secret that signs transactions. Rotate it only if it is lost or compromised. Coinbase CDP allows one wallet secret per project, so it is replaced in place and signing can be briefly interrupted.</p></li>
    /// </ul>
    pub fn get_secrets(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::CoinbaseCdpSecret>> {
        &self.secrets
    }
    /// Consumes the builder and constructs a [`CoinbaseCdpRotationTargets`](crate::types::CoinbaseCdpRotationTargets).
    /// This method will fail if any of the following fields are not set:
    /// - [`secrets`](crate::types::builders::CoinbaseCdpRotationTargetsBuilder::secrets)
    pub fn build(self) -> ::std::result::Result<crate::types::CoinbaseCdpRotationTargets, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::CoinbaseCdpRotationTargets {
            secrets: self.secrets.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "secrets",
                    "secrets was not specified but it is required when building CoinbaseCdpRotationTargets",
                )
            })?,
        })
    }
}
