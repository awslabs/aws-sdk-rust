// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>This structure contains the configuration for a composite service level indicator (SLI) that aggregates metrics across multiple operations of a service for service-level SLOs.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct CompositeSliConfig {
    /// <p>Specifies how operations are selected for this service-level SLO. Operations can be selected explicitly by listing them, by specifying a prefix to match operation names, or by providing a regular expression pattern.</p>
    pub selection_config: ::std::option::Option<crate::types::SelectionConfig>,
    /// <p>The list of operations included in this composite SLI. You must specify between 2 and 20 components. Each component is a <code>CompositeSliComponent</code> that identifies a single operation by its <code>OperationName</code>.</p>
    pub components: ::std::option::Option<::std::vec::Vec<crate::types::CompositeSliComponent>>,
}
impl CompositeSliConfig {
    /// <p>Specifies how operations are selected for this service-level SLO. Operations can be selected explicitly by listing them, by specifying a prefix to match operation names, or by providing a regular expression pattern.</p>
    pub fn selection_config(&self) -> ::std::option::Option<&crate::types::SelectionConfig> {
        self.selection_config.as_ref()
    }
    /// <p>The list of operations included in this composite SLI. You must specify between 2 and 20 components. Each component is a <code>CompositeSliComponent</code> that identifies a single operation by its <code>OperationName</code>.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.components.is_none()`.
    pub fn components(&self) -> &[crate::types::CompositeSliComponent] {
        self.components.as_deref().unwrap_or_default()
    }
}
impl CompositeSliConfig {
    /// Creates a new builder-style object to manufacture [`CompositeSliConfig`](crate::types::CompositeSliConfig).
    pub fn builder() -> crate::types::builders::CompositeSliConfigBuilder {
        crate::types::builders::CompositeSliConfigBuilder::default()
    }
}

/// A builder for [`CompositeSliConfig`](crate::types::CompositeSliConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct CompositeSliConfigBuilder {
    pub(crate) selection_config: ::std::option::Option<crate::types::SelectionConfig>,
    pub(crate) components: ::std::option::Option<::std::vec::Vec<crate::types::CompositeSliComponent>>,
}
impl CompositeSliConfigBuilder {
    /// <p>Specifies how operations are selected for this service-level SLO. Operations can be selected explicitly by listing them, by specifying a prefix to match operation names, or by providing a regular expression pattern.</p>
    /// This field is required.
    pub fn selection_config(mut self, input: crate::types::SelectionConfig) -> Self {
        self.selection_config = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies how operations are selected for this service-level SLO. Operations can be selected explicitly by listing them, by specifying a prefix to match operation names, or by providing a regular expression pattern.</p>
    pub fn set_selection_config(mut self, input: ::std::option::Option<crate::types::SelectionConfig>) -> Self {
        self.selection_config = input;
        self
    }
    /// <p>Specifies how operations are selected for this service-level SLO. Operations can be selected explicitly by listing them, by specifying a prefix to match operation names, or by providing a regular expression pattern.</p>
    pub fn get_selection_config(&self) -> &::std::option::Option<crate::types::SelectionConfig> {
        &self.selection_config
    }
    /// Appends an item to `components`.
    ///
    /// To override the contents of this collection use [`set_components`](Self::set_components).
    ///
    /// <p>The list of operations included in this composite SLI. You must specify between 2 and 20 components. Each component is a <code>CompositeSliComponent</code> that identifies a single operation by its <code>OperationName</code>.</p>
    pub fn components(mut self, input: crate::types::CompositeSliComponent) -> Self {
        let mut v = self.components.unwrap_or_default();
        v.push(input);
        self.components = ::std::option::Option::Some(v);
        self
    }
    /// <p>The list of operations included in this composite SLI. You must specify between 2 and 20 components. Each component is a <code>CompositeSliComponent</code> that identifies a single operation by its <code>OperationName</code>.</p>
    pub fn set_components(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::CompositeSliComponent>>) -> Self {
        self.components = input;
        self
    }
    /// <p>The list of operations included in this composite SLI. You must specify between 2 and 20 components. Each component is a <code>CompositeSliComponent</code> that identifies a single operation by its <code>OperationName</code>.</p>
    pub fn get_components(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::CompositeSliComponent>> {
        &self.components
    }
    /// Consumes the builder and constructs a [`CompositeSliConfig`](crate::types::CompositeSliConfig).
    pub fn build(self) -> crate::types::CompositeSliConfig {
        crate::types::CompositeSliConfig {
            selection_config: self.selection_config,
            components: self.components,
        }
    }
}
