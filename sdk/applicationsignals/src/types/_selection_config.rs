// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Defines how operations are selected for a service-level SLO.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct SelectionConfig {
    /// <p>The strategy for selecting operations to include in a service-level SLO.</p>
    /// <ul>
    /// <li>
    /// <p><code>EXPLICIT</code> — You provide a specific list of operations in the <code>Components</code> field of <code>CompositeSliConfig</code>.</p></li>
    /// <li>
    /// <p><code>PREFIX</code> — You provide a prefix string in the <code>Pattern</code> field of <code>SelectionConfig</code>, and all operations whose names start with the prefix are included.</p></li>
    /// <li>
    /// <p><code>REGEX</code> — You provide a regular expression in the <code>Pattern</code> field of <code>SelectionConfig</code>, and all operations whose names match the pattern are included.</p></li>
    /// </ul>
    pub r#type: crate::types::SelectionType,
    /// <p>A prefix string or regular expression that specifies which operations to include in a service-level SLO. When <code>SelectionType</code> is <code>PREFIX</code>, this value is a prefix string that matches the beginning of operation names. When <code>SelectionType</code> is <code>REGEX</code>, this value is a regular expression that matches operation names.</p>
    pub pattern: ::std::option::Option<::std::string::String>,
}
impl SelectionConfig {
    /// <p>The strategy for selecting operations to include in a service-level SLO.</p>
    /// <ul>
    /// <li>
    /// <p><code>EXPLICIT</code> — You provide a specific list of operations in the <code>Components</code> field of <code>CompositeSliConfig</code>.</p></li>
    /// <li>
    /// <p><code>PREFIX</code> — You provide a prefix string in the <code>Pattern</code> field of <code>SelectionConfig</code>, and all operations whose names start with the prefix are included.</p></li>
    /// <li>
    /// <p><code>REGEX</code> — You provide a regular expression in the <code>Pattern</code> field of <code>SelectionConfig</code>, and all operations whose names match the pattern are included.</p></li>
    /// </ul>
    pub fn r#type(&self) -> &crate::types::SelectionType {
        &self.r#type
    }
    /// <p>A prefix string or regular expression that specifies which operations to include in a service-level SLO. When <code>SelectionType</code> is <code>PREFIX</code>, this value is a prefix string that matches the beginning of operation names. When <code>SelectionType</code> is <code>REGEX</code>, this value is a regular expression that matches operation names.</p>
    pub fn pattern(&self) -> ::std::option::Option<&str> {
        self.pattern.as_deref()
    }
}
impl SelectionConfig {
    /// Creates a new builder-style object to manufacture [`SelectionConfig`](crate::types::SelectionConfig).
    pub fn builder() -> crate::types::builders::SelectionConfigBuilder {
        crate::types::builders::SelectionConfigBuilder::default()
    }
}

/// A builder for [`SelectionConfig`](crate::types::SelectionConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct SelectionConfigBuilder {
    pub(crate) r#type: ::std::option::Option<crate::types::SelectionType>,
    pub(crate) pattern: ::std::option::Option<::std::string::String>,
}
impl SelectionConfigBuilder {
    /// <p>The strategy for selecting operations to include in a service-level SLO.</p>
    /// <ul>
    /// <li>
    /// <p><code>EXPLICIT</code> — You provide a specific list of operations in the <code>Components</code> field of <code>CompositeSliConfig</code>.</p></li>
    /// <li>
    /// <p><code>PREFIX</code> — You provide a prefix string in the <code>Pattern</code> field of <code>SelectionConfig</code>, and all operations whose names start with the prefix are included.</p></li>
    /// <li>
    /// <p><code>REGEX</code> — You provide a regular expression in the <code>Pattern</code> field of <code>SelectionConfig</code>, and all operations whose names match the pattern are included.</p></li>
    /// </ul>
    /// This field is required.
    pub fn r#type(mut self, input: crate::types::SelectionType) -> Self {
        self.r#type = ::std::option::Option::Some(input);
        self
    }
    /// <p>The strategy for selecting operations to include in a service-level SLO.</p>
    /// <ul>
    /// <li>
    /// <p><code>EXPLICIT</code> — You provide a specific list of operations in the <code>Components</code> field of <code>CompositeSliConfig</code>.</p></li>
    /// <li>
    /// <p><code>PREFIX</code> — You provide a prefix string in the <code>Pattern</code> field of <code>SelectionConfig</code>, and all operations whose names start with the prefix are included.</p></li>
    /// <li>
    /// <p><code>REGEX</code> — You provide a regular expression in the <code>Pattern</code> field of <code>SelectionConfig</code>, and all operations whose names match the pattern are included.</p></li>
    /// </ul>
    pub fn set_type(mut self, input: ::std::option::Option<crate::types::SelectionType>) -> Self {
        self.r#type = input;
        self
    }
    /// <p>The strategy for selecting operations to include in a service-level SLO.</p>
    /// <ul>
    /// <li>
    /// <p><code>EXPLICIT</code> — You provide a specific list of operations in the <code>Components</code> field of <code>CompositeSliConfig</code>.</p></li>
    /// <li>
    /// <p><code>PREFIX</code> — You provide a prefix string in the <code>Pattern</code> field of <code>SelectionConfig</code>, and all operations whose names start with the prefix are included.</p></li>
    /// <li>
    /// <p><code>REGEX</code> — You provide a regular expression in the <code>Pattern</code> field of <code>SelectionConfig</code>, and all operations whose names match the pattern are included.</p></li>
    /// </ul>
    pub fn get_type(&self) -> &::std::option::Option<crate::types::SelectionType> {
        &self.r#type
    }
    /// <p>A prefix string or regular expression that specifies which operations to include in a service-level SLO. When <code>SelectionType</code> is <code>PREFIX</code>, this value is a prefix string that matches the beginning of operation names. When <code>SelectionType</code> is <code>REGEX</code>, this value is a regular expression that matches operation names.</p>
    pub fn pattern(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.pattern = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>A prefix string or regular expression that specifies which operations to include in a service-level SLO. When <code>SelectionType</code> is <code>PREFIX</code>, this value is a prefix string that matches the beginning of operation names. When <code>SelectionType</code> is <code>REGEX</code>, this value is a regular expression that matches operation names.</p>
    pub fn set_pattern(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.pattern = input;
        self
    }
    /// <p>A prefix string or regular expression that specifies which operations to include in a service-level SLO. When <code>SelectionType</code> is <code>PREFIX</code>, this value is a prefix string that matches the beginning of operation names. When <code>SelectionType</code> is <code>REGEX</code>, this value is a regular expression that matches operation names.</p>
    pub fn get_pattern(&self) -> &::std::option::Option<::std::string::String> {
        &self.pattern
    }
    /// Consumes the builder and constructs a [`SelectionConfig`](crate::types::SelectionConfig).
    /// This method will fail if any of the following fields are not set:
    /// - [`r#type`](crate::types::builders::SelectionConfigBuilder::type)
    pub fn build(self) -> ::std::result::Result<crate::types::SelectionConfig, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::SelectionConfig {
            r#type: self.r#type.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "r#type",
                    "r#type was not specified but it is required when building SelectionConfig",
                )
            })?,
            pattern: self.pattern,
        })
    }
}
