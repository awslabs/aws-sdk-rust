// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>An object that defines the list of matching rules to run in an ID mapping workflow.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct IdMappingRuleBasedProperties {
    /// <p>The rules that can be used for ID mapping.</p>
    pub rules: ::std::option::Option<::std::vec::Vec<crate::types::Rule>>,
    /// <p>The set of rules you can use in an ID mapping workflow. The limitations specified for the source or target to define the match rules must be compatible.</p>
    pub rule_definition_type: crate::types::IdMappingWorkflowRuleDefinitionType,
    /// <p>The comparison type. You can either choose <code>ONE_TO_ONE</code> or <code>MANY_TO_MANY</code> as the <code>attributeMatchingModel</code>.</p>
    /// <p>If you choose <code>ONE_TO_ONE</code>, the system can only match attributes if the sub-types are an exact match. For example, for the <code>Email</code> attribute type, the system will only consider it a match if the value of the <code>Email</code> field of Profile A matches the value of the <code>Email</code> field of Profile B.</p>
    /// <p>If you choose <code>MANY_TO_MANY</code>, the system can match attributes across the sub-types of an attribute type. For example, if the value of the <code>Email</code> field of Profile A matches the value of the <code>BusinessEmail</code> field of Profile B, the two profiles are matched on the <code>Email</code> attribute type.</p>
    pub attribute_matching_model: crate::types::AttributeMatchingModel,
    /// <p>The type of matching record that is allowed to be used in an ID mapping workflow.</p>
    /// <p>If the value is set to <code>ONE_SOURCE_TO_ONE_TARGET</code>, only one record in the source can be matched to the same record in the target.</p>
    /// <p>If the value is set to <code>MANY_SOURCE_TO_ONE_TARGET</code>, multiple records in the source can be matched to one record in the target.</p>
    pub record_matching_model: crate::types::RecordMatchingModel,
}
impl IdMappingRuleBasedProperties {
    /// <p>The rules that can be used for ID mapping.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.rules.is_none()`.
    pub fn rules(&self) -> &[crate::types::Rule] {
        self.rules.as_deref().unwrap_or_default()
    }
    /// <p>The set of rules you can use in an ID mapping workflow. The limitations specified for the source or target to define the match rules must be compatible.</p>
    pub fn rule_definition_type(&self) -> &crate::types::IdMappingWorkflowRuleDefinitionType {
        &self.rule_definition_type
    }
    /// <p>The comparison type. You can either choose <code>ONE_TO_ONE</code> or <code>MANY_TO_MANY</code> as the <code>attributeMatchingModel</code>.</p>
    /// <p>If you choose <code>ONE_TO_ONE</code>, the system can only match attributes if the sub-types are an exact match. For example, for the <code>Email</code> attribute type, the system will only consider it a match if the value of the <code>Email</code> field of Profile A matches the value of the <code>Email</code> field of Profile B.</p>
    /// <p>If you choose <code>MANY_TO_MANY</code>, the system can match attributes across the sub-types of an attribute type. For example, if the value of the <code>Email</code> field of Profile A matches the value of the <code>BusinessEmail</code> field of Profile B, the two profiles are matched on the <code>Email</code> attribute type.</p>
    pub fn attribute_matching_model(&self) -> &crate::types::AttributeMatchingModel {
        &self.attribute_matching_model
    }
    /// <p>The type of matching record that is allowed to be used in an ID mapping workflow.</p>
    /// <p>If the value is set to <code>ONE_SOURCE_TO_ONE_TARGET</code>, only one record in the source can be matched to the same record in the target.</p>
    /// <p>If the value is set to <code>MANY_SOURCE_TO_ONE_TARGET</code>, multiple records in the source can be matched to one record in the target.</p>
    pub fn record_matching_model(&self) -> &crate::types::RecordMatchingModel {
        &self.record_matching_model
    }
}
impl IdMappingRuleBasedProperties {
    /// Creates a new builder-style object to manufacture [`IdMappingRuleBasedProperties`](crate::types::IdMappingRuleBasedProperties).
    pub fn builder() -> crate::types::builders::IdMappingRuleBasedPropertiesBuilder {
        crate::types::builders::IdMappingRuleBasedPropertiesBuilder::default()
    }
}

/// A builder for [`IdMappingRuleBasedProperties`](crate::types::IdMappingRuleBasedProperties).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct IdMappingRuleBasedPropertiesBuilder {
    pub(crate) rules: ::std::option::Option<::std::vec::Vec<crate::types::Rule>>,
    pub(crate) rule_definition_type: ::std::option::Option<crate::types::IdMappingWorkflowRuleDefinitionType>,
    pub(crate) attribute_matching_model: ::std::option::Option<crate::types::AttributeMatchingModel>,
    pub(crate) record_matching_model: ::std::option::Option<crate::types::RecordMatchingModel>,
}
impl IdMappingRuleBasedPropertiesBuilder {
    /// Appends an item to `rules`.
    ///
    /// To override the contents of this collection use [`set_rules`](Self::set_rules).
    ///
    /// <p>The rules that can be used for ID mapping.</p>
    pub fn rules(mut self, input: crate::types::Rule) -> Self {
        let mut v = self.rules.unwrap_or_default();
        v.push(input);
        self.rules = ::std::option::Option::Some(v);
        self
    }
    /// <p>The rules that can be used for ID mapping.</p>
    pub fn set_rules(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::Rule>>) -> Self {
        self.rules = input;
        self
    }
    /// <p>The rules that can be used for ID mapping.</p>
    pub fn get_rules(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::Rule>> {
        &self.rules
    }
    /// <p>The set of rules you can use in an ID mapping workflow. The limitations specified for the source or target to define the match rules must be compatible.</p>
    /// This field is required.
    pub fn rule_definition_type(mut self, input: crate::types::IdMappingWorkflowRuleDefinitionType) -> Self {
        self.rule_definition_type = ::std::option::Option::Some(input);
        self
    }
    /// <p>The set of rules you can use in an ID mapping workflow. The limitations specified for the source or target to define the match rules must be compatible.</p>
    pub fn set_rule_definition_type(mut self, input: ::std::option::Option<crate::types::IdMappingWorkflowRuleDefinitionType>) -> Self {
        self.rule_definition_type = input;
        self
    }
    /// <p>The set of rules you can use in an ID mapping workflow. The limitations specified for the source or target to define the match rules must be compatible.</p>
    pub fn get_rule_definition_type(&self) -> &::std::option::Option<crate::types::IdMappingWorkflowRuleDefinitionType> {
        &self.rule_definition_type
    }
    /// <p>The comparison type. You can either choose <code>ONE_TO_ONE</code> or <code>MANY_TO_MANY</code> as the <code>attributeMatchingModel</code>.</p>
    /// <p>If you choose <code>ONE_TO_ONE</code>, the system can only match attributes if the sub-types are an exact match. For example, for the <code>Email</code> attribute type, the system will only consider it a match if the value of the <code>Email</code> field of Profile A matches the value of the <code>Email</code> field of Profile B.</p>
    /// <p>If you choose <code>MANY_TO_MANY</code>, the system can match attributes across the sub-types of an attribute type. For example, if the value of the <code>Email</code> field of Profile A matches the value of the <code>BusinessEmail</code> field of Profile B, the two profiles are matched on the <code>Email</code> attribute type.</p>
    /// This field is required.
    pub fn attribute_matching_model(mut self, input: crate::types::AttributeMatchingModel) -> Self {
        self.attribute_matching_model = ::std::option::Option::Some(input);
        self
    }
    /// <p>The comparison type. You can either choose <code>ONE_TO_ONE</code> or <code>MANY_TO_MANY</code> as the <code>attributeMatchingModel</code>.</p>
    /// <p>If you choose <code>ONE_TO_ONE</code>, the system can only match attributes if the sub-types are an exact match. For example, for the <code>Email</code> attribute type, the system will only consider it a match if the value of the <code>Email</code> field of Profile A matches the value of the <code>Email</code> field of Profile B.</p>
    /// <p>If you choose <code>MANY_TO_MANY</code>, the system can match attributes across the sub-types of an attribute type. For example, if the value of the <code>Email</code> field of Profile A matches the value of the <code>BusinessEmail</code> field of Profile B, the two profiles are matched on the <code>Email</code> attribute type.</p>
    pub fn set_attribute_matching_model(mut self, input: ::std::option::Option<crate::types::AttributeMatchingModel>) -> Self {
        self.attribute_matching_model = input;
        self
    }
    /// <p>The comparison type. You can either choose <code>ONE_TO_ONE</code> or <code>MANY_TO_MANY</code> as the <code>attributeMatchingModel</code>.</p>
    /// <p>If you choose <code>ONE_TO_ONE</code>, the system can only match attributes if the sub-types are an exact match. For example, for the <code>Email</code> attribute type, the system will only consider it a match if the value of the <code>Email</code> field of Profile A matches the value of the <code>Email</code> field of Profile B.</p>
    /// <p>If you choose <code>MANY_TO_MANY</code>, the system can match attributes across the sub-types of an attribute type. For example, if the value of the <code>Email</code> field of Profile A matches the value of the <code>BusinessEmail</code> field of Profile B, the two profiles are matched on the <code>Email</code> attribute type.</p>
    pub fn get_attribute_matching_model(&self) -> &::std::option::Option<crate::types::AttributeMatchingModel> {
        &self.attribute_matching_model
    }
    /// <p>The type of matching record that is allowed to be used in an ID mapping workflow.</p>
    /// <p>If the value is set to <code>ONE_SOURCE_TO_ONE_TARGET</code>, only one record in the source can be matched to the same record in the target.</p>
    /// <p>If the value is set to <code>MANY_SOURCE_TO_ONE_TARGET</code>, multiple records in the source can be matched to one record in the target.</p>
    /// This field is required.
    pub fn record_matching_model(mut self, input: crate::types::RecordMatchingModel) -> Self {
        self.record_matching_model = ::std::option::Option::Some(input);
        self
    }
    /// <p>The type of matching record that is allowed to be used in an ID mapping workflow.</p>
    /// <p>If the value is set to <code>ONE_SOURCE_TO_ONE_TARGET</code>, only one record in the source can be matched to the same record in the target.</p>
    /// <p>If the value is set to <code>MANY_SOURCE_TO_ONE_TARGET</code>, multiple records in the source can be matched to one record in the target.</p>
    pub fn set_record_matching_model(mut self, input: ::std::option::Option<crate::types::RecordMatchingModel>) -> Self {
        self.record_matching_model = input;
        self
    }
    /// <p>The type of matching record that is allowed to be used in an ID mapping workflow.</p>
    /// <p>If the value is set to <code>ONE_SOURCE_TO_ONE_TARGET</code>, only one record in the source can be matched to the same record in the target.</p>
    /// <p>If the value is set to <code>MANY_SOURCE_TO_ONE_TARGET</code>, multiple records in the source can be matched to one record in the target.</p>
    pub fn get_record_matching_model(&self) -> &::std::option::Option<crate::types::RecordMatchingModel> {
        &self.record_matching_model
    }
    /// Consumes the builder and constructs a [`IdMappingRuleBasedProperties`](crate::types::IdMappingRuleBasedProperties).
    /// This method will fail if any of the following fields are not set:
    /// - [`rule_definition_type`](crate::types::builders::IdMappingRuleBasedPropertiesBuilder::rule_definition_type)
    /// - [`attribute_matching_model`](crate::types::builders::IdMappingRuleBasedPropertiesBuilder::attribute_matching_model)
    /// - [`record_matching_model`](crate::types::builders::IdMappingRuleBasedPropertiesBuilder::record_matching_model)
    pub fn build(self) -> ::std::result::Result<crate::types::IdMappingRuleBasedProperties, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::IdMappingRuleBasedProperties {
            rules: self.rules,
            rule_definition_type: self.rule_definition_type.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "rule_definition_type",
                    "rule_definition_type was not specified but it is required when building IdMappingRuleBasedProperties",
                )
            })?,
            attribute_matching_model: self.attribute_matching_model.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "attribute_matching_model",
                    "attribute_matching_model was not specified but it is required when building IdMappingRuleBasedProperties",
                )
            })?,
            record_matching_model: self.record_matching_model.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "record_matching_model",
                    "record_matching_model was not specified but it is required when building IdMappingRuleBasedProperties",
                )
            })?,
        })
    }
}
