// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// When writing a match expression against `AccessGrantPrincipalType`, it is important to ensure
/// your code is forward-compatible. That is, if a match arm handles a case for a
/// feature that is supported by the service but has not been represented as an enum
/// variant in a current version of SDK, your code should continue to work when you
/// upgrade SDK to a future version in which the enum does include a variant for that
/// feature.
///
/// Here is an example of how you can make a match expression forward-compatible:
///
/// ```text
/// # let accessgrantprincipaltype = unimplemented!();
/// match accessgrantprincipaltype {
///     AccessGrantPrincipalType::AccessProfile => { /* ... */ },
///     AccessGrantPrincipalType::Agent => { /* ... */ },
///     AccessGrantPrincipalType::Alert => { /* ... */ },
///     AccessGrantPrincipalType::IamRole => { /* ... */ },
///     AccessGrantPrincipalType::IamRoot => { /* ... */ },
///     AccessGrantPrincipalType::IamUser => { /* ... */ },
///     AccessGrantPrincipalType::IdcGroup => { /* ... */ },
///     AccessGrantPrincipalType::IdcUser => { /* ... */ },
///     other @ _ if other.as_str() == "NewFeature" => { /* handles a case for `NewFeature` */ },
///     _ => { /* ... */ },
/// }
/// ```
/// The above code demonstrates that when `accessgrantprincipaltype` represents
/// `NewFeature`, the execution path will lead to the second last match arm,
/// even though the enum does not contain a variant `AccessGrantPrincipalType::NewFeature`
/// in the current version of SDK. The reason is that the variable `other`,
/// created by the `@` operator, is bound to
/// `AccessGrantPrincipalType::Unknown(UnknownVariantValue("NewFeature".to_owned()))`
/// and calling `as_str` on it yields `"NewFeature"`.
/// This match expression is forward-compatible when executed with a newer
/// version of SDK where the variant `AccessGrantPrincipalType::NewFeature` is defined.
/// Specifically, when `accessgrantprincipaltype` represents `NewFeature`,
/// the execution path will hit the second last match arm as before by virtue of
/// calling `as_str` on `AccessGrantPrincipalType::NewFeature` also yielding `"NewFeature"`.
///
/// Explicitly matching on the `Unknown` variant should
/// be avoided for two reasons:
/// - The inner data `UnknownVariantValue` is opaque, and no further information can be extracted.
/// - It might inadvertently shadow other intended match arms.
///
/// Type of principal receiving the grant.
#[non_exhaustive]
#[derive(
    ::std::clone::Clone, ::std::cmp::Eq, ::std::cmp::Ord, ::std::cmp::PartialEq, ::std::cmp::PartialOrd, ::std::fmt::Debug, ::std::hash::Hash,
)]
pub enum AccessGrantPrincipalType {
    /// An access profile.
    AccessProfile,
    /// An agent workload principal.
    Agent,
    /// An alert.
    Alert,
    /// An IAM role.
    IamRole,
    /// An AWS account root principal.
    IamRoot,
    /// An IAM user.
    IamUser,
    /// An Identity Center group.
    IdcGroup,
    /// An Identity Center user.
    IdcUser,
    /// `Unknown` contains new variants that have been added since this code was generated.
    #[deprecated(note = "Don't directly match on `Unknown`. See the docs on this enum for the correct way to handle unknown variants.")]
    Unknown(crate::primitives::sealed_enum_unknown::UnknownVariantValue),
}
impl ::std::convert::From<&str> for AccessGrantPrincipalType {
    fn from(s: &str) -> Self {
        match s {
            "ACCESS_PROFILE" => AccessGrantPrincipalType::AccessProfile,
            "AGENT" => AccessGrantPrincipalType::Agent,
            "ALERT" => AccessGrantPrincipalType::Alert,
            "IAM_ROLE" => AccessGrantPrincipalType::IamRole,
            "IAM_ROOT" => AccessGrantPrincipalType::IamRoot,
            "IAM_USER" => AccessGrantPrincipalType::IamUser,
            "IDC_GROUP" => AccessGrantPrincipalType::IdcGroup,
            "IDC_USER" => AccessGrantPrincipalType::IdcUser,
            other => AccessGrantPrincipalType::Unknown(crate::primitives::sealed_enum_unknown::UnknownVariantValue(other.to_owned())),
        }
    }
}
impl ::std::str::FromStr for AccessGrantPrincipalType {
    type Err = ::std::convert::Infallible;

    fn from_str(s: &str) -> ::std::result::Result<Self, <Self as ::std::str::FromStr>::Err> {
        ::std::result::Result::Ok(AccessGrantPrincipalType::from(s))
    }
}
impl AccessGrantPrincipalType {
    /// Returns the `&str` value of the enum member.
    pub fn as_str(&self) -> &str {
        match self {
            AccessGrantPrincipalType::AccessProfile => "ACCESS_PROFILE",
            AccessGrantPrincipalType::Agent => "AGENT",
            AccessGrantPrincipalType::Alert => "ALERT",
            AccessGrantPrincipalType::IamRole => "IAM_ROLE",
            AccessGrantPrincipalType::IamRoot => "IAM_ROOT",
            AccessGrantPrincipalType::IamUser => "IAM_USER",
            AccessGrantPrincipalType::IdcGroup => "IDC_GROUP",
            AccessGrantPrincipalType::IdcUser => "IDC_USER",
            AccessGrantPrincipalType::Unknown(value) => value.as_str(),
        }
    }
    /// Returns all the `&str` representations of the enum members.
    pub const fn values() -> &'static [&'static str] {
        &[
            "ACCESS_PROFILE",
            "AGENT",
            "ALERT",
            "IAM_ROLE",
            "IAM_ROOT",
            "IAM_USER",
            "IDC_GROUP",
            "IDC_USER",
        ]
    }
}
impl ::std::convert::AsRef<str> for AccessGrantPrincipalType {
    fn as_ref(&self) -> &str {
        self.as_str()
    }
}
impl AccessGrantPrincipalType {
    /// Parses the enum value while disallowing unknown variants.
    ///
    /// Unknown variants will result in an error.
    pub fn try_parse(value: &str) -> ::std::result::Result<Self, crate::error::UnknownVariantError> {
        match Self::from(value) {
            #[allow(deprecated)]
            Self::Unknown(_) => ::std::result::Result::Err(crate::error::UnknownVariantError::new(value)),
            known => Ok(known),
        }
    }
}
impl ::std::fmt::Display for AccessGrantPrincipalType {
    fn fmt(&self, f: &mut ::std::fmt::Formatter) -> ::std::fmt::Result {
        match self {
            AccessGrantPrincipalType::AccessProfile => write!(f, "ACCESS_PROFILE"),
            AccessGrantPrincipalType::Agent => write!(f, "AGENT"),
            AccessGrantPrincipalType::Alert => write!(f, "ALERT"),
            AccessGrantPrincipalType::IamRole => write!(f, "IAM_ROLE"),
            AccessGrantPrincipalType::IamRoot => write!(f, "IAM_ROOT"),
            AccessGrantPrincipalType::IamUser => write!(f, "IAM_USER"),
            AccessGrantPrincipalType::IdcGroup => write!(f, "IDC_GROUP"),
            AccessGrantPrincipalType::IdcUser => write!(f, "IDC_USER"),
            AccessGrantPrincipalType::Unknown(value) => write!(f, "{value}"),
        }
    }
}
