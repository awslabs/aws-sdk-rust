// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Specifies the minimum number of distinct identities that each query output group must represent.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct AggregationThreshold {
    /// <p>The identity column, such as <code>user_id</code>, whose distinct values Clean Rooms counts to enforce minimum aggregation thresholds. Currently, you can specify only one column, and its data type must be string, varchar, or char.</p>
    pub identity_columns: ::std::vec::Vec<::std::string::String>,
    /// <p>The minimum number of distinct identities that each query output group must represent. This threshold applies to all output columns in the table. To override this threshold for a specific column, use <code>outputColumnThresholds</code>.</p>
    pub minimum_identity_count: i32,
    /// <p>The type of aggregation that the threshold enforces. Currently, the only supported value is <code>COUNT_DISTINCT</code>, which counts the distinct values in the identity column.</p>
    pub r#type: crate::types::AggregationThresholdType,
    /// <p>The per-column overrides of <code>minimumIdentityCount</code>. An output column without an override uses <code>minimumIdentityCount</code>.</p>
    pub output_column_thresholds: ::std::option::Option<::std::vec::Vec<crate::types::OutputColumnThreshold>>,
    /// <p>Specifies whether a query can aggregate a transformed column. This applies to the arguments of both aggregate and window functions. Valid values are:</p>
    /// <p><code>COLUMNS_ONLY</code> – A query can aggregate only a direct column reference, such as <code>SUM(amount)</code>, or a constant. Clean Rooms rejects a query that transforms a column and then aggregates it, such as <code>SUM(amount * 2)</code> or <code>SUM(ROUND(amount))</code>.</p>
    /// <p><code>ANY_EXPRESSION</code> – A query can aggregate any expression. This includes arithmetic, such as <code>SUM(price * quantity)</code>; a cast, such as <code>SUM(CAST(amount AS DECIMAL))</code>; a nested function call, such as <code>SUM(COALESCE(amount, 0))</code>; and a conditional, such as <code>SUM(CASE WHEN region = 'EU' THEN amount ELSE 0 END)</code>.</p>
    pub allowed_aggregate_expression_type: crate::types::AllowedAggregateExpressionType,
}
impl AggregationThreshold {
    /// <p>The identity column, such as <code>user_id</code>, whose distinct values Clean Rooms counts to enforce minimum aggregation thresholds. Currently, you can specify only one column, and its data type must be string, varchar, or char.</p>
    pub fn identity_columns(&self) -> &[::std::string::String] {
        use std::ops::Deref;
        self.identity_columns.deref()
    }
    /// <p>The minimum number of distinct identities that each query output group must represent. This threshold applies to all output columns in the table. To override this threshold for a specific column, use <code>outputColumnThresholds</code>.</p>
    pub fn minimum_identity_count(&self) -> i32 {
        self.minimum_identity_count
    }
    /// <p>The type of aggregation that the threshold enforces. Currently, the only supported value is <code>COUNT_DISTINCT</code>, which counts the distinct values in the identity column.</p>
    pub fn r#type(&self) -> &crate::types::AggregationThresholdType {
        &self.r#type
    }
    /// <p>The per-column overrides of <code>minimumIdentityCount</code>. An output column without an override uses <code>minimumIdentityCount</code>.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.output_column_thresholds.is_none()`.
    pub fn output_column_thresholds(&self) -> &[crate::types::OutputColumnThreshold] {
        self.output_column_thresholds.as_deref().unwrap_or_default()
    }
    /// <p>Specifies whether a query can aggregate a transformed column. This applies to the arguments of both aggregate and window functions. Valid values are:</p>
    /// <p><code>COLUMNS_ONLY</code> – A query can aggregate only a direct column reference, such as <code>SUM(amount)</code>, or a constant. Clean Rooms rejects a query that transforms a column and then aggregates it, such as <code>SUM(amount * 2)</code> or <code>SUM(ROUND(amount))</code>.</p>
    /// <p><code>ANY_EXPRESSION</code> – A query can aggregate any expression. This includes arithmetic, such as <code>SUM(price * quantity)</code>; a cast, such as <code>SUM(CAST(amount AS DECIMAL))</code>; a nested function call, such as <code>SUM(COALESCE(amount, 0))</code>; and a conditional, such as <code>SUM(CASE WHEN region = 'EU' THEN amount ELSE 0 END)</code>.</p>
    pub fn allowed_aggregate_expression_type(&self) -> &crate::types::AllowedAggregateExpressionType {
        &self.allowed_aggregate_expression_type
    }
}
impl AggregationThreshold {
    /// Creates a new builder-style object to manufacture [`AggregationThreshold`](crate::types::AggregationThreshold).
    pub fn builder() -> crate::types::builders::AggregationThresholdBuilder {
        crate::types::builders::AggregationThresholdBuilder::default()
    }
}

/// A builder for [`AggregationThreshold`](crate::types::AggregationThreshold).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct AggregationThresholdBuilder {
    pub(crate) identity_columns: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    pub(crate) minimum_identity_count: ::std::option::Option<i32>,
    pub(crate) r#type: ::std::option::Option<crate::types::AggregationThresholdType>,
    pub(crate) output_column_thresholds: ::std::option::Option<::std::vec::Vec<crate::types::OutputColumnThreshold>>,
    pub(crate) allowed_aggregate_expression_type: ::std::option::Option<crate::types::AllowedAggregateExpressionType>,
}
impl AggregationThresholdBuilder {
    /// Appends an item to `identity_columns`.
    ///
    /// To override the contents of this collection use [`set_identity_columns`](Self::set_identity_columns).
    ///
    /// <p>The identity column, such as <code>user_id</code>, whose distinct values Clean Rooms counts to enforce minimum aggregation thresholds. Currently, you can specify only one column, and its data type must be string, varchar, or char.</p>
    pub fn identity_columns(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.identity_columns.unwrap_or_default();
        v.push(input.into());
        self.identity_columns = ::std::option::Option::Some(v);
        self
    }
    /// <p>The identity column, such as <code>user_id</code>, whose distinct values Clean Rooms counts to enforce minimum aggregation thresholds. Currently, you can specify only one column, and its data type must be string, varchar, or char.</p>
    pub fn set_identity_columns(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.identity_columns = input;
        self
    }
    /// <p>The identity column, such as <code>user_id</code>, whose distinct values Clean Rooms counts to enforce minimum aggregation thresholds. Currently, you can specify only one column, and its data type must be string, varchar, or char.</p>
    pub fn get_identity_columns(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.identity_columns
    }
    /// <p>The minimum number of distinct identities that each query output group must represent. This threshold applies to all output columns in the table. To override this threshold for a specific column, use <code>outputColumnThresholds</code>.</p>
    /// This field is required.
    pub fn minimum_identity_count(mut self, input: i32) -> Self {
        self.minimum_identity_count = ::std::option::Option::Some(input);
        self
    }
    /// <p>The minimum number of distinct identities that each query output group must represent. This threshold applies to all output columns in the table. To override this threshold for a specific column, use <code>outputColumnThresholds</code>.</p>
    pub fn set_minimum_identity_count(mut self, input: ::std::option::Option<i32>) -> Self {
        self.minimum_identity_count = input;
        self
    }
    /// <p>The minimum number of distinct identities that each query output group must represent. This threshold applies to all output columns in the table. To override this threshold for a specific column, use <code>outputColumnThresholds</code>.</p>
    pub fn get_minimum_identity_count(&self) -> &::std::option::Option<i32> {
        &self.minimum_identity_count
    }
    /// <p>The type of aggregation that the threshold enforces. Currently, the only supported value is <code>COUNT_DISTINCT</code>, which counts the distinct values in the identity column.</p>
    /// This field is required.
    pub fn r#type(mut self, input: crate::types::AggregationThresholdType) -> Self {
        self.r#type = ::std::option::Option::Some(input);
        self
    }
    /// <p>The type of aggregation that the threshold enforces. Currently, the only supported value is <code>COUNT_DISTINCT</code>, which counts the distinct values in the identity column.</p>
    pub fn set_type(mut self, input: ::std::option::Option<crate::types::AggregationThresholdType>) -> Self {
        self.r#type = input;
        self
    }
    /// <p>The type of aggregation that the threshold enforces. Currently, the only supported value is <code>COUNT_DISTINCT</code>, which counts the distinct values in the identity column.</p>
    pub fn get_type(&self) -> &::std::option::Option<crate::types::AggregationThresholdType> {
        &self.r#type
    }
    /// Appends an item to `output_column_thresholds`.
    ///
    /// To override the contents of this collection use [`set_output_column_thresholds`](Self::set_output_column_thresholds).
    ///
    /// <p>The per-column overrides of <code>minimumIdentityCount</code>. An output column without an override uses <code>minimumIdentityCount</code>.</p>
    pub fn output_column_thresholds(mut self, input: crate::types::OutputColumnThreshold) -> Self {
        let mut v = self.output_column_thresholds.unwrap_or_default();
        v.push(input);
        self.output_column_thresholds = ::std::option::Option::Some(v);
        self
    }
    /// <p>The per-column overrides of <code>minimumIdentityCount</code>. An output column without an override uses <code>minimumIdentityCount</code>.</p>
    pub fn set_output_column_thresholds(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::OutputColumnThreshold>>) -> Self {
        self.output_column_thresholds = input;
        self
    }
    /// <p>The per-column overrides of <code>minimumIdentityCount</code>. An output column without an override uses <code>minimumIdentityCount</code>.</p>
    pub fn get_output_column_thresholds(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::OutputColumnThreshold>> {
        &self.output_column_thresholds
    }
    /// <p>Specifies whether a query can aggregate a transformed column. This applies to the arguments of both aggregate and window functions. Valid values are:</p>
    /// <p><code>COLUMNS_ONLY</code> – A query can aggregate only a direct column reference, such as <code>SUM(amount)</code>, or a constant. Clean Rooms rejects a query that transforms a column and then aggregates it, such as <code>SUM(amount * 2)</code> or <code>SUM(ROUND(amount))</code>.</p>
    /// <p><code>ANY_EXPRESSION</code> – A query can aggregate any expression. This includes arithmetic, such as <code>SUM(price * quantity)</code>; a cast, such as <code>SUM(CAST(amount AS DECIMAL))</code>; a nested function call, such as <code>SUM(COALESCE(amount, 0))</code>; and a conditional, such as <code>SUM(CASE WHEN region = 'EU' THEN amount ELSE 0 END)</code>.</p>
    /// This field is required.
    pub fn allowed_aggregate_expression_type(mut self, input: crate::types::AllowedAggregateExpressionType) -> Self {
        self.allowed_aggregate_expression_type = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether a query can aggregate a transformed column. This applies to the arguments of both aggregate and window functions. Valid values are:</p>
    /// <p><code>COLUMNS_ONLY</code> – A query can aggregate only a direct column reference, such as <code>SUM(amount)</code>, or a constant. Clean Rooms rejects a query that transforms a column and then aggregates it, such as <code>SUM(amount * 2)</code> or <code>SUM(ROUND(amount))</code>.</p>
    /// <p><code>ANY_EXPRESSION</code> – A query can aggregate any expression. This includes arithmetic, such as <code>SUM(price * quantity)</code>; a cast, such as <code>SUM(CAST(amount AS DECIMAL))</code>; a nested function call, such as <code>SUM(COALESCE(amount, 0))</code>; and a conditional, such as <code>SUM(CASE WHEN region = 'EU' THEN amount ELSE 0 END)</code>.</p>
    pub fn set_allowed_aggregate_expression_type(mut self, input: ::std::option::Option<crate::types::AllowedAggregateExpressionType>) -> Self {
        self.allowed_aggregate_expression_type = input;
        self
    }
    /// <p>Specifies whether a query can aggregate a transformed column. This applies to the arguments of both aggregate and window functions. Valid values are:</p>
    /// <p><code>COLUMNS_ONLY</code> – A query can aggregate only a direct column reference, such as <code>SUM(amount)</code>, or a constant. Clean Rooms rejects a query that transforms a column and then aggregates it, such as <code>SUM(amount * 2)</code> or <code>SUM(ROUND(amount))</code>.</p>
    /// <p><code>ANY_EXPRESSION</code> – A query can aggregate any expression. This includes arithmetic, such as <code>SUM(price * quantity)</code>; a cast, such as <code>SUM(CAST(amount AS DECIMAL))</code>; a nested function call, such as <code>SUM(COALESCE(amount, 0))</code>; and a conditional, such as <code>SUM(CASE WHEN region = 'EU' THEN amount ELSE 0 END)</code>.</p>
    pub fn get_allowed_aggregate_expression_type(&self) -> &::std::option::Option<crate::types::AllowedAggregateExpressionType> {
        &self.allowed_aggregate_expression_type
    }
    /// Consumes the builder and constructs a [`AggregationThreshold`](crate::types::AggregationThreshold).
    /// This method will fail if any of the following fields are not set:
    /// - [`identity_columns`](crate::types::builders::AggregationThresholdBuilder::identity_columns)
    /// - [`minimum_identity_count`](crate::types::builders::AggregationThresholdBuilder::minimum_identity_count)
    /// - [`r#type`](crate::types::builders::AggregationThresholdBuilder::type)
    /// - [`allowed_aggregate_expression_type`](crate::types::builders::AggregationThresholdBuilder::allowed_aggregate_expression_type)
    pub fn build(self) -> ::std::result::Result<crate::types::AggregationThreshold, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::AggregationThreshold {
            identity_columns: self.identity_columns.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "identity_columns",
                    "identity_columns was not specified but it is required when building AggregationThreshold",
                )
            })?,
            minimum_identity_count: self.minimum_identity_count.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "minimum_identity_count",
                    "minimum_identity_count was not specified but it is required when building AggregationThreshold",
                )
            })?,
            r#type: self.r#type.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "r#type",
                    "r#type was not specified but it is required when building AggregationThreshold",
                )
            })?,
            output_column_thresholds: self.output_column_thresholds,
            allowed_aggregate_expression_type: self.allowed_aggregate_expression_type.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "allowed_aggregate_expression_type",
                    "allowed_aggregate_expression_type was not specified but it is required when building AggregationThreshold",
                )
            })?,
        })
    }
}
