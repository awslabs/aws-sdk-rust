// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains output information for an analysis log export with an S3 output type.</p>
/// <p>The exported logs are written under the bucket and key prefix that you specify. The path includes the collaboration ID, the protected query ID, and the analysis log export ID. Because the path includes the export ID, exporting the same query more than once doesn't overwrite the logs from an earlier export.</p><note>
/// <p>The exported logs are encrypted using the default encryption configuration of the destination bucket. Clean Rooms doesn't accept a KMS key for log export. To encrypt the exported logs with a customer managed key, configure the bucket's default encryption to use that key before you export.</p>
/// </note>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct AnalysisLogExportS3OutputConfiguration {
    /// <p>The S3 bucket that the exported analysis logs are written to. The bucket must be in the same Amazon Web Services Region as the collaboration.</p>
    pub bucket: ::std::string::String,
    /// <p>The S3 key prefix under which the exported analysis logs are written.</p>
    /// <p>Only one export can be in progress at a time for a given query and destination. To export the same query twice at once, use a different key prefix for the second export.</p>
    pub key_prefix: ::std::option::Option<::std::string::String>,
}
impl AnalysisLogExportS3OutputConfiguration {
    /// <p>The S3 bucket that the exported analysis logs are written to. The bucket must be in the same Amazon Web Services Region as the collaboration.</p>
    pub fn bucket(&self) -> &str {
        use std::ops::Deref;
        self.bucket.deref()
    }
    /// <p>The S3 key prefix under which the exported analysis logs are written.</p>
    /// <p>Only one export can be in progress at a time for a given query and destination. To export the same query twice at once, use a different key prefix for the second export.</p>
    pub fn key_prefix(&self) -> ::std::option::Option<&str> {
        self.key_prefix.as_deref()
    }
}
impl AnalysisLogExportS3OutputConfiguration {
    /// Creates a new builder-style object to manufacture [`AnalysisLogExportS3OutputConfiguration`](crate::types::AnalysisLogExportS3OutputConfiguration).
    pub fn builder() -> crate::types::builders::AnalysisLogExportS3OutputConfigurationBuilder {
        crate::types::builders::AnalysisLogExportS3OutputConfigurationBuilder::default()
    }
}

/// A builder for [`AnalysisLogExportS3OutputConfiguration`](crate::types::AnalysisLogExportS3OutputConfiguration).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct AnalysisLogExportS3OutputConfigurationBuilder {
    pub(crate) bucket: ::std::option::Option<::std::string::String>,
    pub(crate) key_prefix: ::std::option::Option<::std::string::String>,
}
impl AnalysisLogExportS3OutputConfigurationBuilder {
    /// <p>The S3 bucket that the exported analysis logs are written to. The bucket must be in the same Amazon Web Services Region as the collaboration.</p>
    /// This field is required.
    pub fn bucket(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.bucket = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The S3 bucket that the exported analysis logs are written to. The bucket must be in the same Amazon Web Services Region as the collaboration.</p>
    pub fn set_bucket(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.bucket = input;
        self
    }
    /// <p>The S3 bucket that the exported analysis logs are written to. The bucket must be in the same Amazon Web Services Region as the collaboration.</p>
    pub fn get_bucket(&self) -> &::std::option::Option<::std::string::String> {
        &self.bucket
    }
    /// <p>The S3 key prefix under which the exported analysis logs are written.</p>
    /// <p>Only one export can be in progress at a time for a given query and destination. To export the same query twice at once, use a different key prefix for the second export.</p>
    pub fn key_prefix(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.key_prefix = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The S3 key prefix under which the exported analysis logs are written.</p>
    /// <p>Only one export can be in progress at a time for a given query and destination. To export the same query twice at once, use a different key prefix for the second export.</p>
    pub fn set_key_prefix(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.key_prefix = input;
        self
    }
    /// <p>The S3 key prefix under which the exported analysis logs are written.</p>
    /// <p>Only one export can be in progress at a time for a given query and destination. To export the same query twice at once, use a different key prefix for the second export.</p>
    pub fn get_key_prefix(&self) -> &::std::option::Option<::std::string::String> {
        &self.key_prefix
    }
    /// Consumes the builder and constructs a [`AnalysisLogExportS3OutputConfiguration`](crate::types::AnalysisLogExportS3OutputConfiguration).
    /// This method will fail if any of the following fields are not set:
    /// - [`bucket`](crate::types::builders::AnalysisLogExportS3OutputConfigurationBuilder::bucket)
    pub fn build(
        self,
    ) -> ::std::result::Result<crate::types::AnalysisLogExportS3OutputConfiguration, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::AnalysisLogExportS3OutputConfiguration {
            bucket: self.bucket.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "bucket",
                    "bucket was not specified but it is required when building AnalysisLogExportS3OutputConfiguration",
                )
            })?,
            key_prefix: self.key_prefix,
        })
    }
}
