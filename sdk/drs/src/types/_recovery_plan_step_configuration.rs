// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// Type-specific configuration for a recovery plan step. Exactly one member must be set.
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum RecoveryPlanStepConfiguration {
    /// Configuration for a SERVER type step.
    ServerStepConfiguration(crate::types::ServerStepConfiguration),
    /// Configuration for a WAIT type step.
    WaitStepConfiguration(crate::types::WaitStepConfiguration),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl RecoveryPlanStepConfiguration {
    /// Tries to convert the enum instance into [`ServerStepConfiguration`](crate::types::RecoveryPlanStepConfiguration::ServerStepConfiguration), extracting the inner [`ServerStepConfiguration`](crate::types::ServerStepConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_server_step_configuration(&self) -> ::std::result::Result<&crate::types::ServerStepConfiguration, &Self> {
        if let RecoveryPlanStepConfiguration::ServerStepConfiguration(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`ServerStepConfiguration`](crate::types::RecoveryPlanStepConfiguration::ServerStepConfiguration).
    pub fn is_server_step_configuration(&self) -> bool {
        self.as_server_step_configuration().is_ok()
    }
    /// Tries to convert the enum instance into [`WaitStepConfiguration`](crate::types::RecoveryPlanStepConfiguration::WaitStepConfiguration), extracting the inner [`WaitStepConfiguration`](crate::types::WaitStepConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_wait_step_configuration(&self) -> ::std::result::Result<&crate::types::WaitStepConfiguration, &Self> {
        if let RecoveryPlanStepConfiguration::WaitStepConfiguration(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`WaitStepConfiguration`](crate::types::RecoveryPlanStepConfiguration::WaitStepConfiguration).
    pub fn is_wait_step_configuration(&self) -> bool {
        self.as_wait_step_configuration().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
