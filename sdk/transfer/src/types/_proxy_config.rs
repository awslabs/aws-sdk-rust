// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Contains configuration for PROXY protocol version 2 (PPv2) support on an Transfer Family server. When enabled, Transfer Family reads the added PPv2 header from incoming connections to extract the original client IP address. This address is then available in Amazon CloudWatch Logs entries and is passed to custom identity providers during authentication, enabling IP-based access policies. For more information, see <a href="https://docs.aws.amazon.com/transfer/latest/userguide/working-with-nlb.html">Working with Network Load Balancers</a>.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ProxyConfig {
    /// <p>Specifies whether the Transfer Family server requires or ignores a PPv2 header containing the original client IP address on incoming SFTP connections. If you don't specify a value, the default is <code>NONE</code></p>
    /// <ul>
    /// <li>
    /// <p><code>NONE</code>: the server reads and ignores any PPv2 header on incoming SFTP connections. This is the default value. Use this value when your SFTP server is not behind an NLB, or when you do not need to preserve client source IP addresses through an NLB.</p></li>
    /// <li>
    /// <p><code>PROXY_PROTOCOL_V2_ENFORCED</code>: the server requires a valid PPv2 header on every incoming SFTP connection. When a valid header is present, the server applies it and uses the client IP address from the header. If a connection arrives without a PPv2 header, the server refuses the connection and logs an error to Amazon CloudWatch Logs indicating that the expected PPv2 header was missing. Use this value when your SFTP server is behind an NLB with PPv2 enabled on the target group.</p><important>
    /// <p>When you enable <code>PROXY_PROTOCOL_V2_ENFORCED</code>, the server trusts the source IP address in the PPv2 header. You must configure security groups on your server's VPC endpoint to restrict inbound traffic to only the NLB's private IP addresses. For the full requirements, see <a href="https://docs.aws.amazon.com/transfer/latest/userguide/working-with-nlb.html">Working with Network Load Balancers</a>.</p>
    /// </important></li>
    /// </ul>
    pub sftp_mode: ::std::option::Option<crate::types::ProxyMode>,
}
impl ProxyConfig {
    /// <p>Specifies whether the Transfer Family server requires or ignores a PPv2 header containing the original client IP address on incoming SFTP connections. If you don't specify a value, the default is <code>NONE</code></p>
    /// <ul>
    /// <li>
    /// <p><code>NONE</code>: the server reads and ignores any PPv2 header on incoming SFTP connections. This is the default value. Use this value when your SFTP server is not behind an NLB, or when you do not need to preserve client source IP addresses through an NLB.</p></li>
    /// <li>
    /// <p><code>PROXY_PROTOCOL_V2_ENFORCED</code>: the server requires a valid PPv2 header on every incoming SFTP connection. When a valid header is present, the server applies it and uses the client IP address from the header. If a connection arrives without a PPv2 header, the server refuses the connection and logs an error to Amazon CloudWatch Logs indicating that the expected PPv2 header was missing. Use this value when your SFTP server is behind an NLB with PPv2 enabled on the target group.</p><important>
    /// <p>When you enable <code>PROXY_PROTOCOL_V2_ENFORCED</code>, the server trusts the source IP address in the PPv2 header. You must configure security groups on your server's VPC endpoint to restrict inbound traffic to only the NLB's private IP addresses. For the full requirements, see <a href="https://docs.aws.amazon.com/transfer/latest/userguide/working-with-nlb.html">Working with Network Load Balancers</a>.</p>
    /// </important></li>
    /// </ul>
    pub fn sftp_mode(&self) -> ::std::option::Option<&crate::types::ProxyMode> {
        self.sftp_mode.as_ref()
    }
}
impl ProxyConfig {
    /// Creates a new builder-style object to manufacture [`ProxyConfig`](crate::types::ProxyConfig).
    pub fn builder() -> crate::types::builders::ProxyConfigBuilder {
        crate::types::builders::ProxyConfigBuilder::default()
    }
}

/// A builder for [`ProxyConfig`](crate::types::ProxyConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ProxyConfigBuilder {
    pub(crate) sftp_mode: ::std::option::Option<crate::types::ProxyMode>,
}
impl ProxyConfigBuilder {
    /// <p>Specifies whether the Transfer Family server requires or ignores a PPv2 header containing the original client IP address on incoming SFTP connections. If you don't specify a value, the default is <code>NONE</code></p>
    /// <ul>
    /// <li>
    /// <p><code>NONE</code>: the server reads and ignores any PPv2 header on incoming SFTP connections. This is the default value. Use this value when your SFTP server is not behind an NLB, or when you do not need to preserve client source IP addresses through an NLB.</p></li>
    /// <li>
    /// <p><code>PROXY_PROTOCOL_V2_ENFORCED</code>: the server requires a valid PPv2 header on every incoming SFTP connection. When a valid header is present, the server applies it and uses the client IP address from the header. If a connection arrives without a PPv2 header, the server refuses the connection and logs an error to Amazon CloudWatch Logs indicating that the expected PPv2 header was missing. Use this value when your SFTP server is behind an NLB with PPv2 enabled on the target group.</p><important>
    /// <p>When you enable <code>PROXY_PROTOCOL_V2_ENFORCED</code>, the server trusts the source IP address in the PPv2 header. You must configure security groups on your server's VPC endpoint to restrict inbound traffic to only the NLB's private IP addresses. For the full requirements, see <a href="https://docs.aws.amazon.com/transfer/latest/userguide/working-with-nlb.html">Working with Network Load Balancers</a>.</p>
    /// </important></li>
    /// </ul>
    pub fn sftp_mode(mut self, input: crate::types::ProxyMode) -> Self {
        self.sftp_mode = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether the Transfer Family server requires or ignores a PPv2 header containing the original client IP address on incoming SFTP connections. If you don't specify a value, the default is <code>NONE</code></p>
    /// <ul>
    /// <li>
    /// <p><code>NONE</code>: the server reads and ignores any PPv2 header on incoming SFTP connections. This is the default value. Use this value when your SFTP server is not behind an NLB, or when you do not need to preserve client source IP addresses through an NLB.</p></li>
    /// <li>
    /// <p><code>PROXY_PROTOCOL_V2_ENFORCED</code>: the server requires a valid PPv2 header on every incoming SFTP connection. When a valid header is present, the server applies it and uses the client IP address from the header. If a connection arrives without a PPv2 header, the server refuses the connection and logs an error to Amazon CloudWatch Logs indicating that the expected PPv2 header was missing. Use this value when your SFTP server is behind an NLB with PPv2 enabled on the target group.</p><important>
    /// <p>When you enable <code>PROXY_PROTOCOL_V2_ENFORCED</code>, the server trusts the source IP address in the PPv2 header. You must configure security groups on your server's VPC endpoint to restrict inbound traffic to only the NLB's private IP addresses. For the full requirements, see <a href="https://docs.aws.amazon.com/transfer/latest/userguide/working-with-nlb.html">Working with Network Load Balancers</a>.</p>
    /// </important></li>
    /// </ul>
    pub fn set_sftp_mode(mut self, input: ::std::option::Option<crate::types::ProxyMode>) -> Self {
        self.sftp_mode = input;
        self
    }
    /// <p>Specifies whether the Transfer Family server requires or ignores a PPv2 header containing the original client IP address on incoming SFTP connections. If you don't specify a value, the default is <code>NONE</code></p>
    /// <ul>
    /// <li>
    /// <p><code>NONE</code>: the server reads and ignores any PPv2 header on incoming SFTP connections. This is the default value. Use this value when your SFTP server is not behind an NLB, or when you do not need to preserve client source IP addresses through an NLB.</p></li>
    /// <li>
    /// <p><code>PROXY_PROTOCOL_V2_ENFORCED</code>: the server requires a valid PPv2 header on every incoming SFTP connection. When a valid header is present, the server applies it and uses the client IP address from the header. If a connection arrives without a PPv2 header, the server refuses the connection and logs an error to Amazon CloudWatch Logs indicating that the expected PPv2 header was missing. Use this value when your SFTP server is behind an NLB with PPv2 enabled on the target group.</p><important>
    /// <p>When you enable <code>PROXY_PROTOCOL_V2_ENFORCED</code>, the server trusts the source IP address in the PPv2 header. You must configure security groups on your server's VPC endpoint to restrict inbound traffic to only the NLB's private IP addresses. For the full requirements, see <a href="https://docs.aws.amazon.com/transfer/latest/userguide/working-with-nlb.html">Working with Network Load Balancers</a>.</p>
    /// </important></li>
    /// </ul>
    pub fn get_sftp_mode(&self) -> &::std::option::Option<crate::types::ProxyMode> {
        &self.sftp_mode
    }
    /// Consumes the builder and constructs a [`ProxyConfig`](crate::types::ProxyConfig).
    pub fn build(self) -> crate::types::ProxyConfig {
        crate::types::ProxyConfig { sftp_mode: self.sftp_mode }
    }
}
