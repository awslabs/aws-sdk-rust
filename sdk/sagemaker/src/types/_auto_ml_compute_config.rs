// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <note>
/// <p>This data type is intended for use exclusively by SageMaker Canvas and cannot be used in other contexts at the moment.</p>
/// </note>
/// <p>Specifies the compute configuration for an AutoML job V2.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct AutoMlComputeConfig {
    /// <p>The configuration for using <a href="https://docs.aws.amazon.com/emr/latest/EMR-Serverless-UserGuide/emr-serverless.html"> EMR Serverless</a> to run the AutoML job V2.</p>
    /// <p>To allow your AutoML job V2 to automatically initiate a remote job on EMR Serverless when additional compute resources are needed to process large datasets, you need to provide an <code>EmrServerlessComputeConfig</code> object, which includes an <code>ExecutionRoleARN</code> attribute, to the <code>AutoMLComputeConfig</code> of the AutoML job V2 input request.</p>
    /// <p>By seamlessly transitioning to EMR Serverless when required, the AutoML job can handle datasets that would otherwise exceed the initially provisioned resources, without any manual intervention from you.</p>
    /// <p>EMR Serverless is available for the tabular and time series problem types. We recommend setting up this option for tabular datasets larger than 5 GB and time series datasets larger than 30 GB.</p>
    pub emr_serverless_compute_config: ::std::option::Option<crate::types::EmrServerlessComputeConfig>,
}
impl AutoMlComputeConfig {
    /// <p>The configuration for using <a href="https://docs.aws.amazon.com/emr/latest/EMR-Serverless-UserGuide/emr-serverless.html"> EMR Serverless</a> to run the AutoML job V2.</p>
    /// <p>To allow your AutoML job V2 to automatically initiate a remote job on EMR Serverless when additional compute resources are needed to process large datasets, you need to provide an <code>EmrServerlessComputeConfig</code> object, which includes an <code>ExecutionRoleARN</code> attribute, to the <code>AutoMLComputeConfig</code> of the AutoML job V2 input request.</p>
    /// <p>By seamlessly transitioning to EMR Serverless when required, the AutoML job can handle datasets that would otherwise exceed the initially provisioned resources, without any manual intervention from you.</p>
    /// <p>EMR Serverless is available for the tabular and time series problem types. We recommend setting up this option for tabular datasets larger than 5 GB and time series datasets larger than 30 GB.</p>
    pub fn emr_serverless_compute_config(&self) -> ::std::option::Option<&crate::types::EmrServerlessComputeConfig> {
        self.emr_serverless_compute_config.as_ref()
    }
}
impl AutoMlComputeConfig {
    /// Creates a new builder-style object to manufacture [`AutoMlComputeConfig`](crate::types::AutoMlComputeConfig).
    pub fn builder() -> crate::types::builders::AutoMlComputeConfigBuilder {
        crate::types::builders::AutoMlComputeConfigBuilder::default()
    }
}

/// A builder for [`AutoMlComputeConfig`](crate::types::AutoMlComputeConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct AutoMlComputeConfigBuilder {
    pub(crate) emr_serverless_compute_config: ::std::option::Option<crate::types::EmrServerlessComputeConfig>,
}
impl AutoMlComputeConfigBuilder {
    /// <p>The configuration for using <a href="https://docs.aws.amazon.com/emr/latest/EMR-Serverless-UserGuide/emr-serverless.html"> EMR Serverless</a> to run the AutoML job V2.</p>
    /// <p>To allow your AutoML job V2 to automatically initiate a remote job on EMR Serverless when additional compute resources are needed to process large datasets, you need to provide an <code>EmrServerlessComputeConfig</code> object, which includes an <code>ExecutionRoleARN</code> attribute, to the <code>AutoMLComputeConfig</code> of the AutoML job V2 input request.</p>
    /// <p>By seamlessly transitioning to EMR Serverless when required, the AutoML job can handle datasets that would otherwise exceed the initially provisioned resources, without any manual intervention from you.</p>
    /// <p>EMR Serverless is available for the tabular and time series problem types. We recommend setting up this option for tabular datasets larger than 5 GB and time series datasets larger than 30 GB.</p>
    pub fn emr_serverless_compute_config(mut self, input: crate::types::EmrServerlessComputeConfig) -> Self {
        self.emr_serverless_compute_config = ::std::option::Option::Some(input);
        self
    }
    /// <p>The configuration for using <a href="https://docs.aws.amazon.com/emr/latest/EMR-Serverless-UserGuide/emr-serverless.html"> EMR Serverless</a> to run the AutoML job V2.</p>
    /// <p>To allow your AutoML job V2 to automatically initiate a remote job on EMR Serverless when additional compute resources are needed to process large datasets, you need to provide an <code>EmrServerlessComputeConfig</code> object, which includes an <code>ExecutionRoleARN</code> attribute, to the <code>AutoMLComputeConfig</code> of the AutoML job V2 input request.</p>
    /// <p>By seamlessly transitioning to EMR Serverless when required, the AutoML job can handle datasets that would otherwise exceed the initially provisioned resources, without any manual intervention from you.</p>
    /// <p>EMR Serverless is available for the tabular and time series problem types. We recommend setting up this option for tabular datasets larger than 5 GB and time series datasets larger than 30 GB.</p>
    pub fn set_emr_serverless_compute_config(mut self, input: ::std::option::Option<crate::types::EmrServerlessComputeConfig>) -> Self {
        self.emr_serverless_compute_config = input;
        self
    }
    /// <p>The configuration for using <a href="https://docs.aws.amazon.com/emr/latest/EMR-Serverless-UserGuide/emr-serverless.html"> EMR Serverless</a> to run the AutoML job V2.</p>
    /// <p>To allow your AutoML job V2 to automatically initiate a remote job on EMR Serverless when additional compute resources are needed to process large datasets, you need to provide an <code>EmrServerlessComputeConfig</code> object, which includes an <code>ExecutionRoleARN</code> attribute, to the <code>AutoMLComputeConfig</code> of the AutoML job V2 input request.</p>
    /// <p>By seamlessly transitioning to EMR Serverless when required, the AutoML job can handle datasets that would otherwise exceed the initially provisioned resources, without any manual intervention from you.</p>
    /// <p>EMR Serverless is available for the tabular and time series problem types. We recommend setting up this option for tabular datasets larger than 5 GB and time series datasets larger than 30 GB.</p>
    pub fn get_emr_serverless_compute_config(&self) -> &::std::option::Option<crate::types::EmrServerlessComputeConfig> {
        &self.emr_serverless_compute_config
    }
    /// Consumes the builder and constructs a [`AutoMlComputeConfig`](crate::types::AutoMlComputeConfig).
    pub fn build(self) -> crate::types::AutoMlComputeConfig {
        crate::types::AutoMlComputeConfig {
            emr_serverless_compute_config: self.emr_serverless_compute_config,
        }
    }
}
