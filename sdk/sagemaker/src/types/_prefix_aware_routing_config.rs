// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration for prefix-aware routing on a SageMaker real-time inference endpoint. Specify <code>PrefixLength</code> and <code>ConcurrencyThreshold</code> to control routing behavior.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct PrefixAwareRoutingConfig {
    /// <p>The maximum length of the prefix used for routing decisions. Required when <code>RoutingStrategy</code> is <code>PREFIX_AWARE</code>.</p>
    /// <ul>
    /// <li>
    /// <p>For the SageMaker Runtime <code>InvokeEndpoint</code> and <code>InvokeEndpointWithResponseStream</code> APIs, this value specifies the number of bytes from the beginning of the request body.</p></li>
    /// <li>
    /// <p>For OpenAI-compatible API, this value specifies the number of characters from the text content of the messages array.</p></li>
    /// </ul>
    /// <p>The endpoint routes requests that share the same prefix to the same instance. Set this value to cover shared content (such as system prompts) plus enough unique content to distribute workloads across instances.</p>
    pub prefix_length: ::std::option::Option<i32>,
    /// <p>The maximum number of in-flight requests on the target instance before the endpoint routes to another instance. Required when <code>RoutingStrategy</code> is <code>PREFIX_AWARE</code>. When in-flight requests on the prefix-selected instance reach this threshold, the endpoint routes the request to an instance with more available capacity.</p>
    pub concurrency_threshold: ::std::option::Option<i32>,
}
impl PrefixAwareRoutingConfig {
    /// <p>The maximum length of the prefix used for routing decisions. Required when <code>RoutingStrategy</code> is <code>PREFIX_AWARE</code>.</p>
    /// <ul>
    /// <li>
    /// <p>For the SageMaker Runtime <code>InvokeEndpoint</code> and <code>InvokeEndpointWithResponseStream</code> APIs, this value specifies the number of bytes from the beginning of the request body.</p></li>
    /// <li>
    /// <p>For OpenAI-compatible API, this value specifies the number of characters from the text content of the messages array.</p></li>
    /// </ul>
    /// <p>The endpoint routes requests that share the same prefix to the same instance. Set this value to cover shared content (such as system prompts) plus enough unique content to distribute workloads across instances.</p>
    pub fn prefix_length(&self) -> ::std::option::Option<i32> {
        self.prefix_length
    }
    /// <p>The maximum number of in-flight requests on the target instance before the endpoint routes to another instance. Required when <code>RoutingStrategy</code> is <code>PREFIX_AWARE</code>. When in-flight requests on the prefix-selected instance reach this threshold, the endpoint routes the request to an instance with more available capacity.</p>
    pub fn concurrency_threshold(&self) -> ::std::option::Option<i32> {
        self.concurrency_threshold
    }
}
impl PrefixAwareRoutingConfig {
    /// Creates a new builder-style object to manufacture [`PrefixAwareRoutingConfig`](crate::types::PrefixAwareRoutingConfig).
    pub fn builder() -> crate::types::builders::PrefixAwareRoutingConfigBuilder {
        crate::types::builders::PrefixAwareRoutingConfigBuilder::default()
    }
}

/// A builder for [`PrefixAwareRoutingConfig`](crate::types::PrefixAwareRoutingConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct PrefixAwareRoutingConfigBuilder {
    pub(crate) prefix_length: ::std::option::Option<i32>,
    pub(crate) concurrency_threshold: ::std::option::Option<i32>,
}
impl PrefixAwareRoutingConfigBuilder {
    /// <p>The maximum length of the prefix used for routing decisions. Required when <code>RoutingStrategy</code> is <code>PREFIX_AWARE</code>.</p>
    /// <ul>
    /// <li>
    /// <p>For the SageMaker Runtime <code>InvokeEndpoint</code> and <code>InvokeEndpointWithResponseStream</code> APIs, this value specifies the number of bytes from the beginning of the request body.</p></li>
    /// <li>
    /// <p>For OpenAI-compatible API, this value specifies the number of characters from the text content of the messages array.</p></li>
    /// </ul>
    /// <p>The endpoint routes requests that share the same prefix to the same instance. Set this value to cover shared content (such as system prompts) plus enough unique content to distribute workloads across instances.</p>
    pub fn prefix_length(mut self, input: i32) -> Self {
        self.prefix_length = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum length of the prefix used for routing decisions. Required when <code>RoutingStrategy</code> is <code>PREFIX_AWARE</code>.</p>
    /// <ul>
    /// <li>
    /// <p>For the SageMaker Runtime <code>InvokeEndpoint</code> and <code>InvokeEndpointWithResponseStream</code> APIs, this value specifies the number of bytes from the beginning of the request body.</p></li>
    /// <li>
    /// <p>For OpenAI-compatible API, this value specifies the number of characters from the text content of the messages array.</p></li>
    /// </ul>
    /// <p>The endpoint routes requests that share the same prefix to the same instance. Set this value to cover shared content (such as system prompts) plus enough unique content to distribute workloads across instances.</p>
    pub fn set_prefix_length(mut self, input: ::std::option::Option<i32>) -> Self {
        self.prefix_length = input;
        self
    }
    /// <p>The maximum length of the prefix used for routing decisions. Required when <code>RoutingStrategy</code> is <code>PREFIX_AWARE</code>.</p>
    /// <ul>
    /// <li>
    /// <p>For the SageMaker Runtime <code>InvokeEndpoint</code> and <code>InvokeEndpointWithResponseStream</code> APIs, this value specifies the number of bytes from the beginning of the request body.</p></li>
    /// <li>
    /// <p>For OpenAI-compatible API, this value specifies the number of characters from the text content of the messages array.</p></li>
    /// </ul>
    /// <p>The endpoint routes requests that share the same prefix to the same instance. Set this value to cover shared content (such as system prompts) plus enough unique content to distribute workloads across instances.</p>
    pub fn get_prefix_length(&self) -> &::std::option::Option<i32> {
        &self.prefix_length
    }
    /// <p>The maximum number of in-flight requests on the target instance before the endpoint routes to another instance. Required when <code>RoutingStrategy</code> is <code>PREFIX_AWARE</code>. When in-flight requests on the prefix-selected instance reach this threshold, the endpoint routes the request to an instance with more available capacity.</p>
    pub fn concurrency_threshold(mut self, input: i32) -> Self {
        self.concurrency_threshold = ::std::option::Option::Some(input);
        self
    }
    /// <p>The maximum number of in-flight requests on the target instance before the endpoint routes to another instance. Required when <code>RoutingStrategy</code> is <code>PREFIX_AWARE</code>. When in-flight requests on the prefix-selected instance reach this threshold, the endpoint routes the request to an instance with more available capacity.</p>
    pub fn set_concurrency_threshold(mut self, input: ::std::option::Option<i32>) -> Self {
        self.concurrency_threshold = input;
        self
    }
    /// <p>The maximum number of in-flight requests on the target instance before the endpoint routes to another instance. Required when <code>RoutingStrategy</code> is <code>PREFIX_AWARE</code>. When in-flight requests on the prefix-selected instance reach this threshold, the endpoint routes the request to an instance with more available capacity.</p>
    pub fn get_concurrency_threshold(&self) -> &::std::option::Option<i32> {
        &self.concurrency_threshold
    }
    /// Consumes the builder and constructs a [`PrefixAwareRoutingConfig`](crate::types::PrefixAwareRoutingConfig).
    pub fn build(self) -> crate::types::PrefixAwareRoutingConfig {
        crate::types::PrefixAwareRoutingConfig {
            prefix_length: self.prefix_length,
            concurrency_threshold: self.concurrency_threshold,
        }
    }
}
