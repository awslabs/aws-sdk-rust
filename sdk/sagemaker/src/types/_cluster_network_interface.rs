// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The network interface configuration for a Amazon SageMaker HyperPod cluster instance group.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ClusterNetworkInterface {
    /// <p>The type of network interface for the instance group. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>efa</code> – An EFA with ENA interface, which provides both the EFA device for low-latency, high-throughput communication and the ENA device for IP networking.</p></li>
    /// <li>
    /// <p><code>efa-only</code> – An EFA-only interface, which provides only the EFA device capabilities without the ENA device for traditional IP networking.</p></li>
    /// </ul>
    /// <p>For more information, see <a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/efa.html">Elastic Fabric Adapter</a>.</p>
    pub interface_type: ::std::option::Option<crate::types::ClusterInterfaceType>,
}
impl ClusterNetworkInterface {
    /// <p>The type of network interface for the instance group. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>efa</code> – An EFA with ENA interface, which provides both the EFA device for low-latency, high-throughput communication and the ENA device for IP networking.</p></li>
    /// <li>
    /// <p><code>efa-only</code> – An EFA-only interface, which provides only the EFA device capabilities without the ENA device for traditional IP networking.</p></li>
    /// </ul>
    /// <p>For more information, see <a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/efa.html">Elastic Fabric Adapter</a>.</p>
    pub fn interface_type(&self) -> ::std::option::Option<&crate::types::ClusterInterfaceType> {
        self.interface_type.as_ref()
    }
}
impl ClusterNetworkInterface {
    /// Creates a new builder-style object to manufacture [`ClusterNetworkInterface`](crate::types::ClusterNetworkInterface).
    pub fn builder() -> crate::types::builders::ClusterNetworkInterfaceBuilder {
        crate::types::builders::ClusterNetworkInterfaceBuilder::default()
    }
}

/// A builder for [`ClusterNetworkInterface`](crate::types::ClusterNetworkInterface).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ClusterNetworkInterfaceBuilder {
    pub(crate) interface_type: ::std::option::Option<crate::types::ClusterInterfaceType>,
}
impl ClusterNetworkInterfaceBuilder {
    /// <p>The type of network interface for the instance group. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>efa</code> – An EFA with ENA interface, which provides both the EFA device for low-latency, high-throughput communication and the ENA device for IP networking.</p></li>
    /// <li>
    /// <p><code>efa-only</code> – An EFA-only interface, which provides only the EFA device capabilities without the ENA device for traditional IP networking.</p></li>
    /// </ul>
    /// <p>For more information, see <a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/efa.html">Elastic Fabric Adapter</a>.</p>
    pub fn interface_type(mut self, input: crate::types::ClusterInterfaceType) -> Self {
        self.interface_type = ::std::option::Option::Some(input);
        self
    }
    /// <p>The type of network interface for the instance group. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>efa</code> – An EFA with ENA interface, which provides both the EFA device for low-latency, high-throughput communication and the ENA device for IP networking.</p></li>
    /// <li>
    /// <p><code>efa-only</code> – An EFA-only interface, which provides only the EFA device capabilities without the ENA device for traditional IP networking.</p></li>
    /// </ul>
    /// <p>For more information, see <a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/efa.html">Elastic Fabric Adapter</a>.</p>
    pub fn set_interface_type(mut self, input: ::std::option::Option<crate::types::ClusterInterfaceType>) -> Self {
        self.interface_type = input;
        self
    }
    /// <p>The type of network interface for the instance group. Valid values:</p>
    /// <ul>
    /// <li>
    /// <p><code>efa</code> – An EFA with ENA interface, which provides both the EFA device for low-latency, high-throughput communication and the ENA device for IP networking.</p></li>
    /// <li>
    /// <p><code>efa-only</code> – An EFA-only interface, which provides only the EFA device capabilities without the ENA device for traditional IP networking.</p></li>
    /// </ul>
    /// <p>For more information, see <a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/efa.html">Elastic Fabric Adapter</a>.</p>
    pub fn get_interface_type(&self) -> &::std::option::Option<crate::types::ClusterInterfaceType> {
        &self.interface_type
    }
    /// Consumes the builder and constructs a [`ClusterNetworkInterface`](crate::types::ClusterNetworkInterface).
    pub fn build(self) -> crate::types::ClusterNetworkInterface {
        crate::types::ClusterNetworkInterface {
            interface_type: self.interface_type,
        }
    }
}
