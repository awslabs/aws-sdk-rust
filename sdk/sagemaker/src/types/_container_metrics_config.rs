// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration for container-level metrics scraping. Use this configuration to specify a custom metrics endpoint path and publishing frequency for container metrics. When <code>EnableDetailedObservability</code> is set to <code>True</code> in <code>MetricsConfig</code>, metrics are scraped from the container's Prometheus endpoint. If this configuration is not provided, the default path <code>/metrics</code> on port <code>8080</code> is used with a default publishing frequency of <code>60</code> seconds. For first-party and Deep Learning Containers (DLC), the endpoint path is determined automatically and this configuration is optional.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct ContainerMetricsConfig {
    /// <p>A list of metrics endpoints to scrape from the container. Each endpoint specifies the path where the container exposes Prometheus-formatted metrics and the frequency at which to publish them. You can specify a maximum of 1 endpoint.</p>
    pub metrics_endpoints: ::std::option::Option<::std::vec::Vec<crate::types::MetricsEndpoint>>,
}
impl ContainerMetricsConfig {
    /// <p>A list of metrics endpoints to scrape from the container. Each endpoint specifies the path where the container exposes Prometheus-formatted metrics and the frequency at which to publish them. You can specify a maximum of 1 endpoint.</p>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.metrics_endpoints.is_none()`.
    pub fn metrics_endpoints(&self) -> &[crate::types::MetricsEndpoint] {
        self.metrics_endpoints.as_deref().unwrap_or_default()
    }
}
impl ContainerMetricsConfig {
    /// Creates a new builder-style object to manufacture [`ContainerMetricsConfig`](crate::types::ContainerMetricsConfig).
    pub fn builder() -> crate::types::builders::ContainerMetricsConfigBuilder {
        crate::types::builders::ContainerMetricsConfigBuilder::default()
    }
}

/// A builder for [`ContainerMetricsConfig`](crate::types::ContainerMetricsConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct ContainerMetricsConfigBuilder {
    pub(crate) metrics_endpoints: ::std::option::Option<::std::vec::Vec<crate::types::MetricsEndpoint>>,
}
impl ContainerMetricsConfigBuilder {
    /// Appends an item to `metrics_endpoints`.
    ///
    /// To override the contents of this collection use [`set_metrics_endpoints`](Self::set_metrics_endpoints).
    ///
    /// <p>A list of metrics endpoints to scrape from the container. Each endpoint specifies the path where the container exposes Prometheus-formatted metrics and the frequency at which to publish them. You can specify a maximum of 1 endpoint.</p>
    pub fn metrics_endpoints(mut self, input: crate::types::MetricsEndpoint) -> Self {
        let mut v = self.metrics_endpoints.unwrap_or_default();
        v.push(input);
        self.metrics_endpoints = ::std::option::Option::Some(v);
        self
    }
    /// <p>A list of metrics endpoints to scrape from the container. Each endpoint specifies the path where the container exposes Prometheus-formatted metrics and the frequency at which to publish them. You can specify a maximum of 1 endpoint.</p>
    pub fn set_metrics_endpoints(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::MetricsEndpoint>>) -> Self {
        self.metrics_endpoints = input;
        self
    }
    /// <p>A list of metrics endpoints to scrape from the container. Each endpoint specifies the path where the container exposes Prometheus-formatted metrics and the frequency at which to publish them. You can specify a maximum of 1 endpoint.</p>
    pub fn get_metrics_endpoints(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::MetricsEndpoint>> {
        &self.metrics_endpoints
    }
    /// Consumes the builder and constructs a [`ContainerMetricsConfig`](crate::types::ContainerMetricsConfig).
    pub fn build(self) -> crate::types::ContainerMetricsConfig {
        crate::types::ContainerMetricsConfig {
            metrics_endpoints: self.metrics_endpoints,
        }
    }
}
