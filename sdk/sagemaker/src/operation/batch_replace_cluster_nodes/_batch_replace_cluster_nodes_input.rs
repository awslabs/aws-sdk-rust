// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
#[allow(missing_docs)] // documentation missing in model
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct BatchReplaceClusterNodesInput {
    /// <p>The name or Amazon Resource Name (ARN) of the SageMaker HyperPod cluster containing the nodes to replace.</p>
    pub cluster_name: ::std::option::Option<::std::string::String>,
    /// <p>A list of EC2 instance IDs to replace with new hardware. You can specify between 1 and 25 instance IDs.</p><important>
    /// <p>Replace operations destroy all instance volumes (root and secondary). Ensure you have backed up any important data before proceeding.</p>
    /// </important> <note>
    /// <ul>
    /// <li>
    /// <p>Either <code>NodeIds</code> or <code>NodeLogicalIds</code> must be provided (or both), but at least one is required.</p></li>
    /// <li>
    /// <p>Each instance ID must follow the pattern <code>i-</code> followed by 17 hexadecimal characters (for example, <code>i-0123456789abcdef0</code>).</p></li>
    /// <li>
    /// <p>For SageMaker HyperPod clusters using the Slurm workload manager, you cannot replace instances that are configured as Slurm controller nodes.</p></li>
    /// </ul>
    /// </note>
    pub node_ids: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    /// <p>A list of logical node IDs to replace with new hardware. You can specify between 1 and 25 logical node IDs.</p>
    /// <p>The <code>NodeLogicalId</code> is a unique identifier that persists throughout the node's lifecycle and can be used to track nodes that are still being provisioned and don't yet have an EC2 instance ID assigned.</p><important>
    /// <ul>
    /// <li>
    /// <p>Replace operations destroy all instance volumes (root and secondary). Ensure you have backed up any important data before proceeding.</p></li>
    /// <li>
    /// <p>This parameter is only supported for clusters using <code>Continuous</code> as the <code>NodeProvisioningMode</code>. For clusters using the default provisioning mode, use <code>NodeIds</code> instead.</p></li>
    /// <li>
    /// <p>Either <code>NodeIds</code> or <code>NodeLogicalIds</code> must be provided (or both), but at least one is required.</p></li>
    /// </ul>
    /// </important>
    pub node_logical_ids: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
}
impl BatchReplaceClusterNodesInput {
    /// <p>The name or Amazon Resource Name (ARN) of the SageMaker HyperPod cluster containing the nodes to replace.</p>
    pub fn cluster_name(&self) -> ::std::option::Option<&str> {
        self.cluster_name.as_deref()
    }
    /// <p>A list of EC2 instance IDs to replace with new hardware. You can specify between 1 and 25 instance IDs.</p><important>
    /// <p>Replace operations destroy all instance volumes (root and secondary). Ensure you have backed up any important data before proceeding.</p>
    /// </important> <note>
    /// <ul>
    /// <li>
    /// <p>Either <code>NodeIds</code> or <code>NodeLogicalIds</code> must be provided (or both), but at least one is required.</p></li>
    /// <li>
    /// <p>Each instance ID must follow the pattern <code>i-</code> followed by 17 hexadecimal characters (for example, <code>i-0123456789abcdef0</code>).</p></li>
    /// <li>
    /// <p>For SageMaker HyperPod clusters using the Slurm workload manager, you cannot replace instances that are configured as Slurm controller nodes.</p></li>
    /// </ul>
    /// </note>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.node_ids.is_none()`.
    pub fn node_ids(&self) -> &[::std::string::String] {
        self.node_ids.as_deref().unwrap_or_default()
    }
    /// <p>A list of logical node IDs to replace with new hardware. You can specify between 1 and 25 logical node IDs.</p>
    /// <p>The <code>NodeLogicalId</code> is a unique identifier that persists throughout the node's lifecycle and can be used to track nodes that are still being provisioned and don't yet have an EC2 instance ID assigned.</p><important>
    /// <ul>
    /// <li>
    /// <p>Replace operations destroy all instance volumes (root and secondary). Ensure you have backed up any important data before proceeding.</p></li>
    /// <li>
    /// <p>This parameter is only supported for clusters using <code>Continuous</code> as the <code>NodeProvisioningMode</code>. For clusters using the default provisioning mode, use <code>NodeIds</code> instead.</p></li>
    /// <li>
    /// <p>Either <code>NodeIds</code> or <code>NodeLogicalIds</code> must be provided (or both), but at least one is required.</p></li>
    /// </ul>
    /// </important>
    ///
    /// If no value was sent for this field, a default will be set. If you want to determine if no value was sent, use `.node_logical_ids.is_none()`.
    pub fn node_logical_ids(&self) -> &[::std::string::String] {
        self.node_logical_ids.as_deref().unwrap_or_default()
    }
}
impl BatchReplaceClusterNodesInput {
    /// Creates a new builder-style object to manufacture [`BatchReplaceClusterNodesInput`](crate::operation::batch_replace_cluster_nodes::BatchReplaceClusterNodesInput).
    pub fn builder() -> crate::operation::batch_replace_cluster_nodes::builders::BatchReplaceClusterNodesInputBuilder {
        crate::operation::batch_replace_cluster_nodes::builders::BatchReplaceClusterNodesInputBuilder::default()
    }
}

/// A builder for [`BatchReplaceClusterNodesInput`](crate::operation::batch_replace_cluster_nodes::BatchReplaceClusterNodesInput).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct BatchReplaceClusterNodesInputBuilder {
    pub(crate) cluster_name: ::std::option::Option<::std::string::String>,
    pub(crate) node_ids: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
    pub(crate) node_logical_ids: ::std::option::Option<::std::vec::Vec<::std::string::String>>,
}
impl BatchReplaceClusterNodesInputBuilder {
    /// <p>The name or Amazon Resource Name (ARN) of the SageMaker HyperPod cluster containing the nodes to replace.</p>
    /// This field is required.
    pub fn cluster_name(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.cluster_name = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The name or Amazon Resource Name (ARN) of the SageMaker HyperPod cluster containing the nodes to replace.</p>
    pub fn set_cluster_name(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.cluster_name = input;
        self
    }
    /// <p>The name or Amazon Resource Name (ARN) of the SageMaker HyperPod cluster containing the nodes to replace.</p>
    pub fn get_cluster_name(&self) -> &::std::option::Option<::std::string::String> {
        &self.cluster_name
    }
    /// Appends an item to `node_ids`.
    ///
    /// To override the contents of this collection use [`set_node_ids`](Self::set_node_ids).
    ///
    /// <p>A list of EC2 instance IDs to replace with new hardware. You can specify between 1 and 25 instance IDs.</p><important>
    /// <p>Replace operations destroy all instance volumes (root and secondary). Ensure you have backed up any important data before proceeding.</p>
    /// </important> <note>
    /// <ul>
    /// <li>
    /// <p>Either <code>NodeIds</code> or <code>NodeLogicalIds</code> must be provided (or both), but at least one is required.</p></li>
    /// <li>
    /// <p>Each instance ID must follow the pattern <code>i-</code> followed by 17 hexadecimal characters (for example, <code>i-0123456789abcdef0</code>).</p></li>
    /// <li>
    /// <p>For SageMaker HyperPod clusters using the Slurm workload manager, you cannot replace instances that are configured as Slurm controller nodes.</p></li>
    /// </ul>
    /// </note>
    pub fn node_ids(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.node_ids.unwrap_or_default();
        v.push(input.into());
        self.node_ids = ::std::option::Option::Some(v);
        self
    }
    /// <p>A list of EC2 instance IDs to replace with new hardware. You can specify between 1 and 25 instance IDs.</p><important>
    /// <p>Replace operations destroy all instance volumes (root and secondary). Ensure you have backed up any important data before proceeding.</p>
    /// </important> <note>
    /// <ul>
    /// <li>
    /// <p>Either <code>NodeIds</code> or <code>NodeLogicalIds</code> must be provided (or both), but at least one is required.</p></li>
    /// <li>
    /// <p>Each instance ID must follow the pattern <code>i-</code> followed by 17 hexadecimal characters (for example, <code>i-0123456789abcdef0</code>).</p></li>
    /// <li>
    /// <p>For SageMaker HyperPod clusters using the Slurm workload manager, you cannot replace instances that are configured as Slurm controller nodes.</p></li>
    /// </ul>
    /// </note>
    pub fn set_node_ids(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.node_ids = input;
        self
    }
    /// <p>A list of EC2 instance IDs to replace with new hardware. You can specify between 1 and 25 instance IDs.</p><important>
    /// <p>Replace operations destroy all instance volumes (root and secondary). Ensure you have backed up any important data before proceeding.</p>
    /// </important> <note>
    /// <ul>
    /// <li>
    /// <p>Either <code>NodeIds</code> or <code>NodeLogicalIds</code> must be provided (or both), but at least one is required.</p></li>
    /// <li>
    /// <p>Each instance ID must follow the pattern <code>i-</code> followed by 17 hexadecimal characters (for example, <code>i-0123456789abcdef0</code>).</p></li>
    /// <li>
    /// <p>For SageMaker HyperPod clusters using the Slurm workload manager, you cannot replace instances that are configured as Slurm controller nodes.</p></li>
    /// </ul>
    /// </note>
    pub fn get_node_ids(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.node_ids
    }
    /// Appends an item to `node_logical_ids`.
    ///
    /// To override the contents of this collection use [`set_node_logical_ids`](Self::set_node_logical_ids).
    ///
    /// <p>A list of logical node IDs to replace with new hardware. You can specify between 1 and 25 logical node IDs.</p>
    /// <p>The <code>NodeLogicalId</code> is a unique identifier that persists throughout the node's lifecycle and can be used to track nodes that are still being provisioned and don't yet have an EC2 instance ID assigned.</p><important>
    /// <ul>
    /// <li>
    /// <p>Replace operations destroy all instance volumes (root and secondary). Ensure you have backed up any important data before proceeding.</p></li>
    /// <li>
    /// <p>This parameter is only supported for clusters using <code>Continuous</code> as the <code>NodeProvisioningMode</code>. For clusters using the default provisioning mode, use <code>NodeIds</code> instead.</p></li>
    /// <li>
    /// <p>Either <code>NodeIds</code> or <code>NodeLogicalIds</code> must be provided (or both), but at least one is required.</p></li>
    /// </ul>
    /// </important>
    pub fn node_logical_ids(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        let mut v = self.node_logical_ids.unwrap_or_default();
        v.push(input.into());
        self.node_logical_ids = ::std::option::Option::Some(v);
        self
    }
    /// <p>A list of logical node IDs to replace with new hardware. You can specify between 1 and 25 logical node IDs.</p>
    /// <p>The <code>NodeLogicalId</code> is a unique identifier that persists throughout the node's lifecycle and can be used to track nodes that are still being provisioned and don't yet have an EC2 instance ID assigned.</p><important>
    /// <ul>
    /// <li>
    /// <p>Replace operations destroy all instance volumes (root and secondary). Ensure you have backed up any important data before proceeding.</p></li>
    /// <li>
    /// <p>This parameter is only supported for clusters using <code>Continuous</code> as the <code>NodeProvisioningMode</code>. For clusters using the default provisioning mode, use <code>NodeIds</code> instead.</p></li>
    /// <li>
    /// <p>Either <code>NodeIds</code> or <code>NodeLogicalIds</code> must be provided (or both), but at least one is required.</p></li>
    /// </ul>
    /// </important>
    pub fn set_node_logical_ids(mut self, input: ::std::option::Option<::std::vec::Vec<::std::string::String>>) -> Self {
        self.node_logical_ids = input;
        self
    }
    /// <p>A list of logical node IDs to replace with new hardware. You can specify between 1 and 25 logical node IDs.</p>
    /// <p>The <code>NodeLogicalId</code> is a unique identifier that persists throughout the node's lifecycle and can be used to track nodes that are still being provisioned and don't yet have an EC2 instance ID assigned.</p><important>
    /// <ul>
    /// <li>
    /// <p>Replace operations destroy all instance volumes (root and secondary). Ensure you have backed up any important data before proceeding.</p></li>
    /// <li>
    /// <p>This parameter is only supported for clusters using <code>Continuous</code> as the <code>NodeProvisioningMode</code>. For clusters using the default provisioning mode, use <code>NodeIds</code> instead.</p></li>
    /// <li>
    /// <p>Either <code>NodeIds</code> or <code>NodeLogicalIds</code> must be provided (or both), but at least one is required.</p></li>
    /// </ul>
    /// </important>
    pub fn get_node_logical_ids(&self) -> &::std::option::Option<::std::vec::Vec<::std::string::String>> {
        &self.node_logical_ids
    }
    /// Consumes the builder and constructs a [`BatchReplaceClusterNodesInput`](crate::operation::batch_replace_cluster_nodes::BatchReplaceClusterNodesInput).
    pub fn build(
        self,
    ) -> ::std::result::Result<
        crate::operation::batch_replace_cluster_nodes::BatchReplaceClusterNodesInput,
        ::aws_smithy_types::error::operation::BuildError,
    > {
        ::std::result::Result::Ok(crate::operation::batch_replace_cluster_nodes::BatchReplaceClusterNodesInput {
            cluster_name: self.cluster_name,
            node_ids: self.node_ids,
            node_logical_ids: self.node_logical_ids,
        })
    }
}
