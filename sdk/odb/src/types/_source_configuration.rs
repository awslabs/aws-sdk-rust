// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The configuration details for the source used to create an Autonomous Database. This is a union, so only one of the following members can be specified.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum SourceConfiguration {
    /// <p>The configuration for creating the Autonomous Database as a refreshable clone.</p>
    CloneToRefreshable(crate::types::CloneToRefreshableConfiguration),
    /// <p>The configuration for creating the Autonomous Database as a cross-Region Oracle Data Guard peer.</p>
    CrossRegionDataGuard(crate::types::CrossRegionDataGuardConfiguration),
    /// <p>The configuration for creating the Autonomous Database as a cross-Region disaster recovery peer.</p>
    CrossRegionDisasterRecovery(crate::types::CrossRegionDisasterRecoveryConfiguration),
    /// <p>The configuration for creating the Autonomous Database as a clone of an existing database.</p>
    DatabaseClone(crate::types::DatabaseCloneConfiguration),
    /// <p>The configuration for creating the Autonomous Database by restoring to a point in time.</p>
    PointInTimeRestore(crate::types::PointInTimeRestoreConfiguration),
    /// <p>The configuration for creating the Autonomous Database by restoring from a backup.</p>
    RestoreFromBackup(crate::types::RestoreFromBackupConfiguration),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl SourceConfiguration {
    /// Tries to convert the enum instance into [`CloneToRefreshable`](crate::types::SourceConfiguration::CloneToRefreshable), extracting the inner [`CloneToRefreshableConfiguration`](crate::types::CloneToRefreshableConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_clone_to_refreshable(&self) -> ::std::result::Result<&crate::types::CloneToRefreshableConfiguration, &Self> {
        if let SourceConfiguration::CloneToRefreshable(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`CloneToRefreshable`](crate::types::SourceConfiguration::CloneToRefreshable).
    pub fn is_clone_to_refreshable(&self) -> bool {
        self.as_clone_to_refreshable().is_ok()
    }
    /// Tries to convert the enum instance into [`CrossRegionDataGuard`](crate::types::SourceConfiguration::CrossRegionDataGuard), extracting the inner [`CrossRegionDataGuardConfiguration`](crate::types::CrossRegionDataGuardConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_cross_region_data_guard(&self) -> ::std::result::Result<&crate::types::CrossRegionDataGuardConfiguration, &Self> {
        if let SourceConfiguration::CrossRegionDataGuard(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`CrossRegionDataGuard`](crate::types::SourceConfiguration::CrossRegionDataGuard).
    pub fn is_cross_region_data_guard(&self) -> bool {
        self.as_cross_region_data_guard().is_ok()
    }
    /// Tries to convert the enum instance into [`CrossRegionDisasterRecovery`](crate::types::SourceConfiguration::CrossRegionDisasterRecovery), extracting the inner [`CrossRegionDisasterRecoveryConfiguration`](crate::types::CrossRegionDisasterRecoveryConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_cross_region_disaster_recovery(&self) -> ::std::result::Result<&crate::types::CrossRegionDisasterRecoveryConfiguration, &Self> {
        if let SourceConfiguration::CrossRegionDisasterRecovery(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`CrossRegionDisasterRecovery`](crate::types::SourceConfiguration::CrossRegionDisasterRecovery).
    pub fn is_cross_region_disaster_recovery(&self) -> bool {
        self.as_cross_region_disaster_recovery().is_ok()
    }
    /// Tries to convert the enum instance into [`DatabaseClone`](crate::types::SourceConfiguration::DatabaseClone), extracting the inner [`DatabaseCloneConfiguration`](crate::types::DatabaseCloneConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_database_clone(&self) -> ::std::result::Result<&crate::types::DatabaseCloneConfiguration, &Self> {
        if let SourceConfiguration::DatabaseClone(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`DatabaseClone`](crate::types::SourceConfiguration::DatabaseClone).
    pub fn is_database_clone(&self) -> bool {
        self.as_database_clone().is_ok()
    }
    /// Tries to convert the enum instance into [`PointInTimeRestore`](crate::types::SourceConfiguration::PointInTimeRestore), extracting the inner [`PointInTimeRestoreConfiguration`](crate::types::PointInTimeRestoreConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_point_in_time_restore(&self) -> ::std::result::Result<&crate::types::PointInTimeRestoreConfiguration, &Self> {
        if let SourceConfiguration::PointInTimeRestore(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`PointInTimeRestore`](crate::types::SourceConfiguration::PointInTimeRestore).
    pub fn is_point_in_time_restore(&self) -> bool {
        self.as_point_in_time_restore().is_ok()
    }
    /// Tries to convert the enum instance into [`RestoreFromBackup`](crate::types::SourceConfiguration::RestoreFromBackup), extracting the inner [`RestoreFromBackupConfiguration`](crate::types::RestoreFromBackupConfiguration).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_restore_from_backup(&self) -> ::std::result::Result<&crate::types::RestoreFromBackupConfiguration, &Self> {
        if let SourceConfiguration::RestoreFromBackup(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`RestoreFromBackup`](crate::types::SourceConfiguration::RestoreFromBackup).
    pub fn is_restore_from_backup(&self) -> bool {
        self.as_restore_from_backup().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
