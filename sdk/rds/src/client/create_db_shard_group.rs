// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.
impl super::Client {
    /// Constructs a fluent builder for the [`CreateDBShardGroup`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder) operation.
    ///
    /// - The fluent builder is configurable:
    ///   - [`db_shard_group_identifier(impl Into<String>)`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::db_shard_group_identifier) / [`set_db_shard_group_identifier(Option<String>)`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::set_db_shard_group_identifier):<br>required: **true**<br><p>The name of the DB shard group.</p><br>
    ///   - [`db_cluster_identifier(impl Into<String>)`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::db_cluster_identifier) / [`set_db_cluster_identifier(Option<String>)`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::set_db_cluster_identifier):<br>required: **true**<br><p>The name of the primary DB cluster for the DB shard group.</p><br>
    ///   - [`compute_redundancy(i32)`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::compute_redundancy) / [`set_compute_redundancy(Option<i32>)`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::set_compute_redundancy):<br>required: **false**<br><p>Specifies whether to create standby instances for the DB shard group. Valid values are the following:</p> <ul>  <li>   <p>0 - Creates a single, primary DB instance for each physical shard. This is the default value, and the only one supported for the preview.</p></li>  <li>   <p>1 - Creates a primary DB instance and a standby instance in a different Availability Zone (AZ) for each physical shard.</p></li>  <li>   <p>2 - Creates a primary DB instance and two standby instances in different AZs for each physical shard.</p></li> </ul><br>
    ///   - [`max_acu(f64)`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::max_acu) / [`set_max_acu(Option<f64>)`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::set_max_acu):<br>required: **true**<br><p>The maximum capacity of the DB shard group in Aurora capacity units (ACUs).</p><br>
    ///   - [`publicly_accessible(bool)`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::publicly_accessible) / [`set_publicly_accessible(Option<bool>)`](crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::set_publicly_accessible):<br>required: **false**<br><p>Specifies whether the DB shard group is publicly accessible.</p> <p>When the DB shard group is publicly accessible, its Domain Name System (DNS) endpoint resolves to the private IP address from within the DB shard group's virtual private cloud (VPC). It resolves to the public IP address from outside of the DB shard group's VPC. Access to the DB shard group is ultimately controlled by the security group it uses. That public access is not permitted if the security group assigned to the DB shard group doesn't permit it.</p> <p>When the DB shard group isn't publicly accessible, it is an internal DB shard group with a DNS name that resolves to a private IP address.</p> <p>Default: The default behavior varies depending on whether <code>DBSubnetGroupName</code> is specified.</p> <p>If <code>DBSubnetGroupName</code> isn't specified, and <code>PubliclyAccessible</code> isn't specified, the following applies:</p> <ul>  <li>   <p>If the default VPC in the target Region doesn’t have an internet gateway attached to it, the DB shard group is private.</p></li>  <li>   <p>If the default VPC in the target Region has an internet gateway attached to it, the DB shard group is public.</p></li> </ul> <p>If <code>DBSubnetGroupName</code> is specified, and <code>PubliclyAccessible</code> isn't specified, the following applies:</p> <ul>  <li>   <p>If the subnets are part of a VPC that doesn’t have an internet gateway attached to it, the DB shard group is private.</p></li>  <li>   <p>If the subnets are part of a VPC that has an internet gateway attached to it, the DB shard group is public.</p></li> </ul><br>
    /// - On success, responds with [`CreateDbShardGroupOutput`](crate::operation::create_db_shard_group::CreateDbShardGroupOutput) with field(s):
    ///   - [`db_shard_group_resource_id(Option<String>)`](crate::operation::create_db_shard_group::CreateDbShardGroupOutput::db_shard_group_resource_id): <p>The Amazon Web Services Region-unique, immutable identifier for the DB shard group.</p>
    ///   - [`db_shard_group_identifier(Option<String>)`](crate::operation::create_db_shard_group::CreateDbShardGroupOutput::db_shard_group_identifier): <p>The name of the DB shard group.</p>
    ///   - [`db_cluster_identifier(Option<String>)`](crate::operation::create_db_shard_group::CreateDbShardGroupOutput::db_cluster_identifier): <p>The name of the primary DB cluster for the DB shard group.</p>
    ///   - [`max_acu(Option<f64>)`](crate::operation::create_db_shard_group::CreateDbShardGroupOutput::max_acu): <p>The maximum capacity of the DB shard group in Aurora capacity units (ACUs).</p>
    ///   - [`compute_redundancy(Option<i32>)`](crate::operation::create_db_shard_group::CreateDbShardGroupOutput::compute_redundancy): <p>Specifies whether to create standby instances for the DB shard group. Valid values are the following:</p> <ul>  <li>   <p>0 - Creates a single, primary DB instance for each physical shard. This is the default value, and the only one supported for the preview.</p></li>  <li>   <p>1 - Creates a primary DB instance and a standby instance in a different Availability Zone (AZ) for each physical shard.</p></li>  <li>   <p>2 - Creates a primary DB instance and two standby instances in different AZs for each physical shard.</p></li> </ul>
    ///   - [`status(Option<String>)`](crate::operation::create_db_shard_group::CreateDbShardGroupOutput::status): <p>The status of the DB shard group.</p>
    ///   - [`publicly_accessible(Option<bool>)`](crate::operation::create_db_shard_group::CreateDbShardGroupOutput::publicly_accessible): <p>Indicates whether the DB shard group is publicly accessible.</p> <p>When the DB shard group is publicly accessible, its Domain Name System (DNS) endpoint resolves to the private IP address from within the DB shard group's virtual private cloud (VPC). It resolves to the public IP address from outside of the DB shard group's VPC. Access to the DB shard group is ultimately controlled by the security group it uses. That public access isn't permitted if the security group assigned to the DB shard group doesn't permit it.</p> <p>When the DB shard group isn't publicly accessible, it is an internal DB shard group with a DNS name that resolves to a private IP address.</p> <p>For more information, see <code>CreateDBShardGroup</code>.</p> <p>This setting is only for Aurora Limitless Database.</p>
    ///   - [`endpoint(Option<String>)`](crate::operation::create_db_shard_group::CreateDbShardGroupOutput::endpoint): <p>The connection endpoint for the DB shard group.</p>
    /// - On failure, responds with [`SdkError<CreateDBShardGroupError>`](crate::operation::create_db_shard_group::CreateDBShardGroupError)
    pub fn create_db_shard_group(&self) -> crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder {
        crate::operation::create_db_shard_group::builders::CreateDBShardGroupFluentBuilder::new(self.handle.clone())
    }
}
