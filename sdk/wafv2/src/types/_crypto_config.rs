// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The cryptocurrency payment configuration for AI bot monetization. Contains the list of blockchain payment networks where you receive payments.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct CryptoConfig {
    /// <p>The blockchain payment networks configured to receive payments. You can specify 1 to 2 networks. All networks must be in the same environment-either all production networks (Base, Solana) or all test networks (Base Sepolia, Solana Devnet).</p>
    pub payment_networks: ::std::vec::Vec<crate::types::PaymentNetwork>,
}
impl CryptoConfig {
    /// <p>The blockchain payment networks configured to receive payments. You can specify 1 to 2 networks. All networks must be in the same environment-either all production networks (Base, Solana) or all test networks (Base Sepolia, Solana Devnet).</p>
    pub fn payment_networks(&self) -> &[crate::types::PaymentNetwork] {
        use std::ops::Deref;
        self.payment_networks.deref()
    }
}
impl CryptoConfig {
    /// Creates a new builder-style object to manufacture [`CryptoConfig`](crate::types::CryptoConfig).
    pub fn builder() -> crate::types::builders::CryptoConfigBuilder {
        crate::types::builders::CryptoConfigBuilder::default()
    }
}

/// A builder for [`CryptoConfig`](crate::types::CryptoConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct CryptoConfigBuilder {
    pub(crate) payment_networks: ::std::option::Option<::std::vec::Vec<crate::types::PaymentNetwork>>,
}
impl CryptoConfigBuilder {
    /// Appends an item to `payment_networks`.
    ///
    /// To override the contents of this collection use [`set_payment_networks`](Self::set_payment_networks).
    ///
    /// <p>The blockchain payment networks configured to receive payments. You can specify 1 to 2 networks. All networks must be in the same environment-either all production networks (Base, Solana) or all test networks (Base Sepolia, Solana Devnet).</p>
    pub fn payment_networks(mut self, input: crate::types::PaymentNetwork) -> Self {
        let mut v = self.payment_networks.unwrap_or_default();
        v.push(input);
        self.payment_networks = ::std::option::Option::Some(v);
        self
    }
    /// <p>The blockchain payment networks configured to receive payments. You can specify 1 to 2 networks. All networks must be in the same environment-either all production networks (Base, Solana) or all test networks (Base Sepolia, Solana Devnet).</p>
    pub fn set_payment_networks(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::PaymentNetwork>>) -> Self {
        self.payment_networks = input;
        self
    }
    /// <p>The blockchain payment networks configured to receive payments. You can specify 1 to 2 networks. All networks must be in the same environment-either all production networks (Base, Solana) or all test networks (Base Sepolia, Solana Devnet).</p>
    pub fn get_payment_networks(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::PaymentNetwork>> {
        &self.payment_networks
    }
    /// Consumes the builder and constructs a [`CryptoConfig`](crate::types::CryptoConfig).
    /// This method will fail if any of the following fields are not set:
    /// - [`payment_networks`](crate::types::builders::CryptoConfigBuilder::payment_networks)
    pub fn build(self) -> ::std::result::Result<crate::types::CryptoConfig, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::CryptoConfig {
            payment_networks: self.payment_networks.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "payment_networks",
                    "payment_networks was not specified but it is required when building CryptoConfig",
                )
            })?,
        })
    }
}
