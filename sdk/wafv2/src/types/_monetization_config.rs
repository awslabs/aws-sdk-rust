// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The monetization configuration for a web ACL or rule group. Specifies the cryptocurrency payment networks and currency mode for AI bot monetization. You must provide this configuration when any rule in the web ACL or rule group uses the <code>Monetize</code> action.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct MonetizationConfig {
    /// <p>The cryptocurrency payment configuration, including the blockchain networks and wallet addresses where you receive payments.</p>
    pub crypto_config: ::std::option::Option<crate::types::CryptoConfig>,
    /// <p>Specifies whether the configuration uses real or test currency. Set to <code>REAL</code> to settle payments in USDC on production blockchain networks (Base, Solana). Set to <code>TEST</code> to settle on testnet networks (Base Sepolia, Solana Devnet) with tokens that have no monetary value. If not specified, defaults to <code>REAL</code>.</p>
    pub currency_mode: ::std::option::Option<crate::types::CurrencyMode>,
}
impl MonetizationConfig {
    /// <p>The cryptocurrency payment configuration, including the blockchain networks and wallet addresses where you receive payments.</p>
    pub fn crypto_config(&self) -> ::std::option::Option<&crate::types::CryptoConfig> {
        self.crypto_config.as_ref()
    }
    /// <p>Specifies whether the configuration uses real or test currency. Set to <code>REAL</code> to settle payments in USDC on production blockchain networks (Base, Solana). Set to <code>TEST</code> to settle on testnet networks (Base Sepolia, Solana Devnet) with tokens that have no monetary value. If not specified, defaults to <code>REAL</code>.</p>
    pub fn currency_mode(&self) -> ::std::option::Option<&crate::types::CurrencyMode> {
        self.currency_mode.as_ref()
    }
}
impl MonetizationConfig {
    /// Creates a new builder-style object to manufacture [`MonetizationConfig`](crate::types::MonetizationConfig).
    pub fn builder() -> crate::types::builders::MonetizationConfigBuilder {
        crate::types::builders::MonetizationConfigBuilder::default()
    }
}

/// A builder for [`MonetizationConfig`](crate::types::MonetizationConfig).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct MonetizationConfigBuilder {
    pub(crate) crypto_config: ::std::option::Option<crate::types::CryptoConfig>,
    pub(crate) currency_mode: ::std::option::Option<crate::types::CurrencyMode>,
}
impl MonetizationConfigBuilder {
    /// <p>The cryptocurrency payment configuration, including the blockchain networks and wallet addresses where you receive payments.</p>
    pub fn crypto_config(mut self, input: crate::types::CryptoConfig) -> Self {
        self.crypto_config = ::std::option::Option::Some(input);
        self
    }
    /// <p>The cryptocurrency payment configuration, including the blockchain networks and wallet addresses where you receive payments.</p>
    pub fn set_crypto_config(mut self, input: ::std::option::Option<crate::types::CryptoConfig>) -> Self {
        self.crypto_config = input;
        self
    }
    /// <p>The cryptocurrency payment configuration, including the blockchain networks and wallet addresses where you receive payments.</p>
    pub fn get_crypto_config(&self) -> &::std::option::Option<crate::types::CryptoConfig> {
        &self.crypto_config
    }
    /// <p>Specifies whether the configuration uses real or test currency. Set to <code>REAL</code> to settle payments in USDC on production blockchain networks (Base, Solana). Set to <code>TEST</code> to settle on testnet networks (Base Sepolia, Solana Devnet) with tokens that have no monetary value. If not specified, defaults to <code>REAL</code>.</p>
    pub fn currency_mode(mut self, input: crate::types::CurrencyMode) -> Self {
        self.currency_mode = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether the configuration uses real or test currency. Set to <code>REAL</code> to settle payments in USDC on production blockchain networks (Base, Solana). Set to <code>TEST</code> to settle on testnet networks (Base Sepolia, Solana Devnet) with tokens that have no monetary value. If not specified, defaults to <code>REAL</code>.</p>
    pub fn set_currency_mode(mut self, input: ::std::option::Option<crate::types::CurrencyMode>) -> Self {
        self.currency_mode = input;
        self
    }
    /// <p>Specifies whether the configuration uses real or test currency. Set to <code>REAL</code> to settle payments in USDC on production blockchain networks (Base, Solana). Set to <code>TEST</code> to settle on testnet networks (Base Sepolia, Solana Devnet) with tokens that have no monetary value. If not specified, defaults to <code>REAL</code>.</p>
    pub fn get_currency_mode(&self) -> &::std::option::Option<crate::types::CurrencyMode> {
        &self.currency_mode
    }
    /// Consumes the builder and constructs a [`MonetizationConfig`](crate::types::MonetizationConfig).
    pub fn build(self) -> crate::types::MonetizationConfig {
        crate::types::MonetizationConfig {
            crypto_config: self.crypto_config,
            currency_mode: self.currency_mode,
        }
    }
}
