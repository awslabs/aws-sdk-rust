// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A blockchain payment network configuration for receiving AI bot monetization payments. Specifies the blockchain chain, your wallet address on that chain, and the price per request.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct PaymentNetwork {
    /// <p>The blockchain network for receiving payments. Production networks: <code>BASE</code> (Base mainnet), <code>SOLANA</code> (Solana mainnet). Test networks: <code>BASE_SEPOLIA</code> (Base Sepolia testnet), <code>SOLANA_DEVNET</code> (Solana Devnet).</p>
    pub chain: crate::types::BlockchainChain,
    /// <p>Your wallet address on the specified blockchain where payments are sent. For EVM chains (Base, Base Sepolia), provide a valid Ethereum address (42 characters including 0x prefix). For Solana chains, provide a valid Base58-encoded public key (32-44 characters).</p>
    /// <p>For EVM addresses, WAF performs EIP-55 checksum validation for typo detection when the address uses a mix of lower and upper case letters. You can bypass this validation by providing the address in all lowercase or all uppercase.</p>
    pub wallet_address: ::std::string::String,
    /// <p>The price configuration for this payment network. Currently supports a single price entry in USDC.</p>
    pub prices: ::std::vec::Vec<crate::types::Price>,
}
impl PaymentNetwork {
    /// <p>The blockchain network for receiving payments. Production networks: <code>BASE</code> (Base mainnet), <code>SOLANA</code> (Solana mainnet). Test networks: <code>BASE_SEPOLIA</code> (Base Sepolia testnet), <code>SOLANA_DEVNET</code> (Solana Devnet).</p>
    pub fn chain(&self) -> &crate::types::BlockchainChain {
        &self.chain
    }
    /// <p>Your wallet address on the specified blockchain where payments are sent. For EVM chains (Base, Base Sepolia), provide a valid Ethereum address (42 characters including 0x prefix). For Solana chains, provide a valid Base58-encoded public key (32-44 characters).</p>
    /// <p>For EVM addresses, WAF performs EIP-55 checksum validation for typo detection when the address uses a mix of lower and upper case letters. You can bypass this validation by providing the address in all lowercase or all uppercase.</p>
    pub fn wallet_address(&self) -> &str {
        use std::ops::Deref;
        self.wallet_address.deref()
    }
    /// <p>The price configuration for this payment network. Currently supports a single price entry in USDC.</p>
    pub fn prices(&self) -> &[crate::types::Price] {
        use std::ops::Deref;
        self.prices.deref()
    }
}
impl PaymentNetwork {
    /// Creates a new builder-style object to manufacture [`PaymentNetwork`](crate::types::PaymentNetwork).
    pub fn builder() -> crate::types::builders::PaymentNetworkBuilder {
        crate::types::builders::PaymentNetworkBuilder::default()
    }
}

/// A builder for [`PaymentNetwork`](crate::types::PaymentNetwork).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct PaymentNetworkBuilder {
    pub(crate) chain: ::std::option::Option<crate::types::BlockchainChain>,
    pub(crate) wallet_address: ::std::option::Option<::std::string::String>,
    pub(crate) prices: ::std::option::Option<::std::vec::Vec<crate::types::Price>>,
}
impl PaymentNetworkBuilder {
    /// <p>The blockchain network for receiving payments. Production networks: <code>BASE</code> (Base mainnet), <code>SOLANA</code> (Solana mainnet). Test networks: <code>BASE_SEPOLIA</code> (Base Sepolia testnet), <code>SOLANA_DEVNET</code> (Solana Devnet).</p>
    /// This field is required.
    pub fn chain(mut self, input: crate::types::BlockchainChain) -> Self {
        self.chain = ::std::option::Option::Some(input);
        self
    }
    /// <p>The blockchain network for receiving payments. Production networks: <code>BASE</code> (Base mainnet), <code>SOLANA</code> (Solana mainnet). Test networks: <code>BASE_SEPOLIA</code> (Base Sepolia testnet), <code>SOLANA_DEVNET</code> (Solana Devnet).</p>
    pub fn set_chain(mut self, input: ::std::option::Option<crate::types::BlockchainChain>) -> Self {
        self.chain = input;
        self
    }
    /// <p>The blockchain network for receiving payments. Production networks: <code>BASE</code> (Base mainnet), <code>SOLANA</code> (Solana mainnet). Test networks: <code>BASE_SEPOLIA</code> (Base Sepolia testnet), <code>SOLANA_DEVNET</code> (Solana Devnet).</p>
    pub fn get_chain(&self) -> &::std::option::Option<crate::types::BlockchainChain> {
        &self.chain
    }
    /// <p>Your wallet address on the specified blockchain where payments are sent. For EVM chains (Base, Base Sepolia), provide a valid Ethereum address (42 characters including 0x prefix). For Solana chains, provide a valid Base58-encoded public key (32-44 characters).</p>
    /// <p>For EVM addresses, WAF performs EIP-55 checksum validation for typo detection when the address uses a mix of lower and upper case letters. You can bypass this validation by providing the address in all lowercase or all uppercase.</p>
    /// This field is required.
    pub fn wallet_address(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.wallet_address = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>Your wallet address on the specified blockchain where payments are sent. For EVM chains (Base, Base Sepolia), provide a valid Ethereum address (42 characters including 0x prefix). For Solana chains, provide a valid Base58-encoded public key (32-44 characters).</p>
    /// <p>For EVM addresses, WAF performs EIP-55 checksum validation for typo detection when the address uses a mix of lower and upper case letters. You can bypass this validation by providing the address in all lowercase or all uppercase.</p>
    pub fn set_wallet_address(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.wallet_address = input;
        self
    }
    /// <p>Your wallet address on the specified blockchain where payments are sent. For EVM chains (Base, Base Sepolia), provide a valid Ethereum address (42 characters including 0x prefix). For Solana chains, provide a valid Base58-encoded public key (32-44 characters).</p>
    /// <p>For EVM addresses, WAF performs EIP-55 checksum validation for typo detection when the address uses a mix of lower and upper case letters. You can bypass this validation by providing the address in all lowercase or all uppercase.</p>
    pub fn get_wallet_address(&self) -> &::std::option::Option<::std::string::String> {
        &self.wallet_address
    }
    /// Appends an item to `prices`.
    ///
    /// To override the contents of this collection use [`set_prices`](Self::set_prices).
    ///
    /// <p>The price configuration for this payment network. Currently supports a single price entry in USDC.</p>
    pub fn prices(mut self, input: crate::types::Price) -> Self {
        let mut v = self.prices.unwrap_or_default();
        v.push(input);
        self.prices = ::std::option::Option::Some(v);
        self
    }
    /// <p>The price configuration for this payment network. Currently supports a single price entry in USDC.</p>
    pub fn set_prices(mut self, input: ::std::option::Option<::std::vec::Vec<crate::types::Price>>) -> Self {
        self.prices = input;
        self
    }
    /// <p>The price configuration for this payment network. Currently supports a single price entry in USDC.</p>
    pub fn get_prices(&self) -> &::std::option::Option<::std::vec::Vec<crate::types::Price>> {
        &self.prices
    }
    /// Consumes the builder and constructs a [`PaymentNetwork`](crate::types::PaymentNetwork).
    /// This method will fail if any of the following fields are not set:
    /// - [`chain`](crate::types::builders::PaymentNetworkBuilder::chain)
    /// - [`wallet_address`](crate::types::builders::PaymentNetworkBuilder::wallet_address)
    /// - [`prices`](crate::types::builders::PaymentNetworkBuilder::prices)
    pub fn build(self) -> ::std::result::Result<crate::types::PaymentNetwork, ::aws_smithy_types::error::operation::BuildError> {
        ::std::result::Result::Ok(crate::types::PaymentNetwork {
            chain: self.chain.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "chain",
                    "chain was not specified but it is required when building PaymentNetwork",
                )
            })?,
            wallet_address: self.wallet_address.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "wallet_address",
                    "wallet_address was not specified but it is required when building PaymentNetwork",
                )
            })?,
            prices: self.prices.ok_or_else(|| {
                ::aws_smithy_types::error::operation::BuildError::missing_field(
                    "prices",
                    "prices was not specified but it is required when building PaymentNetwork",
                )
            })?,
        })
    }
}
