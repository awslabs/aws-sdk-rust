// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>A list of namespaced kernel parameters to set in the container. This parameter maps to <code>Sysctls</code> in the <a href="https://docs.docker.com/engine/api/v1.35/#operation/ContainerCreate">Create a container</a> section of the <a href="https://docs.docker.com/engine/api/v1.35/">Docker Remote API</a> and the <code>--sysctl</code> option to <a href="https://docs.docker.com/engine/reference/run/#security-configuration">docker run</a>. For example, you can configure <code>net.ipv4.tcp_keepalive_time</code> setting to maintain longer lived connections.</p>
/// <p>We don't recommend that you specify network-related <code>systemControls</code> parameters for multiple containers in a single task that also uses either the <code>awsvpc</code> or <code>host</code> network mode. Doing this has the following disadvantages:</p>
/// <ul>
/// <li>
/// <p>For tasks that use the <code>awsvpc</code> network mode including Fargate, if you set <code>systemControls</code> for any container, it applies to all containers in the task. If you set different <code>systemControls</code> for multiple containers in a single task, the container that's started last determines which <code>systemControls</code> take effect.</p></li>
/// <li>
/// <p>For tasks that use the <code>host</code> network mode, the network namespace <code>systemControls</code> aren't supported.</p></li>
/// </ul>
/// <p>If you're setting an IPC resource namespace to use for the containers in the task, the following conditions apply to your system controls. For more information, see <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_definition_parameters.html#task_definition_ipcmode">IPC mode</a>.</p>
/// <ul>
/// <li>
/// <p>For tasks that use the <code>host</code> IPC mode, IPC namespace <code>systemControls</code> aren't supported.</p></li>
/// <li>
/// <p>For tasks that use the <code>task</code> IPC mode, IPC namespace <code>systemControls</code> values apply to all containers within a task.</p></li>
/// </ul><note>
/// <p>This parameter is not supported for Windows containers.</p>
/// </note> <note>
/// <p>This parameter is only supported for tasks that are hosted on Fargate if the tasks are using platform version <code>1.4.0</code> or later (Linux). This isn't supported for Windows containers on Fargate.</p>
/// </note>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct SystemControl {
    /// <p>The namespaced kernel parameter to set a <code>value</code> for.</p>
    pub namespace: ::std::option::Option<::std::string::String>,
    /// <p>The namespaced kernel parameter to set a <code>value</code> for.</p>
    /// <p>Valid IPC namespace values: <code>"kernel.msgmax" | "kernel.msgmnb" | "kernel.msgmni" | "kernel.sem" | "kernel.shmall" | "kernel.shmmax" | "kernel.shmmni" | "kernel.shm_rmid_forced"</code>, and <code>Sysctls</code> that start with <code>"fs.mqueue.*"</code></p>
    /// <p>Valid network namespace values: <code>Sysctls</code> that start with <code>"net.*"</code></p>
    /// <p>All of these values are supported by Fargate.</p>
    pub value: ::std::option::Option<::std::string::String>,
}
impl SystemControl {
    /// <p>The namespaced kernel parameter to set a <code>value</code> for.</p>
    pub fn namespace(&self) -> ::std::option::Option<&str> {
        self.namespace.as_deref()
    }
    /// <p>The namespaced kernel parameter to set a <code>value</code> for.</p>
    /// <p>Valid IPC namespace values: <code>"kernel.msgmax" | "kernel.msgmnb" | "kernel.msgmni" | "kernel.sem" | "kernel.shmall" | "kernel.shmmax" | "kernel.shmmni" | "kernel.shm_rmid_forced"</code>, and <code>Sysctls</code> that start with <code>"fs.mqueue.*"</code></p>
    /// <p>Valid network namespace values: <code>Sysctls</code> that start with <code>"net.*"</code></p>
    /// <p>All of these values are supported by Fargate.</p>
    pub fn value(&self) -> ::std::option::Option<&str> {
        self.value.as_deref()
    }
}
impl SystemControl {
    /// Creates a new builder-style object to manufacture [`SystemControl`](crate::types::SystemControl).
    pub fn builder() -> crate::types::builders::SystemControlBuilder {
        crate::types::builders::SystemControlBuilder::default()
    }
}

/// A builder for [`SystemControl`](crate::types::SystemControl).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct SystemControlBuilder {
    pub(crate) namespace: ::std::option::Option<::std::string::String>,
    pub(crate) value: ::std::option::Option<::std::string::String>,
}
impl SystemControlBuilder {
    /// <p>The namespaced kernel parameter to set a <code>value</code> for.</p>
    pub fn namespace(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.namespace = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The namespaced kernel parameter to set a <code>value</code> for.</p>
    pub fn set_namespace(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.namespace = input;
        self
    }
    /// <p>The namespaced kernel parameter to set a <code>value</code> for.</p>
    pub fn get_namespace(&self) -> &::std::option::Option<::std::string::String> {
        &self.namespace
    }
    /// <p>The namespaced kernel parameter to set a <code>value</code> for.</p>
    /// <p>Valid IPC namespace values: <code>"kernel.msgmax" | "kernel.msgmnb" | "kernel.msgmni" | "kernel.sem" | "kernel.shmall" | "kernel.shmmax" | "kernel.shmmni" | "kernel.shm_rmid_forced"</code>, and <code>Sysctls</code> that start with <code>"fs.mqueue.*"</code></p>
    /// <p>Valid network namespace values: <code>Sysctls</code> that start with <code>"net.*"</code></p>
    /// <p>All of these values are supported by Fargate.</p>
    pub fn value(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.value = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The namespaced kernel parameter to set a <code>value</code> for.</p>
    /// <p>Valid IPC namespace values: <code>"kernel.msgmax" | "kernel.msgmnb" | "kernel.msgmni" | "kernel.sem" | "kernel.shmall" | "kernel.shmmax" | "kernel.shmmni" | "kernel.shm_rmid_forced"</code>, and <code>Sysctls</code> that start with <code>"fs.mqueue.*"</code></p>
    /// <p>Valid network namespace values: <code>Sysctls</code> that start with <code>"net.*"</code></p>
    /// <p>All of these values are supported by Fargate.</p>
    pub fn set_value(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.value = input;
        self
    }
    /// <p>The namespaced kernel parameter to set a <code>value</code> for.</p>
    /// <p>Valid IPC namespace values: <code>"kernel.msgmax" | "kernel.msgmnb" | "kernel.msgmni" | "kernel.sem" | "kernel.shmall" | "kernel.shmmax" | "kernel.shmmni" | "kernel.shm_rmid_forced"</code>, and <code>Sysctls</code> that start with <code>"fs.mqueue.*"</code></p>
    /// <p>Valid network namespace values: <code>Sysctls</code> that start with <code>"net.*"</code></p>
    /// <p>All of these values are supported by Fargate.</p>
    pub fn get_value(&self) -> &::std::option::Option<::std::string::String> {
        &self.value
    }
    /// Consumes the builder and constructs a [`SystemControl`](crate::types::SystemControl).
    pub fn build(self) -> crate::types::SystemControl {
        crate::types::SystemControl {
            namespace: self.namespace,
            value: self.value,
        }
    }
}
