// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The Capacity Reservation configurations to be used when using the <code>RESERVED</code> capacity option type.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct CapacityReservationRequest {
    /// <p>The ARN of the Capacity Reservation resource group in which to run the instance.</p>
    pub reservation_group_arn: ::std::option::Option<::std::string::String>,
    /// <p>The preference on when capacity reservations should be used.</p>
    /// <p>Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>RESERVATIONS_ONLY</code> - Exclusively launch instances into capacity reservations that match the instance requirements configured for the capacity provider. If none exist, instances will fail to provision.</p></li>
    /// <li>
    /// <p><code>RESERVATIONS_FIRST</code> - Prefer to launch instances into a capacity reservation if any exist that match the instance requirements configured for the capacity provider. If none exist, fall back to launching instances On-Demand.</p></li>
    /// <li>
    /// <p><code>RESERVATIONS_EXCLUDED</code> - Avoid using capacity reservations and launch exclusively On-Demand.</p></li>
    /// </ul>
    pub reservation_preference: ::std::option::Option<crate::types::CapacityReservationPreference>,
}
impl CapacityReservationRequest {
    /// <p>The ARN of the Capacity Reservation resource group in which to run the instance.</p>
    pub fn reservation_group_arn(&self) -> ::std::option::Option<&str> {
        self.reservation_group_arn.as_deref()
    }
    /// <p>The preference on when capacity reservations should be used.</p>
    /// <p>Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>RESERVATIONS_ONLY</code> - Exclusively launch instances into capacity reservations that match the instance requirements configured for the capacity provider. If none exist, instances will fail to provision.</p></li>
    /// <li>
    /// <p><code>RESERVATIONS_FIRST</code> - Prefer to launch instances into a capacity reservation if any exist that match the instance requirements configured for the capacity provider. If none exist, fall back to launching instances On-Demand.</p></li>
    /// <li>
    /// <p><code>RESERVATIONS_EXCLUDED</code> - Avoid using capacity reservations and launch exclusively On-Demand.</p></li>
    /// </ul>
    pub fn reservation_preference(&self) -> ::std::option::Option<&crate::types::CapacityReservationPreference> {
        self.reservation_preference.as_ref()
    }
}
impl CapacityReservationRequest {
    /// Creates a new builder-style object to manufacture [`CapacityReservationRequest`](crate::types::CapacityReservationRequest).
    pub fn builder() -> crate::types::builders::CapacityReservationRequestBuilder {
        crate::types::builders::CapacityReservationRequestBuilder::default()
    }
}

/// A builder for [`CapacityReservationRequest`](crate::types::CapacityReservationRequest).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct CapacityReservationRequestBuilder {
    pub(crate) reservation_group_arn: ::std::option::Option<::std::string::String>,
    pub(crate) reservation_preference: ::std::option::Option<crate::types::CapacityReservationPreference>,
}
impl CapacityReservationRequestBuilder {
    /// <p>The ARN of the Capacity Reservation resource group in which to run the instance.</p>
    pub fn reservation_group_arn(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.reservation_group_arn = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The ARN of the Capacity Reservation resource group in which to run the instance.</p>
    pub fn set_reservation_group_arn(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.reservation_group_arn = input;
        self
    }
    /// <p>The ARN of the Capacity Reservation resource group in which to run the instance.</p>
    pub fn get_reservation_group_arn(&self) -> &::std::option::Option<::std::string::String> {
        &self.reservation_group_arn
    }
    /// <p>The preference on when capacity reservations should be used.</p>
    /// <p>Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>RESERVATIONS_ONLY</code> - Exclusively launch instances into capacity reservations that match the instance requirements configured for the capacity provider. If none exist, instances will fail to provision.</p></li>
    /// <li>
    /// <p><code>RESERVATIONS_FIRST</code> - Prefer to launch instances into a capacity reservation if any exist that match the instance requirements configured for the capacity provider. If none exist, fall back to launching instances On-Demand.</p></li>
    /// <li>
    /// <p><code>RESERVATIONS_EXCLUDED</code> - Avoid using capacity reservations and launch exclusively On-Demand.</p></li>
    /// </ul>
    pub fn reservation_preference(mut self, input: crate::types::CapacityReservationPreference) -> Self {
        self.reservation_preference = ::std::option::Option::Some(input);
        self
    }
    /// <p>The preference on when capacity reservations should be used.</p>
    /// <p>Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>RESERVATIONS_ONLY</code> - Exclusively launch instances into capacity reservations that match the instance requirements configured for the capacity provider. If none exist, instances will fail to provision.</p></li>
    /// <li>
    /// <p><code>RESERVATIONS_FIRST</code> - Prefer to launch instances into a capacity reservation if any exist that match the instance requirements configured for the capacity provider. If none exist, fall back to launching instances On-Demand.</p></li>
    /// <li>
    /// <p><code>RESERVATIONS_EXCLUDED</code> - Avoid using capacity reservations and launch exclusively On-Demand.</p></li>
    /// </ul>
    pub fn set_reservation_preference(mut self, input: ::std::option::Option<crate::types::CapacityReservationPreference>) -> Self {
        self.reservation_preference = input;
        self
    }
    /// <p>The preference on when capacity reservations should be used.</p>
    /// <p>Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>RESERVATIONS_ONLY</code> - Exclusively launch instances into capacity reservations that match the instance requirements configured for the capacity provider. If none exist, instances will fail to provision.</p></li>
    /// <li>
    /// <p><code>RESERVATIONS_FIRST</code> - Prefer to launch instances into a capacity reservation if any exist that match the instance requirements configured for the capacity provider. If none exist, fall back to launching instances On-Demand.</p></li>
    /// <li>
    /// <p><code>RESERVATIONS_EXCLUDED</code> - Avoid using capacity reservations and launch exclusively On-Demand.</p></li>
    /// </ul>
    pub fn get_reservation_preference(&self) -> &::std::option::Option<crate::types::CapacityReservationPreference> {
        &self.reservation_preference
    }
    /// Consumes the builder and constructs a [`CapacityReservationRequest`](crate::types::CapacityReservationRequest).
    pub fn build(self) -> crate::types::CapacityReservationRequest {
        crate::types::CapacityReservationRequest {
            reservation_group_arn: self.reservation_group_arn,
            reservation_preference: self.reservation_preference,
        }
    }
}
