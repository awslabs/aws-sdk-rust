// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <note>
/// <p>You can use early success criteria only with rolling deployment strategy.</p>
/// </note>
/// <p>The configuration that determines when a rolling update deployment is considered successful. Early success criteria defines the percentage of tasks that must be healthy before a deployment completes. It also controls whether Amazon ECS must remove the previous tasks before a deployment completes.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub struct DeploymentEarlySuccessCriteria {
    /// <p>Specifies whether to use the early success criteria for the service deployment. When set to <code>false</code>, the deployment uses the default behavior, where Amazon ECS considers the deployment successful when the target service revision fully stabilizes and the previous tasks are removed. The default value is <code>false</code>.</p>
    /// <p>When set to <code>true</code>, Amazon ECS monitors the deployment to meet early success criteria. You must also specify <code>healthyPercent</code> and <code>sourceServiceRevisionCleanup</code>.</p>
    pub enable: bool,
    /// <p>The percentage of healthy tasks that the target service revision must reach before Amazon ECS considers the deployment successful. This percentage is relative to the service's <code>desiredCount</code> and must be an integer between <code>0</code> and <code>100</code>. This value must be greater than or equal to the <code>minimumHealthyPercent</code> value.</p>
    /// <p>After this percentage of tasks is healthy and the bake time elapses, Amazon ECS completes the deployment. Amazon ECS continues to scale the target service revision to 100 percent in the background.</p>
    pub healthy_percent: ::std::option::Option<i32>,
    /// <p>The time when Amazon ECS removes the source revisions' tasks relative to deployment completion. The valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>BLOCKING</code>—Amazon ECS removes the previous tasks before it marks the deployment as successful.</p></li>
    /// <li>
    /// <p><code>DEFERRED</code>—Amazon ECS marks the deployment successful, and then removes the previous tasks in the background.</p></li>
    /// </ul>
    pub source_service_revision_cleanup: ::std::option::Option<crate::types::ServiceRevisionCleanup>,
}
impl DeploymentEarlySuccessCriteria {
    /// <p>Specifies whether to use the early success criteria for the service deployment. When set to <code>false</code>, the deployment uses the default behavior, where Amazon ECS considers the deployment successful when the target service revision fully stabilizes and the previous tasks are removed. The default value is <code>false</code>.</p>
    /// <p>When set to <code>true</code>, Amazon ECS monitors the deployment to meet early success criteria. You must also specify <code>healthyPercent</code> and <code>sourceServiceRevisionCleanup</code>.</p>
    pub fn enable(&self) -> bool {
        self.enable
    }
    /// <p>The percentage of healthy tasks that the target service revision must reach before Amazon ECS considers the deployment successful. This percentage is relative to the service's <code>desiredCount</code> and must be an integer between <code>0</code> and <code>100</code>. This value must be greater than or equal to the <code>minimumHealthyPercent</code> value.</p>
    /// <p>After this percentage of tasks is healthy and the bake time elapses, Amazon ECS completes the deployment. Amazon ECS continues to scale the target service revision to 100 percent in the background.</p>
    pub fn healthy_percent(&self) -> ::std::option::Option<i32> {
        self.healthy_percent
    }
    /// <p>The time when Amazon ECS removes the source revisions' tasks relative to deployment completion. The valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>BLOCKING</code>—Amazon ECS removes the previous tasks before it marks the deployment as successful.</p></li>
    /// <li>
    /// <p><code>DEFERRED</code>—Amazon ECS marks the deployment successful, and then removes the previous tasks in the background.</p></li>
    /// </ul>
    pub fn source_service_revision_cleanup(&self) -> ::std::option::Option<&crate::types::ServiceRevisionCleanup> {
        self.source_service_revision_cleanup.as_ref()
    }
}
impl DeploymentEarlySuccessCriteria {
    /// Creates a new builder-style object to manufacture [`DeploymentEarlySuccessCriteria`](crate::types::DeploymentEarlySuccessCriteria).
    pub fn builder() -> crate::types::builders::DeploymentEarlySuccessCriteriaBuilder {
        crate::types::builders::DeploymentEarlySuccessCriteriaBuilder::default()
    }
}

/// A builder for [`DeploymentEarlySuccessCriteria`](crate::types::DeploymentEarlySuccessCriteria).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default, ::std::fmt::Debug)]
#[non_exhaustive]
pub struct DeploymentEarlySuccessCriteriaBuilder {
    pub(crate) enable: ::std::option::Option<bool>,
    pub(crate) healthy_percent: ::std::option::Option<i32>,
    pub(crate) source_service_revision_cleanup: ::std::option::Option<crate::types::ServiceRevisionCleanup>,
}
impl DeploymentEarlySuccessCriteriaBuilder {
    /// <p>Specifies whether to use the early success criteria for the service deployment. When set to <code>false</code>, the deployment uses the default behavior, where Amazon ECS considers the deployment successful when the target service revision fully stabilizes and the previous tasks are removed. The default value is <code>false</code>.</p>
    /// <p>When set to <code>true</code>, Amazon ECS monitors the deployment to meet early success criteria. You must also specify <code>healthyPercent</code> and <code>sourceServiceRevisionCleanup</code>.</p>
    /// This field is required.
    pub fn enable(mut self, input: bool) -> Self {
        self.enable = ::std::option::Option::Some(input);
        self
    }
    /// <p>Specifies whether to use the early success criteria for the service deployment. When set to <code>false</code>, the deployment uses the default behavior, where Amazon ECS considers the deployment successful when the target service revision fully stabilizes and the previous tasks are removed. The default value is <code>false</code>.</p>
    /// <p>When set to <code>true</code>, Amazon ECS monitors the deployment to meet early success criteria. You must also specify <code>healthyPercent</code> and <code>sourceServiceRevisionCleanup</code>.</p>
    pub fn set_enable(mut self, input: ::std::option::Option<bool>) -> Self {
        self.enable = input;
        self
    }
    /// <p>Specifies whether to use the early success criteria for the service deployment. When set to <code>false</code>, the deployment uses the default behavior, where Amazon ECS considers the deployment successful when the target service revision fully stabilizes and the previous tasks are removed. The default value is <code>false</code>.</p>
    /// <p>When set to <code>true</code>, Amazon ECS monitors the deployment to meet early success criteria. You must also specify <code>healthyPercent</code> and <code>sourceServiceRevisionCleanup</code>.</p>
    pub fn get_enable(&self) -> &::std::option::Option<bool> {
        &self.enable
    }
    /// <p>The percentage of healthy tasks that the target service revision must reach before Amazon ECS considers the deployment successful. This percentage is relative to the service's <code>desiredCount</code> and must be an integer between <code>0</code> and <code>100</code>. This value must be greater than or equal to the <code>minimumHealthyPercent</code> value.</p>
    /// <p>After this percentage of tasks is healthy and the bake time elapses, Amazon ECS completes the deployment. Amazon ECS continues to scale the target service revision to 100 percent in the background.</p>
    pub fn healthy_percent(mut self, input: i32) -> Self {
        self.healthy_percent = ::std::option::Option::Some(input);
        self
    }
    /// <p>The percentage of healthy tasks that the target service revision must reach before Amazon ECS considers the deployment successful. This percentage is relative to the service's <code>desiredCount</code> and must be an integer between <code>0</code> and <code>100</code>. This value must be greater than or equal to the <code>minimumHealthyPercent</code> value.</p>
    /// <p>After this percentage of tasks is healthy and the bake time elapses, Amazon ECS completes the deployment. Amazon ECS continues to scale the target service revision to 100 percent in the background.</p>
    pub fn set_healthy_percent(mut self, input: ::std::option::Option<i32>) -> Self {
        self.healthy_percent = input;
        self
    }
    /// <p>The percentage of healthy tasks that the target service revision must reach before Amazon ECS considers the deployment successful. This percentage is relative to the service's <code>desiredCount</code> and must be an integer between <code>0</code> and <code>100</code>. This value must be greater than or equal to the <code>minimumHealthyPercent</code> value.</p>
    /// <p>After this percentage of tasks is healthy and the bake time elapses, Amazon ECS completes the deployment. Amazon ECS continues to scale the target service revision to 100 percent in the background.</p>
    pub fn get_healthy_percent(&self) -> &::std::option::Option<i32> {
        &self.healthy_percent
    }
    /// <p>The time when Amazon ECS removes the source revisions' tasks relative to deployment completion. The valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>BLOCKING</code>—Amazon ECS removes the previous tasks before it marks the deployment as successful.</p></li>
    /// <li>
    /// <p><code>DEFERRED</code>—Amazon ECS marks the deployment successful, and then removes the previous tasks in the background.</p></li>
    /// </ul>
    pub fn source_service_revision_cleanup(mut self, input: crate::types::ServiceRevisionCleanup) -> Self {
        self.source_service_revision_cleanup = ::std::option::Option::Some(input);
        self
    }
    /// <p>The time when Amazon ECS removes the source revisions' tasks relative to deployment completion. The valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>BLOCKING</code>—Amazon ECS removes the previous tasks before it marks the deployment as successful.</p></li>
    /// <li>
    /// <p><code>DEFERRED</code>—Amazon ECS marks the deployment successful, and then removes the previous tasks in the background.</p></li>
    /// </ul>
    pub fn set_source_service_revision_cleanup(mut self, input: ::std::option::Option<crate::types::ServiceRevisionCleanup>) -> Self {
        self.source_service_revision_cleanup = input;
        self
    }
    /// <p>The time when Amazon ECS removes the source revisions' tasks relative to deployment completion. The valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>BLOCKING</code>—Amazon ECS removes the previous tasks before it marks the deployment as successful.</p></li>
    /// <li>
    /// <p><code>DEFERRED</code>—Amazon ECS marks the deployment successful, and then removes the previous tasks in the background.</p></li>
    /// </ul>
    pub fn get_source_service_revision_cleanup(&self) -> &::std::option::Option<crate::types::ServiceRevisionCleanup> {
        &self.source_service_revision_cleanup
    }
    /// Consumes the builder and constructs a [`DeploymentEarlySuccessCriteria`](crate::types::DeploymentEarlySuccessCriteria).
    pub fn build(self) -> crate::types::DeploymentEarlySuccessCriteria {
        crate::types::DeploymentEarlySuccessCriteria {
            enable: self.enable.unwrap_or_default(),
            healthy_percent: self.healthy_percent,
            source_service_revision_cleanup: self.source_service_revision_cleanup,
        }
    }
}
