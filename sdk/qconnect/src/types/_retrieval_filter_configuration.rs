// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>Configuration for filtering content during retrieval operations.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::fmt::Debug)]
pub enum RetrievalFilterConfiguration {
    /// <p>Filter configuration that requires all conditions to be met.</p>
    AndAll(::std::vec::Vec<crate::types::RetrievalFilterConfiguration>),
    /// <p>Filter configuration for exact equality matching.</p>
    Equals(crate::types::FilterAttribute),
    /// <p>Filter configuration for greater than comparison.</p>
    GreaterThan(crate::types::FilterAttribute),
    /// <p>Filter configuration for greater than or equal comparison.</p>
    GreaterThanOrEquals(crate::types::FilterAttribute),
    /// <p>Filter configuration for membership in a set of values.</p>
    In(crate::types::FilterAttribute),
    /// <p>Filter configuration for less than comparison.</p>
    LessThan(crate::types::FilterAttribute),
    /// <p>Filter configuration for less than or equal comparison.</p>
    LessThanOrEquals(crate::types::FilterAttribute),
    /// <p>Filter configuration for checking if a list contains a value.</p>
    ListContains(crate::types::FilterAttribute),
    /// <p>Filter configuration for inequality matching.</p>
    NotEquals(crate::types::FilterAttribute),
    /// <p>Filter configuration for exclusion from a set of values.</p>
    NotIn(crate::types::FilterAttribute),
    /// <p>Filter configuration where any condition can be met.</p>
    OrAll(::std::vec::Vec<crate::types::RetrievalFilterConfiguration>),
    /// <p>Filter configuration for prefix matching.</p>
    StartsWith(crate::types::FilterAttribute),
    /// <p>Filter configuration for substring matching.</p>
    StringContains(crate::types::FilterAttribute),
    /// The `Unknown` variant represents cases where new union variant was received. Consider upgrading the SDK to the latest available version.
    /// An unknown enum variant
    ///
    /// _Note: If you encounter this error, consider upgrading your SDK to the latest version._
    /// The `Unknown` variant represents cases where the server sent a value that wasn't recognized
    /// by the client. This can happen when the server adds new functionality, but the client has not been updated.
    /// To investigate this, consider turning on debug logging to print the raw HTTP response.
    #[non_exhaustive]
    Unknown,
}
impl RetrievalFilterConfiguration {
    /// Tries to convert the enum instance into [`AndAll`](crate::types::RetrievalFilterConfiguration::AndAll), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_and_all(&self) -> ::std::result::Result<&::std::vec::Vec<crate::types::RetrievalFilterConfiguration>, &Self> {
        if let RetrievalFilterConfiguration::AndAll(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`AndAll`](crate::types::RetrievalFilterConfiguration::AndAll).
    pub fn is_and_all(&self) -> bool {
        self.as_and_all().is_ok()
    }
    /// Tries to convert the enum instance into [`Equals`](crate::types::RetrievalFilterConfiguration::Equals), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_equals(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::Equals(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`Equals`](crate::types::RetrievalFilterConfiguration::Equals).
    pub fn is_equals(&self) -> bool {
        self.as_equals().is_ok()
    }
    /// Tries to convert the enum instance into [`GreaterThan`](crate::types::RetrievalFilterConfiguration::GreaterThan), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_greater_than(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::GreaterThan(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`GreaterThan`](crate::types::RetrievalFilterConfiguration::GreaterThan).
    pub fn is_greater_than(&self) -> bool {
        self.as_greater_than().is_ok()
    }
    /// Tries to convert the enum instance into [`GreaterThanOrEquals`](crate::types::RetrievalFilterConfiguration::GreaterThanOrEquals), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_greater_than_or_equals(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::GreaterThanOrEquals(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`GreaterThanOrEquals`](crate::types::RetrievalFilterConfiguration::GreaterThanOrEquals).
    pub fn is_greater_than_or_equals(&self) -> bool {
        self.as_greater_than_or_equals().is_ok()
    }
    /// Tries to convert the enum instance into [`In`](crate::types::RetrievalFilterConfiguration::In), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_in(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::In(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`In`](crate::types::RetrievalFilterConfiguration::In).
    pub fn is_in(&self) -> bool {
        self.as_in().is_ok()
    }
    /// Tries to convert the enum instance into [`LessThan`](crate::types::RetrievalFilterConfiguration::LessThan), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_less_than(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::LessThan(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`LessThan`](crate::types::RetrievalFilterConfiguration::LessThan).
    pub fn is_less_than(&self) -> bool {
        self.as_less_than().is_ok()
    }
    /// Tries to convert the enum instance into [`LessThanOrEquals`](crate::types::RetrievalFilterConfiguration::LessThanOrEquals), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_less_than_or_equals(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::LessThanOrEquals(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`LessThanOrEquals`](crate::types::RetrievalFilterConfiguration::LessThanOrEquals).
    pub fn is_less_than_or_equals(&self) -> bool {
        self.as_less_than_or_equals().is_ok()
    }
    /// Tries to convert the enum instance into [`ListContains`](crate::types::RetrievalFilterConfiguration::ListContains), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_list_contains(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::ListContains(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`ListContains`](crate::types::RetrievalFilterConfiguration::ListContains).
    pub fn is_list_contains(&self) -> bool {
        self.as_list_contains().is_ok()
    }
    /// Tries to convert the enum instance into [`NotEquals`](crate::types::RetrievalFilterConfiguration::NotEquals), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_not_equals(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::NotEquals(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`NotEquals`](crate::types::RetrievalFilterConfiguration::NotEquals).
    pub fn is_not_equals(&self) -> bool {
        self.as_not_equals().is_ok()
    }
    /// Tries to convert the enum instance into [`NotIn`](crate::types::RetrievalFilterConfiguration::NotIn), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_not_in(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::NotIn(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`NotIn`](crate::types::RetrievalFilterConfiguration::NotIn).
    pub fn is_not_in(&self) -> bool {
        self.as_not_in().is_ok()
    }
    /// Tries to convert the enum instance into [`OrAll`](crate::types::RetrievalFilterConfiguration::OrAll), extracting the inner [`Vec`](::std::vec::Vec).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_or_all(&self) -> ::std::result::Result<&::std::vec::Vec<crate::types::RetrievalFilterConfiguration>, &Self> {
        if let RetrievalFilterConfiguration::OrAll(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`OrAll`](crate::types::RetrievalFilterConfiguration::OrAll).
    pub fn is_or_all(&self) -> bool {
        self.as_or_all().is_ok()
    }
    /// Tries to convert the enum instance into [`StartsWith`](crate::types::RetrievalFilterConfiguration::StartsWith), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_starts_with(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::StartsWith(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`StartsWith`](crate::types::RetrievalFilterConfiguration::StartsWith).
    pub fn is_starts_with(&self) -> bool {
        self.as_starts_with().is_ok()
    }
    /// Tries to convert the enum instance into [`StringContains`](crate::types::RetrievalFilterConfiguration::StringContains), extracting the inner [`FilterAttribute`](crate::types::FilterAttribute).
    /// Returns `Err(&Self)` if it can't be converted.
    pub fn as_string_contains(&self) -> ::std::result::Result<&crate::types::FilterAttribute, &Self> {
        if let RetrievalFilterConfiguration::StringContains(val) = &self {
            ::std::result::Result::Ok(val)
        } else {
            ::std::result::Result::Err(self)
        }
    }
    /// Returns true if this is a [`StringContains`](crate::types::RetrievalFilterConfiguration::StringContains).
    pub fn is_string_contains(&self) -> bool {
        self.as_string_contains().is_ok()
    }
    /// Returns true if the enum instance is the `Unknown` variant.
    pub fn is_unknown(&self) -> bool {
        matches!(self, Self::Unknown)
    }
}
