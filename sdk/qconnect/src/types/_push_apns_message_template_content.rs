// Code generated by software.amazon.smithy.rust.codegen.smithy-rs. DO NOT EDIT.

/// <p>The content of the push message template that applies to APNS (Apple Push Notification service) notification service.</p>
#[non_exhaustive]
#[derive(::std::clone::Clone, ::std::cmp::PartialEq)]
pub struct PushApnsMessageTemplateContent {
    /// <p>The title to use in a push notification that's based on the message template. This title appears above the notification message on a recipient's device.</p>
    pub title: ::std::option::Option<::std::string::String>,
    /// <p>The message body to use in a push notification that is based on the message template.</p>
    pub body: ::std::option::Option<crate::types::MessageTemplateBodyContentProvider>,
    /// <p>The action to occur if a recipient taps a push notification that is based on the message template. Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>OPEN_APP</code> - Your app opens or it becomes the foreground app if it was sent to the background. This is the default action.</p></li>
    /// <li>
    /// <p><code>DEEP_LINK</code> - Your app opens and displays a designated user interface in the app. This action uses the deep-linking features of the iOS platform.</p></li>
    /// <li>
    /// <p><code>URL</code> - The default mobile browser on the recipient's device opens and loads the web page at a URL that you specify.</p></li>
    /// </ul>
    pub action: ::std::option::Option<crate::types::PushMessageAction>,
    /// <p>The key for the sound to play when the recipient receives a push notification that's based on the message template. The value for this key is the name of a sound file in your app's main bundle or the <code>Library/Sounds</code> folder in your app's data container. If the sound file can't be found or you specify <code>default</code> for the value, the system plays the default alert sound.</p>
    pub sound: ::std::option::Option<::std::string::String>,
    /// <p>The URL to open in a recipient's default mobile browser, if a recipient taps a push notification that's based on the message template and the value of the <code>action</code> property is <code>URL</code>.</p>
    pub url: ::std::option::Option<::std::string::String>,
    /// <p>The URL of an image or video to display in push notifications that are based on the message template.</p>
    pub media_url: ::std::option::Option<::std::string::String>,
    /// <p>The raw, JSON-formatted string to use as the payload for a push notification that's based on the message template. If specified, this value overrides all other content for the message template.</p>
    pub raw_content: ::std::option::Option<crate::types::MessageTemplateBodyContentProvider>,
}
impl PushApnsMessageTemplateContent {
    /// <p>The title to use in a push notification that's based on the message template. This title appears above the notification message on a recipient's device.</p>
    pub fn title(&self) -> ::std::option::Option<&str> {
        self.title.as_deref()
    }
    /// <p>The message body to use in a push notification that is based on the message template.</p>
    pub fn body(&self) -> ::std::option::Option<&crate::types::MessageTemplateBodyContentProvider> {
        self.body.as_ref()
    }
    /// <p>The action to occur if a recipient taps a push notification that is based on the message template. Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>OPEN_APP</code> - Your app opens or it becomes the foreground app if it was sent to the background. This is the default action.</p></li>
    /// <li>
    /// <p><code>DEEP_LINK</code> - Your app opens and displays a designated user interface in the app. This action uses the deep-linking features of the iOS platform.</p></li>
    /// <li>
    /// <p><code>URL</code> - The default mobile browser on the recipient's device opens and loads the web page at a URL that you specify.</p></li>
    /// </ul>
    pub fn action(&self) -> ::std::option::Option<&crate::types::PushMessageAction> {
        self.action.as_ref()
    }
    /// <p>The key for the sound to play when the recipient receives a push notification that's based on the message template. The value for this key is the name of a sound file in your app's main bundle or the <code>Library/Sounds</code> folder in your app's data container. If the sound file can't be found or you specify <code>default</code> for the value, the system plays the default alert sound.</p>
    pub fn sound(&self) -> ::std::option::Option<&str> {
        self.sound.as_deref()
    }
    /// <p>The URL to open in a recipient's default mobile browser, if a recipient taps a push notification that's based on the message template and the value of the <code>action</code> property is <code>URL</code>.</p>
    pub fn url(&self) -> ::std::option::Option<&str> {
        self.url.as_deref()
    }
    /// <p>The URL of an image or video to display in push notifications that are based on the message template.</p>
    pub fn media_url(&self) -> ::std::option::Option<&str> {
        self.media_url.as_deref()
    }
    /// <p>The raw, JSON-formatted string to use as the payload for a push notification that's based on the message template. If specified, this value overrides all other content for the message template.</p>
    pub fn raw_content(&self) -> ::std::option::Option<&crate::types::MessageTemplateBodyContentProvider> {
        self.raw_content.as_ref()
    }
}
impl ::std::fmt::Debug for PushApnsMessageTemplateContent {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        let mut formatter = f.debug_struct("PushApnsMessageTemplateContent");
        formatter.field("title", &"*** Sensitive Data Redacted ***");
        formatter.field("body", &self.body);
        formatter.field("action", &self.action);
        formatter.field("sound", &"*** Sensitive Data Redacted ***");
        formatter.field("url", &"*** Sensitive Data Redacted ***");
        formatter.field("media_url", &"*** Sensitive Data Redacted ***");
        formatter.field("raw_content", &self.raw_content);
        formatter.finish()
    }
}
impl PushApnsMessageTemplateContent {
    /// Creates a new builder-style object to manufacture [`PushApnsMessageTemplateContent`](crate::types::PushApnsMessageTemplateContent).
    pub fn builder() -> crate::types::builders::PushApnsMessageTemplateContentBuilder {
        crate::types::builders::PushApnsMessageTemplateContentBuilder::default()
    }
}

/// A builder for [`PushApnsMessageTemplateContent`](crate::types::PushApnsMessageTemplateContent).
#[derive(::std::clone::Clone, ::std::cmp::PartialEq, ::std::default::Default)]
#[non_exhaustive]
pub struct PushApnsMessageTemplateContentBuilder {
    pub(crate) title: ::std::option::Option<::std::string::String>,
    pub(crate) body: ::std::option::Option<crate::types::MessageTemplateBodyContentProvider>,
    pub(crate) action: ::std::option::Option<crate::types::PushMessageAction>,
    pub(crate) sound: ::std::option::Option<::std::string::String>,
    pub(crate) url: ::std::option::Option<::std::string::String>,
    pub(crate) media_url: ::std::option::Option<::std::string::String>,
    pub(crate) raw_content: ::std::option::Option<crate::types::MessageTemplateBodyContentProvider>,
}
impl PushApnsMessageTemplateContentBuilder {
    /// <p>The title to use in a push notification that's based on the message template. This title appears above the notification message on a recipient's device.</p>
    pub fn title(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.title = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The title to use in a push notification that's based on the message template. This title appears above the notification message on a recipient's device.</p>
    pub fn set_title(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.title = input;
        self
    }
    /// <p>The title to use in a push notification that's based on the message template. This title appears above the notification message on a recipient's device.</p>
    pub fn get_title(&self) -> &::std::option::Option<::std::string::String> {
        &self.title
    }
    /// <p>The message body to use in a push notification that is based on the message template.</p>
    pub fn body(mut self, input: crate::types::MessageTemplateBodyContentProvider) -> Self {
        self.body = ::std::option::Option::Some(input);
        self
    }
    /// <p>The message body to use in a push notification that is based on the message template.</p>
    pub fn set_body(mut self, input: ::std::option::Option<crate::types::MessageTemplateBodyContentProvider>) -> Self {
        self.body = input;
        self
    }
    /// <p>The message body to use in a push notification that is based on the message template.</p>
    pub fn get_body(&self) -> &::std::option::Option<crate::types::MessageTemplateBodyContentProvider> {
        &self.body
    }
    /// <p>The action to occur if a recipient taps a push notification that is based on the message template. Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>OPEN_APP</code> - Your app opens or it becomes the foreground app if it was sent to the background. This is the default action.</p></li>
    /// <li>
    /// <p><code>DEEP_LINK</code> - Your app opens and displays a designated user interface in the app. This action uses the deep-linking features of the iOS platform.</p></li>
    /// <li>
    /// <p><code>URL</code> - The default mobile browser on the recipient's device opens and loads the web page at a URL that you specify.</p></li>
    /// </ul>
    pub fn action(mut self, input: crate::types::PushMessageAction) -> Self {
        self.action = ::std::option::Option::Some(input);
        self
    }
    /// <p>The action to occur if a recipient taps a push notification that is based on the message template. Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>OPEN_APP</code> - Your app opens or it becomes the foreground app if it was sent to the background. This is the default action.</p></li>
    /// <li>
    /// <p><code>DEEP_LINK</code> - Your app opens and displays a designated user interface in the app. This action uses the deep-linking features of the iOS platform.</p></li>
    /// <li>
    /// <p><code>URL</code> - The default mobile browser on the recipient's device opens and loads the web page at a URL that you specify.</p></li>
    /// </ul>
    pub fn set_action(mut self, input: ::std::option::Option<crate::types::PushMessageAction>) -> Self {
        self.action = input;
        self
    }
    /// <p>The action to occur if a recipient taps a push notification that is based on the message template. Valid values are:</p>
    /// <ul>
    /// <li>
    /// <p><code>OPEN_APP</code> - Your app opens or it becomes the foreground app if it was sent to the background. This is the default action.</p></li>
    /// <li>
    /// <p><code>DEEP_LINK</code> - Your app opens and displays a designated user interface in the app. This action uses the deep-linking features of the iOS platform.</p></li>
    /// <li>
    /// <p><code>URL</code> - The default mobile browser on the recipient's device opens and loads the web page at a URL that you specify.</p></li>
    /// </ul>
    pub fn get_action(&self) -> &::std::option::Option<crate::types::PushMessageAction> {
        &self.action
    }
    /// <p>The key for the sound to play when the recipient receives a push notification that's based on the message template. The value for this key is the name of a sound file in your app's main bundle or the <code>Library/Sounds</code> folder in your app's data container. If the sound file can't be found or you specify <code>default</code> for the value, the system plays the default alert sound.</p>
    pub fn sound(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.sound = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The key for the sound to play when the recipient receives a push notification that's based on the message template. The value for this key is the name of a sound file in your app's main bundle or the <code>Library/Sounds</code> folder in your app's data container. If the sound file can't be found or you specify <code>default</code> for the value, the system plays the default alert sound.</p>
    pub fn set_sound(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.sound = input;
        self
    }
    /// <p>The key for the sound to play when the recipient receives a push notification that's based on the message template. The value for this key is the name of a sound file in your app's main bundle or the <code>Library/Sounds</code> folder in your app's data container. If the sound file can't be found or you specify <code>default</code> for the value, the system plays the default alert sound.</p>
    pub fn get_sound(&self) -> &::std::option::Option<::std::string::String> {
        &self.sound
    }
    /// <p>The URL to open in a recipient's default mobile browser, if a recipient taps a push notification that's based on the message template and the value of the <code>action</code> property is <code>URL</code>.</p>
    pub fn url(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.url = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The URL to open in a recipient's default mobile browser, if a recipient taps a push notification that's based on the message template and the value of the <code>action</code> property is <code>URL</code>.</p>
    pub fn set_url(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.url = input;
        self
    }
    /// <p>The URL to open in a recipient's default mobile browser, if a recipient taps a push notification that's based on the message template and the value of the <code>action</code> property is <code>URL</code>.</p>
    pub fn get_url(&self) -> &::std::option::Option<::std::string::String> {
        &self.url
    }
    /// <p>The URL of an image or video to display in push notifications that are based on the message template.</p>
    pub fn media_url(mut self, input: impl ::std::convert::Into<::std::string::String>) -> Self {
        self.media_url = ::std::option::Option::Some(input.into());
        self
    }
    /// <p>The URL of an image or video to display in push notifications that are based on the message template.</p>
    pub fn set_media_url(mut self, input: ::std::option::Option<::std::string::String>) -> Self {
        self.media_url = input;
        self
    }
    /// <p>The URL of an image or video to display in push notifications that are based on the message template.</p>
    pub fn get_media_url(&self) -> &::std::option::Option<::std::string::String> {
        &self.media_url
    }
    /// <p>The raw, JSON-formatted string to use as the payload for a push notification that's based on the message template. If specified, this value overrides all other content for the message template.</p>
    pub fn raw_content(mut self, input: crate::types::MessageTemplateBodyContentProvider) -> Self {
        self.raw_content = ::std::option::Option::Some(input);
        self
    }
    /// <p>The raw, JSON-formatted string to use as the payload for a push notification that's based on the message template. If specified, this value overrides all other content for the message template.</p>
    pub fn set_raw_content(mut self, input: ::std::option::Option<crate::types::MessageTemplateBodyContentProvider>) -> Self {
        self.raw_content = input;
        self
    }
    /// <p>The raw, JSON-formatted string to use as the payload for a push notification that's based on the message template. If specified, this value overrides all other content for the message template.</p>
    pub fn get_raw_content(&self) -> &::std::option::Option<crate::types::MessageTemplateBodyContentProvider> {
        &self.raw_content
    }
    /// Consumes the builder and constructs a [`PushApnsMessageTemplateContent`](crate::types::PushApnsMessageTemplateContent).
    pub fn build(self) -> crate::types::PushApnsMessageTemplateContent {
        crate::types::PushApnsMessageTemplateContent {
            title: self.title,
            body: self.body,
            action: self.action,
            sound: self.sound,
            url: self.url,
            media_url: self.media_url,
            raw_content: self.raw_content,
        }
    }
}
impl ::std::fmt::Debug for PushApnsMessageTemplateContentBuilder {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        let mut formatter = f.debug_struct("PushApnsMessageTemplateContentBuilder");
        formatter.field("title", &"*** Sensitive Data Redacted ***");
        formatter.field("body", &self.body);
        formatter.field("action", &self.action);
        formatter.field("sound", &"*** Sensitive Data Redacted ***");
        formatter.field("url", &"*** Sensitive Data Redacted ***");
        formatter.field("media_url", &"*** Sensitive Data Redacted ***");
        formatter.field("raw_content", &self.raw_content);
        formatter.finish()
    }
}
